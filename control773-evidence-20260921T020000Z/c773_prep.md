## Control-773 (Sep 21 04:00 +02 / 02:00 UTC Mon Sep 21): Strict read-only control - **HEALTHY 2.8.1 (docs + evidence only, NO deploy, NO code change / NO correction needed)** - independent GET-only verification one hour past Control-772 CONFIRMS the post-#19 steady state + the 2.8.1 releases (FINDING-1 fix + Trading Analytics + crypto-analytics) continue CLEAN through the Sun:Mon overnight boundary (run 14999 @ Mon 02:01:16z) with NO new suppression in this window (the only gap historically remains the already-escalated C-739-flagged isolated daytrading slot-miss, which lies before this window's start). Continuity: repo HEAD prior = **c1ef7d4 (Control-772 docs/evidence)**; deployed src commit **2ea5e9d** re-confirmed (`git log 2ea5e9d..HEAD -- src/` empty = **ZERO src diff**, `git diff --check` clean, RELEASE_VERSION **2.8.1** in src/version.ts + /health + config), working tree clean. True clock **02:00:42 UTC Mon Sep 21** (`date -u`; injected context matches). US market **CLOSED** (overnight pre-market; daytrading crons `skipped` = market_open=false MARKET_CLOSED, book EOD-flat; nextOpen Mon Sep 21 09:30 ET = **C-726 min_conf 0.8 rejection verify at ~13:30z**). GET endpoints returned HTTP 200 real data, no D1_ERROR (/health ok **2.8.1** + /api/account broker-direct + /api/config release_version 2.8.1 + /api/dashboard + /api/positions source=alpaca + /api/runs limit=300 + /api/trades limit=50, all 200, fetched 02:00-02:02 UTC). Min_confidence **0.8 LIVE** (FINDING 1 FIXED via 2.7.1): /api/config min_confidence **0.8**, source-normalization fix confirmed in resolveDaytradingConfig (src/index.ts snake_case min_confidence -> camelCase minConfidence, D1 override merges over FALLBACK_CONFIG 0.7; risk-manager.ts:170-171 `Confidence X below minimum 0.8` rejection path). Guard keys live UNCHANGED (daily_loss_limit_usd **150** / swing **100** / crypto **100**, account_equity_floor_usd **97000**, crypto_trading_enabled **false**). Caps **5000/3700/2000** verified in dashboard capitalCaps (daytrading 5000 / swing 3700 / crypto 2000) + /api/config max_capital_usd 5000 / swing_max_capital_usd 3700 / crypto_max_capital_usd 2000 - **UNCHANGED**. Crypto edge-gate wired config-disabled (crypto_max_capital_usd **2000**, crypto_min_edge_after_costs **8**, crypto_trading_enabled **false**, computeCalibratedEdgeBps producer src/technical-analysis.ts:414 + rawEdgeBps + CRYPTO_DISABLED_BY_CONFIG fail-closed path src/crypto-strategy.ts:284; every crypto run CRYPTO_DISABLED_BY_CONFIG fail-closed). **Broker-authoritative source=alpaca** (dashboard freshness current_state_source=**alpaca**, observed **2026-09-21T02:01:08Z**, metadata_source=d1, semantics="Broker position quantities... current-state evidence; D1 = metadata only"): **11 swing** (MV **$4,785.55**, cost_basis **$4,992.42** legacy CAPITAL_CAP held book vs $3,700 cap, all strategy=swing) + **0 daytrading** (EOD-flat) + **0 crypto/null** (0 null-strategy). **Equity (dashboard account broker-direct) $97,153.50**, POSITIVE direction (equity $97,153.50 > last_equity $97,135.8064 = **+$17.69** signed delta, change_today +$17.69, buying_power $382,506.61, cash $92,367.95, **$153.50 above the $97,000 floor, NEVER touched**, **trend-bend extends through C-773** (C-530..C-773, 244 consecutive)). **Delivery - NO new suppression in this window:** 300-run window **14700(Sun 11:06:18)->14999(Mon 02:01:16) id-contiguous 300/300, 0 id-gaps, 0 status=error runs, 0 lease-hold, 0 inter-run timing gap >15min** (cron 180 + reconcile_cron 90 + crypto_cron 30 = 300; 0 errors). Crypto :07/:37 cadence **EXACT 30/30** (15 on :07 + 15 on :37, all CRYPTO_DISABLED_BY_CONFIG fail-closed), fresh through **14991@01:37:11**. **Reconcile */10 EXACT 90** all `ok` (MAINTENANCE_ONLY), minute mod10 exact, max step 10.3 min, fresh through **14998@02:00:31 ok**. **Daytrading 5-min cadence: whole-window daytrading-trigger check (180 runs) max step 5.03 min - ZERO gaps >6min = NO new suppression anywhere** (crons `skipped` = market_open=false MARKET_CLOSED, correct closed-market behavior). **Swing: 0 runs in window** - CORRECT (swing schedule `0 22 * * 2-6` Mon-Fri; next fire **Mon Sep 21 22:00z = 6th post-redeploy cadence test = escalation predicate gate**). **Live schedules: all four registered** (daytrading `1-59/5 * * * *`, swing `0 22 * * 2-6`, crypto `7-59/30 * * * *`, reconcile `*/10 * * * *`) - run-log cadence authoritative and confirmed contiguous through this window. **Trade lifecycle: 50/50 filled (daytrading), 0 pending, 0 canceled/expired/failed/replaced, 0 null-strategy**, conservative `filled_lot_exact_unavailable` accounting 50/50 (gross/fee/net null-by-design, fee_attribution none-recorded conservative), **net = gross - fee consistent, 0 inconsistencies**, broker lifecycle timestamps present (submitted_at / broker_updated_at / last_reconciled_at / filled_qty / leaves_qty / client_order_id), FIFO lot-matching item-5 remains queued. **Filtered-run observability WORKING**: 300/300 runs carry structured skip entries (MAINTENANCE_ONLY reconcile / MARKET_CLOSED daytrading / RECONCILIATION_DEFERRED_TO_MAINTENANCE / CRYPTO_DISABLED_BY_CONFIG / EQUITY_DIRECTION_FALLBACK) + analyzed_candidates/filtered_candidates fields - all benign and explainable on a closed market, none unexplained; per-trigger filtered GET (reconcile_cron/crypto_cron) returns correctly. **Equity-direction fallback ACTIVE**: account equity $97,153.50 > last_equity $97,135.8064 = positive signed delta, EQUITY_DIRECTION_FALLBACK skip-code confirms the fallback path. **Version-surface note**: top-level release_version = **2.8.1** (/health + /api/config + src/version.ts); nested config.version = 2.7.0 = documented D1-seed separation, NOT a defect. Reconciliation is lease-protected read-only; **this control issued ONLY GET calls (no trigger/submit/cancel/close/replace/retry/migration/broker-mutating endpoint)**. Validation: typecheck `bun run typecheck` exit **0**, `git diff --check` clean, `git log 2ea5e9d..HEAD -- src/` empty (src lineage clean), RELEASE_VERSION 2.8.1, **263 tests / 969 assertions PASS** (`bun test`, 0 fail). Evidence /workspace/control773-evidence-20260921T020000Z/ (health/config/dashboard/positions/runs/runs_300/runs_60/trades/account + runs_300 id-contiguous). Docs identify deployed src commit **2ea5e9d** (44 mentions each in README/OPERATIONS/RUNBOOK) + RELEASE_VERSION 2.8.1. **FINDINGS: none new - HEALTHY** (the ONLY historical observation remains the already-escalated C-739-flagged isolated daytrading 20-min slot-miss, self-healed 0 impact; NO code/config defect - NO correction, NO deploy needed). NEXT: Mon Sep 21 ~13:30z US open - min_confidence 0.8 rejections must read "below minimum 0.8" (C-726 verification) and MSTR-class 0.70-0.79 approvals must cease; Mon 22:00z swing fire = **6th post-redeploy cadence test = escalation predicate gate**; ANY further suppression/slot-miss recurrence = ESCALATE to Joachim as worsening CF platform instability. Pending Joachim: D1 paid-tier, daytrading cap fill-drift broker-qty re-check (2.7.x), FIFO item-5, **and the OPEN push-blocker from C-750: origin/main is STILL FORK-DIVERGED (remote main 1b4cfa1 / at C-736, missing deployed src 2ea5e9d; local HEAD c1ef7d4 ahead; non-fast-forward). Reconciliation (authoritative line / force-push ok) = Joachim decision, NOT done autonomously; C-773 docs committed locally only.** C-773 docs + evidence entry (this control).
