## Wednesday, September 2, 2026 Control-292 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; /health ok 2.6.0 /api/config OK caps 5000/3700/2000 broker_ledger_synced_until 04:00:41 UTC Sep 2 fee telemetry stale Aug 19 cryptoFeeAsOf 2026-08-19T02:47:31Z unavailable last_prune 2026-09-02 /api/dashboard OK 886 trades equity $97,874.16 -$284.99/-0.29% today cash $85,385.40 long MV $12,488.76 23 positions ACTIVE not PDT trading_blocked false /api/positions OK 23 broker-authoritative metadata_source=d1 all 23 20 swing cost $8,052.83 MV $7,739.16 pre-existing + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,751.94 UNDER $5k cap 0 crypto $0 fail-closed /api/runs OK contiguous 5990-6089 0 gaps 0 leases 0 errors reconcile durations 9685-15329ms avg 12369ms WATCH /api/trades OK 886 filled all gross/fee/net null filled_lot_exact_unavailable none-recorded conservative; 4 CF schedules verified LIVE via CF API all modified 2026-08-31T22:06:50Z daytrading 1-59/5 swing 0 22 * * 2-6 crypto 7-59/30 reconcile */10; crypto edge-gate CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT validTA=0 wired not reached :07/:37 cadence 10 crypto_cron runs all fail-closed; equity direction EQUITY_DIRECTION_FALLBACK observability-only; lease/error skips RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED + MAINTENANCE_ONLY 0 leases; repo HEAD 7c91410 (Control-291 docs) deployed 0b3b2a3 no source diff git diff --check clean 224 tests/845 assertions pass typecheck clean; no defect no code change no deploy; FOLLOW-UP verify D1 reads <5M throughout Sep 2 post-reset healthy 4h+ clean D1 paid-tier needs Joachim decision confirm swing_cron Wed 22:00 UTC DOW=4 first clean 2-6 fire must NOT fire Sun Sep 6 DOW=1 fee telemetry + crypto bar freshness reconcile duration WATCH ~12s; caps 5000/3700/2000 unchanged)

Control-292 at ~04:00 UTC Sep 2 (~06:00 +02). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy, no code change. D1 post-reset healthy (4th hour of clean operation, all endpoints 200 with real data, Control-283 read-budget optimizations holding). broker_ledger_synced_until 2026-09-02T04:00:41Z (fresh, updating every 10min via reconcile_cron). 23 positions broker-authoritative (metadata_source=d1 for all 23: quantities/prices/MV from broker, strategy/stops metadata from D1): 20 swing cost $8,052.83 MV $7,739.16 (pre-existing bypass era, no new entries) + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,751.94 UNDER $5k cap, 0 crypto $0 fail-closed. Equity $97,874.16 -$284.99 (-0.29%) today, ACTIVE not PDT, trading_blocked false. Run-log 5990-6089 (100 runs) contiguous 0 gaps 0 leases 0 errors. Reconcile durations 9685-15329ms avg 12369ms (15329ms is post-reset cold-start run 6009, subsequent runs ~12s) WATCH. Crypto :07/:37 cadence verified (10 crypto_cron runs, all fail-closed: CRYPTO_BARS_STALE + CRYPTO_BARS_UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT validTA=0<3, edge gate wired not reached). Fee telemetry stale (cryptoFeeAsOf 2026-08-19T02:47:31Z, cryptoFeeTelemetryStatus=unavailable). Equity direction EQUITY_DIRECTION_FALLBACK observability-only. 4 CF schedules verified LIVE via Cloudflare API (all modified 2026-08-31T22:06:50Z): daytrading 1-59/5, swing 0 22 * * 2-6 (Mon-Fri), crypto 7-59/30 (:07/:37), reconcile */10. Repo HEAD 7c91410 (Control-291 docs commit), deployed 0b3b2a3 (Control-283); git diff 0b3b2a3 HEAD -- src/ empty, git diff --check clean, 224 tests/845 assertions pass, typecheck clean. No defect, no code change, no deploy. FOLLOW-UP: verify D1 reads stay <5M throughout Sep 2 (post-reset healthy 4h+ now); D1 paid-tier upgrade needs Joachim decision (separate from Workers Paid); confirm swing_cron Wed 22:00 UTC (DOW=4) outcome (first clean 2-6 fire if D1 healthy, must NOT fire Sun Sep 6 DOW=1); fee telemetry + crypto bar freshness; reconcile duration WATCH (~12s plateau). Caps 5000/3700/2000 unchanged.

## Wednesday, September 2, 2026 Control-291 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; /health ok 2.6.0 /api/config OK caps 5000/3700/2000 broker_ledger_synced_until 03:00:41 UTC Sep 2 fee telemetry stale Aug 19 cryptoFeeAsOf 2026-08-19T02:47:31Z unavailable last_prune 2026-09-02 /api/dashboard OK 886 trades equity $97,891.02 -$268.13/-0.27% today cash $85,385.40 long MV $12,505.62 23 positions ACTIVE not PDT trading_blocked false /api/positions OK 23 broker-authoritative metadata_source=d1 all 23 20 swing cost $8,052.83 MV $7,742.19 pre-existing + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,763.38 UNDER $5k cap 0 crypto $0 fail-closed /api/runs OK contiguous 5970-6069 0 gaps 0 leases 0 errors reconcile durations ~12s 12103-12588ms WATCH /api/trades OK 886 filled all gross/fee/net null filled_lot_exact_unavailable none-recorded conservative; 4 CF schedules verified LIVE via CF API all modified 2026-08-31T22:06:50Z daytrading 1-59/5 swing 0 22 * * 2-6 crypto 7-59/30 reconcile */10; crypto edge-gate CRYPTO_BARS_STALE LINKUSD/SOLUSD/ETHUSD ~79-80k s + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_DATA_INSUFFICIENT validTA=0 wired not reached :07/:37 cadence 10 crypto_cron runs all fail-closed; equity direction EQUITY_DIRECTION_FALLBACK observability-only; lease/error skips RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED + MAINTENANCE_ONLY 0 leases; repo HEAD 0539954 (Control-290 docs) deployed 0b3b2a3 no source diff git diff --check clean 224 tests/845 assertions pass typecheck clean; no defect no code change no deploy; FOLLOW-UP verify D1 reads <5M throughout Sep 2 post-reset healthy 3h clean D1 paid-tier needs Joachim decision confirm swing_cron Wed 22:00 UTC DOW=4 first clean 2-6 fire must NOT fire Sun Sep 6 DOW=1 fee telemetry + crypto bar freshness reconcile duration WATCH ~12s; caps 5000/3700/2000 unchanged)

Control-291 at ~03:00 UTC Sep 2 (~05:00 +02). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy, no code change. D1 post-reset healthy (3rd hour of clean operation, all endpoints 200 with real data). broker_ledger_synced_until 2026-09-02T03:00:41Z (fresh, updating every 10min via reconcile_cron). 23 positions broker-authoritative (metadata_source=d1 for all 23: quantities/prices/MV from broker, strategy/stops metadata from D1): 20 swing cost $8,052.83 MV $7,742.19 (pre-existing bypass era, no new entries) + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,763.38 UNDER $5k cap, 0 crypto $0 fail-closed. Equity $97,891.02 -$268.13 (-0.27%) today, ACTIVE not PDT, trading_blocked false. Run-log 5970-6069 (100 runs) contiguous 0 gaps 0 leases 0 errors (Sep 1 D1 blockage error runs 5963/5964 scrolled out of view). Reconcile durations ~12s (12103-12588ms) — WATCH (elevated but below 15s threshold). Crypto :07/:37 cadence verified (10 crypto_cron runs, all fail-closed: CRYPTO_BARS_STALE symbol rotation LINKUSD/SOLUSD/ETHUSD ~79-80k s stale + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_DATA_INSUFFICIENT validTA=0<3, edge gate wired not reached). Fee telemetry stale (cryptoFeeAsOf 2026-08-19T02:47:31Z, cryptoFeeTelemetryStatus=unavailable). Equity direction EQUITY_DIRECTION_FALLBACK observability-only. 4 CF schedules verified LIVE via Cloudflare API (all modified 2026-08-31T22:06:50Z): daytrading 1-59/5, swing 0 22 * * 2-6 (Mon-Fri), crypto 7-59/30 (:07/:37), reconcile */10. Repo HEAD 0539954 (Control-290 docs commit), deployed 0b3b2a3 (Control-283); git diff --check clean, 224 tests/845 assertions pass, typecheck clean. No defect, no code change, no deploy. FOLLOW-UP: verify D1 reads stay <5M throughout Sep 2 (post-reset healthy 3h now); D1 paid-tier upgrade needs Joachim decision (separate from Workers Paid); confirm swing_cron Wed 22:00 UTC (DOW=4) outcome (first clean 2-6 fire if D1 healthy, must NOT fire Sun Sep 6 DOW=1); fee telemetry + crypto bar freshness; reconcile duration WATCH (~12s plateau). Caps 5000/3700/2000 unchanged.


## Wednesday, September 2, 2026 Control-290 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; /health ok 2.6.0 /api/config OK caps 5000/3700/2000 broker_ledger_synced_until 02:00:41 UTC Sep 2 fee telemetry stale Aug 19 last_prune 2026-09-02 /api/dashboard OK 886 trades equity $97,881.32 -$277.83/-0.28% today cash $85,385.40 long MV $12,495.92 23 positions ACTIVE not PDT trading_blocked false /api/positions OK 23 broker-authoritative metadata_source=d1 20 swing cost $8,052.83 MV $7,741.37 pre-existing + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,754.55 UNDER $5k cap 0 crypto $0 /api/runs OK contiguous 5950-6049 0 gaps 0 leases 2 error runs 5963 subrequest spike + 5964 QCOM qty mismatch both Sep 1 D1 blockage window known patterns recovered reconcile durations ~12s 12044-12354ms WATCH /api/trades OK 886 filled all gross/fee/net null filled_lot_exact_unavailable none-recorded conservative; 4 CF schedules daytrading 1-59/5 swing 0 22 * * 2-6 crypto 7-59/30 reconcile */10 wrangler.toml; crypto edge-gate CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT validTA=0 wired not reached :07/:37 cadence 10 crypto_cron runs all fail-closed; equity direction EQUITY_DIRECTION_FALLBACK observability-only; lease/error skips RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED + MAINTENANCE_ONLY 0 leases; repo HEAD bb8d850 (Control-289 docs) deployed 0b3b2a3 no source diff git diff --stat 0b3b2a3..HEAD -- src/ wrangler.toml package.json schema.sql empty git diff --check clean 224 tests/845 assertions pass typecheck clean; no defect no code change no deploy; FOLLOW-UP verify D1 reads <5M throughout Sep 2 post-reset healthy 2h clean D1 paid-tier needs Joachim decision confirm swing_cron Wed 22:00 UTC DOW=4 first clean 2-6 fire must NOT fire Sun Sep 6 DOW=1 fee telemetry + crypto bar freshness reconcile duration WATCH ~12s; caps 5000/3700/2000 unchanged)

Control-290 at ~02:00 UTC Sep 2 (~04:00 +02). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy, no code change. D1 post-reset healthy (2nd hour of clean operation, all endpoints 200 with real data). broker_ledger_synced_until 2026-09-02T02:00:41Z (fresh, updating every 10min via reconcile_cron). 23 positions broker-authoritative (metadata_source=d1 for all 23: quantities/prices/MV from broker, strategy/stops metadata from D1): 20 swing cost $8,052.83 MV $7,741.37 (pre-existing bypass era, no new entries) + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,754.55 UNDER $5k cap, 0 crypto $0 fail-closed. Equity $97,881.32 -$277.83 (-0.28%) today, ACTIVE not PDT, trading_blocked false. Run-log 5950-6049 (100 runs) contiguous 0 gaps 0 leases; 2 error-status runs: 5963 (Sep 1 16:41:58 UTC, subrequest spike 16173ms, known Cloudflare platform issue) + 5964 (Sep 1 16:46:53 UTC, QCOM qty mismatch internal 0.01 vs broker 5.0, broker-authoritative quantity persisted, safety mechanism), both from Sep 1 D1 blockage window, both recovered, NOT new defects. Reconcile durations ~12s (12044-12354ms) across 5 reconcile_cron runs — WATCH (elevated but below 15s threshold). Crypto :07/:37 cadence verified (10 crypto_cron runs 5951-6042, all fail-closed: CRYPTO_BARS_STALE + CRYPTO_BARS_UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate wired not reached). Fee telemetry stale (cryptoFeeAsOf 2026-08-19T02:47:31Z, cryptoFeeTelemetryStatus=unavailable). 4 CF schedules in wrangler.toml: daytrading 1-59/5, swing 0 22 * * 2-6 (Mon-Fri), crypto 7-59/30 (:07/:37), reconcile */10; prior controls verified LIVE via CF API all modified 2026-08-31T22:06:50Z. Repo HEAD bb8d850 (Control-289 docs commit), deployed 0b3b2a3 (Control-283); git diff --stat 0b3b2a3..HEAD -- src/ wrangler.toml package.json schema.sql EMPTY, git diff --check clean, 224 tests/845 assertions pass, typecheck clean. No defect, no code change, no deploy. FOLLOW-UP: verify D1 reads stay <5M throughout Sep 2 (post-reset healthy 2h now); D1 paid-tier upgrade needs Joachim decision (separate from Workers Paid); confirm swing_cron Wed 22:00 UTC (DOW=4) outcome (first clean 2-6 fire if D1 healthy, must NOT fire Sun Sep 6 DOW=1); fee telemetry + crypto bar freshness; reconcile duration WATCH (~12s plateau). Caps 5000/3700/2000 unchanged.

## Wednesday, September 2, 2026 Control-289 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; /health ok 2.6.0 /api/config OK caps 5000/3700/2000 broker_ledger_synced_until 01:00:42 UTC Sep 2 fee telemetry stale Aug 19 last_prune 2026-09-02 /api/dashboard OK 886 trades equity $97,957.20 -$201.95/-0.2057% today cash $85,385.40 long MV $12,571.80 23 positions ACTIVE not PDT trading_blocked false /api/positions OK 23 broker-authoritative source=alpaca observed 01:00:46Z 20 swing cost ~$8,053 pre-existing + 3 daytrading NIO/NOW/SOFI cost $4,875.28 UNDER $5k cap 0 crypto $0 /api/runs OK contiguous 6019-6030 0 gaps 0 leases 0 errors reconcile_cron every 10min ok daytrading cron every 5min MARKET_CLOSED crypto_cron 00:37 fail-closed /api/trades OK 886 filled all daytrading lifecycle submitted->filled->broker_updated->reconciled gross/fee/net all null filled_lot_exact_unavailable none-recorded conservative; 4 CF schedules daytrading 1-59/5 swing 0 22 * * 2-6 crypto 7-59/30 reconcile */10; crypto edge-gate CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT wired not reached :07/:37 cadence; equity direction EQUITY_DIRECTION_FALLBACK observability-only; lease/error skips RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED + MAINTENANCE_ONLY 0 leases; repo HEAD d75d979 deployed 0b3b2a3 no source diff 224 tests/845 assertions pass typecheck clean; docs HEAD Control-288 references local HEAD 2f10b35 current HEAD d75d979 is Control-288 docs commit itself docs-only; no defect no code change no deploy; FOLLOW-UP verify D1 reads <5M throughout Sep 2 D1 paid-tier needs Joachim decision confirm swing_cron Wed 22:00 DOW=4 fee/crypto freshness reconcile duration WATCH; caps 5000/3700/2000 unchanged)

Control-289 at ~01:02 UTC Sep 2 (~03:02 +02). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy, no code change. D1 post-reset healthy: all endpoints return real data, run-log advancing (6019-6030 contiguous, 0 gaps/leases/errors). broker_ledger_synced_until 2026-09-02T01:00:42Z (fresh). 23 positions broker-authoritative (source=alpaca, current_state_observed 01:00:46Z; metadata_source=d1 2026-09-01 18:56:53 stale-by-design): 20 swing cost ~$8,053 (pre-existing bypass era, no new entries, SWING_OWNED_EXCLUDE exercised) + 3 daytrading NIO/NOW/SOFI cost $4,875.28 UNDER $5k cap, 0 crypto $0 fail-closed. Equity $97,957.20 -$201.95 (-0.2057%) today, ACTIVE not PDT, trading_blocked false. Crypto :07/:37 fail-closed at bar gate (ETHUSD stale ~78756s, MATICUSD empty, validTA=0<3, edge gate wired not reached). Equity direction EQUITY_DIRECTION_FALLBACK observability-only (broker change_today_pct=0, fallback -0.2187%, risk controls not weakened). Reconciliation read-only/deferred (RECONCILIATION_DEFERRED_TO_MAINTENANCE + MAINTENANCE_ONLY), 0 leases, 0 errors. Trade lifecycle submitted->filled->broker_updated->reconciled all present; gross/fee/net all null, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded (conservative unattributed). Fee telemetry stale (Aug 19). 4 CF schedules in wrangler.toml: daytrading 1-59/5, swing 0 22 * * 2-6 (Mon-Fri Vixie DOW 2-6), crypto 7-59/30 (:07/:37), reconcile */10; prior controls verified LIVE via CF API all modified 2026-08-31T22:06:50Z. Repo HEAD d75d979 (Control-288 docs commit), deployed 0b3b2a3 (Control-283); git diff --stat 0b3b2a3..HEAD -- src/ wrangler.toml package.json schema.sql EMPTY, git diff --check clean, 224 tests/845 assertions pass, typecheck clean. Docs HEAD: Control-288 entry references local HEAD 2f10b35 (HEAD observed at control time); current HEAD d75d979 is the Control-288 docs commit itself (docs-only, no source diff); deployed 0b3b2a3 correctly identified throughout. No defect, no code change, no deploy. FOLLOW-UP: verify D1 reads stay <5M throughout Sep 2 (post-reset healthy now); D1 paid-tier upgrade needs Joachim decision; confirm swing_cron Wed 22:00 UTC (DOW=4) outcome (first clean 2-6 fire if D1 healthy, must NOT fire Sun Sep 6 DOW=1); fee telemetry + crypto bar freshness; reconcile duration WATCH. Caps 5000/3700/2000 unchanged.

## Wednesday, September 2, 2026 Control-288 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, D1 RESET CONFIRMED all endpoints back; /health ok 2.6.0 /api/config OK caps 5000/3700/2000 broker_ledger_synced_until 00:00:43 UTC fee telemetry stale Aug 19 /api/dashboard OK 886 trades equity $97,930.53 -$228.62/-0.23% today long MV $12,545.13 /api/positions OK 23 broker-authoritative 20 swing cost $8,053 + 3 daytrading cost $4,875 UNDER $5k cap 0 crypto $0 /api/runs OK post-reset runs 6009 reconcile 00:00:45 ok + 6010 daytrading 00:01:31 MARKET_CLOSED /api/trades OK 886 filled; 4 CF schedules verified LIVE; swing_cron 22:00 UTC Sep 1 CONFIRMED BLOCKED by D1 next real fire Wed 22:00 DOW=4; crypto :07/:37 fail-closed edge gate wired not reached; deployed 0b3b2a3 local HEAD 2f10b35 no source diff; 224 tests/845 assertions pass typecheck clean; FOLLOW-UP verify D1 reads <5M throughout Sep 2 D1 paid-tier needs Joachim decision confirm swing_cron Wed 22:00; caps 5000/3700/2000 unchanged)

Control-288 at ~00:00 UTC Sep 2 (~02:00 +02). Strict GET-only on 6 endpoints + /api/account (all 200). D1 free-tier daily quota RESET CONFIRMED at 00:00 UTC — all D1-dependent endpoints return 200 with real data for first time since ~16:31 UTC Sep 1. Post-reset crons landing: run 6009 (reconcile_cron 00:00:45 UTC status=ok dur=15329ms broker_ledger_synced_until updated to 00:00:43 UTC Sep 2), run 6010 (daytrading 00:01:31 status=skipped MARKET_CLOSED nextOpen Wed 13:30 UTC). Run-log 5981-6010 contiguous 0 gaps. Equity $97,930.53 -$228.62 (-0.23%) today ACTIVE not PDT cash $85,385.40 long MV $12,545.13. 23 positions broker-authoritative: 20 swing cost $8,052.82 (pre-existing bypass era), 3 daytrading NIO/NOW/SOFI cost $4,875.28 UNDER $5k cap, 0 crypto $0 fail-closed. Crypto :07/:37 fail-closed CRYPTO_BARS_STALE/UNAVAILABLE edge gate wired not reached. Fee telemetry stale Aug 19. SWING_OWNED_EXCLUDE exercised (COIN/DDOG). NO_ENTRY_RISK exercised. 4 CF schedules verified LIVE (all modified 2026-08-31T22:06:50Z). swing_cron 22:00 UTC Sep 1 (Tue DOW=3) CONFIRMED BLOCKED by D1 no swing run between 6008 and 6009. Next real fire Wed 22:00 UTC (DOW=4). Deployed 0b3b2a3 (Control-283), local HEAD 2f10b35 docs-only, no source diff. 224 tests/845 assertions pass, typecheck clean, git diff --check clean. No deploy no code change. FOLLOW-UP: verify D1 reads <5M throughout Sep 2; D1 paid-tier upgrade needs Joachim decision; confirm swing_cron Wed 22:00 outcome; fee/crypto freshness; reconcile duration WATCH (6009 15329ms). Caps 5000/3700/2000 unchanged.

## Wednesday, September 2, 2026 Control-287 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external D1 free-tier limits still exceeded ~1h before 00:00 UTC reset, no deploy needed; /health ok /api/account OK equity $97,980.01 -$179.14/-0.18% today long MV $12,594.61 ACTIVE /api/config cached caps 5000/3700/2000 fee telemetry stale Aug 19 ledger synced 19:01 UTC; /api/dashboard+/api/positions+/api/runs+/api/trades ALL D1_ERROR run log frozen 6008/19:01 UTC; /api/runs first attempt returned 6008 then D1_ERROR intermittent near limit boundary; 4 CF schedules verified LIVE all modified 2026-08-31T22:06:50Z; deployed 0b3b2a3 local HEAD 5d29311 no source diff docs-only; 224 tests/845 assertions pass typecheck clean; FOLLOW-UP verify D1 reads <5M post-reset D1 paid-tier needs Joachim decision confirm swing_cron + run-log resume; caps 5000/3700/2000 unchanged)

Control-287 at ~23:00 UTC Sep 1 (~01:00 +02 Sep 2). Strict GET-only on 6 endpoints. /health ok (2.6.0). /api/account OK (broker-direct): equity $97,980.01, change_today -$179.14 (-0.18%), long MV $12,594.61, ACTIVE not PDT, trading_blocked false. /api/config OK (cached, caps 5000/3700/2000, fee telemetry stale Aug 19, broker_ledger_synced_until 2026-09-01T19:01:01Z). /api/dashboard+/api/positions+/api/runs+/api/trades ALL D1_ERROR (limits exceeded since ~16:31 UTC Sep 1, ~1h before 00:00 reset). /api/runs first attempt returned 6008 then D1_ERROR — intermittent near boundary, not recovered. Run log frozen at 6008/19:01 UTC. Broker-direct long MV $12,594.61 confirms positions intact. 4 CF schedules verified LIVE (all modified 2026-08-31T22:06:50Z): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. No eighth dispatch failure observable (~35h post-Control-257, masked by D1 block). swing_cron 22:00 UTC Tue DOW=3 likely blocked; next real fire Wed 22:00 DOW=4. Deployed 0b3b2a3, local HEAD 5d29311, no source diff. 224 tests/845 assertions pass, typecheck clean, git diff --check clean. No deploy, no code change. 13 assistant schedules verified (all enabled ok). FOLLOW-UP: verify D1 reads <5M post-reset; D1 paid-tier upgrade needs Joachim decision; confirm swing_cron outcome + run-log resume.

## Tuesday, September 1, 2026 Control-284 strict read-only control

Control-284 at ~21:00 UTC Sep 1 (~23:00 +02). Strict GET-only on 6 endpoints. /health ok (2.6.0), /api/account OK (broker-direct): equity $97,958.65, change_today -$200.50 (-0.20%), ACTIVE not PDT. /api/config OK (cached, caps 5000/3700/2000). /api/dashboard + /api/positions D1_ERROR. /api/runs partial (offset 0-60 OK, 120+ D1_ERROR). /api/trades OK (20 trades). D1 free-tier read limit exceeded since ~16:31 UTC, write limit likely exhausted by ~19:01 UTC (last run 6008 at 19:01, ~2h gap). Run-log 5889-6008 contiguous, 0 gaps, 0 leases, 5 transient known errors (PLTR/VZ/QCOM qty-mismatch + 2 subrequest spikes, all recovered). Daytrading active (64 market_open runs, trades 867-886 all filled, accounting_status=filled_lot_exact_unavailable, gross/fee/net=None conservative). 4 CF schedules verified live (all modified 2026-08-31T22:06:50Z). Crypto :07/:37 fail-closed at bar gate. Deployed 0b3b2a3 (Control-283), no source diff. 224 tests/845 assertions pass, typecheck clean. swing_cron 22:00 UTC tonight likely blocked by D1 until 00:00 reset. FOLLOW-UP: verify D1 reads <5M post-reset; D1 paid-tier upgrade (Joachim decision); confirm swing_cron outcome; reconcile duration WATCH (avg 11987ms, max 15626ms, no gaps).

## Tuesday, September 1, 2026 Control-282 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)
Control-282 at ~19:00 UTC Sep 1 (~21:00 +02). Market OPEN (daytrading active). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect, no deploy needed. Deployed code fd2309c (Control-255), no source change since. D1 Sep 1 enforcement ~19h clean under active trading. ~31h post-Control-257, NO eighth cron dispatch failure. 4 CF schedules verified LIVE via Cloudflare API (all modified 2026-08-31T22:06:50): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. Run-log contiguous 5899-6008 (110 runs, 0 gaps, 0 leases). 2 transient known error runs: QCOM qty-mismatch (run 5964, safety, recovered) + subrequest spike (run 5963, fd2309c class, isolated). 23 positions broker-authoritative (metadata_source=d1): 20 swing MV $7,727.83 cost $8,052.83 (pre-existing) + 3 daytrading NIO/NOW/SOFI MV $4,865.71 cost $4,875.28 (WELL UNDER $5k cap), 0 crypto/null. Equity $97,974.23, change_today -$184.92 (-0.188%), ACTIVE not PDT. EQUITY_DIRECTION_FALLBACK (observability-only). Crypto cadence :07/:37 verified fail-closed at bar gate. Fee telemetry stale (Aug 19, gross/fee/net all null conservative). WATCH: reconcile durations 9685-15282ms, most over 12s under trading load, NO gaps. 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD 3df2f42 (Control-281, local). Follow-up: 22:00 UTC swing_cron tonight (Tue DOW=3); reconcile duration WATCH; crypto bar + fee freshness.
Control-283 at ~20:10 UTC Sep 1 (~22:10 +02). D1 free-tier daily read limit (5M rows) EXCEEDED at ~16:31 UTC. All D1-dependent endpoints returning D1_ERROR. Only /health and /api/account (broker-direct) functional. D1 analytics: 5.14M rows read Sep 1, 5.88M Aug 31. DEPLOYED code fix (0b3b2a3 via /content PUT): config cache 120s TTL, fee summary refresh throttled to 1h, getTradesNeedingSync limit 200->50, getSwingTradeSymbols limit 200->50, new indexes on trades+positions. No caps changed. 224 tests/845 assertions pass. D1 resets at 00:00 UTC Sep 2. Follow-up: verify D1 reads under 5M post-reset; investigate D1 paid-tier upgrade; swing_cron 22:00 UTC tonight may be blocked until reset.



## Tuesday, September 1, 2026 Control-281 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)
Control-281 at ~18:05 UTC Sep 1 (~20:05 +02). Market OPEN (daytrading active). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Deployed code fd2309c (Control-255), no source change since (git diff fd2309c..d28cf80 -- src/ wrangler.toml package.json schema.sql empty). D1 Sep 1 enforcement ~18h clean under active trading. ~30h post-Control-257, NO eighth cron dispatch failure. 4 CF schedules verified LIVE via Cloudflare API (schedules endpoint, all modified 2026-08-31T22:06:50): daytrading 1-59/5 * * * *, swing 0 22 * * 2-6, crypto 7-59/30 * * * *, reconcile */10 * * * *. Run-log contiguous 5959-5988 (30 runs, 0 gaps, 0 leases; triggers cron 18/reconcile_cron 9/crypto_cron 3). 2 transient known error runs: QCOM qty-mismatch (run 5964, safety, broker-authoritative, recovered) + subrequest spike (run 5963, fd2309c class, isolated). Both recovered, NOT defects. 22 positions broker-authoritative (metadata_source=d1): 20 swing MV $7,714.04 cost $8,052.83 (pre-existing bypass era) + 2 daytrading BBD/SOFI MV $2,004.68 cost $2,007.07, 0 crypto/null. Daytrading cost $2,007.07 WELL UNDER $5,000 cap (prior overshoot FULLY RESOLVED). Equity $97,978.02 (account portfolio_value/equity live), change_today -$181.13 (-0.185%), last_equity $98,159.15, ACTIVE not PDT, trading_blocked false. Equity direction: broker change_today_pct=0 (zero/unavailable), EQUITY_DIRECTION_FALLBACK to equity_delta_fallback for observability only, risk controls not weakened. Crypto cadence :07/:37 verified fail-closed at bar gate (ETHUSD stale 78765s ~21.9h, MATICUSD empty validTA=0, CRYPTO_DATA_INSUFFICIENT, edge gate not reached, producer wired). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, cached_fee_summary totalUsd $272.82 unattributed). Conservative gross/fee/net: 48 filled trades accounting_status=filled_lot_exact_unavailable fee_attribution=none-recorded (gross/fee/net all null, unattributed rather than forced), 2 pending_new no_fill. Trade lifecycle: submitted_at -> filled_at -> broker_updated_at -> last_reconciled_at all present on filled trades. SWING_OWNED_EXCLUDE exercised (RIVN). NO_ENTRY_RISK exercised (SHOP/SOFI confidence 0.65 < 0.7). DECISION_HOLD exercised. RECONCILIATION_DEFERRED_TO_MAINTENANCE exercised (daytrading + crypto). WATCH: reconcile durations 11607-15282ms, 6/9 over 12s, avg ~13.2s under trading load, NO gaps (matches Aug 31 pressure pattern). 224 tests/845 assertions pass, typecheck clean (bunx tsc --noEmit), git diff --check clean. Repo HEAD d28cf80 (Control-280, local). Docs HEAD: this commit (Control-281). Follow-up: 22:00 UTC swing_cron tonight (Tue DOW=3); reconcile duration pressure WATCH; crypto bar + fee freshness.
**Tuesday, September 1, 2026 Control-279 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~17h clean under active trading; ~28h post-Control-257 no eighth cron dispatch failure; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5819-5967 149 runs 0 gaps 0 leases 5 transient known error runs; 22 positions 20 swing MV $7,715 cost $8,053 + 2 daytrading MV $1,820 cost $1,818 WELL UNDER $5k cap overshoot RESOLVED; equity $97,999 -$159 today; reconcile 7/9 over 12s avg ~13.2s no gaps; crypto fail-closed; fee telemetry stale; caps 5000/3700/2000 unchanged; 224 tests/845 assertions pass typecheck clean; local HEAD e963607 deployed fd2309c no source diff)**

Control-279 at ~17:00 UTC Sep 1 (Tuesday 19:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No code defect found, no deploy needed. 4 CF schedules verified live (all modified Control-257): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. ~28h post-Control-257, no eighth dispatch failure. D1 Sep 1 enforcement ~17h clean. Run-log 5819-5967 contiguous (149 runs), 0 gaps, 0 leases. 5 transient known errors: PLTR/VZ/QCOM POSITION_QTY_MISMATCH (safety, recovered) + 2 subrequest spikes (fd2309c class). WATCH: reconcile durations 10489-15591ms, 7/9 over 12s, avg ~13.2s under trading load, no gaps. 22 positions: 20 swing MV $7,715 cost $8,053 (pre-existing) + 2 daytrading MV $1,820 cost $1,818 (WELL UNDER $5k cap, overshoot RESOLVED). 0 crypto/null, all broker-authoritative. Equity $97,999 -$159 today. Crypto fail-closed at bar gate. Fee telemetry stale. Caps 5000/3700/2000 unchanged. Local HEAD e963607, deployed fd2309c, no source diff. 224 tests/845 assertions pass, typecheck clean. swing_cron next fire 22:00 UTC tonight (Tue DOW=3).

**Tuesday, September 1, 2026 Control-278 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~16h clean under active trading; ~26h post-Control-257 no eighth cron dispatch failure; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5848-5947 0 gaps 0 leases 3 transient known error runs; 25 positions 20 swing MV $7,746 cost $8,053 + 5 daytrading MV $5,001 cost $5,005 $5 OVER $5k cap watch 0 crypto/null all broker-authoritative; equity $98,069 -$91 today; reconcile avg 10509ms but CLIMBING last hour 4/5 runs over 12s max 15626ms under trading load no gaps; crypto cadence :07/:37 verified fail-closed; SWING_OWNED_EXCLUDE exercised AAL/NKE; fee telemetry stale Aug 19; caps 5000/3700/2000 unchanged; 224 tests/845 assertions pass typecheck clean; local HEAD d992d4c deployed fd2309c no source diff)**

Control-278 at ~16:00 UTC Sep 1 (Tuesday 18:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No code defect, no deploy needed. 4 CF schedules verified live (all modified Control-257): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. ~26h post-Control-257, no eighth dispatch failure. D1 Sep 1 enforcement ~16h clean. Run-log 5848-5947 contiguous, 0 gaps, 0 leases. 3 transient known errors: PLTR/VZ POSITION_QTY_MISMATCH (safety, recovered) + 1 subrequest spike (fd2309c class). WATCH: reconcile durations climbing to 14-15.6s under trading load (4/5 last runs over 12s), no gaps resulted. 25 positions: 20 swing MV $7,746 + 5 daytrading MV $5,001 cost $5,005 ($5 over cap, watch). 0 crypto/null, all broker-authoritative. Equity $98,069 -$91 today. Crypto fail-closed at bar gate. Fee telemetry stale. Caps 5000/3700/2000 unchanged. Local HEAD d992d4c, deployed fd2309c, no source diff. 224 tests/845 assertions pass, typecheck clean. swing_cron next fire 22:00 UTC tonight (Tue DOW=3).

**Tuesday, September 1, 2026 Control-276 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; DOCS CORRECTION: deployed code is fd2309c Control-255 NOT bd6205e Control-241 as prior docs stated; D1 Sep 1 enforcement ~15h clean under active trading; ~23h post-Control-257 no eighth cron dispatch failure; run-log contiguous 5728-5927 0 gaps 2 POSITION_QTY_MISMATCH safety events PLTR/VZ recovered next cycle; 25 positions 20 swing MV $7,732 + 5 daytrading MV $4,978 cost_basis $4,988 $12 margin under $5k cap 0 crypto/null all broker-authoritative; equity $98,058 -$101 today; reconcile 8584-11760ms avg ~9700ms; crypto cadence :07/:37 verified fail-closed; SWING_OWNED_EXCLUDE exercised AAL; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-276 at ~15:00 UTC Sep 1 (Tuesday 17:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No code defect found, no deploy needed.

DOCS CORRECTION: Prior control entries (Control-255 through Control-275) identified the deployed source commit as bd6205e (Control-241, swing cron DOW fix). The actual deployed code is fd2309c (Control-255, Worker subrequest limit crash fix during EOD flatten). bd6205e is an ancestor of fd2309c; both are included in the deployed bundle. git diff --stat fd2309c HEAD -- src/ is empty (no source changes since Control-255). The mislabel did not affect any operational decision (all controls correctly noted "no code change" and "no deploy needed"), but the exact commit hash in docs was wrong. Corrected going forward.

D1 Sep 1 enforcement ~15h clean: all runs 00:00-15:00 UTC Sep 1 survived D1 enforcement under active weekday trading load. First weekday open at 13:30 UTC PASSED (confirmed at Control-275). ~23h post-Control-257 with no eighth cron dispatch failure.

Run-log contiguous 5728-5927 (200 runs, 0 gaps). 2 error-status runs: 5909 (14:01 UTC, PLTR qty mismatch internal 9 vs broker 10) and 5920 (14:36 UTC, VZ qty mismatch internal 16.47 vs broker 17.47). Both are POSITION_QTY_MISMATCH safety events - transient D1-vs-broker lag during rapid daytrading fills. Broker-authoritative quantities persisted, new BUY entries blocked for those cycles, risk-reducing exits remained eligible. Both self-resolved on next reconcile (run 5910 recovered to skipped/0 errors, run 5922 reconcile_cron ok/0 errors). Safety mechanism working as designed, NOT a defect. 0 CYCLE_LEASE_HELD runs.

All 3 active schedules dispatching clean: daytrading cron every 5 min (120 runs, market_open=1 after 13:30), reconcile_cron ok every 10 min (60 runs all status=ok), crypto_cron :07/:37 skip (20 runs all on cadence). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). Reconcile durations 8584-11760ms avg ~9700ms, one run at 11760ms close to 12s under trading load, WATCH.

25 positions: 20 swing MV $7,731.64 + 5 daytrading MV $4,978.23 (PLTR 15 shares $2,758, T 48 shares $1,250, VZ 17.53 shares $882, MSTR 0.57 shares $73, FCX 0.25 shares $19), 0 crypto/null. All broker-authoritative (metadata_source=d1, source=alpaca). Daytrading cost_basis $4,987.88, only $12.12 margin under $5,000 cap (CAPITAL_CAP enforcement active: NO_ENTRY_RISK on NOW "max $0.63 at $145.57" and NVDA "max $0.30 at $217.72"). Swing cost_basis $8,053 > $3,700 cap (pre-existing historical fills, known DEGRADED per cap semantics).

Daytrading active: trades 828-837 (VZ/FCX/MSTR buys, T sell, MU sell, NOW buy pending). SWING_OWNED_EXCLUDE exercised (AAL run 5909). NO_ENTRY_RISK (NOW, NVDA, VZ confidence/risk rejects). DECISION_HOLD (AMZN, NIO, PLTR, VZ). DAYTRADING_BARS_SHORT (BBD building history). EQUITY_DIRECTION_FALLBACK observability-only. RECONCILIATION_DEFERRED_TO_MAINTENANCE.

Crypto edge gate still blocked: CRYPTO_BARS_STALE/UNAVAILABLE on all 20 runs (symbol rotation all stale), fail-closed at bar gate, edge producer wired but not reached. Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. broker_ledger_synced_until 2026-09-01T15:01:04Z. last_prune_date 2026-09-01.

Trade lifecycle: 30 recent trades all daytrading, 0 null-strategy, 29 filled + 1 pending_new. All skip codes structured and traceable. Equity $98,057.84, cash $85,340.31 (87%), long MV $12,717.53, change_today -$101.31 (-0.103%), last_equity $98,159.15. Account ACTIVE, not PDT. 835 total trades, win 10.34%, 7717 decisions. config.version 2.6.0 synced.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-276 (~23h post-Control-257), all 3 active schedules still dispatching clean. No eighth dispatch failure. Caps 5000/3700/2000 unchanged. Local HEAD 1b3a733, GitHub main HEAD a7fcbde. Deployed code fd2309c (corrected from prior docs that said bd6205e). 224 tests/845 assertions pass, typecheck clean.

**Tuesday, September 1, 2026 Control-275 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~14h clean FIRST WEEKDAY OPEN UNDER ENFORCEMENT PASSED; ~21h post-Control-257 all 4 schedules dispatching clean no eighth cron dispatch failure; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5709-5908 0 gaps 0 errors 0 lease holds; 22 positions 20 swing MV $7,718 + 2 daytrading MV $2,824 PLTR/T opened 13:51 UTC 0 crypto/null all broker-authoritative; equity $98,032 -$127.38 today; reconcile 8319-11760ms avg 9202ms one run near 12s under trading load; crypto cadence :07/:37 verified; SWING_OWNED_EXCLUDE exercised AAL/CCL/INTC; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-275 at ~14:00 UTC Sep 1 (Tuesday 16:00 +02). Market OPEN (13:30 UTC weekday open fired ~30min ago). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Local HEAD 5679b7d (Control-274 docs), GitHub main HEAD 9c67800 (Control-273 docs).

D1 Sep 1 enforcement ~14h clean: all runs 00:00-14:00 UTC Sep 1 survived with 0 errors. CRITICAL MILESTONE: first weekday open at 13:30 UTC under D1 enforcement PASSED - daytrading runs executing with market_open=1, placing orders, 0 errors. ~21h post-Control-257 with no eighth cron dispatch failure.

Run-log contiguous 5709-5908 (200 runs, 0 gaps, 0 errors, 0 lease holds). All 4 schedules dispatching clean: daytrading cron every 5 min (120 runs, market_open=1 after 13:30), reconcile_cron ok every 10 min (60 runs all status=ok), crypto_cron :07/:37 skip (20 runs all on cadence). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3). 4 CF schedules verified live via API (all modified 2026-08-31T22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto).

22 positions: 20 swing MV $7,718 + 2 daytrading MV $2,824 (PLTR 10 shares, T 38 shares, opened 2026-09-01 13:51:51 UTC, first daytrading entries since Aug 31), 0 crypto/null. All broker-authoritative (source=alpaca, metadata_source=d1). Daytrading cost_basis $2,827 under $5,000 cap. Equity $98,031.77, cash $87,505.66, long MV $10,526.11, change_today -$127.38 (-0.13%). Account ACTIVE, not PDT. 824 trades, 821 executed, win 11.48%, 7600 decisions. Snapshot 1391 (13:56 UTC). broker_ledger_synced_until 2026-09-01T13:50:49Z. last_prune_date 2026-09-01. config.version 2.6.0 synced.

Daytrading active: runs 5899-5907 firing every 5 min with market_open=1. 3 new orders submitted (trades 822-824: PLTR x2, T x1, status=pending_new). SWING_OWNED_EXCLUDE exercised (AAL, CCL, INTC skipped as swing-owned). NO_ENTRY_RISK skips (TSLA conf 0.65, NIO conf 0.68, PLUG conf 0.67 below min 0.7). DAYTRADING_BARS_UNAVAILABLE (SQ empty). EQUITY_DIRECTION_FALLBACK observability-only. RECONCILIATION_DEFERRED_TO_MAINTENANCE.

Crypto edge gate still blocked: CRYPTO_BARS_STALE/UNAVAILABLE/DATA_INSUFFICIENT (validTA=0), all 20 runs fail-closed at bar gate. Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. Trades 722-824: 0 null-strategy. All skip codes structured and traceable. 0 CYCLE_LEASE_HELD, 0 error runs. Reconcile one run at 11760ms (close to 12s threshold under trading load, watching).

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-275 (~21h post-Control-257), all 4 schedules still dispatching clean. No eighth dispatch failure. Caps 5000/3700/2000 unchanged. Local HEAD 5679b7d, GitHub main HEAD 9c67800.

**Tuesday, September 1, 2026 Control-273 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~12h clean all runs 0 errors; ~19h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5668-5867 0 gaps 0 errors 0 lease holds; 20 swing MV $7,738 0 daytrading/crypto/null; equity $98,070 -$89.34; reconcile durations 8308-10559ms avg 9028ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-273 at ~12:00 UTC Sep 1 (Tuesday 14:00 +02). Market CLOSED (next open 13:30 UTC today, ~1.5h away). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Local HEAD 9ec340c, GitHub main HEAD 5a7a7623 (Control-271 docs).

D1 Sep 1 enforcement ~12h clean: all runs 00:00-12:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8308-10559ms avg 9028ms under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load (~1.5h away at this check). ~19h post-Control-257 with no eighth cron dispatch failure.

Run-log contiguous 5668-5867 (200 runs, 0 gaps, 0 errors, 0 lease holds). All 3 active schedules dispatching clean: daytrading cron MARKET_CLOSED skip every 5 min (120 runs), reconcile_cron ok every 10 min (60 runs all status=ok), crypto_cron :07/:37 skip (20 runs all on cadence). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 2026-08-31T22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto). Swing DOW 2-6 confirmed.

20 swing positions MV $7,737.68, all strategy=swing, metadata_source=d1 (broker-authoritative), 0 daytrading/crypto/null positions. Equity $98,069.81, cash $90,332.28 (92.1%), buying_power $382,637, change_today -$89.34 (-0.091%). Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1381 (11:37 UTC). broker_ledger_synced_until 2026-09-01T11:50:51Z. last_prune_date 2026-09-01. config.version 2.6.0 (D1 config sync landed).

Crypto edge gate still blocked: CRYPTO_BARS_STALE (LINKUSD stale ~22h), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0 required=3), EQUITY_DIRECTION_FALLBACK (broker change_today_pct zero, equity_delta fallback exposed). Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. Trades 722-821: all filled, 94 daytrading + 6 swing, 0 null-strategy. All skip codes structured and traceable. 0 CYCLE_LEASE_HELD, 0 error runs.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-273 (~19h post-Control-257), all 3 active schedules still dispatching clean. No eighth dispatch failure. Caps 5000/3700/2000 unchanged. Local HEAD 9ec340c, GitHub main HEAD 5a7a7623.


**Tuesday, September 1, 2026 Control-271 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~11h clean all runs 0 errors; ~17h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5648-5847 0 gaps 0 errors 0 lease holds; 20 swing MV $7,775 0 daytrading/crypto/null; equity $98,107 -$52.14; reconcile durations 8308-10559ms avg 8915ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-271 at ~11:00 UTC Sep 1 (Tuesday 13:00 +02). Market CLOSED (next open 13:30 UTC today, 2.5h away). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 95b2139 (Control-270).

D1 Sep 1 enforcement ~11h clean: all runs 00:00-11:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8308-10559ms avg 8915ms under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load (~2.5h away at this check). ~17h post-Control-257 with no eighth cron dispatch failure.

Run-log contiguous 5648-5847 (200 runs, 0 gaps, 0 errors, 0 lease holds). All 3 active schedules dispatching clean: daytrading cron MARKET_CLOSED skip every 5 min, reconcile_cron ok every 10 min (60 runs, all status=ok), crypto_cron :07/:37 skip (20 runs, all on cadence). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live (all modified 2026-08-31T22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto). Swing DOW 2-6 confirmed.

20 swing positions MV $7,774.73, all strategy=swing, metadata_source=d1 (broker-authoritative, source=alpaca), 0 daytrading/crypto/null positions. Equity $98,107.01, cash $90,332.28 (92.0%), buying_power $382,739.10, change_today -$52.14 (-0.053%). Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1379 (10:37 UTC). broker_ledger_synced_until 2026-09-01T10:50:49Z. last_prune_date 2026-09-01.

Crypto edge gate still blocked: CRYPTO_BARS_STALE (LINKUSD stale ~22h), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0 required=3), EQUITY_DIRECTION_FALLBACK (broker change_today_pct zero, equity_delta fallback exposed). Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. Trades 722-821: all filled, 94 daytrading + 6 swing, 0 null-strategy. All skip codes structured and traceable. 0 CYCLE_LEASE_HELD, 0 error runs.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-271 (~17h post-Control-257), all 3 active schedules still dispatching clean. No eighth dispatch failure. Caps 5000/3700/2000 unchanged. Repo HEAD 95b2139.

**Tuesday, September 1, 2026 Control-270 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~10h clean all runs 0 errors; ~15h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5729-5828 0 gaps 0 errors; 20 swing MV $7,763 0 daytrading/crypto/null; equity $98,096 -$63.54; reconcile durations 8319-10559ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-270 at ~10:00 UTC Sep 1 (Tuesday 12:00 +02). Market CLOSED (next open 13:30 UTC today, 3.5h away). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD c6aa8bd (Control-268).

D1 Sep 1 enforcement ~10h clean: all runs 00:00-10:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8319-10559ms under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load. ~15h post-Control-257 with no eighth cron dispatch failure.

**Tuesday, September 1, 2026 Control-268 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~9h clean all runs 0 errors; ~13h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5708-5808 0 gaps 0 errors; 20 swing MV $7,756 0 daytrading/crypto/null; equity $98,088 -$71.10; reconcile durations 8319-10559ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-268 at ~09:00 UTC Sep 1 (Tuesday 11:00 +02). Market CLOSED (next open 13:30 UTC today). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 9b0a960 (Control-267).

D1 Sep 1 enforcement ~9h clean: all runs 00:00-09:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8319-10559ms avg ~9000ms, under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load.

Run-log contiguous 5708-5808 (100 runs, 0 gaps, 0 errors). ~13h post-Control-257: all 3 active schedules dispatching clean (daytrading cron MARKET_CLOSED skip every 5 min, reconcile_cron ok every 10 min, crypto_cron :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 2026-08-31T22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto). Swing DOW 2-6 confirmed.

20 swing MV $7,755.91, all strategy=swing, metadata_source=d1 (broker-authoritative), 0 daytrading/crypto/null positions. Equity $98,088.05, cash $90,332.28 (92.0%), buying_power $382,686.72, change_today -$71.10 (-0.072%). Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1375 (08:37 UTC).

Crypto edge gate still blocked: CRYPTO_BARS_STALE, CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0), EQUITY_DIRECTION_FALLBACK. Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. All skip codes structured and traceable. 0 CYCLE_LEASE_HELD, 0 error runs.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-268 (~13h post-Control-257), all 3 active schedules still dispatching clean. No eighth dispatch failure. Caps 5000/3700/2000 unchanged. Repo HEAD 9b0a960.

**Tuesday, September 1, 2026 Control-267 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~8h clean all runs 0 errors; ~12h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5738-5787 0 gaps 0 errors; 20 swing MV $7,850 0 daytrading/crypto/null; equity $98,180 +$20.63 positive; reconcile durations 8319-10559ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-267 at ~08:00 UTC Sep 1 (Tuesday 10:00 +02). Market CLOSED (next open 13:30 UTC today). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 66d4a83 (Control-266).

D1 Sep 1 enforcement ~8h clean: all runs 00:00-08:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8319-10559ms, under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load.

Run-log contiguous 5738-5787 (50 runs, 0 gaps, 0 errors). ~12h post-Control-257: all 3 active schedules dispatching clean (daytrading cron MARKET_CLOSED skip, reconcile_cron ok, crypto_cron :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto). Swing DOW 2-6 confirmed.

20 swing MV $7,849.86, all strategy=swing, metadata_source=d1 (broker-authoritative), 0 daytrading/crypto/null positions. Equity $98,179.78, cash $90,332.28 (92.0%), buying_power $382,938.05, change_today +$20.63 (+0.021%). Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1373 (07:37 UTC).

Strategy comparison: swing 20 open MV $7,854 unrealized -$198.43 (0 wins/5 losses, 65 trades); daytrading 0 open realized +$2.98 (28.57% win, 414 trades); crypto 0 open realized -$56.62 gross / -$325.73 net ($269.11 fees broker-attributed, 11.11% win, 250 trades).

Crypto edge gate still blocked: CRYPTO_BARS_STALE (ETHUSD ~21.9h stale latestBarAt 2026-08-31T09:45Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. EQUITY_DIRECTION_FALLBACK in crypto runs (broker change_today_pct zero/unavailable, equity delta exposed for observability).

Skip observability: daytrading skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED; crypto skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT. All skip codes structured and traceable.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-267 (~12h post-Control-257), all 3 active schedules still dispatching clean. Caps 5000/3700/2000 unchanged. Repo HEAD 66d4a83.

**Tuesday, September 1, 2026 Control-266 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~9h clean all runs 0 errors; ~11h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5738-5767 0 gaps 0 errors; 20 swing MV $7,854 0 daytrading/crypto/null positions; equity $98,186; reconcile durations 8319-9836ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-266 at ~07:00 UTC Sep 1 (Tuesday 09:00 +02). Market CLOSED (next open 13:30 UTC today). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD c433f9d (Control-265).

D1 Sep 1 enforcement ~9h clean: all runs 00:00-07:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8319-9836ms avg ~8500ms, under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1) under trading load.

Run-log contiguous 5738-5767 (30 runs fetched, 0 gaps, 0 errors). ~11h post-Control-257: all 3 active schedules dispatching clean (daytrading cron MARKET_CLOSED skip every 5 min, reconcile_cron ok every 10 min, crypto_cron :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 2026-08-31T22:06:50Z Control-257, swing DOW 2-6).

20 swing MV $7,853.52, all strategy=swing, metadata_source=d1 (broker-authoritative current_state_source=alpaca), 0 daytrading/crypto/null positions. Equity $98,186.45, cash $90,332.93 (92.0%), buying_power $382,959.03, change_today -$32.61 (-0.033%). 821 trades, win 11.11%, 7567 decisions. Snapshot 1371 (07:00 UTC).

Strategy comparison: swing 20 open MV $7,854 unrealized -$199.31 (0 wins/5 losses, 65 trades, fees unattributed); daytrading 0 open realized +$2.98 (28.57% win, 414 trades); crypto 0 open realized -$56.62 gross / -$325.73 net (269.11 fees broker-attributed, 11.11% win, 250 trades).

Crypto edge gate still blocked: CRYPTO_BARS_STALE (SOLUSD ~22h stale latestBarAt 2026-08-31T08:30Z, LINKUSD ~22h, ETHUSD ~22h latestBarAt 2026-08-31T07:45Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0 required=3). Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. EQUITY_DIRECTION_FALLBACK in crypto runs (broker change_today_pct zero/unavailable, equity delta exposed for observability).

Skip observability: daytrading skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED; crypto skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT. All skip codes structured and traceable.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-266 (~11h post-Control-257), all 3 active schedules still dispatching clean. Caps 5000/3700/2000 unchanged. Repo HEAD c433f9d.



**Tuesday, September 1, 2026 Control-265 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement ~8h clean all runs 0 errors; ~10h post-Control-257 all 3 active schedules dispatching; 4 CF schedules verified live swing DOW 2-6; run-log contiguous 5650-5749 0 gaps 0 errors; 20 swing MV $7,831 0 daytrading/crypto/null positions; equity $98,164; reconcile durations 8308-10495ms under 12s stable; crypto cadence :07/:37 verified; crypto edge gate still blocked; fee telemetry stale; caps 5000/3700/2000 unchanged)**

Control-265 at ~06:00 UTC Sep 1 (Tuesday 08:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD dc862d4 (Control-264).

D1 Sep 1 enforcement ~8h clean: all runs 00:00-06:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8308-10495ms avg 8517ms, under 12s stable. Key upcoming test: first weekday open at 13:30 UTC today (Tue Sep 1).

Run-log contiguous 5650-5749 (100 runs, 0 gaps, 0 errors). ~10h post-Control-257: all 3 active schedules dispatching clean (daytrading cron MARKET_CLOSED skip, reconcile_cron ok, crypto_cron :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257, swing DOW 2-6).

20 swing MV $7,830.81, all strategy=swing, source=alpaca (broker-authoritative), 0 daytrading/crypto/null positions. Equity $98,163.74, cash $90,332.93 (92.0%), buying_power $382,895.44, change_today -$55.32 (-0.056%). Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1369 (05:37 UTC).

Strategy comparison: swing 20 open MV $7,831 unrealized -$222.03 (0 wins/5 losses, 65 trades); daytrading 0 open realized +$2.98 (28.57% win, 414 trades); crypto 0 open realized -$56.62 gross / -$325.73 net (269.11 fees broker-attributed, 11.11% win, 250 trades).

Crypto edge gate still blocked: CRYPTO_BARS_STALE (ETHUSD 21.9h stale latestBarAt 2026-08-31T07:45Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). SOLUSD and LINKUSD also stale (~22h). Fee telemetry stale (Aug 19), gross/fee/net=null conservative, accounting_status=filled_lot_exact_unavailable. EQUITY_DIRECTION_FALLBACK in crypto runs (broker change_today_pct zero/unavailable, equity delta exposed for observability).

Skip observability: daytrading skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED; crypto skips = RECONCILIATION_DEFERRED_TO_MAINTENANCE + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT. All skip codes structured and traceable.

Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). As of Control-265 (~10h post-Control-257), all 3 active schedules still dispatching clean. Caps 5000/3700/2000 unchanged. Repo HEAD dc862d4.


**Tuesday, September 1, 2026 Control-264 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement first 5h clean, ~7h post-Control-257 all 3 active schedules dispatching, 4 CF schedules verified live swing DOW 2-6)**

Control-264 at ~05:00 UTC Sep 1 (Tuesday 07:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD e3d8322 (Control-263).

D1 Sep 1 enforcement: first 5 hours (00:00-05:00 UTC) clean. All runs 0 errors. Reconcile durations 8332-9565ms (avg ~8450ms, 15 reconcile runs observed), under 12s, stable. last_prune_date=2026-09-01. broker_ledger_synced_until=2026-09-01T04:50:49Z. RECONCILIATION_DEFERRED_TO_MAINTENANCE D1 optimization confirmed working (daytrading + crypto defer broker reconciliation to reconcile_cron every 10 min).

Run-log contiguous 5678-5727 (50 runs, 0 gaps, 0 errors). ~7h post-Control-257: all 3 active schedules dispatching clean. Daytrading cron every 5 min (MARKET_CLOSED skip, next open 2026-09-01T09:30:00-04:00). Reconcile_cron every 10 min (ok, 8332-9565ms). Crypto_cron :07/:37 cadence (skip CRYPTO_BARS_STALE SOLUSD 79665s ~22h stale, CRYPTO_BARS_UNAVAILABLE MATICUSD, CRYPTO_DATA_INSUFFICIENT validTA=0). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification).

4 CF schedules verified live via GET /schedules API (all modified 22:06:50Z Control-257): `0 22 * * 2-6` (swing Mon-Fri), `*/10 * * * *` (reconcile), `1-59/5 * * * *` (daytrading), `7-59/30 * * * *` (crypto). Swing DOW 2-6 confirmed.

20 positions: all strategy=swing, source=alpaca (broker-authoritative), metadata_source=d1, 0 daytrading/crypto/null. Swing MV $7,829.47. Equity $98,162.70, cash $90,332.93 (92.0%), buying_power $382,892.54, down $56.36 (-0.057%). last_equity $98,219.06. Account ACTIVE, not PDT. 821 trades, win 11.11%, 7567 decisions. Snapshot 1367 (04:37:43 UTC).

Trades 817-821: daytrading sells from Aug 31 19:46-19:47 UTC (SOFI, SLB, SHOP, NOW, AMZN), all filled, gross/fee/net=null (conservative), accounting_status=filled_lot_exact_unavailable, filled_notional and lifecycle timestamps populated. SWING_OWNED_EXCLUDE working (decision 7567 COIN BUY skipped as swing-owned). EQUITY_DIRECTION_FALLBACK in crypto runs. Crypto edge gate still blocked (bar staleness + fee telemetry stale Aug 19). Fee telemetry: cryptoFeeAsOf 2026-08-19, status unavailable, gross/fee/net=null conservative.

Caps live-confirmed: daytrading=5000, swing=3700, crypto=2000 USD. Unchanged. No deploy needed. Key upcoming: D1 enforcement first weekday test at 13:30 UTC open today (Tue Sep 1). swing_cron next fire tonight 22:00 UTC (DOW=3).


**Tuesday, September 1, 2026 Control-263 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement first 4h clean, ~6h post-Control-257 all 3 active schedules dispatching, 4 CF schedules verified live swing DOW 2-6)**

Control-263 at ~04:00 UTC Sep 1 (Tuesday 06:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 2dfd5a8 (Control-262).

D1 Sep 1 enforcement: first 4 hours (00:00-04:00 UTC) clean. All runs 0 errors. Reconcile durations 8308-10495ms (avg 8631ms, 15 reconcile runs observed), under 12s, stable. last_prune_date=2026-09-01. broker_ledger_synced_until=2026-09-01T03:50:49Z. RECONCILIATION_DEFERRED_TO_MAINTENANCE D1 optimization confirmed working (daytrading + crypto defer broker reconciliation to reconcile_cron every 10 min).

Run-log: contiguous 5658-5707 (50 runs, 0 gaps, 0 errors). ~6h post-Control-257: all 3 active schedules dispatching clean (daytrading MARKET_CLOSED skip, reconcile ok 8360-8379ms, crypto :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257): swing `0 22 * * 2-6` (Mon-Fri) ✓, daytrading `1-59/5 * * * *` ✓, crypto `7-59/30 * * * *` ✓, reconcile `*/10 * * * *` ✓.

Positions: 20 swing MV $7,832, all strategy=swing, source=alpaca (broker-authoritative), 0 daytrading/crypto/null. Equity $98,165, cash $90,333 (92.0%), down $54 (-0.056%). 821 trades, win 11.11%, 7567 decisions. Snapshot 1365 (03:37 UTC). SWING_OWNED_EXCLUDE working (decision 7567 COIN BUY skipped as swing-owned). POSITION_QTY_MISMATCH (SLB, PFE from Aug 31) corrected via broker-authoritative persistence. Crypto edge gate still blocked (AVAXUSD 21.6h stale, MATICUSD unavailable, CRYPTO_DATA_INSUFFICIENT validTA=0). Fee telemetry stale (Aug 19), gross/fee/net=null conservative. EQUITY_DIRECTION_FALLBACK in crypto runs.

Historical error runs (outside current window, all well-understood): 5544 (Aug 31 19:47, subrequest ceiling, non-fatal), 5448/5445 (Aug 31 14:xx, POSITION_QTY_MISMATCH SLB/PFE, broker-authoritative correction working). No new errors since.

Caps 5000/3700/2000 USD unchanged. No deploy, no notification (no material delta from Control-262). Cron dispatch failure count remains SEVEN (Control-172, 186, 203, 217, 251, 253, 257). Key upcoming: D1 enforcement first weekday test at 13:30 UTC open today (Tue Sep 1); swing_cron next fire tonight 22:00 UTC (DOW=3, second 2-6 verification).

---

**Tuesday, September 1, 2026 Control-262 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement first 3h clean, ~5h post-Control-257 all 3 active schedules dispatching, 4 CF schedules verified live)**

Control-262 at ~03:00 UTC Sep 1 (Tuesday 05:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD be9e193 (Control-261).

D1 Sep 1 enforcement: first 3 hours (00:00-03:00 UTC) clean. All runs 0 errors. Reconcile durations 8308-10495ms (30 reconcile runs observed), under 12s, stable. last_prune_date=2026-09-01. broker_ledger_synced_until=2026-09-01T02:50:49Z. RECONCILIATION_DEFERRED_TO_MAINTENANCE D1 optimization confirmed working (daytrading + crypto defer broker reconciliation to reconcile_cron every 10 min).

Run-log: contiguous 5588-5687 (100 runs, 0 gaps, 0 errors). ~5h post-Control-257: all 3 active schedules dispatching clean (daytrading MARKET_CLOSED skip, reconcile ok 8308-10495ms, crypto :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257, swing DOW 2-6 confirmed).

Positions: 20 swing MV $7,825.17, all strategy=swing, source=alpaca (broker-authoritative), 0 daytrading/crypto/null. Equity $98,158.10, cash $90,332.93 (92.0%), down $60.96 today (-0.062%). 821 trades, win 11.11%. Account ACTIVE, not PDT. All trades accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). Crypto edge gate still blocked (CRYPTO_BARS_STALE AVAXUSD 21.6h stale, CRYPTO_BARS_UNAVAILABLE MATICUSD). Fee telemetry stale (Aug 19). EQUITY_DIRECTION_FALLBACK in crypto runs (broker change_today_pct=0). Caps 5000/3700/2000 unchanged.
**Tuesday, September 1, 2026 Control-261 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement first 2h clean, ~4h post-Control-257 all 3 active schedules dispatching, 4 CF schedules verified live)**

Control-261 at ~02:00 UTC Sep 1 (Tuesday 04:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 29cc460 (Control-260).

D1 Sep 1 enforcement: first 2 hours (00:00-02:00 UTC) clean. All runs 0 errors. Reconcile durations 8308-10495ms (avg 8631ms), under 12s, stable. last_prune_date=2026-09-01. broker_ledger_synced_until=2026-09-01T01:50:49Z. RECONCILIATION_DEFERRED_TO_MAINTENANCE D1 optimization confirmed working (daytrading + crypto defer broker reconciliation to reconcile_cron every 10 min).

Run-log: contiguous 5619-5668 (50 runs, 0 gaps, 0 errors). ~4h post-Control-257: all 3 active schedules dispatching clean (daytrading MARKET_CLOSED skip, reconcile ok 8308-10495ms, crypto :07/:37 skip). swing_cron ABSENT (expected; next fire tonight 22:00 UTC DOW=3, second 2-6 verification). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257, swing DOW 2-6).

Positions: 20 swing MV $7,829.30, all strategy=swing, source=alpaca (broker-authoritative), 0 daytrading/crypto/null. Equity $98,162.23, cash $90,332.93 (92.0%), down $56.83 today (-0.058%). 821 trades, win 11.11%. Account ACTIVE, not PDT. SWING_OWNED_EXCLUDE working (decision 7567 COIN BUY skipped as swing-owned). All trades accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). Crypto edge gate still blocked (CRYPTO_BARS_STALE LINKUSD 22h stale, CRYPTO_BARS_UNAVAILABLE MATICUSD). Fee telemetry stale (Aug 19). EQUITY_DIRECTION_FALLBACK in crypto runs (broker change_today_pct=0). Caps 5000/3700/2000 unchanged.

**Tuesday, September 1, 2026 Control-260 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement first hour clean, all 3 active schedules dispatching, 4 CF schedules verified live)**

Control-260 at ~01:00 UTC Sep 1 (Tuesday 03:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (bd6205e). Repo HEAD 729321b (Control-259).

D1 Sep 1 ENFORCEMENT UPDATE: ~1h into enforcement. All runs from 00:00 through 01:00 UTC Sep 1 survived with 0 errors. Reconcile durations 8345-11544ms (avg 9277ms), stable under 12s. Run-log contiguous 5549-5648, 0 gaps. Key test remains: 13:30 UTC weekday market open today.

CRON DISPATCH: ~3h post-Control-257 redeploy. All 3 active schedules (daytrading, crypto, reconcile) dispatching clean. swing_cron absent (expected; next fire tonight 22:00 UTC, DOW=3 in 2-6 range). 4 CF schedules verified live via API (all modified 22:06:50Z Control-257, swing DOW 2-6 confirmed).

D1 OPTIMIZATION CONFIRMED: RECONCILIATION_DEFERRED_TO_MAINTENANCE skip code working correctly — daytrading and crypto crons defer broker reconciliation to reconcile_cron (every 10 min), conserving D1 budget. All skips include MARKET_CLOSED during overnight hours.

METRICS: 20 swing positions MV $7,830.17, 0 daytrading/crypto/null. Equity $98,163.10 (down $55.96 from $98,219.06, -0.057% overnight drift). Cash $90,332.93 (92.0%). 821 trades, win 11.11%. Account ACTIVE, not PDT, not blocked. Broker-authoritative positions (source=alpaca, all metadata_source=d1). Fee telemetry stale (asOf Aug 19), gross/fee/net=null conservative. Caps 5000/3700/2000 unchanged. 224 tests/845 assertions pass, typecheck clean.

**Tuesday, September 1, 2026 Control-259 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 Sep 1 enforcement STARTED — first run survived, 4 CF schedules verified live)**

Control-259 at ~00:00 UTC Sep 1 (Tuesday 02:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (fd2309c). Repo HEAD bf626d3.

D1 Sep 1 ENFORCEMENT MILESTONE: Run 5628 at 2026-09-01 00:00:55 UTC = first reconcile_cron run past midnight = ok, 8855ms, 0 errors. D1 enforcement did not kill the first run. Full test: weekday trading hours (13:30 UTC open today). Continue WATCH.

RUN-LOG: contiguous 5589-5628 (40 runs, 0 ID gaps, 0 errors). Triggers: cron (daytrading) 24/reconcile_cron 12/crypto_cron 4. Reconcile every 10 min all ok, durations 8345-9692ms (under 12s, stable). Crypto :07/:37 cadence confirmed (5591 22:07, 5601 22:37, 5611 23:07, 5621 23:37 — all skipped, bars stale). Daytrading cron every 5 min, all MARKET_CLOSED skip (nextOpen 2026-09-01T09:30-04:00). swing_cron next fire: Tue Sep 1 22:00 UTC (DOW=3, second DOW 2-6 verification). No new cron dispatch failure since Control-257 redeploy (~2h ago).

SCHEDULES: 4 confirmed live via CF GET /schedules API, all modified 2026-08-31T22:06:50Z (Control-257). swing `0 22 * * 2-6` ✓, daytrading `1-59/5 * * * *` ✓, crypto `7-59/30 * * * *` ✓, reconcile `*/10 * * * *` ✓.

POSITIONS: 20 swing, MV $7,824.15, all metadata_source=d1, 0 daytrading/crypto/null. Broker-authoritative, 0 unattributed.

EQUITY: $98,157.12 (dashboard account), snapshot 1357 at 23:37:43 UTC equity $98,161.64. Change today -$61.94 (-0.063%). Cash $90,332.93 (92.0%). ACTIVE, not PDT. 821 trades, win 11.11%.

TRADES: Latest 10 all daytrading sells/buys filled Aug 31 19:46-19:47 UTC. accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded. Conservative gross/fee/net=null. Fee telemetry stale (cryptoFeeAsOf Aug 19, status unavailable).

CAPS: daytrading 5000 / swing 3700 / crypto 2000 USD unchanged. 224 tests / 845 assertions pass, typecheck clean.

---

**Tuesday, September 1, 2026 Control-258 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; ~54 min post-Control-257 redeploy, all 3 active schedules dispatching clean, 4 CF schedules verified live)**

Control-258 at ~23:00 UTC Aug 31 (Tuesday 01:00 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No new defect found, no deploy needed. Code unchanged from Control-255 (fd2309c). Docs ordering defect found and fixed: Control-257 entry was appended at end of all 3 docs instead of prepended at top — corrected this control.

RUN-LOG: contiguous 5569-5608 (40 runs, 0 ID gaps, 0 errors). Triggers: cron 24/reconcile_cron 12/crypto_cron 4. Reconcile every 10 min all ok, durations 8368-11544ms (under 12s, stable, trending down from 17.3s peak at Control-256). Crypto :07/:37 cadence confirmed (5571 21:07, 5581 21:37, 5591 22:07, 5601 22:37 — all skipped, bars stale). Daytrading cron every 5 min, all MARKET_CLOSED skip (nextOpen 2026-09-01T09:30-04:00). swing_cron ABSENT from run-log at 22:00 UTC Aug 31 — 7th cron dispatch failure, already remediated at Control-257 redeploy (22:06 UTC). Next swing fire: Tue Sep 1 22:00 UTC (DOW=3, second DOW 2-6 verification).

CLOUDFLARE SCHEDULES API VERIFIED (live): all 4 cron schedules confirmed via GET /accounts/{id}/workers/scripts/{name}/schedules: (1) swing `0 22 * * 2-6` (Mon-Fri, created 2026-08-31T20:09:35Z), (2) reconcile `*/10 * * * *`, (3) daytrading `1-59/5 * * * *`, (4) crypto `7-59/30 * * * *`. All modified 2026-08-31T22:06:50Z (Control-257). wrangler.toml matches (crons = ["1-59/5 * * * *", "0 22 * * 2-6", "7-59/30 * * * *", "*/10 * * * *"]).

POSITIONS: 20 broker-authoritative (20 swing MV $7,818.82, 0 daytrading — all EOD-flattened at 19:46-19:47 UTC, 0 crypto, 0 unattributed, 0 null strategy), metadata_source=d1 all 20. Symbols: AAL, AVGO, BA, BAC, C, CCL, CVX, DAL, DDOG, F, FCEL, GOOGL, INTC, NEE, SIRI, SNOW, TXN, WFC, WMT, XOM. Swing MV $7,819 over $3,700 cap (pre-existing fills, known DEGRADED per cap semantics: cap = initial investment not MV ceiling).

ACCOUNT: equity $98,151.75, cash $90,332.93 (92%), buying_power $382,862.48, long_market_value $7,818.82, ACTIVE, not PDT, change_today -$67.31 (-0.069%), last_equity $98,219.06. 821 total trades / 821 executed, win 11.11%. Latest trades 817-821 all daytrading sells (SOFI, SLB, SHOP, NOW, AMZN), all filled with complete timestamps, gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded, estimated_value_basis=order_time_estimate). 0 null strategy in latest 5 trades. broker_ledger_synced_until 2026-08-31T22:50:49Z (fresh). last_prune_date 2026-08-31.

SKIP REASONS: structured (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, MAINTENANCE_ONLY for reconcile, CRYPTO_BARS_UNAVAILABLE/STALE/DATA_INSUFFICIENT, EQUITY_DIRECTION_FALLBACK). 0 lease errors. Crypto edge gate wiring verified (producer deployed, not reached — bars stale). Fee telemetry stale (asOf Aug 19, cryptoFeeTelemetryStatus=unavailable).

CAPS: 5000/3700/2000 USD unchanged (config max_capital_usd=5000, swing_max_capital_usd=3700, crypto_max_capital_usd=2000 + dashboard confirmed). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD 5a6c32d (Control-257). Docs HEAD: this commit (Control-258).

Follow-up: Tue Sep 1 22:00 UTC swing_cron DOW 2-6 second fire test (DOW=3), D1 Sep 1 enforcement starts 00:00 UTC tonight (~1h — first real test), reconcile duration WATCH (8.4-11.5s stable under 12s), cron dispatch recurrence WATCH (7th occurrence, ~54 min post-Control-257), crypto bar freshness + fee telemetry freshness, subrequest budget guard verification during next EOD window (~19:47 UTC Tue Sep 1).

**Monday, August 31, 2026 Control-257 strict read-only control + authorized reliability redeploy for seventh cron dispatch failure (swing_cron DOW 2-6 first fire MISS, HEALTHY code/deploy 2.6.0, DEGRADED external, deploy executed)**

Control-257 at ~22:05 UTC Aug 31 (Tuesday 00:05 +02). Market CLOSED. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. Seventh cron dispatch failure found and fixed via authorized reliability redeploy.

DEFECT: swing_cron `0 22 * * 2-6` MISSED its first real fire at 22:00 UTC Aug 31 (Monday = DOW 2 in Cloudflare 1=Sunday system). Schedule confirmed LIVE via Cloudflare GET /schedules API (cron=`0 22 * * 2-6`, created 2026-08-31T20:09:35Z by Control-255). No swing_cron run appeared in run-log — only reconcile_cron (5588 @ 22:00:55) and daytrading cron (5589 @ 22:01:46) fired. The swing schedule was registered correctly but Cloudflare did not dispatch it. This is the seventh occurrence of the recurring cron dispatch failure pattern (Control-172, 186, 203, 217, 251, 253, now 257). ~2h post-Control-255 schedule re-registration.

REMEDIATION: Authorized reliability redeploy (1) PUT /content endpoint (deployment modified 2026-08-31T22:06:31Z, same code bd6205e/fd2309c, no code changes), (2) PUT /schedules API re-registered all 4 cron triggers (modified 2026-08-31T22:06:50Z). PUT /content does NOT update cron triggers (confirmed again — schedules modified_on stayed 20:09:35Z after PUT /content, only changed after PUT /schedules).

POST-DEPLOY VERIFICATION: All 3 active schedules confirmed dispatching within 5 min (0 errors): daytrading cron 5590 @ 22:06:48 ok skipped, crypto_cron 5591 @ 22:07:51 ok skipped, reconcile_cron 5592 @ 22:10:51 ok dur 9692ms. Swing_cron next fire: Tuesday Sep 1 22:00 UTC (DOW=3, in 2-6 range) — this is the next real DOW 2-6 verification.

Run-log contiguous 5560-5592 (33 runs, 0 ID gaps, 0 errors; triggers cron 19/reconcile_cron 10/crypto_cron 3). 20 swing positions broker-authoritative (metadata_source=d1 all 20), 0 unattributed, 0 strategy=None, MV sum $7,829.12. Daytrading MV $0 (EOD flattened 19:46-19:47 UTC), crypto MV $0 fail-closed. Equity $98,162.09, cash $90,332.93 (92%), buying_power $382,890.83, ACTIVE not PDT, change_today -$56.97. 821 trades win 11.11%. Crypto :07/:37 cadence confirmed (5591 crypto_cron fail-closed: MATICUSD empty + LINKUSD stale ageSeconds 79670 vs maxStale 2700, validTA=0, edge gate not reached producer wired). Daytrading cron MARKET_CLOSED (nextOpen 2026-09-01T09:30-04:00). Skip reasons structured (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE/DATA_INSUFFICIENT, EQUITY_DIRECTION_FALLBACK, MAINTENANCE_ONLY), 0 lease errors. Trade lifecycle: 807-821 all filled daytrading sells/buys with timestamps, 0 pending. Gross/fee/net=null conservative. Null-strategy trades 0 in 15-trade window. Fee telemetry stale (Aug 19). Caps 5000/3700/2000 USD unchanged (config + dashboard confirmed). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD 80d6cae (Control-256, unchanged — no code change this control). Docs HEAD: this commit (Control-257).

Follow-up: Tuesday Sep 1 22:00 UTC swing_cron DOW 2-6 second fire test (DOW=3), D1 Sep 1 enforcement starts 00:00 UTC tonight (~2h), reconcile duration WATCH (9692ms post-redeploy), cron dispatch recurrence WATCH (7th occurrence, ~2h post-Control-255), crypto bar freshness + fee telemetry freshness, subrequest budget guard verification during next EOD window.

**Monday, August 31, 2026 Control-256 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; ~50 min post-Control-255, all 4 schedules verified live via Cloudflare API, swing DOW 2-6 confirmed on schedules)**

Control-256 at ~21:00 UTC Aug 31 (23:00 +02). Market CLOSED (closed 20:00 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code unchanged from Control-255 (fd2309c).

CLOUDFLARE SCHEDULES API VERIFIED (live, not just wrangler.toml): all 4 cron schedules confirmed via GET /accounts/{id}/workers/scripts/{name}/schedules: (1) swing `0 22 * * 2-6` (Mon-Fri, created 2026-08-31T20:09:35Z — Control-255 applied the DOW fix to live schedules), (2) reconcile `*/10 * * * *`, (3) daytrading `1-59/5 * * * *`, (4) crypto `7-59/30 * * * *`. This confirms the critical Control-255 finding: PUT /content does NOT update cron triggers; the separate schedules API was required and was called.

POST-CONTROL-255 EVIDENCE: Control-255 deployed subrequest fix (ce2f6e7c @ 20:09:12 UTC). ~50 min post-deploy, all runs clean. Run 5544 @ 19:47:10 UTC error (subrequest crash) is pre-deploy, already fixed, NOT a new defect. No post-deploy error-status runs. Cron durations dropped to 580-1100ms during post-close no-entry window (EOD skip working). Reconcile durations 9977-11448ms (10-11.4s), trending DOWN from 17.3s peak at 5538 — WATCH but improving.

Run-log contiguous 5519-5569 (51 runs, 0 ID gaps; triggers cron 30/reconcile_cron 15/crypto_cron 5; statuses ok 19/skipped 31/error 1). Crypto :07/:37 cadence confirmed (5521 18:37, 5531 19:07, 5541 19:37, 5551 20:07, 5561 20:37). Daytrading cron every 5 min (skipped, market closed). Reconcile every 10 min (ok).

Positions: 20 broker-authoritative (20 swing MV $7,825.72, 0 daytrading — all EOD-flattened at 19:46-19:47 UTC, 0 crypto, 0 unattributed, 0 null), source=alpaca observed via dashboard. Equity $98,158.66, cash $90,332.93 (92%), buying_power $382,881.63, ACTIVE not PDT, change_today -$60.40 (-0.061%), last_equity $98,219.06. 821 total trades / 821 executed, win 11.11%. Swing MV $7,826 over $3,700 cap (pre-existing fills, known DEGRADED per cap semantics: cap = initial investment not MV ceiling). Crypto MV $0 fail-closed (MATICUSD empty, AVAXUSD/LINKUSD/ETHUSD stale ~22h, CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Fee telemetry stale (asOf Aug 19, cryptoFeeTelemetryStatus=unavailable). broker_ledger_synced_until 2026-08-31T20:50:54Z (fresh). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded).

Skip codes structured (SWING_OWNED_EXCLUDE on NCLH, CAPITAL_CAP on PLUG, DECISION_HOLD, EQUITY_DIRECTION_FALLBACK, RECONCILIATION_DEFERRED_TO_MAINTENANCE — all in run 5544 error_details). Caps 5000/3700/2000 USD unchanged (config + dashboard confirmed). Swing cron DOW fix verified live (CF schedules API: `0 22 * * 2-6`). Edge gate wiring verified (technical-analysis.ts:414 computeCalibratedEdgeBps, crypto-strategy.ts:130 minEdgeAfterCosts=8). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD fd2309c (Control-255). Docs HEAD: this commit (Control-256).

Follow-up: 22:00 UTC swing_cron DOW 2-6 first REAL fire (TONIGHT — schedule confirmed live, ~1h from now), D1 Sep 1 enforcement starts 00:00 UTC tonight (~3h), reconcile duration WATCH (10-11.4s trending down, monitor creep past 15s), crypto bar freshness + fee telemetry freshness, subrequest budget guard verification during next EOD window (tomorrow ~19:47 UTC), cron dispatch recurrence WATCH (~50 min post-Control-255).

**Monday, August 31, 2026 Control-255 strict read-only control + authorized reliability fix for Worker subrequest limit crash (HEALTHY code/deploy 2.6.0, DEGRADED external, deploy executed)**

Control-255 at ~20:00 UTC Aug 31 (22:00 +02). Market OPEN (closes 20:00 UTC). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. Subrequest limit crash found and fixed via authorized reliability fix + deploy.

DEFECT: Run 5544 @ 19:47:10 UTC (cron, dur=24442ms) hit "Fatal: Too many subrequests by single Worker invocation" — Cloudflare hard subrequest limit killed the Worker mid-cycle during EOD no-entry window (~13 min to close). Cycle was simultaneously submitting EOD flatten closePosition orders + fetching bars for 25 held positions + 20 new candidates, even though noNewEntries blocked all new entries. System self-recovered (5545-5548 ok/skipped, no stuck lease).

FIX (reliability-only, caps/trading preserved): (1) AlpacaClient.trackedFetch() wraps all 9 fetch() calls, exposes getSubrequestCount(). (2) Universe scan skipped during EOD no-entry window. (3) New candidate bar analysis skipped during EOD no-entry window (entries blocked by EOD_NO_ENTRY; held-position exits still analyzed). (4) Subrequest budget guard: subrequestsUsed >= 400 skips new candidate analysis. No caps changed, no trading behavior changed. 224 tests/845 assertions pass, typecheck clean.

LIVE STATE: 20 positions broker-authoritative (20 swing MV $7,827.31, 0 daytrading, 0 crypto, 0 null), source=alpaca observed 20:01:03 UTC. Equity $98,160.24, cash $90,332.93 (92%), ACTIVE not PDT, change_today -$58.82. 821 trades, 0 null strategy, all filled, gross/fee/net=null conservative. Crypto fail-closed (bars stale, edge gate not reached, producer wired). Run-log 5519-5548 (30 runs, 0 gaps; cron 18/reconcile_cron 9/crypto_cron 3; 1 error-status run = subrequest crash). Crypto :07/:37 cadence confirmed. Reconcile durations 10-17s (5538 crept to 17289ms — WATCH). broker_ledger_synced_until 2026-08-31T20:00:58Z (fresh). Skip codes structured. Fee telemetry stale (Aug 19). Caps 5000/3700/2000 unchanged. Swing cron DOW fix verified (wrangler.toml:19 "0 22 * * 2-6"). Edge gate wiring verified (technical-analysis.ts:414, crypto-strategy.ts:130). 224 tests/845 assertions pass, typecheck clean. Repo HEAD 154e59d (Control-254). Docs HEAD: this commit (Control-255). Follow-up: watch all 4 schedules post-deploy, 22:00 UTC swing_cron DOW 2-6 first fire (TONIGHT critical), D1 Sep 1 enforcement 00:00 UTC tonight, reconcile WATCH (crept to 17.3s), crypto bar + fee telemetry freshness, subrequest budget guard verification during next EOD window.

**Monday, August 31, 2026 Control-254 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; ~1h post-Control-253, all 4 schedules dispatching clean)**

**Monday, August 31, 2026 Control-253 strict read-only control + authorized reliability redeploy for sixth cron dispatch failure (HEALTHY code/deploy 2.6.0, DEGRADED external, deploy executed)**

Control-253 at ~18:00 UTC Aug 31 (20:00 +02). Market OPEN (closes 20:00 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. SIXTH cron dispatch failure found and fixed via authorized reliability redeploy. Code bd6205e (Control-241, unchanged — same code, redeploy re-registers cron triggers). Deploy: direct Cloudflare API PUT /content endpoint, deployment_id 9a1fd4bf, modified_on 2026-08-31T18:05:22Z. No code change; reliability-only redeploy under standing maintenance rule.

**Monday, August 31, 2026 Control-252 strict read-only post-redeploy control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; ~1h post-Control-251, all 4 schedules dispatching, reconcile elevated but stable)**

Control-252 at ~17:00 UTC Aug 31 (19:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect, no deploy. Code bd6205e (unchanged). Control-251 redeploy holding ~1h: all 4 schedules dispatching. Run-log 5463-5492 contiguous (30 runs, 0 gaps, 0 errors). Reconcile durations 11-14s elevated but oscillating/stable (not creep) — consistent with active market-hours D1 load vs previous weekend/overnight ~3.5s. WATCH creep past 15s. 25 positions (20 swing MV $7,787.22 + 5 daytrading MV $4,913.26, 0 crypto, 0 null). Equity $98,143.80, cash $85,443.23 (87%), ACTIVE not PDT, change_today -$75.26. 793 trades, 0 null strategy. Caps 5000/3700/2000 unchanged. Fee telemetry stale (Aug 19). Crypto fail-closed (ETHUSD stale, MATICUSD empty). SWING_OWNED_EXCLUDE + NO_ENTRY_RISK exercised. 224 tests/845 assertions, typecheck clean. Repo HEAD a9f3336. Follow-up: watch schedules through 20:00 UTC close, 22:00 UTC swing_cron DOW 2-6 test tonight, D1 Sep 1 enforcement 00:00 UTC, reconcile WATCH, cron recurrence WATCH (~1h post-Control-251).

**Monday, August 31, 2026 Control-251 strict read-only control + authorized reliability redeploy for fifth cron dispatch failure (HEALTHY code/deploy 2.6.0, DEGRADED external, deploy executed)**

Control-251 at ~16:00 UTC Aug 31 (18:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. FIFTH cron dispatch failure found and fixed via authorized reliability redeploy. Code bd6205e (Control-241, unchanged — same code, redeploy re-registers cron triggers). Deploy: direct Cloudflare API PUT /content endpoint, deployment_id 866b7143, modified_on 2026-08-31T16:05:45Z. No code change; reliability-only redeploy under standing maintenance rule. FIFTH CRON DISPATCH FAILURE (partial, ~31h post-Control-217): starting ~15:30 UTC, reconcile_cron+crypto_cron stopped, daytrading cron degraded (CYCLE_LEASE_HELD, stuck lease from killed Worker invocation). Post-deploy: all 4 schedules confirmed dispatching (run 5474 cron ok 5787ms, run 5475/5476 crypto_cron, run 5477 reconcile_cron ok 11577ms). Daytrading immediately active (3 new trades pending 783-785, PLTR sell 782 filled). Run-log 5333-5477 contiguous (145 runs, 0 gaps; 2 error-status = POSITION_QTY_MISMATCH safety events NOT defects). Positions 26 (20 swing MV $7,784.06 + 6 daytrading MV $5,000.82, 0 crypto), 0 unattributed, 0 null. Equity $98,143.76, cash $85,358.88 (87%), ACTIVE not PDT. 785 trades win 12.28%. Caps 5000/3700/2000 unchanged. 224 tests/845 assertions typecheck clean. Repo HEAD f4f9f75 (local). Follow-up: watch all 4 schedules through 20:00 UTC close, 22:00 UTC swing_cron first DOW 2-6 dispatch test, D1 Sep 1 enforcement tonight, cron dispatch recurrence WATCH (~0h post-Control-251 redeploy).

**Monday, August 31, 2026 Control-250 strict read-only post-open control (HEALTHY code/deploy, DEGRADED external, no deploy needed; daytrading at cap, 2 POSITION_QTY_MISMATCH safety events self-resolved)**

Control-250 at ~15:00 UTC Aug 31 (17:00 +02). Market OPEN. Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). Control-217 reliability redeploy holding ~29h: NO fifth cron dispatch failure (longest clean stretch ever continues through first weekday open since Aug 28). All 4 schedules dispatching (daytrading cron every 5 min POST-OPEN active dur 3988-5253ms placing+filling orders; reconcile_cron every 10 min ok dur 3177-5318ms avg 3900ms 36 runs NO sustained creep; crypto_cron :07/:37 cadence 12 runs fail-closed MATICUSD empty + AVAXUSD/ETHUSD/ATOMUSD stale validTA=0 edge gate not reached but producer wired; swing_cron no new run since 5120 @ Aug 30 22:00, tonight 22:00 UTC is first DOW 2-6 = Mon-Fri dispatch test). Run-log contiguous 5341-5460 (120 runs, 0 gaps; triggers cron 72/reconcile_cron 36/crypto_cron 12; 2 error-status runs are POSITION_QTY_MISMATCH safety events, NOT defects — see below). Latest run 5460 @ 15:00:17 UTC reconcile_cron ok 4271ms 0 errors. POSITION_QTY_MISMATCH safety events (2): run 5445 @ 14:11 PFE (internal 96 vs broker 106) and run 5448 @ 14:21 SLB (internal 1 vs broker 16) — transient D1-vs-broker lag during rapid daytrading fills; broker-authoritative quantities persisted, new BUY entries blocked for those cycles, risk-reducing exits remained eligible, both self-resolved on next reconcile (positions now show PFE qty 106, SLB qty 16.15, both strategy=daytrading). Safety mechanism working as designed, NOT a defect. MARKET-OPEN ACTIVITY: 7 new trades since Control-249 (774 PFE buy 35 @ 27.97, 775 PLTR buy 5 @ 184.02, 776 SLB buy 16 @ 59.42, 777 PLUG buy 64 @ 2.18, 778 SLB buy 0.15 @ 59.248, 779 PLUG sell 64 @ 2.16, 780 NVDA buy 0.6 @ 219.648); PLUG roundtrip buy 777/sell 779 same-day. 24 positions broker-authoritative (20 swing MV $7,786.87 + 4 daytrading MV $4,997.69, 0 crypto), 0 unattributed, 0 strategy=None, all metadata_source=d1. Daytrading cost basis ~$4,976 within $5,000 cap (MV $4,997.69 at 99.95% of cap — cap enforcement confirmed by NO_ENTRY_RISK skip on MU "max $0.52 at $945.81" = insufficient remaining capital; SWING_OWNED_EXCLUDE exercised on F 5x across runs 5445/5448/5458/5459 = cc9e813 fix proven working live). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Swing MV $7,786.87 (over-cap from pre-existing fills, known DEGRADED per cap semantics). Crypto MV $0 fail-closed. Equity direction: EQUITY_DIRECTION_FALLBACK in post-open runs (broker change_today_pct=0 during intraday unsettled state, equity delta exposed for observability only, no risk weakening). Skip reasons structured (RECONCILIATION_DEFERRED_TO_MAINTENANCE, EQUITY_DIRECTION_FALLBACK, NO_ENTRY_RISK, SWING_OWNED_EXCLUDE, DECISION_HOLD, POSITION_QTY_MISMATCH, MAINTENANCE_ONLY, CRYPTO_BARS_UNAVAILABLE/STALE, CRYPTO_DATA_INSUFFICIENT), 0 lease errors. Trade lifecycle: all 40 trades in window filled with complete timestamps (submitted_at, filled_at, last_reconciled_at). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 0 in 741-780 window. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,134.78 (live account portfolio_value/equity), cash $85,354.25 (87%), buying_power $376,847.66, ACTIVE, not PDT, trading_blocked false, change_today -$84.28 (-0.086%), last_equity $98,219.06. 780 total trades / 780 executed, win 12.07%. Capital caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). Swing cron DOW fix verified in code (wrangler.toml:19 "0 22 * * 2-6" = Mon-Fri). Edge gate wiring verified (technical-analysis.ts:414 computeCalibratedEdgeBps, crypto-strategy.ts:130 minEdgeAfterCosts=8). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD b8da4446 (local, Control-249). Docs HEAD: this commit (Control-250, local only). Follow-up: watch daytrading through 20:00 UTC close, 22:00 UTC swing_cron first DOW 2-6 = Mon-Fri dispatch test (post-Control-241 fix), D1 Sep 1 enforcement starts tonight 00:00 UTC (Sep 1 Tue open = first trading day under enforcement), crypto bar freshness + fee telemetry freshness, cron dispatch recurrence WATCH (~29h post-Control-217, NO fifth occurrence).

**Monday, August 31, 2026 Control-249 strict read-only post-open control (HEALTHY code/deploy, DEGRADED external, no deploy needed; first weekday open since Aug 28 — pending sells filled, daytrading active)**

Control-249 at ~14:00 UTC Aug 31 (16:00 +02). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). Control-217 reliability redeploy holding ~28h: PAST 8-16h recurrence window upper bound, NO fifth cron dispatch failure, longest clean stretch ever continues through first weekday open since Aug 28. All 4 schedules dispatching (daytrading cron every 5 min POST-OPEN active dur 2325-4721ms placing+filling orders; reconcile_cron every 10 min ok dur 3177-5318ms avg 3914ms 30 runs NO sustained creep well below ~20s pre-gap threshold; crypto_cron :07/:37 cadence 10 runs fail-closed MATICUSD empty + AVAXUSD/ETHUSD stale validTA=0 edge gate not reached but producer wired; swing_cron no new run since 5120 @ Aug 30 22:00, tonight 22:00 UTC is first DOW 2-6 = Mon-Fri dispatch test). Run-log contiguous 5341-5440 (100 runs, 0 gaps, 0 errors; triggers cron 60/reconcile_cron 30/crypto_cron 10). Latest run 5440 @ 14:00:17 UTC reconcile_cron ok 4145ms 0 errors. MARKET-OPEN ACTIVITY: pending swing sells 769/770/771 FILLED at 13:30:01Z (partial fills qty 0.35/0.59/0.30, 3 swing positions closed, swing 23->20); daytrading ACTIVE — 2 PFE buys (trades 772 qty 36 @ 13:46:18Z, 773 qty 35 @ 13:56:18Z, total 71 shares MV $1980.55 strategy=daytrading) within $5000 daytrading cap. 21 positions broker-authoritative (20 swing + 1 daytrading PFE, metadata_source=d1 all 21, source=alpaca observed), 0 unattributed, 0 strategy=None, MV sum $9829.82 (matches dashboard long_market_value $9825.68 within live price drift). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $1980.55 (PFE, within cap), crypto MV $0 fail-closed. Equity direction: broker change_today -$47.40 / -0.0483% non-zero (broker provides direction, no fallback needed). Daytrading cron run-status=skipped post-open despite 2 buys placed+filled (run 5436 13:46 / 5439 13:56) — this is the known run-status semantics observability gap where a run placing orders but skipping remaining candidates logs status=skipped; not a new defect, durations 2.3-4.7s confirm real evaluation work vs pre-open 150-340ms MARKET_CLOSED skips. Skip reasons structured (MARKET_CLOSED pre-open, RECONCILIATION_DEFERRED_TO_MAINTENANCE, CRYPTO_BARS_UNAVAILABLE/STALE, CRYPTO_DATA_INSUFFICIENT validTA=0, MAINTENANCE_ONLY), 0 errors, 0 lease errors. Trade lifecycle: 769-771 filled with complete timestamps (filled_at 13:30:01Z), 772/773 filled with complete timestamps, 768 prior filled. Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable for filled, fee_attribution=none-recorded; aggregate fees account-level-unattributed). Null-strategy trades 0 in 759-773 window. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,171.66 (live account portfolio_value/equity), cash $88,345.98 (89.9%), buying power $380,541.42, ACTIVE, not PDT, trading_blocked false, change_today -$47.40, last_equity $98,219.06. 773 total trades / 773 executed, win 13.11%. Strategy comparison: swing 20 open MV $7850.36 win 0% (0/5 closed), daytrading 1 open MV ~$1980 win 29.6%, crypto 0 open. Capital caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard capitalCaps confirmed). Swing cron DOW fix verified in code (wrangler.toml:19 "0 22 * * 2-6" = Mon-Fri). Edge gate wiring verified (technical-analysis.ts:414 computeCalibratedEdgeBps, crypto-strategy.ts:130 minEdgeAfterCosts=8). 224 tests/845 assertions pass, typecheck clean. Repo HEAD b43c86b (local, Control-248). Docs HEAD: this commit (Control-249, local only). Follow-up: watch daytrading activity through close (20:00 UTC), 22:00 UTC swing_cron first DOW 2-6 = Mon-Fri dispatch test (post-Control-241 fix), D1 Sep 1 enforcement starts tonight 00:00 UTC (next trading day Sep 1 Tue open is first under enforcement).

**Monday, August 31, 2026 Control-246 strict read-only pre-open heartbeat (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-246 at ~11:00 UTC Aug 31 (13:00 +02). Pre-open heartbeat, market CLOSED (next open Mon 13:30 UTC TODAY, 2.5h away — first weekday open since Aug 28 + D1 Sep 1 enforcement starts TODAY). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). Control-217 reliability redeploy holding ~25h: PAST 8-16h upper bound, NO fifth cron dispatch failure, longest clean stretch ever continues. All 4 schedules dispatching (daytrading MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile 10m ok 3549-4098ms NO creep; crypto :07/:37 fail-closed MATICUSD empty + ETHUSD stale ~21.9h validTA=0 edge gate not reached producer wired; swing no new run pending tonight 22:00 UTC first DOW 2-6 test). Run-log contiguous (latest run 5380 @ 11:00:17 UTC reconcile ok 4098ms 0 errors). 23 swing positions broker-authoritative (source=alpaca 11:00:22Z, metadata_source=d1 all 23), 0 unattributed, 0 strategy=None in 760-771, MV $8,245.13. D1 fallback guard confirmed. Equity direction: broker change_today_pct -0.0108% non-zero. Skip reasons structured, 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill await Mon 13:30 UTC open, 768 filled. Gross/fee/net=null conservative. Fee telemetry stale (Aug 19). Equity $98,208.45, cash $89,963.32 (91.6%), ACTIVE not PDT, change_today -$10.61. 771/768 trades win 14.5%. broker_ledger_synced_until 2026-08-31T11:00:17Z, last_prune 2026-08-31, snapshot 1258 @ 10:37:14. Caps 5000/3700/2000 unchanged. 224 tests/845 assertions typecheck clean. Repo HEAD 2e2da23 (local). Follow-up: 13:30 UTC market open (pending sells + D1 Sep 1 enforcement test), 22:00 UTC swing_cron first DOW 2-6 test.

**Monday, August 31, 2026 Control-244 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-244 at ~10:00 UTC Aug 31 (12:00 +02). Market CLOSED (next open Mon 13:30 UTC TODAY, first weekday open since Aug 28 + first D1 Sep 1 enforcement trading-day test TODAY). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). No new findings vs Control-242. Control-217 reliability redeploy holding ~23h: PAST 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3562-5318ms avg 4167ms 30 consecutive ok runs NO sustained creep early avg 4345 late avg 3989 -356ms drift, well below ~20s pre-gap threshold; crypto_cron :07/:37 cadence 10 runs fail-closed skipped (CRYPTO_BARS_UNAVAILABLE/STALE, validTA=0, edge gate not reached but producer wired); swing_cron no new run since 5120 @ Aug 30 22:00, 3 sells 769-771 accepted/no_fill pending Mon 13:30 UTC open, tonight 22:00 UTC is first DOW 2-6 dispatch test). Run-log contiguous 5261-5360 (100 runs, 0 gaps, 0 errors; triggers cron 60/reconcile_cron 30/crypto_cron 10). Latest run 5360 @ 10:00:17 UTC reconcile_cron ok 3924ms 0 errors. 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, 0 strategy=None, MV $8,248.46 (positions sum matches dashboard long_market_value $8,249.90 within rounding). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Equity direction: broker change_today_pct -0.006% (non-zero, broker provides direction, no fallback needed this pass). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, CRYPTO_DATA_INSUFFICIENT, MAINTENANCE_ONLY), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells submitted Aug 30 22:00 UTC, fill or expire at Mon open), 768 filled with complete timestamps. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 10-trade window (762-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,213.22 (live account portfolio_value/equity), cash $89,963.32 (91.6%), buying power $382,596.87, ACTIVE, not PDT, change_today -$5.84, last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T10:00:17Z, last_prune_date 2026-08-31, snapshot 1256 @ 09:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. GitHub docs push via OAuth Contents API working (Control-242 pushed to 5dd551d). Repo HEAD a6755f5 (local), GitHub main 5dd551d. Docs HEAD: this commit (Control-244, local only). Prior: a6755f5 (Control-242). Follow-up: verify swing_cron fires tonight Mon 22:00 UTC (first DOW 2-6 dispatch), watch 13:30 UTC market open (pending sells 769-771 fill/expire + first D1 Sep 1 enforcement trading-day test).

**Monday, August 31, 2026 Control-242 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-242 at ~09:00 UTC Aug 31 (11:00 +02). Market CLOSED (next open Mon 13:30 UTC TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). No new findings vs Control-241. Control-217 reliability redeploy holding ~22h: PAST 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3610-5270ms avg 4265ms 36 consecutive ok runs NO sustained creep, well below ~20s pre-gap threshold; crypto_cron :07/:37 cadence 12 runs fail-closed MATICUSD empty (quality=empty, received=0 valid=0) + AVAXUSD stale latestBarAt 2026-08-30T11:00Z age ~77835s ~21.6h validTA=0, edge gate not reached but producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8); swing_cron no new run since 5120 @ Aug 30 22:00, 3 sells 769-771 accepted/no_fill pending Mon 13:30 UTC open, tonight 22:00 UTC is first DOW 2-6 dispatch test). Run-log contiguous 5221-5340 (120 runs, 0 gaps, 0 errors; triggers cron 72/reconcile_cron 36/crypto_cron 12). Reconcile durations post-Control-217 reset: 3610-5270ms across 36 consecutive ok runs, stable with NO sustained creep (avg 4265ms), well below ~20s pre-gap threshold (WATCH continues; ~22h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (current_state_source=alpaca observed 09:00:21Z, metadata_source=d1 all 23), 0 unattributed, MV $8,264.71 (positions sum matches dashboard long_market_value $8,264.71). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback +0.0091%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT, MAINTENANCE_ONLY), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells submitted Aug 30 22:00 UTC, fill or expire at Mon open), 768 filled with complete timestamps (submitted_at/filled_at/broker_updated_at/last_reconciled_at). Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 10-trade window (762-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,228.03 (live account portfolio_value/equity), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today +$8.97, last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T09:00:16.717Z, last_prune_date 2026-08-31, snapshot 1254 @ 08:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. GitHub docs push via OAuth Contents API now working (Control-241 pushed README/OPERATIONS/RUNBOOK to b43b04a). Repo HEAD a5768b3 (local), GitHub main b43b04a. Docs HEAD: this commit (Control-242, local only). Prior: a5768b3 (Control-241). Follow-up: verify swing_cron fires tonight Mon 22:00 UTC (first DOW 2-6 dispatch), watch 13:30 UTC market open (pending sells 769-771 fill + first D1 Sep 1 enforcement trading-day test).

**Monday, August 31, 2026 Control-241: swing cron DOW fix deployed and live-verified (HEALTHY code/deploy, DEGRADED external)**

Control-241 at ~08:12 UTC Aug 31 (10:12 +02). Strict GET-only control on 6 endpoints (all 200). DEFECT FOUND: swing cron DOW expression `0 22 * * 1-5` was firing Sunday-Thursday instead of Monday-Friday. Root cause: Cloudflare cron DOW uses 1=Sunday through 7=Saturday (confirmed against official Cloudflare docs), not Unix 0=Sunday. So 1-5 = Sun-Thu. Evidence: swing_cron run 5120 fired Sunday Aug 30 22:00 UTC (status=skipped, market_open=0, safe), run 3182 fired Sunday Aug 23; no swing_cron run on Friday Aug 28 or Friday Aug 21 (both missing from swing_cron trigger filter). The swing strategy itself was not harmed because market_open=0 on Sundays caused a safe skip, but Friday swing runs (the most important weekday for after-close swing evaluation) were being missed entirely. FIX: changed `0 22 * * 1-5` to `0 22 * * 2-6` in wrangler.toml, src/index.ts dispatch match, test/audit-regressions.test.ts (2 assertions), test/runtime-config-schema.test.ts (1 scheduled event). No cap or trading-behavior change. 224 tests / 845 assertions pass, typecheck clean, git diff --check clean. Deployed via direct CF API PUT (HTTP 200, success: true, bundle 328KB, metadata crons confirmed `0 22 * * 2-6`). Live-verified post-deploy: /health 200 v2.6.0, /api/config caps 5000/3700/2000 unchanged, /api/runs shows daytrading cron + reconcile_cron + crypto_cron all dispatching (run 5325 @ 08:11, 5324 @ 08:10, 5323 @ 08:07). Control-217 reliability redeploy holding ~21h, no fifth cron dispatch failure. Run-log contiguous 5226-5325 (100 runs, 0 gaps, 0 errors). Reconcile 3946-5270ms avg 4351ms 30 runs NO creep. 23 swing positions broker-authoritative MV $8,255.70, 0 unattributed. Equity $98,219.02. 3 pending sells 769-771 await Mon open. Crypto fail-closed (MATICUSD empty + AVAXUSD stale ~22h). Fee telemetry stale (Aug 19). Code bd6205e. Next swing_cron dispatch: Monday Aug 31 22:00 UTC (today) — first correct weekday dispatch under the fix. Follow-up: verify swing_cron fires tonight at 22:00 UTC and does NOT fire on Sunday Sep 6.

**Monday, August 31, 2026 Control-240 weekly read-only review (HEALTHY code/deploy, DEGRADED external, no defect found, no deploy needed)**

Control-240 at ~08:02 UTC Aug 31 (10:02 +02). Weekly read-only review. Market CLOSED (next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No material non-vital defect found; no deploy or code change needed. Code 79583d8 (Control-172, unchanged). Control-217 reliability redeploy holding ~21h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3946-4429ms avg 4105ms 9 consecutive ok runs 5292-5321 NO sustained creep; crypto_cron :07/:37 cadence fail-closed MATICUSD empty + AVAXUSD/ETHUSD stale latestBarAt 2026-08-30T08:45Z age ~77835s ~22h validTA=0, edge gate not reached but producer wired; swing_cron 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5292-5321 (30 runs, 0 gaps, 0 errors; triggers cron 18/reconcile_cron 9/crypto_cron 3). Reconcile durations post-Control-217 reset: 3946-4429ms across 9 consecutive ok runs, stable with NO sustained creep (avg 4105ms), well below ~20s pre-gap threshold (WATCH continues; ~21h, past upper bound, longest clean stretch ever observed).

**Weekly review verification matrix (8 checks, all PASS):**
1. **Broker-confirmed fills in D1 quantities:** 23 swing positions broker-authoritative (current_state_source=alpaca observed 2026-08-31T08:02:31.921Z, metadata_source=d1 all 23), 0 unattributed, MV $8,273.62 (matches dashboard long_market_value $8,273.62). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Pending sells 769 (GE 0.35), 770 (RTX 0.59), 771 (UNH 0.3) all match current broker positions.
2. **Rejected orders no retry/duplicate:** 0 rejected/canceled/expired/failed in 50-trade sample (page 2, IDs 672-721). 0 duplicate client_order_ids across all 50 trades. No retry storms or duplicate submissions detected.
3. **Crypto broker minimum notional:** Crypto fail-closed at CRYPTO_BARS_UNAVAILABLE (MATICUSD empty) + CRYPTO_BARS_STALE (AVAXUSD/ETHUSD ~22h stale, age ~77835s vs maxStaleSeconds 2700) + CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). Edge gate not reached but producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). No crypto orders placed, no broker minimum notional violation possible.
4. **Fees in gross/fee/net analysis:** Aggregate fees $272.82 total (crypto $269.11, regulatory $3.71), ALL unattributed (conservative policy: uncertain fees stay unattributed rather than forced weak matches). Per-trade gross/fee/net=null (accounting_status=filled_lot_exact_unavailable for filled, no_fill for pending, fee_attribution=none-recorded). netTotalPl -$474.86. Fee telemetry stale (asOf 2026-08-19T02:47:31Z, cryptoFeeTelemetryStatus=unavailable). Account-level regulatory fees $3.71 separated from crypto fees.
5. **Skip/error reasons structured:** All skip events carry type/code/scope/message/context. Codes observed: RECONCILIATION_DEFERRED_TO_MAINTENANCE (daytrading+crypto defer to reconcile_cron), MARKET_CLOSED (daytrading off-hours), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_BARS_STALE (AVAXUSD/ETHUSD), CRYPTO_DATA_INSUFFICIENT (validTA=0 required=3), EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, observability-only), MAINTENANCE_ONLY (reconcile brokerOrders=3 pendingLookups=3 lookupFailures=0 ledgerPages=1 not truncated/degraded). 0 errors, 0 lease errors across all 30 runs.
6. **Lifecycle correlation:** 50/50 filled trades in page 2 sample have complete lifecycle timestamps (submitted_at, filled_at, broker_updated_at all present). Trade 768 (T sell, daytrading) shows status=filled, filled_notional=$21.26, accounting_status=filled_lot_exact_unavailable. Pending sells 769-771 show submitted_at present, filled_at=null (correct for unfilled).
7. **Reservation cleanup:** Reconcile run 5320 (MAINTENANCE_ONLY): brokerOrders=3, imported=0, pendingLookups=3, lookupFailures=0, ledgerActivities=0, ledgerPages=1, not truncated/degraded. No active or expired reservations buildup. last_prune_date 2026-08-31.
8. **Caps unchanged:** daytrading=5000, swing=3700, crypto=2000 USD confirmed in /api/config, /api/dashboard capitalCaps, and source code (capital-caps.ts:6-8). Swing cap enforcement active: recent decisions show "Swing capital cap exhausted: $8273.62 allocated against $3700.00" blocking new BUYs (9 BUYs rejected at swing_cron 22:00). Position value MAY exceed cap per Joachim's semantics (cap = total initial investment, not MV ceiling).

**Strategy comparison:** swing 65 trades (0 wins/5 losses, winRate 0%, gross=None, fees=0), crypto 250 trades (1 win/8 losses, winRate 11.1%, fees=$269.11), daytrading 364 trades (8 wins/19 losses, winRate 29.6%, fees=0). Win rates computed on closed positions only; most trades still open. Coverage gap 92 (679 strategy total vs 771 dashboard total, grew +3 from 89 = 3 pending sells not yet in strategy buckets, known pre-existing observability issue).

**Steady-state metrics:** Equity $98,236.94 (live account portfolio_value/equity), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today +$17.88 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T08:00:15Z, last_prune_date 2026-08-31. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-240, local only). Prior: 3fa6f28 (Control-239).

**Monday, August 31, 2026 Control-239 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-239 at ~07:00 UTC Aug 31 (09:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-238. Control-217 reliability redeploy holding ~20h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 2640-6086ms 66 consecutive ok runs 5082-5301 NO sustained creep avg 4056ms; crypto_cron :07/:37 cadence 22 runs fail-closed MATICUSD empty + ETHUSD stale latestBarAt 2026-08-30T08:45Z age ~78735s ~22h validTA=0, edge gate not reached but producer wired; swing_cron 1 run 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5082-5301 (220 runs, 0 gaps, 0 errors; triggers cron 131/reconcile_cron 66/crypto_cron 22/swing_cron 1). Reconcile durations post-Control-217 reset: 2640-6086ms across 66 consecutive ok runs, stable with NO sustained creep (avg 4056ms), well below ~20s pre-gap threshold (WATCH continues; ~20h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,252.23 (matches dashboard long_market_value $8,252.064456). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0 at crypto run time, equity_delta_fallback -0.0045%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in recent trade window. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,215.38 (live account portfolio_value/equity), cash $89,963.32 (91.6%), ACTIVE, not PDT, change_today -$3.68 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T07:00:17.529Z, last_prune_date 2026-08-31, snapshot 1250 @ 06:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-239, local only). Prior: 6542f35 (Control-238).

**Monday, August 31, 2026 Control-238 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-238 at ~06:00 UTC Aug 31 (08:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-237. Control-217 reliability redeploy holding ~19h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 2640-6086ms 60 consecutive ok runs 5081-5280 NO sustained creep; crypto_cron :07/:37 cadence 20 runs fail-closed MATICUSD empty + ETHUSD stale latestBarAt 2026-08-30T07:45Z age ~78735s ~22h validTA=0, edge gate not reached but producer wired; swing_cron 1 run 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5081-5280 (200 runs, 0 gaps, 0 errors; triggers cron 119/reconcile_cron 60/crypto_cron 20/swing_cron 1). Reconcile durations post-Control-217 reset: 2640-6086ms across 60 consecutive ok runs, stable with NO sustained creep, well below ~20s pre-gap threshold (WATCH continues; ~19h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (current_state_source=alpaca observed 06:00:28Z, metadata_source=d1 all 23), 0 unattributed, MV $8,235.75 (matches dashboard long_market_value $8,235.746556). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.020%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in recent trade window. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,199.07 (live account portfolio_value/equity), cash $89,963.32 (91.6%), ACTIVE, not PDT, change_today -$19.99 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T06:00:17.741Z, last_prune_date 2026-08-31, snapshot 1248 @ 05:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-238, local only). Prior: 7f5a206 (Control-237).

**Monday, August 31, 2026 Control-237 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-237 at ~05:01 UTC Aug 31 (07:01 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-236. Control-217 reliability redeploy holding ~18h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3720-6086ms 30 consecutive ok runs 5164-5260 NO sustained creep, 6086ms single spike at run 5190 +1172ms above Control-236 max 4914ms normal variation well below ~10-12s warning threshold, early avg 4582ms late avg 4091ms drift -491ms; crypto_cron :07/:37 cadence 10 runs 5163@00:07/5173@00:37/5183@01:07/5193@01:37/5203@02:07/5213@02:37/5223@03:07/5233@03:37/5243@04:07/5253@04:37 fail-closed MATICUSD empty + symbol rotation SOLUSD/LINKUSD/ETHUSD/AVAXUSD all stale ~22h latestBarAt Aug 30 ~02:00-06:45 UTC age ~78000-79600s validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5162-5261 (100 runs, 0 gaps, 0 errors; triggers cron 60/reconcile_cron 30/crypto_cron 10). Reconcile durations post-Control-217 reset: 3720-6086ms across 30 consecutive ok runs (5164-5260), stable with NO sustained creep (6086ms single spike at 5190, 5270ms at 5250, both surrounded by normal values, early avg 4582ms late avg 4091ms drift -491ms), well below ~20s pre-gap threshold (WATCH continues; ~18h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,222.91 (matches dashboard long_market_value $8,222.910856). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD/LINKUSD/SOLUSD/AVAXUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.033%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 20-trade window (752-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,186.23 (live account portfolio_value/equity), cash $89,963.32 (91.6%), ACTIVE, not PDT, change_today -$32.83 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). snapshot 1246 @ 04:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-237, local only). Prior: 78dd37f (Control-236).

**Monday, August 31, 2026 Control-236 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-236 at ~04:00 UTC Aug 31 (06:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-235. Control-217 reliability redeploy holding ~17h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3720-4914ms 15 consecutive ok runs 5194-5240 NO sustained creep, 4914ms new individual high at 5240 +142ms above Control-235 max 4772ms normal variation well below ~10-12s warning threshold; crypto_cron :07/:37 cadence 5193@01:37/5203@02:07/5213@02:37/5223@03:07/5233@03:37 fail-closed MATICUSD empty + LINKUSD stale latestBarAt 2026-08-30T05:30Z age ~79635s ~22h validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5192-5241 (50 runs, 0 gaps, 0 errors; triggers cron 30/reconcile_cron 15/crypto_cron 5). Reconcile durations post-Control-217 reset: 3720-4914ms across 15 consecutive ok runs (5194-5240), stable with NO sustained creep, well below ~20s pre-gap threshold (WATCH continues; ~17h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,216.63 (positions sum matches account long_market_value $8,216.628456). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + LINKUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.042%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 20-trade window (752-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,179.95 (live account portfolio_value/equity), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$39.11 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T04:00:18Z, last_prune_date 2026-08-31, snapshot 1244 @ 03:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-236, local only). Prior: 83b4326 (Control-235).

**Monday, August 31, 2026 Control-235 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-235 at ~03:00 UTC Aug 31 (05:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-234. Control-217 reliability redeploy holding ~16h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3720-4772ms 9 consecutive ok runs 5194-5220 NO creep; crypto_cron :07/:37 cadence 5193@01:37/5203@02:07/5213@02:37 fail-closed MATICUSD empty + SOLUSD stale latestBarAt 2026-08-30T04:30Z age ~79635s validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5191-5220 (30 runs, 0 gaps, 0 errors; triggers cron 18/reconcile_cron 9/crypto_cron 3). Reconcile durations post-Control-217 reset: 3720-4772ms across 9 consecutive ok runs (5194-5220), stable with NO creep, well below ~20s pre-gap threshold (WATCH continues; ~16h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (current_state_source=alpaca observed 03:00:34Z, metadata_source=d1 all 23), 0 unattributed, MV $8,210.65 (positions sum matches account long_market_value $8,210.65). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + SOLUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.046%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 50-trade window (722-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,173.97 (live account), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$45.09 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T03:00:17.295Z, last_prune_date 2026-08-31. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-235, local only). Prior: daf82f4 (Control-234).

**Monday, August 31, 2026 Control-234 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-234 at ~02:00 UTC Aug 31 (04:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00 — TODAY, first weekday open since Aug 28). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-233. Control-217 reliability redeploy holding ~15h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — longest clean stretch ever observed continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 4296-4614ms 3 consecutive ok runs 5194/5197/5200 NO creep; crypto_cron 5193 @ 01:37:15 fail-closed MATICUSD empty + LINKUSD stale latestBarAt 2026-08-30T03:30Z age ~79635s validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5191-5200 (10-run sample, 0 gaps, 0 errors; triggers cron 6/reconcile_cron 3/crypto_cron 1). Reconcile durations post-Control-217 reset: 4296-4614ms across 3 consecutive ok runs (5194, 5197, 5200), stable with NO creep, well below ~20s pre-gap threshold (WATCH continues; ~15h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (current_state_source=alpaca observed 02:00:29Z, metadata_source=d1 all 23), 0 unattributed, MV $8,210.76 (positions sum matches account long_market_value). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + LINKUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.044%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 10-trade window 762-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,174.08 (live account portfolio_value/equity), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$44.98 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T02:00:17.747Z, last_prune_date 2026-08-31, snapshot 1240 @ 01:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-234, local only). Prior: 4b76f2f (Control-233).

**Monday, August 31, 2026 Control-233 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-233 at ~01:00 UTC Aug 31 (03:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-232. Control-217 reliability redeploy holding ~14h: PAST the 8-16h recurrence window upper bound of all 4 prior cron dispatch occurrences with NO fifth occurrence — first time past the upper bound while still clean. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3705-4756ms 12 consecutive ok runs 5169-5180 NO creep; crypto_cron :07/:37 cadence 5143@23:07/5153@23:37/5163@00:07/5173@00:37 fail-closed MATICUSD empty + ETHUSD stale latestBarAt 2026-08-30T02:30Z age ~79635s validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00, 3 sells 769-771 accepted/no_fill pending Mon open). Run-log contiguous 5141-5180 (40-run sample, 0 gaps, 0 errors; triggers cron 24/reconcile_cron 12/crypto_cron 4). Reconcile durations post-Control-217 reset: 3705-4756ms across 12 consecutive ok runs (5169-5180), stable with NO creep, well below ~20s pre-gap threshold (WATCH continues; ~14h, past upper bound, longest clean stretch ever observed). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,244.14 (positions sum) / $8,245 (account long_MV). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.0006%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells Sun 22:00, fill or expire Mon open), 768 filled. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 8-trade window 764-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,208.32 (live account portfolio_value/equity), cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$10.74 (broker overnight marking, market closed), last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). broker_ledger_synced_until 2026-08-31T01:00:17.821Z, last_prune_date 2026-08-31, snapshot 1238 @ 00:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-233, local only). Prior: ee2bdbc (Control-232).

**Monday, August 31, 2026 Control-232 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-232 at ~00:00 UTC Aug 31 (02:00 +02). HEALTHY code/deploy (2.6.0), DEGRADED external, no deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-231. Control-217 reliability redeploy holding ~13h: all 4 schedules dispatching, reconcile 3494-4756ms (runs 5124-5160, NO creep). Run-log contiguous 4762-5161 (400 runs, 0 gaps, 0 errors). 23 swing positions MV $8,259.53, 0 unattributed. Equity $98,222.85, snapshot 1236 @ 23:37:14. 771 total trades / 768 executed, win 14.5% (no new trades since Control-231). Caps 5000/3700/2000 unchanged. ~13h into ~8-16h cron dispatch recurrence window (PAST longest prior failure interval ~12h, still clean, WATCH; 4 prior occurrences). 224 tests/845 assertions, typecheck clean, git diff --check clean. Docs HEAD: this commit (Control-232, local only). Prior: 86759ec (Control-231).

**Sunday, August 30, 2026 Control-231 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-231 at ~23:00 UTC Aug 30 (Aug 31 01:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-230. Control-217 reliability redeploy holding ~12h: all 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 2640-3931ms 15 consecutive ok runs 5091-5140 NO creep; crypto_cron :07/:37 cadence 5133@22:37 fail-closed MATICUSD empty + ETHUSD stale latestBarAt 2026-08-30T00:30Z age ~79635s validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ 22:00). Run-log contiguous 4821-5140 (320 runs, 0 gaps, 0 errors; triggers cron 29/reconcile_cron 15/crypto_cron 5/swing_cron 1 in 50-run sample). Reconcile durations post-Control-217 reset: 2640-3931ms across 15 consecutive ok runs (5091-5140), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~12h into ~8-16h recurrence window, past lower bound approaching upper bound and still clean; 4 prior occurrences). 23 swing positions broker-authoritative (metadata_source=d1 all 23, current_state_source=alpaca), 0 unattributed, MV $8,255.74. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK), 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 20-trade window 752-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 771 total trades / 768 executed, win 14.5% (no new trades since Control-230). broker_ledger_synced_until 2026-08-30T23:00:16.765Z, last_prune_date 2026-08-30, snapshot 1234 @ 22:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-231, local only). Prior: fca9fdd (Control-230).

**Sunday, August 30, 2026 Control-230 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-230 at ~22:00 UTC Aug 30 (Aug 31 00:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-229. Control-217 reliability redeploy holding ~11h: all 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 2640-3719ms 18 consecutive ok runs 5063-5119 NO creep; crypto_cron :07/:37 cadence 5082@20:07/5092@20:37/5102@21:07/5112@21:37 fail-closed; swing_cron 5120 @ 22:00:24 skipped market_open=0 dur 10537ms). Run-log contiguous 4821-5120 (300 runs, 0 gaps, 0 errors; triggers cron 181/reconcile_cron 88/crypto_cron 30/swing_cron 1). Reconcile durations post-Control-217 reset: 2640-3719ms across 18 consecutive ok runs (5063-5119), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~11h into ~8-16h recurrence window, past lower bound and still clean; 4 prior occurrences). 23 swing positions broker-authoritative (metadata_source=d1 all 23, current_state_source=alpaca), 0 unattributed, MV $8,255.74. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + LINKUSD stale ~22h + CRYPTO_DATA_INSUFFICIENT, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK), 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 60-trade window 712-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 771 total trades / 768 executed, win 14.5% (3 new no_fill swing sells 769-771 since Control-229, executed unchanged). snapshot 1232 @ 22:00:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Observation: swing_cron dispatched at 22:00 UTC Sunday (repo wrangler.toml DOW=1-5 excludes Sunday; live cron registry may differ, but code market_open=0 guard skipped safely with no trading behavior change; low-priority follow-up to verify live cron DOW on next deploy). Docs HEAD: this commit (Control-230, local only). Prior: 9ef4767 (Control-229).

**Sunday, August 30, 2026 Control-229 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-229 at ~21:00 UTC Aug 30 (23:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-228. Control-217 reliability redeploy holding ~10h: all 4 schedules dispatching (daytrading cron every 5 min 4900-5098 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min 4903-5099 ok dur 2641-3797ms 60 consecutive ok runs NO creep; crypto_cron :07/:37 cadence 4902@11:07 through 5092@20:37 fail-closed; swing_cron no weekend run Sunday 0 22 * * 1-5). Run-log contiguous 4900-5099 (200 runs, 0 gaps, 0 errors; triggers cron 120/reconcile_cron 60/crypto_cron 20). Reconcile durations post-Control-217 reset: 60 consecutive runs 4903-5099 all 2641-3797ms (~2.6-3.8s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~10h into ~8-16h recurrence window, past upper bound and still clean; 4 prior occurrences). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 21:00:26Z. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + LINKUSD stale latestBarAt 2026-08-29T22:30Z age ~79635s ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, symbol rotation AVAXUSD->LINKUSD, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 1 in 100-trade window 669-768: id=703 PLD, 0 new since Aug 24). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T21:00:15.916Z, last_prune_date 2026-08-30, snapshot 1229 @ 20:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-229, local only). Prior: 0b7bf53 (Control-228).

**Sunday, August 30, 2026 Control-228 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-228 at ~20:00 UTC Aug 30 (22:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-227. Control-217 reliability redeploy holding ~9h: all 4 schedules dispatching (daytrading cron every 5 min 4980-5078 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min 4983-5079 ok dur 2641-3797ms 30 consecutive ok runs NO creep; crypto_cron :07/:37 cadence 4982@15:07/4992@15:37/5002@16:07/5012@16:37/5022@17:07/5032@17:37/5042@18:07/5052@18:37/5062@19:07/5072@19:37 fail-closed; swing_cron no weekend run Sunday 0 22 * * 1-5). Run-log contiguous 4980-5079 (100 runs, 0 gaps, 0 errors; triggers cron 60/reconcile_cron 30/crypto_cron 10). Reconcile durations post-Control-217 reset: 30 consecutive runs 4983-5079 all 2641-3797ms (~2.6-3.8s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~9h into ~8-16h recurrence window, approaching upper bound; 4 prior occurrences). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 20:00:26Z. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + AVAXUSD stale latestBarAt 2026-08-29T21:30Z age ~79635s ~22h + CRYPTO_DATA_INSUFFICIENT validTA=0, symbol rotation ETHUSD->AVAXUSD, edge gate not reached but producer wired). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T20:00:16.752Z, last_prune_date 2026-08-30, snapshot 1227 @ 19:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-228, local only). Prior: e38c968 (Control-227).

**Sunday, August 30, 2026 Control-227 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-227 at ~19:02 UTC Aug 30 (21:02 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-226. Control-217 reliability redeploy holding ~8h: all 4 schedules dispatching (daytrading cron every 5 min 5001-5060 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min 5003-5059 ok dur 2641-3797ms avg ~2825ms 18 consecutive ok runs NO creep; crypto_cron :07/:37 cadence 5002@16:07/5012@16:37/5022@17:07/5032@17:37/5042@18:07/5052@18:37 fail-closed; swing_cron no weekend run Sunday 0 22 * * 1-5). Run-log contiguous 5001-5060 (60 runs, 0 gaps, 0 errors; triggers cron 36/reconcile_cron 18/crypto_cron 6). Reconcile durations post-Control-217 reset: 18 consecutive runs 5003-5059 all 2641-3797ms (~2.6-3.8s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold. NOW ENTERING ~8-16h cron dispatch recurrence window (4 prior occurrences; ~8h into post-Control-217). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 19:02:27Z. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, observability-only). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 8 (pre-existing, 0 in 60-trade window 709-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T19:00:16.146Z, last_prune_date 2026-08-30, snapshot 1225 @ 18:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, dashboard capitalCaps confirmed). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-227, local only). Prior: c4763a9 (Control-226).

**Sunday, August 30, 2026 Control-226 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-226 at ~18:00 UTC Aug 30 (20:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-225. Control-217 reliability redeploy holding ~7.5h: all 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED skips nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok 2641-3797ms avg 3037ms 18 consecutive ok runs 4983-5039 NO creep; crypto_cron :07/:37 fail-closed MATICUSD empty + LINKUSD/ETHUSD stale latestBarAt 2026-08-29T19:30Z age ~79635s validTA=200, symbol rotation LINKUSD->ETHUSD, edge gate not reached; swing_cron no weekend run Sunday 0 22 * * 1-5). Run-log contiguous 4981-5040 (60 runs, 0 gaps, 0 errors; triggers cron 36/reconcile_cron 18/crypto_cron 6). Reconcile durations post-Control-217 reset: 18 consecutive runs 4983-5039 all 2641-3797ms (~2.6-3.8s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~7.5h into ~8-16h recurrence window). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 60-trade window 709-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. snapshot 1223 @ 17:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-226, local only). Prior: 4aa65a4 (Control-225).

**Sunday, August 30, 2026 Control-224 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-224 at ~17:00 UTC Aug 30 (19:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-223. Control-217 reliability redeploy holding ~6h: all 4 schedules dispatching (daytrading cron every 5 min 4820-5018 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron 5019 @ 17:00:16 ok dur 2768ms MAINTENANCE_ONLY brokerOrders 0 ledgerPages 1 not truncated/degraded, every 10 min 4822-5019; crypto_cron 5012 @ 16:37:15 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE ETHUSD latestBarAt 2026-08-29T18:30Z age ~79635s + CRYPTO_DATA_INSUFFICIENT validTA=0, :07/:37 cadence 4825@07:07 through 5012@16:37; swing_cron no weekend run (Sunday, 0 22 * * 1-5)). Run-log contiguous 4820-5019 (200 runs, 0 gaps, 0 errors; triggers cron 122/reconcile_cron 58/crypto_cron 20). Reconcile durations post-Control-217 reset: 35 consecutive runs 4903-5019 all 2650-3797ms (~2.7-3.8s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~6h into ~8-16h recurrence window). Pre-reset plateau 7799-8960ms (runs 4822-4892). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 17:00:23Z. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h, edge gate not reached; symbol rotation AVAXUSD->ETHUSD). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 1 visible in 100-trade window 669-768: id=703 PLD, 0 new since Aug 24). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T17:00:15.872Z, last_prune_date 2026-08-30, snapshot 1221 @ 16:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-224, local only). Prior: 42ad4de (Control-222).

**Sunday, August 30, 2026 Control-222 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-222 at ~16:00 UTC Aug 30 (18:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-221. Control-217 reliability redeploy holding ~5h: all 4 schedules dispatching (daytrading cron every 5 min 4970-4998 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron 4999 @ 16:00:16 ok dur 3193ms MAINTENANCE_ONLY brokerOrders 0 ledgerPages 1 not truncated/degraded, every 10 min 4973-4999; crypto_cron 4992 @ 15:37:15 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE AVAXUSD latestBarAt 2026-08-29T17:30Z age ~79635s + CRYPTO_DATA_INSUFFICIENT validTA=0, :07/:37 cadence 4972@14:37/4982@15:07/4992@15:37; swing_cron no weekend run (Sunday, 0 22 * * 1-5)). Run-log contiguous 4970-4999 (30-run sample, 0 gaps, 0 errors; triggers cron 18/reconcile_cron 9/crypto_cron 3). Reconcile durations post-Control-217 reset: 9 consecutive runs 4973-4999 all 3040-3640ms (~3.0-3.6s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH, ~5h into ~8-16h recurrence window). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 16:00:38Z. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + AVAXUSD stale ~22h, edge gate not reached; symbol rotation ETHUSD->AVAXUSD). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.0026. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T16:00:16.327Z, last_prune_date 2026-08-30, snapshot 1219 @ 15:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-222, local only). Prior: e711270 (Control-221).

**Sunday, August 30, 2026 Control-221 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-221 at ~15:00 UTC Aug 30 (17:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-220. Control-217 reliability redeploy holding ~4h: all 4 schedules dispatching (daytrading cron 4978 @ 14:56 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00, every 5 min 4960-4978; reconcile_cron 4979 @ 15:00 ok dur 3559ms MAINTENANCE_ONLY brokerOrders 0, every 10 min 4923-4979 18 consecutive runs post-redeploy; crypto_cron 4972 @ 14:37 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE ETHUSD latestBarAt 2026-08-29T16:30Z age ~79635s + CRYPTO_DATA_INSUFFICIENT validTA=0, :07/:37 cadence 4922@12:07/4932@12:37/4942@13:07/4952@13:37/4962@14:07/4972@14:37; swing_cron no weekend run (Sunday, 0 22 * * 1-5)). Run-log contiguous 4920-4979 (60-run sample, 0 gaps, 0 errors; triggers cron 36/reconcile_cron 18/crypto_cron 6). Reconcile durations post-Control-217 reset: 18 consecutive runs 4923-4979 all 2932-3626ms (~2.9-3.6s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca. D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T15:00:16.693Z, last_prune_date 2026-08-30, snapshot 1217 @ 14:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-221, local only). Prior: 2df5c05 (Control-220).

**Sunday, August 30, 2026 Control-220 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-220 at ~14:00 UTC Aug 30 (16:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-219. Control-217 reliability redeploy holding ~3h: all 4 schedules dispatching (daytrading cron 4958 @ 13:56 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00, every 5 min 4951/4954/4955/4957/4958; reconcile_cron 4959 @ 14:00 ok dur 3203ms MAINTENANCE_ONLY brokerOrders 0, every 10 min 4913-4959 15 consecutive runs post-redeploy; crypto_cron 4952 @ 13:37 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE ETHUSD latestBarAt 2026-08-29T15:30Z age ~79635s + CRYPTO_DATA_INSUFFICIENT validTA=0, :07/:37 cadence 4922@12:07/4932@12:37/4942@13:07/4952@13:37; swing_cron no weekend run (Sunday, 0 22 * * 1-5)). Run-log contiguous 4660-4959 (300-run sample, 0 gaps, 0 errors; triggers cron 183/reconcile_cron 87/crypto_cron 30). Reconcile durations post-Control-217 reset: 15 consecutive runs 4913-4959 all 2891-3626ms (~2.9-3.6s), fresh reset holding stable with NO creep, well below ~20s pre-gap threshold (WATCH). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 14:01:07Z; D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution, comment "D1 may provide metadata, but never substitutes for a failed broker read"). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + ETHUSD stale ~22h, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:65/130/308/657 minEdgeAfterCosts=8, EDGE_CALIBRATION_UNAVAILABLE/INSUFFICIENT_NET_EDGE structured). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening; resolveEquityDirection index.ts). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T14:00:16.359Z, last_prune_date 2026-08-30, snapshot 1215 @ 13:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-220, local only). Prior: cd97ff2 (Control-219).

**Sunday, August 30, 2026 Control-219 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-219 at ~13:05 UTC Aug 30 (15:05 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC / 09:30-04:00). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-218. Control-217 reliability redeploy holding ~2h: all 4 schedules dispatching (daytrading cron 4938 @ 12:56 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00, every 5 min 4930-4938; reconcile_cron 4939 @ 13:00 ok dur 3128ms MAINTENANCE_ONLY brokerOrders 0, every 10 min 4933/4936/4939; crypto_cron 4932 @ 12:37 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE SOLUSD + CRYPTO_DATA_INSUFFICIENT validTA=0, :07/:37 cadence 4922@12:07/4932@12:37; swing_cron no weekend run (Sunday, 0 22 * * 1-5)). Run-log contiguous 4841-4940 (100-run sample, 0 gaps, 0 errors; triggers cron 62/reconcile_cron 28/crypto_cron 10; status skipped 72/ok 28). Reconcile durations post-Control-217 reset: 3128/3199/3112ms (~3.1-3.2s), fresh reset holding, well below ~20s pre-gap threshold (WATCH). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74; current_state_source=alpaca observed 13:00:46Z; D1 fallback guard confirmed in code (api.ts: positionsAvailable=false on broker failure, no D1 live-state substitution, comment "D1 may provide metadata, but never substitutes for a failed broker read"). Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + stale symbols, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:29 prepareCryptoRiskDecision, minEdgeAfterCosts=8, EDGE_CALIBRATION_UNAVAILABLE/INSUFFICIENT_NET_EDGE structured). Equity direction: EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback -0.00000265%, observability-only, no risk weakening; resolveEquityDirection index.ts). Skip reasons structured with codes/scopes/context, 0 errors, 0 lease errors. Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, status=filled). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. broker_ledger_synced_until 2026-08-30T13:00:16.280Z, last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-219, local only). Prior: 96ce7be (Control-218).

**Sunday, August 30, 2026 Control-218 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-218 at ~12:00 UTC Aug 30 (14:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-217. Control-217 reliability redeploy holding ~1h: all 4 schedules dispatching (daytrading cron 4918 @ 11:56 MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron 4919 @ 12:00 ok dur 3192ms; crypto_cron 4912 @ 11:37 fail-closed CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE; swing_cron no weekend run). Run-log contiguous 4620-4919 (300-run sample, 0 gaps, 0 errors). Reconcile durations post-Control-217 reset: 3351→2891→3738→3235→2949→3192ms (~2.9-3.7s), fresh reset holding, well below ~20s pre-gap threshold (WATCH). Pre-redeploy max 9390ms (plateau, pre-4892). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (MATICUSD empty + stale symbols, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:29). Skip reasons structured: MARKET_CLOSED, RECONCILIATION_DEFERRED_TO_MAINTENANCE, CRYPTO_BARS_UNAVAILABLE, EQUITY_DIRECTION_FALLBACK (error_details populated, 0 lease errors). Trade lifecycle complete. Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-218, local only). Prior: decde44 (Control-217).

---

**Sunday, August 30, 2026 Control-217 strict read-only control + reliability redeploy (HEALTHY code/deploy, DEGRADED external, cron dispatch fix applied)**

Control-217 at ~11:00 UTC Aug 30 (13:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. KEY FINDING: FOURTH cron dispatch failure detected. Reconcile_cron schedule stopped dispatching after run 4892 @ 10:30:36 UTC (~12h post-Control-203 redeploy Aug 29 ~23:00 UTC, within the ~8-16h recurrence window). Daytrading (cron) and crypto_cron continued normally (daytrading through 4899 @ 10:56, crypto through 4895 @ 10:37). Three reconcile runs missing (10:40, 10:50, 11:00 UTC). DEPLOY EXECUTED: reliability redeploy via direct Cloudflare API PUT (same code 79583d8, re-registered 4 crons in metadata), authorized under Aug 25 standing maintenance rule. Deploy returned HTTP 200 success. Post-deploy verification: /health 200 (2.6.0), all 6 GET endpoints 200, all 4 cron schedules confirmed registered via /schedules API. Reconcile dispatch RESTORED: run 4903 @ 11:10:16 UTC status ok dur 3351ms (fresh post-redeploy reset from ~7799ms plateau, consistent with Control-186/203 pattern). Daytrading 4901 @ 11:06, 4900 @ 11:01. Crypto 4902 @ 11:07. All 4 schedules dispatching. Run-log contiguous 4600-4899 (300-run sample, 0 gaps, 0 errors) pre-deploy; post-deploy 4900-4903 contiguous. 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (CRYPTO_BARS_STALE/UNAVAILABLE, edge gate not reached). Trade lifecycle complete. Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since Aug 24, 0 in 50-trade window 719-768). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. Snapshot 1210 (11:07 UTC). Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Code 79583d8 (Control-172, unchanged). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-217, local only). Prior: 0b0ef10 (Control-216).

**Sunday, August 30, 2026 Control-216 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-216 at ~10:45 UTC Aug 30 (12:45 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-215. Control-203 cron dispatch fix holding ~11.5h: all 4 schedules dispatching (daytrading every 5 min MARKET_CLOSED latest run 4896 @ 10:41:29 UTC nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok latest run 4894 @ 10:30:36 UTC dur 7799ms, MAINTENANCE_ONLY brokerOrders 0 ledger not truncated/degraded; crypto_cron :07/:37 fail-closed latest run 4895 @ 10:37:32 UTC CRYPTO_BARS_STALE/UNAVAILABLE/DATA_INSUFFICIENT, 3521ms; swing_cron no weekend run). Run-log contiguous 4597-4896 (300-run sample, 0 gaps, 0 errors), now ~60h clean since Control-172. Reconcile durations: 17-sample min 7799ms / max 8960ms, recent 7799-8087ms, plateau holding ~7.8-9.0s, still well below ~20s pre-gap threshold (WATCH, lower concern). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (edge gate not reached). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 pre-existing (451/461/565/568/597/645/648/703, no new nulls; 0 visible in 20-trade window 749-768). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT, change_today -$0.003. 768 trades, win 15%. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-216, local only). Prior: e881681 (Control-215).

**Sunday, August 30, 2026 Control-215 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-215 at ~10:00 UTC Aug 30 (12:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-214. Control-203 cron dispatch fix holding ~11h: all 4 schedules dispatching (daytrading 4881 @ 09:56 MARKET_CLOSED, reconcile 4879 @ 09:50 dur 8960ms brokerOrders 0 ledger not truncated/degraded, crypto 4875 @ 09:37 fail-closed, swing none weekend). Run-log contiguous 4582-4881 (300-run sample, 0 gaps, 0 errors). Reconcile dur 8960ms, plateau ~7.8-9.4s below ~20s threshold (WATCH, lower concern). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE ETHUSD ~22h + UNAVAILABLE MATICUSD, edge gate not reached). Edge producer wired (technical-analysis.ts:414; crypto-strategy.ts:26-27). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since trade 703 Aug 24). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1207. last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-215, local only). Prior: d61762d (Control-214).

**Sunday, August 30, 2026 Control-214 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-214 at ~09:00 UTC Aug 30 (11:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-213. Control-203 cron dispatch fix holding ~11h: all 4 schedules dispatching (daytrading 4861 @ 08:56 MARKET_CLOSED, reconcile 4862 @ 09:00 dur 8372ms brokerOrders 0 ledger not truncated/degraded, crypto 4855 @ 08:37 fail-closed, swing none weekend). Run-log contiguous 4563-4862 (300-run sample, 0 gaps, 0 errors). Reconcile dur 8372ms, plateau ~7.8-9.4s below ~20s threshold (WATCH, lower concern). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE LINKUSD ~22h + UNAVAILABLE MATICUSD, edge gate not reached). Edge producer wired (technical-analysis.ts:414; crypto-strategy.ts:26-27). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since trade 703 Aug 24). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1205. last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-214, local only). Prior: 2753ce1 (Control-213).

**Sunday, August 30, 2026 Control-213 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-213 at ~08:45 UTC Aug 30 (10:45 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-212. Control-203 cron dispatch fix holding ~10h: all 4 schedules dispatching (daytrading every 5 min MARKET_CLOSED latest run 4857 @ 08:41:29 UTC; reconcile_cron every 10 min ok latest run 4856 @ 08:40:37 UTC dur 8578ms, MAINTENANCE_ONLY brokerOrders 0 ledger not truncated/degraded; crypto_cron :07/:37 fail-closed latest run 4855 @ 08:37:33 UTC; swing_cron no weekend run). Run-log contiguous 4608-4857 (250-run sample, 0 missing IDs, 0 errors). Reconcile durations: latest 8578ms, window range 7755-9390ms (9390ms C-211 spike in window then reverted), recent 8135-8578ms, plateau holding ~7.8-9.4s, still well below ~20s pre-gap threshold (WATCH, lower concern). 23 swing positions broker-authoritative (metadata_source=d1, all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE LINKUSD latestBarAt 2026-08-29T10:30Z age ~22h + CRYPTO_BARS_UNAVAILABLE MATICUSD empty, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:26-27). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 in 500-trade sample (451/461/565/568/597/645/648/703, all pre-existing, no new nulls since trade 703 Aug 24). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1205 (08:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-213, local only). Prior: 8804f4c (Control-212).

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172, unchanged). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-213, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 8804f4c (Control-212).
**Sunday, August 30, 2026 Control-212 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-212 at ~08:00 UTC Aug 30 (10:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-211. Control-203 cron dispatch fix holding ~9h: all 4 schedules dispatching (daytrading 4841 @ 07:56 MARKET_CLOSED, reconcile 4842 @ 08:00 dur 7877ms broker_ledger_synced_until advancing to 07:50:36Z, crypto 4835 @ 07:37 fail-closed, swing none weekend). Run-log contiguous 4593-4842 (250-run sample, 0 gaps, 0 errors). Reconcile durations: latest 7877ms REVERTED from 9390ms spike at Control-211 (transient, not continued drift), plateau holding ~7.9-9.4s, still below ~20s threshold (WATCH, lower concern). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (CRYPTO_BARS_STALE ETHUSD ~22h + UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since trade 703 Aug 24). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1203 (07:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-212, local only). Prior: 88cd80c (Control-211).

**Sunday, August 30, 2026 Control-211 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-211 at ~07:00 UTC Aug 30 (09:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-210. Control-203 cron dispatch fix holding ~8h: all 4 schedules dispatching (daytrading 4821 @ 06:56 MARKET_CLOSED, reconcile 4819 @ 06:50 dur 9390ms broker_ledger_synced_until advancing to 06:50:37Z, crypto 4815 @ 06:37 fail-closed, swing none weekend). Run-log contiguous 4503-4821 (extended sample, 0 gaps, 0 errors). Reconcile durations: latest 9390ms (new plateau high, prior max 9170ms), 8416ms, 8336ms — slow upward drift within ~8-9s plateau, still well below ~20s pre-gap threshold (continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (CRYPTO_BARS_STALE LINKUSD ~22h + UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since trade 703 Aug 24). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1201 (06:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-211, local only). Prior: e104592 (Control-210).

**Sunday, August 30, 2026 Control-210 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-210 at ~06:00 UTC Aug 30 (08:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). No new findings vs Control-209. Control-203 cron dispatch fix holding ~7h: all 4 schedules dispatching (daytrading 4801 @ 05:56, reconcile 4799 @ 05:50 dur 8209ms broker_ledger_synced_until advancing to 05:50:36Z, crypto 4795 @ 05:37 fail-closed, swing none weekend). Run-log contiguous 4503-4802 (300-run sample, 0 gaps, 0 errors). Reconcile durations plateaued ~8-9s (range 3320-9170ms across 66 runs ~17.5h, recent 8120-9073ms, latest 8209ms — stable, NOT climbing toward ~20s pre-gap threshold; continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1 all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (CRYPTO_BARS_STALE ETHUSD ~22h + UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 8 (451/461/565/568/597/645/648/703, pre-existing, no new since trade 703 Aug 24). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1199 (05:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-210, local only). Prior: c520000 (Control-209).

**Sunday, August 30, 2026 Control-209 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-209 at ~05:00 UTC Aug 30 (07:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-203 cron dispatch fix holding ~6h post-redeploy: all 4 schedules dispatching cleanly (daytrading every 5 min MARKET_CLOSED latest run 4781 @ 04:56:29 UTC; reconcile_cron every 10 min ok latest run 4782 @ 05:00:37 UTC dur 8625ms; crypto_cron :07/:37 fail-closed latest run 4775 @ 04:37:33 UTC; swing_cron no weekend run). Run-log contiguous 4533-4782 (250-run sample, 0 missing IDs, 0 errors). Reconcile durations plateaued at ~8-9s (range 7755-9170ms across ~6h post-redeploy, latest 8625ms, stable — NOT climbing toward ~20s pre-gap threshold; continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1, all 23), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE ETHUSD ~22h + CRYPTO_BARS_UNAVAILABLE MATICUSD empty, edge gate not reached). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps; crypto-strategy.ts:29-32/66/88/472). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 250-sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 8 in 500-trade sample (451/461/565/568/597/645/648/703, all pre-existing, no new nulls since trade 703 Aug 24). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1197 (04:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-209, local only). Prior: 974c341 (Control-208).

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172, unchanged). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-209, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 974c341 (Control-208).

**Sunday, August 30, 2026 Control-208 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-208 at ~04:00 UTC Aug 30 (06:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-203 cron dispatch fix holding ~5h post-redeploy: all 4 schedules dispatching cleanly (daytrading every 5 min MARKET_CLOSED latest run 4761 @ 03:56:29 UTC; reconcile_cron every 10 min ok latest run 4762 @ 04:00:37 UTC dur 8312ms, broker_ledger_synced_until 2026-08-30T03:50:36Z advancing; crypto_cron :07/:37 fail-closed latest run 4755 @ 03:37:33 UTC; swing_cron no weekend run). Run-log contiguous 4563-4762 (200-run sample, 0 missing IDs, 0 errors). Reconcile durations plateaued at ~8-9s (recent range 8009-8850ms, avg 7732ms across ~5h post-redeploy, stable — NOT climbing toward ~20s pre-gap threshold; continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE SOLUSD ~22h + CRYPTO_BARS_UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). NEW FINDING: Null-strategy trades count corrected from 4 to 8 in recent 500-trade sample (additional: 451 AAVE/USD, 461 AAL, 565 PLUG, 568 BBD — all older pre-existing, no new nulls since trade 703 Aug 24). Page 2 (IDs 1-267) reveals many more nulls in earliest trades (IDs 1-101, Aug 1-7 era). No regression — wider sampling, not new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1195 (03:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-208, local only). Prior: 74e1428 (Control-207).

**Sunday, August 30, 2026 Control-207 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-207 at ~03:00 UTC Aug 30 (05:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-203 cron dispatch fix holding ~4h post-redeploy: all 4 schedules dispatching cleanly (daytrading every 5 min MARKET_CLOSED latest run 4741 @ 02:56:29 UTC; reconcile_cron every 10 min ok latest run 4742 @ 03:00:37 UTC dur 8093ms, broker_ledger_synced_until 2026-08-30T02:50:35Z advancing; crypto_cron :07/:37 fail-closed latest run 4735 @ 02:37:33 UTC; swing_cron no weekend run). Run-log contiguous 4623-4742 (120-run sample, 0 missing IDs, 0 errors). Reconcile durations plateaued at ~8-9s (range 7755-9170ms across ~4h post-redeploy, stable — NOT climbing toward ~20s pre-gap threshold; continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1, current_state observed 2026-08-30T03:00:27Z), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE ETHUSD ~22h + CRYPTO_BARS_UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 4 (597 AFRM, 645 DUK, 648 NOW, 703 PLD — all pre-existing, no new nulls). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1193 (02:37 UTC). last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Docs HEAD: this commit (Control-207, local only). Prior: 6032ec7 (Control-206).

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172, unchanged). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-208, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 74e1428 (Control-207).

**Sunday, August 30, 2026 Control-206 strict read-only control + docs defect fix (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-206 at ~02:00 UTC Aug 30 (04:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-203 cron dispatch fix holding ~3h post-redeploy: all 4 schedules dispatching cleanly (daytrading every 5 min MARKET_CLOSED latest run 4723 @ 02:01:29 UTC; reconcile_cron every 10 min ok latest run 4722 @ 02:00:37 UTC dur ~8-9s, broker_ledger_synced_until 2026-08-30T01:50:36Z advancing; crypto_cron :07/:37 fail-closed latest run 4715 @ 01:37:33 UTC; swing_cron no weekend run). Run-log contiguous 4603-4723 (120-run sample, 0 missing IDs, 0 errors). Reconcile durations plateaued at ~8-9s (range 8019-9170ms across ~3h post-redeploy, stable — NOT climbing toward ~20s pre-gap threshold; continue WATCH). 23 swing positions broker-authoritative (metadata_source=d1), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37, CRYPTO_BARS_STALE AVAXUSD/LINKUSD/SOLUSD + CRYPTO_BARS_UNAVAILABLE MATICUSD, edge gate not reached). Trade lifecycle complete (0 incomplete in sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 4 (597/645/648/703, pre-existing; 1 visible in 100-trade sample). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1191. last_prune_date 2026-08-30. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. **Docs defect fixed this control:** the Control-205 commit (3a5b8e9) only updated README.md, leaving OPERATIONS.md + DEPLOYMENT_RUNBOOK.md at Control-204 (stale by one control, docs did not identify the exact current HEAD). Control-206 restores same-change documentation discipline across all surfaces. Docs HEAD: this commit (Control-206). Prior: 3a5b8e9 (Control-205).

**Sunday, August 30, 2026 Control-204 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-204 at ~00:00 UTC Aug 30 (02:00 +02). Market CLOSED (weekend). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-203 cron dispatch fix holding ~1h: all 4 schedules dispatching cleanly. Run-log contiguous 4483-4682 (200 runs, 0 gaps, 0 errors). **New observation: reconcile durations crept ~3.5s → ~8-9s in ~1h post-redeploy** (leading indicator, still well below ~20s threshold — WATCH). 23 swing positions broker-authoritative (metadata_source=d1), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0 fail-closed (:07/:37). Trade lifecycle complete. Gross/fee/net=null conservative. Null-strategy trades 4 (597/645/648/703, pre-existing). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1187. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-204). Prior: d5db73d (Control-203).

## Saturday, August 29, 2026 Control-202 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**
Control-202 at ~22:00 UTC Aug 29 (Aug 30 00:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code 79583d8 (Control-172, unchanged). Control-186 cron dispatch fix holding ~24h post-redeploy: all 4 schedules dispatching cleanly. Run-log window 4151-4650 (500 runs, ~Aug 28 17:52 - Aug 29 21:56 UTC), 0 missing IDs, 0 new errors (1 pre-existing error run 4182 subrequest ceiling 25033ms Aug 28; run 4148 outside window). 312 daytrading MARKET_CLOSED, 124 reconcile MAINTENANCE_ONLY ok (dur 3.3-4.2s, stable below ~20s threshold), 42 crypto fail-closed :07/:37, 18 swing, 3 lease_held. 23 swing positions broker-authoritative (metadata_source=d1, current_state observed 2026-08-29T22:00:23Z), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0. Trade lifecycle complete (0 incomplete in 200-sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 4 (597/645/648/703, pre-existing). Fee telemetry stale (Aug 19). Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1183. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean. Push BLOCKED: github_pat. Docs HEAD: this commit (Control-202). Prior: 3b574a7 (Control-201).

## Saturday, August 29, 2026 Control-201 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-201 at ~21:00 UTC Aug 29 (Aug 29 23:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-200:** Control-186 cron dispatch fix holding ~23h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4638 at 20:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED, nextOpen 2026-08-31T09:30:00-04:00, dur ~124-172ms); reconcile_cron every 10 min ok (131 MAINTENANCE_ONLY runs in 500-row window at minutes :00/:10/:20/:30/:40/:50, recent dur ~3.5-4.0s, stable well below ~20s pre-gap threshold; max 25033ms is the pre-existing Aug 28 run 4182 outlier); crypto_cron :07/:37 fail-closed (43 runs every 30 min at minutes :07/:37, RECONCILIATION_DEFERRED + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached, dur ~1.3-1.6s post-redeploy vs ~5-6s Aug 28); swing_cron no weekend run. Run-log window 4139-4638 (500 runs, ~Aug 28 17:17 - Aug 29 20:56 UTC), 0 missing run IDs, 0 new errors (2 pre-existing error runs from Aug 28: run 4182 subrequest ceiling 25033ms, run 4148 SOFI qty mismatch — both already documented). **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74 (2.23x cap, pre-existing bypass fills). Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169; getSwingTradeSymbols at 513, 622). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T16:10:43Z (reconcile run 4580, no new weekend ledger activity). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1181 (20:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-201, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 5781ee9 (Control-200).

## Saturday, August 29, 2026 Control-200 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-200 at ~20:00 UTC Aug 29 (Aug 29 22:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-199:** Control-186 cron dispatch fix holding ~21h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4626 at 19:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED, nextOpen 2026-08-31T09:30:00-04:00, dur ~149-188ms); reconcile_cron every 10 min ok (161 MAINTENANCE_ONLY runs in 500-row window, recent dur ~3.3-4.2s, stable well below ~20s pre-gap threshold; max 16900ms is a pre-redeploy Aug 28 outlier); crypto_cron :07/:37 fail-closed (46 runs every 30 min at minutes :07/:37, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4127-4626 (500 runs, ~Aug 28 14:37 - Aug 29 19:56 UTC), 0 missing run IDs, 0 new errors (2 pre-existing error runs from Aug 28: run 4182 subrequest ceiling 25033ms, run 4148 SOFI qty mismatch 107 vs 108 — both already documented). **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74 (2.23x cap, pre-existing bypass fills). Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169; getSwingTradeSymbols at 513, 622). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T16:10:43Z (reconcile run 4580, no new weekend ledger activity). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1179 (19:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-200, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: d4f457b (Control-199).

## Saturday, August 29, 2026 Control-199 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-199 at ~19:00 UTC Aug 29 (Aug 29 21:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-197:** Control-186 cron dispatch fix holding ~19h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4614 at 18:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED, nextOpen 2026-08-31T09:30:00-04:00); reconcile_cron every 10 min ok (144 MAINTENANCE_ONLY runs in 500-row window, recent dur 3552-4155ms, stable well below ~20s pre-gap threshold; max 16900ms is a pre-redeploy Aug 28 outlier); crypto_cron :07/:37 fail-closed (49 runs every 30 min at minutes :07/:37, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4115-4614 (500 runs, ~Aug 28 14:37 - Aug 29 18:56 UTC), 0 missing run IDs, 0 new errors (2 pre-existing error runs from Aug 28: run 4182 subrequest ceiling 25033ms, run 4148 SOFI qty mismatch 107 vs 108 — both already documented). **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74 (2.23x cap, pre-existing bypass fills). Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169; getSwingTradeSymbols at 513, 622). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T16:10:43Z (reconcile run 4580). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1177 (18:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-199, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: b087292 (Control-197).

## Saturday, August 29, 2026 Control-197 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-197 at ~18:00 UTC Aug 29 (Aug 29 20:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-196:** Control-186 cron dispatch fix holding ~15h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4602 at 17:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED, nextOpen 2026-08-31T09:30:00-04:00); reconcile_cron every 10 min ok (12 runs in window 4563-4580, dur 3552-4155ms, stable well below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (4 runs every 30 min, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4483-4602 (120 runs, ~11:20-17:56 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74 (2.23x cap, pre-existing bypass fills). Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169; getSwingTradeSymbols at 513, 622). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T16:10:43Z (reconcile run 4580). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1175 (17:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged (capital-caps.ts + per-strategy files). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-197, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: b237b59 (Control-196).

## Saturday, August 29, 2026 Control-196 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-196 at ~17:00 UTC Aug 29 (Aug 29 19:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-195:** Control-186 cron dispatch fix holding ~14h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4590 at 16:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED, nextOpen 2026-08-31T09:30:00-04:00); reconcile_cron every 10 min ok (15 runs in window, dur 3375-4155ms, stable well below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (5 runs 4539/4549/4559/4569/4579 every 30 min, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4531-4590 (60 runs, ~13:30-17:00 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T16:10:43Z (reconcile run 4580). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1173 (16:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-196, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 346b0a0 (Control-195).

## Saturday, August 29, 2026 Control-195 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-195 at ~16:00 UTC Aug 29 (Aug 29 18:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-194:** Control-186 cron dispatch fix holding ~13h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4575 at 15:56:40 UTC); reconcile_cron every 10 min ok (36 runs, dur 3172-4155ms, stable well below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (12 runs every 30 min, latest 4569 at 15:37:41 UTC, CRYPTO_BARS_STALE ETHUSD ~22h stale + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4456-4575 (120 runs, ~10:00-16:00 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T15:50:42Z (reconcile run 4573). Equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1171 (15:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-195, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 920d0d7 (Control-194).

## Saturday, August 29, 2026 Control-194 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-194 at ~15:00 UTC Aug 29 (Aug 29 17:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-193:** Control-186 cron dispatch fix holding ~11h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4555 at 14:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED); reconcile_cron every 10 min ok (runs 4496-4553 dur ~3.3-3.9s, stable well below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (runs 4499/4509/4519/4529/4539/4549 every 30 min, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_BARS_STALE + CRYPTO_DATA_INSUFFICIENT validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4496-4555 (60 runs, ~12:00-15:00 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, metadata_source=d1 (strategy attribution from D1, position/qty/MV broker-authoritative), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472 edge gate + ranking; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, 0 incomplete in 200-sample). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T14:50:42Z (reconcile run 4553). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1169 (14:37 UTC). 768 trades, 768 executed, win 15%. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-194, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: b7341cf (Control-193).

## Saturday, August 29, 2026 Control-193 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-193 at ~14:00 UTC Aug 29 (Aug 29 16:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-192:** Control-186 cron dispatch fix holding ~9h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4535 at 13:56:40 UTC); reconcile_cron every 10 min ok (runs 4516-4533 dur ~3.2-3.9s, stable well below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (runs 4459-4529 every 30 min, CRYPTO_BARS_STALE + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_DATA_INSUFFICIENT, validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4456-4535 (80 runs), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, source=alpaca (broker-authoritative, observed 2026-08-29T14:00:19Z), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0, EQUITY_DIRECTION_FALLBACK active. Gross/fee/net=null on all 200 visible filled trades (conservative). Null-strategy trades 4 in 200-sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing. Fee telemetry stale (Aug 19). broker_ledger_synced_until 2026-08-29T13:50:42Z. Equity $98,219.06, cash $89,963.32 (91.5%), ACTIVE, not PDT. 768 trades, win 15%. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-193, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: a5f620b (Control-192).

## Saturday, August 29, 2026 Control-192 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-192 at ~13:00 UTC Aug 29 (Aug 29 15:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-191:** Control-186 cron dispatch fix holding ~8h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4515 at 12:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED); reconcile_cron every 10 min ok (runs 4506/4510/4513 dur ~3.5-3.8s, stable below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (run 4509 at 12:37:41 UTC, CRYPTO_BARS_STALE AVAXUSD ageSeconds=79661/~22h stale latestBarAt 2026-08-28T14:30Z maxStaleSeconds=2700, CRYPTO_BARS_UNAVAILABLE MATICUSD empty, validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4477-4516 (40 runs, ~11:30-13:00 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, source=alpaca (broker-authoritative, observed 2026-08-29T13:00:18Z), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed, EQUITY_DIRECTION_FALLBACK active (weekend, broker change_today_pct~0, equity delta -0.00026% exposed). SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472 edge gate + ranking; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, trades 763-768 EOD flatten). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T12:50:42Z (reconcile run 4513). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1165 (12:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-192, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 947749e (Control-191).

## Saturday, August 29, 2026 Control-191 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-191 at ~12:00 UTC Aug 29 (Aug 29 14:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-190:** Control-186 cron dispatch fix holding ~7h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (latest run 4495 at 11:56:40 UTC, RECONCILIATION_DEFERRED_TO_MAINTENANCE + MARKET_CLOSED); reconcile_cron every 10 min ok (runs 4466/4470/4473/4476/4480/4483/4486/4490/4493 dur ~3.2-3.7s, stable below ~20s pre-gap threshold); crypto_cron :07/:37 fail-closed (run 4489 at 11:37:41 UTC, CRYPTO_BARS_STALE ETHUSD ageSeconds=79661/~22h stale latestBarAt 2026-08-28T13:30Z maxStaleSeconds=2700, CRYPTO_BARS_UNAVAILABLE MATICUSD empty, validTA=0, edge gate not reached); swing_cron no weekend run. Run-log window 4466-4495 (30 runs, 10:30-11:56 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, source=alpaca (broker-authoritative, observed 2026-08-29T12:00:19Z), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed, EQUITY_DIRECTION_FALLBACK active (weekend, broker change_today_pct~0, equity delta -0.00026% exposed). SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps at 604; crypto-strategy.ts:29-32,66,88,472 edge gate + ranking; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, trades 763-768 EOD flatten). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T11:50:42Z (reconcile run 4493). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1163 (11:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-191, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 17cf0bd (Control-190).

## Saturday, August 29, 2026 Control-190 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-190 at ~11:00 UTC Aug 29 (Aug 29 13:00 +02). Market CLOSED (weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed — Control-186 cron dispatch fix holding, all four schedules dispatching cleanly. Code 79583d8 (Control-172, unchanged). Reconcile runs every 10 min ok (runs 4470/4473 dur ~3.4-3.7s, stable below ~20s pre-gap threshold); crypto :07/:37 fail-closed (run 4469 at 10:37:41 UTC); daytrading MARKET_CLOSED (run 4475 at 10:56:40 UTC). Run-log window 4416-4475 (60 runs, 08:00-10:56 UTC), 0 missing IDs, 0 errors. 23 positions all swing, source=alpaca (broker-authoritative, observed 2026-08-29T11:00:19Z), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0 fail-closed (AVAXUSD ~22h stale, MATICUSD empty, validTA=0, edge gate not reached). EQUITY_DIRECTION_FALLBACK active (weekend). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, trades 763-768 EOD flatten). Null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T10:50:42Z (reconcile run 4473, advancing post-redeploy). Equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1161 (10:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-190, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 0468fcd (Control-189).

## Saturday, August 29, 2026 Control-189 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-189 at ~10:00 UTC Aug 29 (Aug 29 12:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-188:** Control-186 cron dispatch fix holding ~4.5h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (weekend correct); reconcile_cron every 10 min ok (latest run 4453 at 09:50:43 UTC, dur 3395ms, range ~3.4-3.7s, stable below ~20s pre-gap threshold); crypto_cron :07/:37 cadence confirmed (run 4449 at 09:37:41 UTC fail-closed); swing_cron no weekend run (correct). Run-log window 4256-4455 (200 runs, ~Aug 28 23:31 - Aug 29 09:56 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, source=alpaca (broker-authoritative, observed 2026-08-29T10:00:17Z), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed: CRYPTO_BARS_STALE (SOLUSD ageSeconds=79661/~22h stale, latestBarAt 2026-08-28T11:30Z, maxStaleSeconds=2700) / CRYPTO_BARS_UNAVAILABLE (MATICUSD empty) / CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3), EQUITY_DIRECTION_FALLBACK active (weekend, broker change_today_pct~0, equity delta exposed), edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps called at 604; crypto-strategy.ts:29-32,66,88,472 edge gate + ranking; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present, trades 762-768 EOD flatten). **Observability gap refinement:** null-strategy trades 4 in 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — all pre-existing, no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T09:50:42Z (reconcile run 4453, advancing post-redeploy). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1159 (09:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-189, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: fa76cc3 (Control-188).

## Saturday, August 29, 2026 Control-188 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-188 at ~09:00 UTC Aug 29 (Aug 29 11:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-187:** Control-186 cron dispatch fix holding ~3h post-redeploy — all four schedules dispatching cleanly. Daytrading cron every 5 min MARKET_CLOSED (weekend correct); reconcile_cron every 10 min ok (runs 4416/4420/4423/4426/4430/4433, dur ~3.3-3.8s, stable below ~20s pre-gap threshold); crypto_cron :07/:37 cadence confirmed (runs 4399/4409/4419/4429 fail-closed); swing_cron no weekend run (correct). Run-log window 4386-4435 (50 runs, ~06:00-08:56 UTC), 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative, source=alpaca), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed: CRYPTO_BARS_STALE (ETHUSD ~22h stale) / CRYPTO_BARS_UNAVAILABLE (MATICUSD empty) / CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3), EQUITY_DIRECTION_FALLBACK active (weekend, broker change_today_pct=0, equity delta ~0 exposed), edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps, called at 604; crypto-strategy.ts:65-78 edge gate; crypto-runtime.ts:295-296,351-355 ranking). Gross/fee/net=null on all 200 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present). **Observability gap refinement:** null-strategy trades now 4 in the 200-trade sample (597 AFRM, 645 DUK, 648 NOW, 703 PLD) — 597 AFRM is pre-existing, newly visible in the larger sample window, not a new regression. No new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, $272.82 all unattributed). broker_ledger_synced_until advanced (reconcile run 4433 at 08:50:43 UTC). Live account: equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1157 (08:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-188, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 846a936 (Control-187).

## Saturday, August 29, 2026 Control-203 strict read-only control + cron dispatch fix (HEALTHY code/deploy, DEGRADED external → reconciled)

Control-203 at ~23:00 UTC Aug 29 (Aug 30 01:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only audit then authorized reliability redeploy. 6 endpoints 200 (/health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. **New finding: THIRD cron dispatch failure — reconcile + crypto stopped again.** Reconcile_cron (`*/10 * * * *`) last fired run 4580 at 16:10:43 UTC; crypto_cron (`7-59/30 * * * *`) last fired run 4579 at 16:07:41 UTC. Both stopped dispatching ~7h while daytrading (`1-59/5 * * * *`) continued normally (runs 4581-4663, every 5 min). broker_ledger_synced_until frozen at 2026-08-29T16:10:43Z. This is the same DOW=* silent-stop failure class as Control-186 — confirming it recurs. All four schedules still registered via CF API. **Fix: authorized reliability redeploy** (same code 79583d8, no code/cap/trading-behavior change) via direct CF API PUT with crons in metadata to re-register triggers. Deploy HTTP 200 at 23:03:37 UTC, deployment_id 7e3914be. **Verified post-deploy:** crypto_cron run 4665 at 23:07:33 UTC (status=skipped, fail-closed expected, dur=3778ms, RECONCILIATION_DEFERRED + EQUITY_DIRECTION_FALLBACK + CRYPTO_BARS_STALE/UNAVAILABLE + DATA_INSUFFICIENT validTA=0, edge gate not reached), reconcile_cron run 4666 at 23:10:37 UTC (status=ok, broker_ledger_synced_until advanced to 23:10:36Z). Run-log contiguous 4464-4667 (204 runs, 0 missing IDs). 23 swing positions broker-authoritative (metadata_source=d1, current_state observed 2026-08-29T23:00:26Z), 0 unattributed, MV $8,255.74. Daytrading MV $0, crypto MV $0. Trade lifecycle complete (0 incomplete in 200-sample). Gross/fee/net=null conservative (accounting_status=filled_lot_exact_unavailable). Null-strategy trades 4 (597/645/648/703, pre-existing). Fee telemetry stale (Aug 19, $272.82 all unattributed). Equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. 768 trades, win 15%. Snapshot 1185. Caps 5000/3700/2000 USD unchanged. 224 tests/845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault. Rollback target: Control-172 deploy (79583d8); redeploy via direct CF API PUT with crons in metadata.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172, unchanged — redeploy was cron re-registration only). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-203, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 19dc312 (Control-202).

## Saturday, August 29, 2026 Control-187 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)

Control-187 at ~08:00 UTC Aug 29 (Aug 29 10:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-186:** Control-186 cron dispatch fix holding — all four schedules dispatching ~1h post-redeploy. Daytrading every 5 min MARKET_CLOSED; reconcile every 10 min ok (runs 4400/4403/4406/4410/4413, dur ~3.4-3.8s, stable below ~20s threshold); crypto :07/:37 fail-closed; swing no weekend run. 0 missing run IDs, 0 errors. **Verified:** 23 positions all swing, source=alpaca (broker-authoritative, observed 08:00:22Z), 0 unattributed, MV $8,255.74. Daytrading MV $0 (EOD flatten clean). Crypto MV $0 fail-closed (CRYPTO_BARS_STALE ETHUSD ~22h / UNAVAILABLE MATICUSD / DATA_INSUFFICIENT validTA=0), EQUITY_DIRECTION_FALLBACK active, edge gate not reached. SWING_OWNED_EXCLUDE (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414/604; crypto-strategy.ts:65-78; crypto-runtime.ts:295-296,351-355). Gross/fee/net=null (conservative, accounting_status=filled_lot_exact_unavailable). Trade lifecycle complete. Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19, $272.82 all unattributed). broker_ledger_synced_until 2026-08-29T07:50:43Z. Equity $98,219.06, cash $89,963.32 (91.5%), change_today ~0 (weekend), ACTIVE, not PDT. Snapshot 1155 (07:37 UTC). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-187, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: c8c431d (Control-186).

## Saturday, August 29, 2026 Control-186 strict read-only control + cron dispatch fix (HEALTHY code/deploy, DEGRADED external → reconciled)

Control-186 at ~07:00 UTC Aug 29 (Aug 29 09:00 +02). Market CLOSED (weekend, next open Mon 2026-08-31 13:30 UTC). Strict GET-only audit then authorized reliability redeploy. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. **New finding: cron dispatch failure on reconcile + crypto.** Reconcile_cron (`*/10 * * * *`) last fired run 4380 at 05:41:10 UTC; crypto_cron (`7-59/30 * * * *`) last fired run 4379 at 05:38:06 UTC. Both stopped dispatching while daytrading (`1-59/5 * * * *`) continued every 5 min (runs 4381-4396, 05:41-06:56 UTC). All four schedules confirmed still registered via CF API (modified_on 2026-08-28T17:18:20, Control-172 deploy). Gap ~89 min (05:41→07:10 reconcile, 05:38→07:07 crypto). This is the second cron dispatch failure class: Control-172 was DOW-restricted daytrading; this one hit two DOW=* schedules while a third DOW=* schedule continued — confirming Cloudflare cron dispatch can silently stop for individual schedules regardless of DOW restriction. **Fix: authorized reliability redeploy** (same code 79583d8, no code/cap/trading-behavior change) via direct CF API PUT with crons in metadata to re-register triggers. Deploy HTTP 200. **Verified post-deploy:** reconcile_cron run 4400 at 07:10:43 UTC (status=ok, dur=3392ms), crypto_cron run 4399 at 07:07:41 UTC (status=skipped, fail-closed expected), daytrading run 4398 at 07:06:40 UTC continued. broker_ledger_synced_until advanced 05:41:09Z → 07:10:42Z (reconcile resumed and synced). No regression: 23 positions all swing, source=alpaca (broker-authoritative), 0 unattributed, MV $8,255.74, equity $98,219.36 unchanged, cash $89,963.62, change_today -$254.91 (-0.26%), ACTIVE, not PDT. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed (ETHUSD ~22h stale, MATICUSD empty, validTA=0, edge gate not reached). Weekend trading impact zero (positions static, crypto fail-closed). Reconcile durations post-redeploy ~3s (lighter first runs, expected to normalize). 60-run window 4337-4396 (03:31-06:56 UTC), 0 missing daytrading IDs, 0 errors in daytrading runs. Gross/fee/net=null on visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable). Trade lifecycle complete. Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19, $272.82 all unattributed). EQUITY_DIRECTION_FALLBACK active (weekend). Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172, unchanged — redeploy was cron re-registration only). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-186, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: dfb2791 (Control-185).

## Saturday, August 29, 2026 Control-185 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-185 at ~06:00 UTC Aug 29 (Aug 29 08:00 +02). Market CLOSED (weekend, Fri close 20:00 UTC, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-184:** post-close cadence clean every 5 min (daytrading cron), reconcile every 10 min, crypto every 30 min at :08/:38 UTC. No missing run IDs in range 4325-4384 (60 runs, 02:56-05:56 UTC Aug 29). 0 errors in visible window. Reconcile durations ~11s (10854-11402ms, avg ~11150), stable, below ~20s pre-gap threshold. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative, source=alpaca, observed 2026-08-29T06:00:14Z), MV $8,255.74, 0 unattributed. Daytrading MV $0 (EOD flatten clean, no weekend overnight exposure). Crypto MV $0, fail-closed: crypto_cron runs 4309/4319/4329/4339/4349/4359/4369/4379 at :08/:38 UTC — CRYPTO_BARS_STALE (ETHUSD ageSeconds=79685/~22h stale, maxStaleSeconds=2700) / CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3), EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0 on weekend, equity delta -0.26% exposed), edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps, called at 604; crypto-strategy.ts:65-78 edge gate; crypto-runtime.ts:295-296,351-355 ranking). Gross/fee/net=null on all 100 visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present). Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, totalUsd=$272.82 all unattributed). broker_ledger_synced_until 2026-08-29T05:41:09Z (last reconcile run 4380). Live account: equity $98,219.36, cash $89,963.62, long_market_value $8,255.74, change_today -$254.91 (-0.26%), ACTIVE, not PDT. Snapshot 1151 (05:38 UTC): equity $98,219.36. 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-185, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 3147a6f (Control-184).

## Saturday, August 29, 2026 Control-184 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-184 at ~05:00 UTC Aug 29 (Aug 29 07:00 +02). Market CLOSED (weekend, Fri close 20:00 UTC, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-183:** post-close cadence clean every 5 min (daytrading cron), reconcile every 10 min, crypto every 30 min at :08/:38 UTC. No missing run IDs in range 4306-4365 (60 runs, 02:01-04:56 UTC Aug 29). 0 errors in visible window. Reconcile durations ~11s (10958-11794ms, avg 11193), stable, below ~20s pre-gap threshold. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative, source=alpaca, observed 2026-08-29T05:00:13Z), MV $8,255.74, 0 unattributed. Daytrading MV $0 (EOD flatten clean, no weekend overnight exposure). Crypto MV $0, fail-closed: crypto_cron runs 4309/4319/4329/4339/4349/4359 at :08/:38 UTC — CRYPTO_BARS_STALE (LINKUSD ageSeconds=79685/~22h stale, maxStaleSeconds=2700) / CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3), EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0 on weekend, equity delta -0.26% exposed), edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps, called at 604; crypto-strategy.ts:65-78 edge gate; crypto-runtime.ts:295-296,351-355 ranking). Gross/fee/net=null on all visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present). Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable, totalUsd=$272.82 all unattributed). broker_ledger_synced_until 2026-08-29T04:51:09Z (last reconcile run 4363). Live account: equity $98,219.36, cash $89,963.62, long_market_value $8,255.74, change_today -$254.91 (-0.26%), ACTIVE, not PDT. Snapshot 1149 (04:38 UTC): equity $98,219.36. 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-184, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: b7fb1c1 (Control-183).

## Saturday, August 29, 2026 Control-183 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-183 at ~04:00 UTC Aug 29 (Aug 29 06:00 +02). Market CLOSED (weekend, Fri close 20:00 UTC, next open Mon 2026-08-31 13:30 UTC). Strict GET-only. 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-182:** post-close cadence clean every 5 min (daytrading cron), reconcile every 10 min, crypto every 30 min at :08/:38 UTC. No missing run IDs in range 4286-4345 (60 runs, 01:01-03:56 UTC Aug 29). 0 errors in visible window. Reconcile durations ~11s (10973-11794ms, avg ~11150), stable, below ~20s pre-gap threshold. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative, source=alpaca, observed 2026-08-29T03:51:09Z), MV $8,255.74, 0 unattributed. Daytrading MV $0 (EOD flatten clean, no weekend overnight exposure). Crypto MV $0, fail-closed: crypto_cron runs 4289/4299/4309/4319/4329/4339 at :08/:38 UTC — CRYPTO_BARS_STALE (LINKUSD ageSeconds=79685/~22h stale) / CRYPTO_BARS_UNAVAILABLE (MATICUSD empty), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3), EQUITY_DIRECTION_FALLBACK, edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414 computeCalibratedEdgeBps, called at 604; crypto-strategy.ts:65-78 edge gate; crypto-runtime.ts:295-296,351-355 ranking). Gross/fee/net=null on all visible filled trades (conservative, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded). Trade lifecycle complete (submitted_at/filled_at/broker_updated_at/last_reconciled_at present). Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). broker_ledger_synced_until 2026-08-29T03:51:09Z (last reconcile run 4343). Live account: equity $98,219.36, cash $89,963.62, long_market_value $8,255.74, change_today -$254.91 (-0.26%), ACTIVE, not PDT. Snapshot 1147 (03:38 UTC): equity $98,219.36. 768 trades, 768 executed. Caps 5000/3700/2000 USD unchanged. 224 tests / 845 assertions, typecheck clean, git diff --check clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (Control-183, local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 8a53173 (Control-182).

## Saturday, August 29, 2026 Control-182 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-182 at ~03:00 UTC Aug 29 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change, no deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-181:** post-close cadence clean every 5 min, reconcile every 10 min, crypto every 30 min at :08/:38. No missing run IDs in range 4286-4325 (40 runs, 01:01-02:56 UTC). 0 errors. Reconcile durations ~11s (10654-11649ms, avg ~11245) stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, broker-authoritative (source=alpaca, observed 03:00:35Z), MV $8,255.74, 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE (AVAXUSD/LINKUSD/SOLUSD/ETHUSD ~22h stale)/UNAVAILABLE (MATICUSD empty)/DATA_INSUFFICIENT, edge gate not reached. Gross/fee/net=null (conservative). Trade lifecycle complete. broker_ledger_synced_until ~02:51 UTC. Live account equity $98,219.36, -$254.91 (-0.26%); snapshot 1145 $98,219.36. 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 3ede069 (Control-181).

## Saturday, August 29, 2026 Control-181 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-181 at ~02:00 UTC Aug 29 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change, no deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-180:** post-close cadence clean every 5 min, reconcile every 10 min, crypto every 30 min at :08/:38. No missing run IDs in range 4286-4305 (20 runs, 01:01-01:56 UTC). 0 errors. Reconcile durations ~11s (10654-11222ms, avg 11024) stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, broker-authoritative (source=alpaca, observed 02:00:26Z), 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE (LINKUSD ~22h stale)/UNAVAILABLE (MATICUSD empty)/DATA_INSUFFICIENT, edge gate not reached. Gross/fee/net=null (conservative). Trade lifecycle complete. broker_ledger_synced_until Aug 29 01:51 UTC. Live account equity $98,231.26, -$243.01 (-0.25%); snapshot $98,233.54. 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: c3bb0e4 (Control-180).

## Saturday, August 29, 2026 Control-180 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-180 at ~01:00 UTC Aug 29 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change, no deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-179:** post-close cadence clean every 5 min, reconcile every 10 min, crypto every 30 min at :08/:38. No missing run IDs in range 4266-4285 (20 runs, 00:01-00:56 UTC). 0 errors. Reconcile durations ~11s (11479/11106ms latest, avg 9452 across window) stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, broker-authoritative, MV $8,269.92, 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE, edge gate not reached. Gross/fee/net=null (conservative). Trade lifecycle complete. broker_ledger_synced_until Aug 29 00:51 UTC. Equity $98,233.54, -$240.73 (-0.24%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

## Saturday, August 29, 2026 Control-179 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-179 at ~00:00 UTC Aug 29 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change, no deploy needed. Code 79583d8 (Control-172, unchanged). **No new findings since Control-178:** post-close cadence clean every 5 min, reconcile every 10 min, crypto every 30 min at :08/:38. No missing run IDs in range 4216-4265 (50 runs, 21:31-23:56 UTC). 0 errors. Reconcile durations ~11s (10552-11290ms) stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, broker-authoritative, MV $8,269.92, 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE, edge gate not reached. Gross/fee/net=null (conservative). Trade lifecycle complete. Equity $98,233.54, -$240.73 (-0.24%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

## Saturday, August 29, 2026 Control-178 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-178 at ~23:00 UTC Aug 28 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. Code 79583d8 (Control-172, unchanged). **No new findings since Control-177:** post-close cadence clean every 5 min, reconcile every 10 min, crypto every 30 min at :07/:37. No missing run IDs in range 4187-4246 (60 runs, 20:01-23:01 UTC). 0 errors. Reconcile durations ~11s (10552-11290ms, avg 11032), stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative), MV $8,262.06, 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE/DATA_INSUFFICIENT, edge gate not reached. SWING_OWNED_EXCLUDE in source (index.ts:511, 972-974, 1139, 1169). Edge producer wired (technical-analysis.ts:414, called at 604; crypto-strategy.ts:65-78). Gross/fee/net=null on all 100 visible filled trades (conservative). Trade lifecycle complete (filled_at + last_reconciled_at 100/100). Historical null-strategy trade persists (703 PLD); no new nulls. Fee telemetry stale (Aug 19). Equity $98,225.80, -$248.47 (-0.25%), ACTIVE, not PDT. Cash $89,963.62 (91.5%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 9b3f015 (Control-177).

## Saturday, August 29, 2026 Control-177 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-177 at ~22:00 UTC Aug 28 (market CLOSED, weekend). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. Code 79583d8 (Control-172, unchanged). **No new findings since Control-176:** post-close cadence clean every 5 min (runs 4206-4225, 20:56-21:56 UTC), reconcile every 10 min, crypto every 30 min. No missing run IDs in range 4166-4225. 0 errors. Reconcile durations ~11s stable (below ~20s pre-gap threshold). **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative), MV $8,265.76, 0 unattributed. Daytrading MV $0 (EOD flatten clean). Crypto MV $0, fail-closed at CRYPTO_BARS_STALE (AVAXUSD ~22h stale)/UNAVAILABLE (MATICUSD empty), validTA=0, edge gate not reached. EQUITY_DIRECTION_FALLBACK active. Gross/fee/net=null (conservative). Trade lifecycle complete. Historical null-strategy trades persist (703/648/645 plus older); no new nulls. Fee telemetry stale (Aug 19). Equity $98,229.38, -$244.89 (-0.25%), ACTIVE, not PDT. Cash $89,963.62 (91.5%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 747aec0 (Control-176).

## Friday, August 28, 2026 Control-176 strict read-only control (HEALTHY code/deploy, DEGRADED external, docs HEAD corrected)

Control-176 at ~21:00 UTC Aug 28 (market CLOSED, Fri). Strict GET-only. 6 required endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. **Docs HEAD corrected:** Control-175 version identity below says "Docs HEAD: a99e2d0" but actual HEAD is 6427dba (Control-175's own commit — same self-referential defect it corrected for Control-174). Now using "this commit" pattern. Prior docs HEAD: 6427dba (Control-175). Code 79583d8 (Control-172, unchanged). **No new findings since Control-175:** post-close cadence clean every 5 min (runs 4187-4205, 20:01-20:56 UTC), no new run-log gaps, no new errors. Run 4182 ERROR (subrequest ceiling, 19:47 UTC) already documented, self-recovered. Reconcile durations ~11s stable (below ~20s pre-gap threshold). CYCLE_LEASE_HELD at 4174/4175/4177 already documented, resolved by 4178. **Verified:** 23 positions all swing, metadata_source=d1 (broker-authoritative), MV $8,264.21, 0 unattributed. Daytrading EOD flatten clean (trades 765-768, daytrading MV $0). Crypto :08/:38 cadence confirmed, all fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE, edge gate not reached. SWING_OWNED_EXCLUDE exercised (RIVN in run 4182). Gross/fee/net=null (conservative). Equity $98,227.60, -$246.67 (-0.25%), ACTIVE, not PDT. Cash $89,963.62 (91.6%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean. Push BLOCKED: github_pat not in vault.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push BLOCKED: github_pat not in vault). Prior docs HEAD: 6427dba (Control-175).

## Friday, August 28, 2026 Control-175 strict read-only control (HEALTHY code/deploy, DEGRADED external, docs HEAD corrected)

Control-175 at ~20:01 UTC Aug 28 (market CLOSED). Strict GET-only. 6 required endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. **Docs HEAD corrected:** Control-174 entry references docs HEAD a01af70 (HEAD before the a99e2d0 commit — a commit cannot reference its own hash). Actual current HEAD is a99e2d0. Code 79583d8 (Control-172, unchanged). **New findings since Control-174:** (1) Run 4182 ERROR at 19:47 UTC — Cloudflare subrequest ceiling exceeded despite Workers Paid; self-recovered by next run. (2) New cron dispatch gaps 19:01-19:31: 4 missed daytrading, 2 missed reconcile. Clean window ended at 18:57. (3) EOD flatten: 5 daytrading positions flattened to 0. Now 23 swing, MV $8,273 (2.24x cap, normalizing). Daytrading MV $0, crypto MV $0. Crypto :07/:37 confirmed across 17 runs, all fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE. Gross/fee/net=null on all 50 filled trades (conservative). Trade lifecycle complete. Equity $98,236.62, -$237.65 (-0.24%). 768 trades, 768 executed. Caps 5000/3700/2000 unchanged. 224 tests / 845 assertions, typecheck clean.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: a99e2d0 (Control-174 docs-only commit; code unchanged from Control-172). Push BLOCKED: github_pat not in vault.

---

## Friday, August 28, 2026 Control-174 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-174 at ~19:00 UTC Aug 28 (market OPEN). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. Code 79583d8, docs HEAD a01af70 (Control-173 docs-only commit; code unchanged from Control-172). Daytrading cron stable: 30+ consecutive runs every 5 min (4139-4172), no dispatch failures since Control-172 fix. Daytrading actively trading: 5 positions (ENPH, MSTR, PLUG, SLB, SOFI), MV $4,898.47 (0.98x $5,000 cap). SWING_OWNED_EXCLUDE exercised (INTC at run 4172). POSITION_QTY_MISMATCH on SOFI at run 4148 - safety worked, self-resolved by 4149. No new run-log gaps since Control-172 deploy (~3h20m clean window 15:38-18:57 UTC). Reconcile durations 13-18s (elevated, stable, approaching but below ~20s pre-gap threshold). Crypto fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE. 28 positions (23 swing + 5 daytrading), 0 unattributed. Swing MV $8,252.52 (2.23x cap). Equity $98,218.14, -$270.34 (-0.27%). Caps 5000/3700/2000 unchanged.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: a01af70 (local only, push blocked github_pat).

---

## Friday, August 28, 2026 Control-173 strict read-only control (HEALTHY code/deploy, DEGRADED external)

Control-173 at ~18:01 UTC Aug 28 (market OPEN). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No code change or deploy. Code 79583d8, docs HEAD d6b9121 (unchanged). Daytrading cron stable: 9+ consecutive runs every 5 min, no dispatch failures since Control-172 fix. Daytrading actively trading: 4 positions (MSTR, SOFI, NOW, CRM), MV $5,005.82 (1.00x $5,000 cap). SWING_OWNED_EXCLUDE exercised 5+ times (RIVN, AFRM, BAC, COF). POSITION_QTY_MISMATCH on SOFI at run 4148 (internal:107, broker:108) - safety worked, self-resolved by 4149. No new run-log gaps since Control-172 deploy (~1h clean). Reconcile durations 13-16s (elevated, stable). Crypto fail-closed at CRYPTO_BARS_STALE/UNAVAILABLE. 27 positions (23 swing + 4 daytrading), 0 unattributed. Swing MV $8,258.61 (2.23x cap). Equity $98,229.74, -$244.53 (-0.25%). Caps 5000/3700/2000 unchanged.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: d6b9121 (local only, push blocked github_pat).

---

## Friday, August 28, 2026 Control-172 strict read-only control + daytrading cron dispatch fix (VERIFIED)

Control-172 at ~17:01 UTC Aug 28 (market OPEN). Strict GET-only. 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. **Critical fix deployed AND live-verified:** Cloudflare stopped dispatching the daytrading cron schedule on Aug 28. Both `*/5 13-21 * * 1-5` and `*/5 * * * 1-5` were registered but never fired (zero `_diag` entries, zero `unknown_cron`, zero daytrading runs for 19+ hours). Root cause: Cloudflare platform issue with day-of-week restrictions (`1-5`) in cron triggers — the two working schedules (`*/10 * * * *`, `7-59/30 * * * *`) both use `*` for DOW. Fix: `1-59/5 * * * *` (every 5 min offset by 1 to avoid `*/10` overlap, `*` for DOW). **VERIFIED: 3+ consecutive daytrading_cron runs fired (4139 at 17:17, 4141 at 17:22).** SWING_OWNED_EXCLUDE first exercise (COIN skipped as swing-owned, cc9e813 proven). RECONCILIATION_DEFERRED_TO_MAINTENANCE working (f79866d). MSTR BUY 7 @ $127.25 filled (trade 741), second pending (trade 742). Daytrading MV $1,775 (0.36x cap). 224 tests / 845 assertions, typecheck clean. Deployed via direct CF API PUT (deployment_id 348c28999d87463c80f77edcfa045ab5) with `crons` in metadata. Schedules confirmed via CF API. Code 79583d8. Caps 5000/3700/2000 unchanged.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 79583d8 (Control-172 cron dispatch fix). Four schedules: `1-59/5 * * * *`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push blocked github_pat).

## Friday, August 28, 2026 Control-170-172 strict read-only control + daytrading cron dispatch fix

Control-170 at ~15:01 UTC Aug 28 (market OPEN). Strict GET-only. All 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. **Critical fix deployed across Controls 170-172:** daytrading cron stopped dispatching on Aug 28 — zero daytrading runs despite market open. Three root causes fixed: (1) daytrading was the only strategy still running `reconcileBrokerOrders` inline, making it the heaviest invocation and first to silently throw under D1 pressure — deferred to maintenance using proven `RECONCILIATION_DEFERRED_TO_MAINTENANCE` pattern; (2) schema gate fail-closed on transient D1 errors caused silent invocation loss — now fail-open; (3) Cloudflare silently stopped dispatching `*/5 13-21 * * 1-5` after an `exceededResources` streak — switched to `1-59/5 * * * *` (offset to avoid `*/10` overlap, MARKET_CLOSED gates off-hours). First daytrading run (4139) logged at 17:17 UTC showing `SWING_OWNED_EXCLUDE` (cap bypass fix exercised first time). RIVN/AEP sells FILLED and reconciled. Equity $98,352.67, -$121.60 today. 23 positions all swing, MV $8,385 (2.27x cap). Crypto fail-closed at bar staleness. Caps 5000/3700/2000 unchanged. Code: 79583d8 (Control-172). Docs HEAD: this commit (local only, push blocked github_pat).

## Friday, August 28, 2026 Control-171 strict read-only control + daytrading cron dispatch fix

Control-171 at ~16:31 UTC Aug 28 (market OPEN). Strict GET-only. All 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. **Critical fix deployed:** Cloudflare's cron dispatch system stopped sending the `*/5 13-21 * * 1-5` cron on Aug 28 despite it being registered in the schedules API. This caused zero daytrading_cron runs for 19+ hours (entire Aug 28 market session). The hour range `13-21` in the cron expression is the suspected cause — crons with `*` for hours (reconcile `*/10`, crypto `7-59/30`) continued dispatching normally. Fix: widen to `*/5 * * * 1-5` (every 5 min, weekdays only). The code's internal MARKET_CLOSED check already gates execution to market hours (13:30-20:00 UTC). D1 impact: ~180 extra run_log INSERTs/day, negligible against 5M read / 100K write daily limits. Also deployed all prior Control-170/171 reliability fixes (isolate-level schema cache, fail-open schema gate, catch all silent-drop paths, flexible cron matching, top-level CRON_RECEIVED diagnostic, unknown cron D1 logging). 224 tests / 845 assertions, typecheck clean, diff clean. Deployed via direct CF API PUT (HTTP 200, deployment_id bf6a6b70). Post-deploy GET verified (health 200, version 2.6.0, schedules confirmed with new cron). **Daytrading cron fix NOT yet verified — gap 5 in progress (16:51 UTC onward, last run 4133). Must confirm daytrading_cron fires when gap ends.** Reliability-only: no caps, trading behavior, or risk logic changed. Caps 5000/3700/2000 USD unchanged.

**Evidence for cron dispatch diagnosis:** 0 `_diag` runs (top-level diagnostic logs every cron event before branching — zero entries proves Cloudflare never sent the `*/5 13-21 * * 1-5` event). 0 `unknown_cron` runs. 0 `daytrading_cron` or `cron` trigger runs on Aug 28. All four schedules confirmed registered via CF API. Reconcile and crypto crons (with `*` for hours) dispatched normally throughout. Daytrading last ran at Aug 27 21:56 UTC (run 4018, MARKET_CLOSED).

**Live state at control time:** Equity $98,236.42, -$237.85 today (-0.24%), ACTIVE, not PDT. Cash $89,967.71 (91.4%), long MV $8,268, buying power $383K. 23 positions all swing, 0 unattributed, broker-authoritative. Swing MV ~$8,263 (2.23x $3,700 cap, normalizing). Daytrading MV $0 (0x cap). Crypto MV $0 (0x cap, fail-closed at bar staleness). RIVN/AEP sells CONFIRMED FILLED (trades 739/740, status=filled, filled_qty matches). POSITION_QTY_MISMATCH self-resolved at broker. 3 null-strategy trades persistent (703/648/645). 98+ filled trades gross/fee/net=null (conservative). EQUITY_DIRECTION_FALLBACK active. Fee telemetry stale (asOf Aug 19, unavailable). 0 CYCLE_LEASE_HELD. Caps 5000/3700/2000 unchanged.

**Run-log gap 5:** Started ~16:41 UTC (after run 4132, 10-min gap before run 4133 at 16:51, then ongoing). Reconcile durations ~15s (approaching ~20s pre-gap threshold). Fourth/fifth gap since Workers Paid upgrade. Root cause: accumulating D1/Worker state pressure causing silent throws.

**Version identity:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0, package.json=2.6.0, src/version.ts=2.6.0. Code 38381a4 (Control-171 cron dispatch fix). Four schedules: `*/5 * * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Docs HEAD: this commit (local only, push blocked github_pat).

## Friday, August 28, 2026 Control-170 strict read-only control + daytrading reconciliation deferral fix

Control-170 at ~15:01 UTC Aug 28 (market OPEN). Strict GET-only. All 8 endpoints 200, 3 404 (expected). HEALTHY code/deploy (2.6.0), DEGRADED external. **Critical fix deployed:** daytrading was the only strategy still running `reconcileBrokerOrders` inline, making it the heaviest invocation and first to silently throw under D1 pressure — zero daytrading_cron runs logged on Aug 28 despite market open. Fix defers daytrading reconciliation to maintenance using the proven `RECONCILIATION_DEFERRED_TO_MAINTENANCE` pattern (matching crypto/swing). Second fix: schema gate `positionsStrategySchemaReady` now fail-open on transient D1 errors — fail-closed caused silent invocation loss because the fallback `logSchemaBlockedRun` also requires D1. Reliability-only: no caps, trading behavior, or risk logic changed. 224 tests / 845 assertions, typecheck clean, diff clean. Deployed via direct CF API PUT (HTTP 200). Post-deploy GET verified. First post-deploy daytrading_cron run pending next `*/5` tick.

**Follow-up at ~15:31 UTC:** Tests 224/224, 845 assertions. Code f79866d confirmed. All three strategies now defer reconciliation to maintenance. RIVN/AEP sells confirmed FILLED in D1. Run-log gap 2 was ~2h47m (11:50-14:37 UTC). **Zero daytrading_cron runs since Aug 27 21:56 UTC — fix NOT yet tested. Next tick 15:35 UTC.** **WARNING: reconcile durations jumped to ~15s post-deploy (from ~2s) — approaching ~20s pre-gap threshold. Monitor for another gap.** Equity $98,336.03, -$138.24 today. 23 positions all swing, MV $8,368 (2.26x cap). Crypto fail-closed. Caps 5000/3700/2000 unchanged. RIVN/AEP sells FILLED and reconciled in D1 (trades 739/740, status=filled). Run-log gap from 11:50 UTC resolved (runs resumed 14:37 UTC). Equity $98,352.67, -$121.60 today. 23 positions all swing, MV $8,385 (2.27x cap). Crypto fail-closed at bar staleness. Caps 5000/3700/2000 unchanged. Code: new commit (daytrading reconciliation deferral). Docs HEAD: this commit (local only, push blocked github_pat).

## Friday, August 28, 2026 Control-169 strict read-only control

Control-169 at ~14:01 UTC Aug 28 (Aug 28 16:01 +02). Market OPEN (opened 13:30 UTC). Strict GET-only. All 8 endpoints 200 (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`; `/api/equity`, `/api/fees`, `/api/snapshot` 404 as expected). HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Code `b58e7ea` (Control-162 crypto edge producer, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Caps 5000/3700/2000 USD unchanged.

NEW findings:
1. Run-log gap EXTENDED to ~2h11m+ (11:50:48 - 14:01 UTC), spanning market open (13:30 UTC). Third gap since Workers Paid upgrade. Last run 4114 at 11:50:48 UTC. ~25+ missing runs (~13 reconcile + ~4 crypto + ~7 daytrading). Worker responsive to all GET endpoints (health 200, dashboard/positions/runs/trades return data). Same silent-throw D1/Worker state pressure pattern as Control-163/168 gaps. MOST concerning: spans active market hours and blocks SWING_OWNED_EXCLUDE first test verification. Follow-up: monitor resumption, investigate D1 read volume.
2. RIVN/AEP sells appear FILLED at broker level. Positions dropped 25->23, RIVN and AEP absent from broker-authoritative positions, cash jumped $86,761 -> $89,968 (+$3,207, consistent with RIVN ~$3,086 + AEP ~$87). D1 trade status still "new"/filled_qty 0 because no reconcile_cron has run since 11:50 UTC to update them. POSITION_QTY_MISMATCH self-resolving at broker level, pending D1 reconciliation when runs resume.
3. Swing MV normalized to ~$8,440 (2.28x $3,700 cap), down from $11,717 (3.17x). Still over cap but improving as RIVN/AEP sold off. Pre-existing inflation from pre-fix daytrading buys now clearing.
4. SWING_OWNED_EXCLUDE (cc9e813): 0 occurrences. First real test BLOCKED by run-log gap - 0 daytrading_cron runs logged since market opened. Cannot verify fix is working. Critical follow-up once runs resume.

Live state: Equity $98,407.37, change_today -$66.90 (-0.068%), ACTIVE, not PDT, not blocked. Cash $89,967.71 (91.4%), long MV $8,439.66, buying power $383,144. 23 positions ALL swing, 0 unattributed, broker-authoritative (metadata_source=d1, updated 22:01 UTC Aug 27). broker_ledger synced until 11:50:47Z (stale due to gap). Source: alpaca, observed 14:00:46 UTC.

Crypto (b58e7ea): Edge producer deployed and wired but all runs skip at CRYPTO_BARS_STALE (AVAXUSD ~22h stale, latestBarAt 2026-08-27T13:30Z) / UNAVAILABLE (MATICUSD empty) / DATA_INSUFFICIENT (validTA=0). Fee telemetry stale (asOf Aug 19, status unavailable). Edge gate cannot be exercised.

3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). 98 filled trades gross/fee/net=null (conservative, accounting_status=filled_lot_exact_unavailable). EQUITY_DIRECTION_FALLBACK active (observability only, risk controls intact). 0 CYCLE_LEASE_HELD. 1 known error (4020 swing safety block, historical).

Four schedules confirmed: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`. Daytrading stale-bar threshold DAYTRADING_MAX_BAR_STALE_INTERVALS=4 (20 min). Crypto edge gate rawEdgeBps producer wired via computeCalibratedEdgeBps().

## Friday, August 28, 2026 Control-168 strict read-only control

Control-168 at ~13:01 UTC Aug 28. Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed, no correction needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Caps 5000/3700/2000 USD unchanged.

**NEW finding: ~71-min run-log gap (11:50-13:01 UTC).** Last run 4114 at 11:50:48Z. ~8 missing runs (6 reconcile + 2 crypto). Worker responsive (health 200, all endpoints return data). Second gap since Workers Paid upgrade — same pattern as 05:01-07:11 gap. Post-Control-162 clean window ~4h40m then sudden gap. Root cause: accumulating D1/Worker state pressure, silent throws. Not fully resolved by Workers Paid or Control-162 deploy. D1 free-tier daily read limits may be factor (Sep 1 enforcement risk). Follow-up: monitor resumption; investigate D1 read volume in reconcile path if recurring.

**Live state:** Equity $98,478.07 (+$3.80 today, +0.004%, first positive day), ACTIVE. 25 positions all swing, MV $11,717 (3.17x cap), metadata_source=d1, updated 22:01 UTC Aug 27. Cash $86,761 (88%). RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) status "new", unfilled — pending 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (0 daytrading runs today, first test 13:30 UTC open). Crypto edge producer deployed but all runs skip at CRYPTO_BARS_STALE (AVAXUSD ~22h stale) / UNAVAILABLE (MATICUSD empty) before edge gate. Fee telemetry stale (Aug 19). EQUITY_DIRECTION_FALLBACK active (observability, risk controls intact). 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 11:50:47Z. Post-gap clean was ~4h40m (4077-4114) before new gap. 0 CYCLE_LEASE_HELD, 1 known error (4020).

---

## Friday, August 28, 2026 Control-167 strict read-only control

Control-167 at ~12:00 UTC Aug 28. Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed, no correction needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Caps 5000/3700/2000 USD unchanged.

**Live state:** Equity $98,423.65 (-$50.62 today), ACTIVE. 25 positions all swing, MV $11,661.80 (3.15x cap), metadata_source=d1, updated 22:01 UTC Aug 27. Cash $86,761 (88%). RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) status "new", unfilled — pending Aug 28 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (0 daytrading runs today, first test 13:30 UTC open). Crypto edge producer deployed (b58e7ea) but all runs skip at CRYPTO_BARS_STALE (AVAXUSD ~22h stale) / UNAVAILABLE (MATICUSD empty) before edge gate. Fee telemetry stale (Aug 19). 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 11:50:47Z. Post-gap clean ~4h40m (4077-4114), reconcile durations holding ~2s, 0 CYCLE_LEASE_HELD, 1 known error (4020).

---

## Friday, August 28, 2026 Control-166 strict read-only control

Control-166 at ~11:00 UTC Aug 28. Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed, no correction needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Caps 5000/3700/2000 USD unchanged.

**Run-log:** 130-min gap (4076→4077, 05:01-07:11 UTC) unchanged from Control-163. Post-gap CLEAN: runs 4077-4107 (~3h49m, 0 new gaps). Reconcile durations holding ~2s (range 1667-2203ms) ~3h40m stable — pre-gap ~20s pressure has NOT returned. Crypto cadence :07/:37 confirmed. 0 CYCLE_LEASE_HELD, 0 errors. 2 known historical errors (4020, 3975).

**Live state:** Equity $98,429.10 (-$45.17 today), ACTIVE. 25 positions all swing, MV $11,667.63 (3.15x cap), metadata_source=d1, updated 22:01 UTC Aug 27. Cash $86,761 (88%). RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) status "new", unfilled — pending Aug 28 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (first test 13:30 UTC open). Crypto edge producer deployed (b58e7ea) but all runs skip at CRYPTO_BARS_STALE (SOLUSD ~22h stale) / UNAVAILABLE (MATICUSD empty) before edge gate. Fee telemetry stale (Aug 19). 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 10:50:47Z.

## Friday, August 28, 2026 Control-165 strict read-only control

Control-165 at ~10:00 UTC Aug 28. Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed, no correction needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean, git diff --check clean. Caps 5000/3700/2000 USD unchanged.

**Run-log delivery:** One gap (unchanged from Control-163/164): 4076 (05:01:11Z) → 4077 (07:11:11Z) = 130 min, ~17 missing runs. **Post-gap clean: 22 runs 4077-4098 (07:11-09:50:48Z), ~2h40m, 0 new gaps.** Reconcile durations holding ~2s (not creeping back to ~20s pre-gap). Crypto cadence :08/:38 confirmed. 0 CYCLE_LEASE_HELD. 2 known errors (4020 safety block, 3975 historical). No new errors.

**Live state:** Equity $98,431.22 (-$43.05 today), ACTIVE. 25 positions all swing, MV $11,669.75 (3.15x cap), 0 unattributed, broker-authoritative. Cash $86,761 (88%). RIVN sell 186.29 + AEP sell 1 unfilled (status "new"), pending 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (20 post-deploy daytrading runs all skipped, market closed; first test 13:30 UTC open). Crypto edge producer deployed but bars stale (LINKUSD ~22h, MATICUSD empty), fee telemetry stale (Aug 19). 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 09:50:47Z.

## Friday, August 28, 2026 Control-164 strict read-only control

Control-164 at ~09:00 UTC Aug 28. Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed, no correction needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean (no source change since b58e7ea). Caps 5000/3700/2000 USD unchanged.

**Run-log delivery:** One gap (unchanged from Control-163): 4076 (05:01:11Z) → 4077 (07:11:11Z) = 130 min, ~17 missing runs. **Post-gap clean: 15 runs 4077-4091 (07:11-09:00:47Z), ~109 min, 0 new gaps.** Reconcile durations holding ~2s (not creeping back to ~20s pre-gap). Crypto cadence 07:37/08:07/08:37 confirmed. 0 CYCLE_LEASE_HELD. 2 known errors (4020 safety block, 3975 historical). No new errors.

**Live state:** Equity $98,425.61 (-$48.66 today), ACTIVE. 25 positions all swing, MV $11,664 (3.15x cap), 0 unattributed, broker-authoritative. Cash $86,761 (88%). RIVN sell 186.29 + AEP sell 1 unfilled (status "new"), pending 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (first test 13:30 UTC open). Crypto edge producer deployed but bars stale (LINKUSD ~22h, MATICUSD empty), fee telemetry stale (Aug 19). 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 08:50:47Z.

## Friday, August 28, 2026 Control-163 strict read-only control

Control-163 at ~08:00 UTC Aug 28 (Aug 28 10:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No deploy needed. Code `b58e7ea` (Control-162, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**NEW: ~2h10m run-log gap (05:01-07:11 UTC Aug 28).** Run 4076 (05:01:11Z) → 4077 (07:11:11Z), IDs consecutive, ~17 missing scheduled runs. First significant gap since Workers Paid upgrade. Before gap: reconcile durations ~19-20s. After Control-162 deploy ~07:10Z: durations dropped to ~2s. Deploy appears to have cleared accumulated D1/Worker state pressure. Runs 4077-4083 clean and fast. Follow-up: monitor for ~20s duration buildup recurrence. Possible D1 free-tier pressure preview before Sep 1 enforcement.

**Live state:** Equity $98,409.41 (-$64.86 today), ACTIVE. 25 positions all swing, MV $11,648 (3.15x cap), source=alpaca (broker-authoritative, 08:00:15Z). Cash $86,761 (88%). RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) status "new", unfilled — pending Aug 28 13:30 UTC open. SWING_OWNED_EXCLUDE 0 occurrences (first test 13:30 UTC open). Crypto edge producer deployed (b58e7ea) but all runs skip at CRYPTO_BARS_STALE/UNAVAILABLE before edge gate. 3 null trades persistent (703/648/645). 98 filled trades gross/fee/net=null (conservative). broker_ledger synced until 07:50:47Z.

## Friday, August 28, 2026 Control-162 crypto edge producer deploy

**Change:** Added `computeCalibratedEdgeBps()` function to `src/technical-analysis.ts` that computes a calibrated gross edge in basis points from actual price dislocation metrics. The function uses three signals: short-term reversal (Jegadeesh/Lehmann), VWAP reversion, and Bollinger Band dislocation. It takes the max of the three (conservative, avoids double-counting correlated signals) with a 0.3 reversion factor (captures 30% of observed dislocation). The edge is wired into `generateSignal()` return as `rawEdgeBps`, making it available to both crypto and daytrading paths. Crypto was previously 100% fail-closed because no code ever populated `rawEdgeBps` — the field existed in the `TASignal` interface but `generateSignal()` never set it. With `requireCalibratedEdge: true` and `minEdgeAfterCosts: 8`, every crypto BUY was blocked with `EDGE_CALIBRATION_UNAVAILABLE`.

**Rationale:** The edge is derived from measurable price metrics (shortTermReturn, vwapDeviation, bbPosition, atrPct), NOT from confidence. This satisfies the design constraint that edge must never be inferred from confidence. For a -2% short-term return, edge = 60 bps; after ~15 bps crypto costs, net = 45 bps, well above 8 bps minimum. For weak signals (-0.5% return), edge = 15 bps; after costs = ~0 bps, blocked by gate. The gate remains meaningful — it filters weak signals while allowing strong dislocations through.

**Validation:** 224 tests / 845 assertions pass, 0 fail. Typecheck clean. Updated 1 test from asserting old fail-closed behavior to asserting new edge-producing behavior. No capital cap changed (5000/3700/2000 USD preserved). Crypto cap $2,000 unchanged.

**Deploy:** Direct Cloudflare API PUT from `/workspace/alpaca-trading-bot`. Post-deploy GET verification pending.

**Follow-up:** Monitor first crypto BUYs after deploy. Verify that only strong-dislocation signals pass the edge gate. Fee telemetry freshness (asOf Aug 19) still needs investigation — `observedFeeBps` defaults to 0 when telemetry is stale, which underestimates costs. Crypto bar freshness (AVAXUSD stale ~22h, MATICUSD empty) also needs investigation — even with an edge producer, stale bars will still block via `CRYPTO_BARS_STALE`.

## Friday, August 28, 2026 Control-161 strict read-only control

Control-161 at ~07:00 UTC Aug 28 (Aug 28 09:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-160. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `8c484f4` (Control-160). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,415.96, -$1.75 today, cash $86,761.76 (88% idle), long MV $11,654.20. 25 positions ALL swing, 0 unattributed, broker-authoritative. Swing MV $11,654 (3.15x $3,700 cap, pre-existing fills). RIVN 186.29, AVGO 2.35, AEP 1 (all swing). 100-run window 3977-4076 (Aug 27 19:51 - Aug 28 05:01 UTC): 0 gaps >15 min (~14h+ clean since 17:36 UTC recovery). Triggers: reconcile_cron 55, crypto_cron 18, cron 26, swing_cron 1. 1 known error (4020 swing qty mismatch safety block). 0 CYCLE_LEASE_HELD. SWING_OWNED_EXCLUDE 0 occurrences (first test Aug 28 13:30 UTC open). Crypto cadence :08/:38, 0 gaps >40 min, all fail-closed. RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) accepted not filled. 75 daytrading trades confirm stale-bar fix active. 3 null trades persistent (703/648/645). All trades gross/fee/net=null (conservative).

**Follow-ups:** Verify SWING_OWNED_EXCLUDE + RIVN/AEP sell fills + qty mismatch at Aug 28 13:30 UTC open. Watch subrequest errors under Workers Paid. Sep 1 D1 enforcement. Crypto rawEdgeBps producer. 3 null trades (pending Joachim).

## Friday, August 28, 2026 Control-160 strict read-only control
Control-160 at ~06:00 UTC Aug 28 (Aug 28 08:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-159. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `274e0d9` (Control-159). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.
## Friday, August 28, 2026 Control-159 strict read-only control
Control-159 at ~05:00 UTC Aug 28 (Aug 28 07:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-158. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `9f023dd` (Control-158). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.
## Friday, August 28, 2026 Control-158 strict read-only control

Control-158 at ~04:00 UTC Aug 28 (Aug 28 06:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-157. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `54995f1` (Control-157). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,434.56, +$16.85 today, cash $86,761.76 (88% idle), long MV $11,672.80. 25 positions ALL swing, 0 unattributed, broker-authoritative. Swing MV $11,673 (3.15x $3,700 cap, pre-existing fills). RIVN 186.29, AVGO 2.35, AEP 1 (all swing). 100-run window 3968-4067 (Aug 27 19:26 - Aug 28 03:51 UTC): 0 gaps >15 min (~11h clean). Triggers: reconcile_cron 51, crypto_cron 17, cron 31, swing_cron 1. 2 known errors (3975 subrequest historical, 4020 swing qty mismatch safety block). 0 CYCLE_LEASE_HELD. SWING_OWNED_EXCLUDE 0 occurrences (first test Aug 28 13:30 UTC open). Crypto cadence :08/:38, 0 gaps >40 min, all fail-closed. RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) accepted not filled. 3 null trades persistent (703/648/645). All trades gross/fee/net=null (conservative).

**Follow-ups:** Verify SWING_OWNED_EXCLUDE + RIVN/AEP sell fills + qty mismatch at Aug 28 13:30 UTC open. Watch subrequest errors under Workers Paid. Sep 1 D1 enforcement. Crypto rawEdgeBps producer. 3 null trades (pending Joachim).

## Friday, August 28, 2026 Control-157 strict read-only control

Control-157 at ~03:00 UTC Aug 28 (Aug 28 05:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-156. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `f3e7238` (Control-156). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,444.03, ACTIVE, not PDT, not blocked, +$26.32 today (+0.027%). Cash $86,761.76 (88% idle), long MV $11,682.27, buying power $370,732.82. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,682 (3.16x $3,700 cap, inflated by pre-fix daytrading buys of swing-held RIVN/AVGO before cc9e813 deployed ~20:00 UTC Aug 27). broker_ledger fresh (reconcile through 02:51 UTC). Fee summary: cryptoFeeTelemetryStatus=unavailable, cryptoFeeAsOf 2026-08-19, unattributedUsd $272.82 (conservative).

**100-run window 3960-4059 (18:56 UTC Aug 27 - 02:51 UTC Aug 28):** 0 gaps >15 min (~9h+ clean since 17:36 UTC recovery). 2 errors (both known): run 3975 subrequest (historical pre-Workers-Paid), run 4020 swing_cron POSITION_QTY_MISMATCH (safety block, pre-fix daytrading buys). 2 CYCLE_LEASE_HELD (self-healed, runs 3961/3963). Triggers: reconcile_cron 47, crypto_cron 16, swing_cron 1, cron 36 (daytrading MARKET_CLOSED, nextOpen 13:30 UTC Aug 28). Crypto cadence :08/:38, all skipped (AVAXUSD stale 79684s, MATICUSD empty, validTA=0). SWING_OWNED_EXCLUDE 0 occurrences (first test Aug 28 13:30 UTC open). RIVN sell 186.29 (trade 740) + AEP sell 1 (trade 739) accepted not filled. 3 null trades persistent (703/648/645).

## Friday, August 28, 2026 Control-156 strict read-only control

Control-156 at ~02:00 UTC Aug 28 (Aug 28 04:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-155. Code `cc9e813` (Control-150, unchanged). No source changes since cc9e813. Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `5491090` (Control-155). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,442.52, ACTIVE, not PDT, not blocked, +$24.81 today (+0.025%). Cash $86,761.76 (88% idle), long MV $11,680.76, buying power $370,744.25. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,681 (3.16x $3,700 cap, inflated by pre-fix daytrading buys of swing-held RIVN/AVGO before cc9e813 deployed ~20:00 UTC Aug 27). broker_ledger_synced_until 2026-08-28T01:51:09Z (fresh). Fee summary: cryptoFeeTelemetryStatus=unavailable, cryptoFeeAsOf 2026-08-19, unattributedUsd $272.82 (conservative).

**100-run window 3952-4051 (18:31 UTC Aug 27 - 01:51 UTC Aug 28):** 0 gaps >15 min (~7h20m clean since 17:36 UTC recovery). 2 errors (both known): run 3975 subrequest (historical pre-Workers-Paid), run 4020 swing_cron POSITION_QTY_MISMATCH (safety block, pre-fix daytrading buys). 2 CYCLE_LEASE_HELD (self-healed). Triggers: reconcile_cron 43, crypto_cron 15, swing_cron 1, cron 41. Crypto cadence :08/:38, all skipped (LINKUSD stale 79684s, MATICUSD empty, validTA=0). SWING_OWNED_EXCLUDE 0 occurrences (first test Aug 28 13:30 UTC open).

**Pending fills:** RIVN sell 186.29 (trade 740), AEP sell 1 (trade 739), both accepted not filled. First fill: Aug 28 13:30 UTC open. RIVN fill should clear POSITION_QTY_MISMATCH.

**Persistent (unchanged):** 3 null-strategy trades (703/648/645). Crypto fail-closed (no rawEdgeBps producer, fee telemetry stale Aug 19, bars stale). Follow-ups: SWING_OWNED_EXCLUDE + sell fills at open, subrequest watch, Sep 1 D1 enforcement, crypto rawEdgeBps producer, null trades D1 update.

## Friday, August 28, 2026 Control-155 strict read-only control

Control-155 at ~01:00 UTC Aug 28 (Aug 28 03:00 +02). Strict GET-only. All 8 endpoints 200. HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect, no deploy needed, no correction needed. Steady state, unchanged from Control-154. Code `cc9e813` (Control-150, unchanged). Docs HEAD: this commit (local only, push blocked github_pat). Prior docs HEAD `38647a3` (Control-154). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,433.57, ACTIVE, not PDT, not blocked, +$15.86 today (+0.016%). Cash $86,761.76 (88% idle), long MV $11,671.81, buying power $370,720.90. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,672 (3.15x $3,700 cap, inflated by pre-fix daytrading buys of swing-held RIVN/AVGO before cc9e813 deployed ~20:00 UTC Aug 27). broker_ledger_synced_until 2026-08-28T00:51:09Z (fresh, ~9 min old).

**100-run window 3944-4043 (18:11 UTC Aug 27 - 00:51 UTC Aug 28):** 0 gaps >15 min (~6h40m clean since 17:36 UTC recovery). 2 errors (both known): run 3975 subrequest (historical, 19:46 UTC Aug 27), run 4020 swing_cron POSITION_QTY_MISMATCH (22:01 UTC, safety block worked, pre-fix daytrading buys). 0 CYCLE_LEASE_HELD. Triggers: reconcile_cron 40, crypto_cron 14, swing_cron 1, cron (daytrading) 45. Crypto cadence :08/:38 (1-min Cloudflare jitter), all skipped (CRYPTO_BARS_STALE ETHUSD 79684s, CRYPTO_BARS_UNAVAILABLE MATICUSD empty, CRYPTO_DATA_INSUFFICIENT validTA=0). SWING_OWNED_EXCLUDE 0 occurrences (deployed ~20:00 UTC, market closed — first real test Aug 28 13:30 UTC open).

**Pending fills at Aug 28 13:30 UTC open:** RIVN sell 186.29 (trade 740, accepted), AEP sell 1 (trade 739, accepted). Both should fill at open → RIVN position clears → POSITION_QTY_MISMATCH self-resolves.

**Known DEGRADED (unchanged):** 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). Crypto fail-closed: no rawEdgeBps producer in source, fee telemetry asOf Aug 19 (unavailable). All filled trades gross/fee/net=null, accounting_status=filled_lot_exact_unavailable (conservative). GitHub PAT not in vault — docs push blocked, docs HEAD local only.

**Follow-ups (unchanged):** (1) Verify SWING_OWNED_EXCLUDE skips on Aug 28 13:30 UTC open. (2) Confirm RIVN/AEP sell fills + POSITION_QTY_MISMATCH resolution at open. (3) Watch subrequest errors under Workers Paid. (4) Monitor run-log gap elimination. (5) Sep 1 D1 enforcement monitoring. (6) Crypto rawEdgeBps producer (code change + deploy). (7) 3 null-strategy trades (D1 update, pending Joachim's prioritization).

## Friday, August 28, 2026 Control-154 strict read-only control

Control-154 at ~00:00 UTC Aug 28 (Aug 28 02:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called.

**Verdict:** HEALTHY code/deploy (2.6.0), DEGRADED external. No new defects. No deploy needed. No correction needed. Steady state, unchanged from Control-153.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `cc9e813` (Control-150, unchanged). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `362ef17` (Control-153). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,445.84, ACTIVE, not PDT, not blocked, +$28.13 today (+0.029%). Cash $86,761.76 (88% idle), long MV $11,684.08, buying power $370,729.16. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,684 (3.16x $3,700 cap, inflated by pre-fix daytrading buys of swing-held RIVN/AVGO before cc9e813 deployed ~20:00 UTC Aug 27). broker_ledger_synced_until 2026-08-27T23:51:09Z (fresh, ~9 min old).

**100-run window 3936-4035 (17:41-23:51 UTC Aug 27):** 0 gaps >15 min (~6h20m clean since 17:36 UTC recovery). 2 errors (both known): run 3975 subrequest (historical 19:46 UTC), run 4020 swing_cron POSITION_QTY_MISMATCH (22:01 UTC, safety block, pre-fix daytrading buys). 0 CYCLE_LEASE_HELD. Crypto cadence :08/:38, all skipped (ETHUSD stale 79684s, MATICUSD empty, validTA=0). SWING_OWNED_EXCLUDE 0 occurrences (first test Aug 28 13:30 UTC open).

**Pending fills at Aug 28 13:30 UTC open:** RIVN sell 186.29 (trade 740), AEP sell 1 (trade 739), both accepted. Should self-resolve POSITION_QTY_MISMATCH when RIVN sell fills.

**Follow-ups (unchanged):** (1) Verify SWING_OWNED_EXCLUDE skips at Aug 28 13:30 UTC open. (2) Confirm RIVN/AEP sell fills + mismatch resolution. (3) Watch subrequest errors under Workers Paid. (4) Monitor run-log gap elimination. (5) Sep 1 D1 enforcement. (6) Crypto rawEdgeBps producer. (7) 3 null-strategy trades (703/648/645, D1 update pending Joachim's prioritization).

## Friday, August 28, 2026 Control-153 strict read-only control

Control-153 at ~23:00 UTC Aug 27 (Aug 28 01:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called.

**Verdict:** HEALTHY code/deploy (2.6.0), DEGRADED external. No new code defect. No deploy needed. Docs correction applied: README.md and docs/DEPLOYMENT_RUNBOOK.md had stale HEAD references from Control-152 (which edited Control-151 numbers without adding a new entry). Fixed to identify current HEAD.

**New finding — swing_cron run 4020 POSITION_QTY_MISMATCH:** Swing_cron at 22:01 UTC detected broker/D1 quantity mismatches on AVGO (internal 0.35 vs broker 2.35) and RIVN (internal 11.29 vs broker 186.29), caused by pre-fix daytrading buys of swing-held symbols before cc9e813 deployed ~20:00 UTC. Broker-authoritative quantity persisted to D1 (correction). New swing BUY entries blocked (safety). Swing sells submitted for RIVN (186.29) and AEP (1), both accepted, pending fill at market open. LCID/AMD/NXPI stale D1 rows soft-closed. Safety mechanism worked as designed. Should self-resolve at Aug 28 13:30 UTC open when RIVN sell fills.

---

## Thursday, August 27, 2026 Control-152 strict read-only control

Control-152 at ~22:00 UTC (Aug 28 00:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called.

**Verdict:** HEALTHY code/deploy (2.6.0), DEGRADED external. No new defects. No deploy needed. No code/docs/config correction needed.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `cc9e813` (Control-150, unchanged). Docs HEAD `c7ae1f7` (local only — push BLOCKED: github_pat not in vault). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,423.81, ACTIVE, not PDT, not blocked, +$6.10 today (+0.006%). Cash $86,761.76 (88% idle), long MV $11,662.05, buying power $370,736. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,662 (3.15x $3,700 cap, inflated by pre-fix daytrading buys of swing-held RIVN/AVGO before cc9e813 deployed ~20:00 UTC). broker_ledger_synced_until 2026-08-27T21:51:07Z (fresh, ~9 min old).

**Run analysis (100-run window 3919-4018):** 0 time gaps >12min — continuous cadence. Triggers: cron 60, reconcile_cron 29, crypto_cron 11. 1 error run: 3975 (cron, 19:46 UTC, "Fatal: Too many subrequests by single Worker invocation" — post-Workers-Paid-upgrade ~18:54 UTC, historical, recovered immediately). 2 CYCLE_LEASE_HELD: runs 3961 (daytrading) and 3963 (maintenance) — self-healed. Crypto cadence confirmed at :08/:38 UTC (11 runs across window, all skipped with RECONCILIATION_DEFERRED_TO_MAINTENANCE or REQUIRED_SCHEMA_MISSING). Latest run 4018 at 21:56 UTC — continuous to present.

**Trade analysis (limit=100):** 738 total trades. Strategy breakdown: daytrading 77, swing 20, null 3. 3 persistent null-strategy trades (703, 648, 645). accounting_status: all filled_lot_exact_unavailable (conservative — exact per-fill gross/fee/net unavailable). Recent 15 trades (724-738) all daytrading from Aug 27 19:11-19:46 UTC market window.

**Crypto edge gate:** rawEdgeBps field exists in types (technical-analysis.ts:50, crypto-strategy.ts:29-32) but no producer populates it; prepareCryptoRiskDecision defaults to signal.rawEdgeBps ?? signal.taSignal.rawEdgeBps which is always undefined. Crypto BUYs fail-closed. Fee telemetry stale (asOf 2026-08-19). ETHUSD stale ~22h, MATICUSD empty.

**Follow-ups:** (1) Verify SWING_OWNED_EXCLUDE skips on Aug 28 13:30 UTC market open — cc9e813 deployed but untested with live market. (2) Watch subrequest errors under Workers Paid — run 3975 hit one at 19:46 UTC (~52 min post-upgrade); if it recurs on Aug 28 open, Paid plan may not be fully active/propagated. (3) Monitor run-log gap elimination under Workers Paid (0 new gaps in this window, ~4h20m clean since 17:36 UTC recovery). (4) 3 null-strategy trades remain (fixable by targeted D1 update, pending Joachim's prioritization). (5) Crypto fail-closed (requires code change + deploy for rawEdgeBps producer). (6) Sep 1 D1 enforcement monitoring.

## Thursday, August 27, 2026 Control-151 strict read-only control

Control-151 at ~21:00 UTC (Aug 27 23:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/config`, `/api/account`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called.

**Verdict:** HEALTHY code/deploy (2.6.0), DEGRADED external. No new defects. No deploy needed. No code/docs/config correction needed (docs already identify current HEAD).

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `cc9e813` (Control-150, unchanged). Docs HEAD `a5cdb81` (local only — push BLOCKED: github_pat not in vault). 224 tests / 845 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,421.74, ACTIVE, not PDT, not blocked, +$4.03 today (+0.004%). Cash $86,761.76 (88% idle), long MV $11,659.98, buying power $370,724.99. 25 positions ALL swing, 0 unattributed, source=alpaca (broker-authoritative), MV $11,660 (3.15x $3,700 cap). MV inflated by today's pre-fix daytrading buys of RIVN/AVGO (swing-held symbols) that occurred 19:11-19:46 UTC before the cc9e813 fix deployed ~20:00 UTC. The fix prevents future daytrading BUYs on swing-owned symbols but has not yet been tested with market open (next: Aug 28 13:30 UTC). broker_ledger_synced_until 2026-08-27T20:51:07Z (fresh, ~9 min old).

**Run-log gap analysis (100-run window 3899-3998):** 1 error (run 3975 "Fatal: Too many subrequests by single Worker invocation" at 19:46:41 UTC, post-Workers-Paid-upgrade ~18:54 UTC, recovered immediately). 0 CYCLE_LEASE_HELD. 2 historical gaps: 80 min pre-market (3908→3909, 12:11→13:31 UTC, 10 missing runs) + 178 min market-hours (3932→3933, 14:38→17:36 UTC, ~57 missing runs), both pre-17:36 UTC recovery. No new gaps since 17:36 UTC (~3h20m clean through 20:56 UTC). Crypto cadence :07/:37 confirmed (timestamps :08/:38). Daytrading cron confirmed */5 (MARKET_CLOSED after 20:00 UTC).

**Crypto:** Fail-closed. ETHUSD stale 79684s (~22h, latest bar 2026-08-26T22:30Z), MATICUSD empty (0 bars), validTA=0 < required 3, CRYPTO_DATA_INSUFFICIENT. No rawEdgeBps producer in source (technical-analysis.ts:50 type-only, crypto-strategy.ts:29/66 pass-through, risk-manager.ts:192/195 config-sourced). crypto_min_edge_after_costs=8, fee telemetry asOf Aug 19 (cryptoFeeTelemetryStatus=unavailable). RECONCILIATION_DEFERRED_TO_MAINTENANCE + EQUITY_DIRECTION_FALLBACK present.

**Trades:** 738 total, 738 executed. Trades 724-738 all daytrading, all filled during 19:11-19:46 UTC market window. All accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK — all sells, strategy=null, accounting_status=filled_lot_exact_unavailable).

**Follow-ups:** (1) Verify SWING_OWNED_EXCLUDE skips on Aug 28 13:30 UTC market open — cc9e813 deployed but untested with live market. (2) Watch subrequest errors under Workers Paid — run 3975 hit one at 19:46 UTC (~52 min post-upgrade); if it recurs on Aug 28 open, Paid plan may not be fully active/propagated. (3) Monitor run-log gap elimination under Workers Paid (no new gaps since 17:36 UTC, ~3h20m clean). (4) 3 null-strategy trades remain (fixable by targeted D1 update, pending Joachim's prioritization). (5) Crypto fail-closed (requires code change + deploy for rawEdgeBps producer). (6) Sep 1 D1 enforcement monitoring.

## Thursday, August 27, 2026 Control-150 daytrading cap bypass fix - DEPLOY

**Change:** Added a BUY exclusion gate in the daytrading cycle (`src/index.ts`) that prevents daytrading from buying swing-owned symbols. The `swingOwnedSymbols` set (D1 swing positions + swing trade tickers via `getSwingTradeSymbols()`) is now computed early in the cycle and reused in the final sync (saving one DB call). When a daytrading BUY decision targets a swing-owned symbol, it is skipped with structured code `SWING_OWNED_EXCLUDE`. SELL decisions are never blocked. No capital cap changed (5000/3700/2000 USD preserved). No trading behavior changed beyond preventing the cap bypass.

**Rationale:** Control-150's read-only audit found that daytrading (newly unblocked by Control-149's stale-bar fix) was buying swing-held symbols (RIVN, AVGO). The broker combines swing and daytrading positions into one line item tagged strategy='swing' in D1. The daytrading cap check filters by strategy='daytrading' and cannot see the daytrading portion, undercounting gross exposure. Today's session: ~$3,672 uncounted, actual gross ~$8,637 vs $5,000 cap (1.73x). The fix is consistent with the existing design: the final sync already excludes swing-owned symbols from daytrading attribution (Control-117 fix).

**Validation:** 224 tests / 845 assertions pass (was 223/841, +1 test +4 assertions for the new SWING_OWNED_EXCLUDE regression test). Typecheck clean. diff-check clean. Commit cc9e813.

**Deploy:** Direct Cloudflare API PUT from `/workspace/alpaca-trading-bot`. Post-deploy GET verification pending.

**Follow-up:** Monitor next daytrading runs to confirm SWING_OWNED_EXCLUDE skips appear for swing-held symbols and no daytrading BUYs execute on swing-owned symbols. Watch for run-log gaps under Workers Paid plan. Monitor the single subrequest error at run 3975 (19:46 UTC, post-upgrade) — if it recurs, the Paid plan may not be fully active.

## Thursday, August 27, 2026 Control-149 daytrading stale-bar threshold fix - DEPLOY

**Change:** Raised `DAYTRADING_MAX_BAR_STALE_INTERVALS` from 3 to 4 in `src/market-data-quality.ts:64`. This changes the daytrading bar freshness threshold from 15 min (3 x 5-min intervals) to 20 min (4 x 5-min intervals). The Alpaca paper data feed has a consistent ~16-min lag, so bars were ~977s old vs the 900s threshold, causing every daytrading run to skip with `DAYTRADING_BARS_STALE`. The extra interval gives ~4 min of headroom. Crypto threshold unchanged (CRYPTO_MAX_BAR_STALE_INTERVALS=3, 45 min for 15-min candles). No capital cap changed (5000/3700/2000 USD preserved).

**Rationale:** Daytrading has been 100% blocked since the threshold was introduced. 91.8% of capital sits in cash earning nothing. The 16-min paper data feed lag is a persistent external constraint, not a transient issue. One extra interval of staleness on 5-min candles is a modest data-quality relaxation that unblocks the strategy while preserving the fail-closed gate for genuinely stale data.

**Validation:** 223 tests / 841 assertions pass, 0 fail. Typecheck clean. No code defect. Commit pending (push blocked by github_pat).

**Deploy:** Direct Cloudflare API PUT from `/workspace/alpaca-trading-bot`. Post-deploy GET verification pending.

**Follow-up:** Monitor first daytrading runs after next 13:30 UTC market open to confirm bars pass the new 1200s threshold and trades execute. If Alpaca paper feed lag grows beyond 20 min, consider 5 intervals. Workers Paid plan now active (should eliminate run-log gaps).

## Thursday, August 27, 2026 Control-148 strict read-only production control - HEALTHY/DEGRADED

Control-148 at ~19:00 UTC (Aug 27 21:00 +02). Strict GET-only. All seven probed endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Run-log gap status (time-gap analysis across all trigger types, Control-144 lesson applied):** 100-run window 3861-3960. Two known historical gaps remain in the window: 80 min pre-market (runs 3908→3909, 12:11→13:31 UTC) and 178 min market-hours (runs 3932→3933, 14:38→17:36 UTC). Both are pre-recovery at 17:36 UTC. **No new gaps since 17:36 UTC** — cadence continuous for ~1h24m through the 19:00 UTC query. 0 errors, 0 CYCLE_LEASE_HELD across the full window. Workers Paid plan upgraded by Joachim at ~18:54 UTC (~20:54 +02); only ~6 min of post-upgrade run data at query time — too early to confirm the upgrade eliminated the silent-throw gap pattern, but no regression observed. Continued monitoring required to verify the Free-tier ceiling is lifted.

**broker_ledger_synced_until:** 2026-08-27T18:51:07.166Z (fresh, ~9 min at query time — recovered and tracking normally after the 178-min gap freeze at 14:41Z).

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `d831469` (Control-147). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,449.03, ACTIVE, not PDT, not blocked, +$31.32 today (+0.032%). 25 positions all swing, 0 unattributed, MV $8,021.54 (2.17x $3,700 cap, pre-existing fills, no active bypass). 100 runs (3861-3960): 0 errors, 0 CYCLE_LEASE_HELD, 2 historical run-log gaps (both pre-17:36 UTC recovery). Crypto fail-closed: no rawEdgeBps producer in source (technical-analysis.ts:50 type-only, crypto-strategy.ts:29/66 pass-through, risk-manager.ts:192/195 config-sourced — never computed from market data or confidence), crypto_min_edge_after_costs=8, fee telemetry asOf Aug 19 (cryptoFeeTelemetryStatus=unavailable). MATICUSD empty, AVAXUSD stale 79684s (~22h), validTA=0. 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). 100 filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable (conservative). Daytrading cron */5 from 13:31 UTC, skipped on DAYTRADING_BARS_STALE (age ~976s vs 900s max) + EQUITY_DIRECTION_FALLBACK. Crypto cadence :07/:37 confirmed (run 3954 at 18:38:05). No deploy needed. Code 22b3dba unchanged.

---
## Thursday, August 27, 2026 Control-147 strict read-only production control - HEALTHY/DEGRADED

Control-147 at ~18:00 UTC (Aug 27 20:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. `/api/equity`, `/api/fees`, `/api/snapshot` return 404 (expected). No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**CORRECTION to Control-146: the market-hours run-log gap was 178 min, not ~142 min.** Control-146 queried at ~17:00 UTC while the gap was still ongoing (run 3932 frozen at 14:38:05 UTC). Control-147's full timeline shows runs resumed at 17:36:19 UTC (run 3933), making the total gap 178.2 min (14:38→17:36 UTC), the largest observed. ~57 scheduled runs missing (~35 daytrading cron, ~17 reconcile_cron, ~5 crypto_cron). After 17:36 UTC, cadence fully recovered (runs 3933-3942 logged with normal */5, */10, :07/:37 timing). broker_ledger_synced_until recovered to 2026-08-27T17:51:07Z (fresh at query time). No trades missed (daytrading was skipping on stale bars). Silent-throw Free-tier pattern; approved paid-plan upgrade remains the remedy and is increasingly urgent as gaps now reach 3 hours during market hours.

**Second gap (unchanged from Control-145): 80 min pre-market** (runs 3908→3909, 12:11→13:31 UTC). ~10 missing runs (8 reconcile_cron, 2 crypto_cron).

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `dee8e3c` (Control-146). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,452.26, ACTIVE, not PDT, not blocked, +$34.55 today (+0.035%). 25 positions all swing, 0 unattributed, MV $8,024.21 (2.17x $3,700 cap, pre-existing fills, no active bypass). 100 runs (3841-3940): 0 errors, 0 CYCLE_LEASE_HELD, 2 run-log gaps (80 min pre-market 3908→3909, 178 min market-hours 3932→3933). Crypto fail-closed: no rawEdgeBps producer in source (technical-analysis.ts:50 type-only, crypto-strategy.ts:29/66 pass-through, risk-manager.ts:192/195 config-sourced — never computed from market data or confidence), crypto_min_edge_after_costs=8, fee telemetry asOf Aug 19 (cryptoFeeTelemetryStatus=unavailable). MATICUSD empty, AVAXUSD stale 79684s (~22h), validTA=0. 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). 100 filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable (conservative). Daytrading cron */5 from 13:31 UTC, skipped on DAYTRADING_BARS_STALE (age ~976s vs 900s max) + EQUITY_DIRECTION_FALLBACK. Crypto cadence :07/:37 confirmed (timestamps :08/:38). No deploy needed. Code 22b3dba unchanged.

---

## Thursday, August 27, 2026 Control-146 strict read-only production control - HEALTHY/DEGRADED

Control-146 at ~17:00 UTC (Aug 27 19:00 +02). Strict GET-only. All eight endpoints returned HTTP 200, 0 errors. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**NEW FINDING: ~142-min run-log delivery gap during market hours (run 3932 newest at 14:38:05 UTC, query at ~17:00 UTC).** ~46 scheduled runs missing (~28 daytrading, ~14 reconcile, ~4 crypto). Silent-throw Free-tier pattern, now during market hours, much larger than the 80-min pre-market gap Control-145 found. broker_ledger_synced_until frozen at 14:41:07Z. No trades missed (daytrading was skipping on stale bars), but observability and reconciliation severely degraded. Paid-plan upgrade increasingly urgent.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `658240f` (Control-145). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,484.25, ACTIVE, not PDT, not blocked. 25 positions all swing, MV $8,056.93 (2.18x cap). 100 runs (3833-3932): 0 errors, 0 lease holds, 2 gaps (80 min pre-market, ~142 min market-hours). Crypto fail-closed (SOLUSD stale, MATICUSD empty, no rawEdgeBps producer, fee telemetry asOf Aug 19). 3 null-strategy trades (703 PLD, 648 NOW, 645 DUK). All 100 trades gross/fee/net=null, accounting_status=filled_lot_exact_unavailable. No deploy needed. Code 22b3dba unchanged.

---

## Thursday, August 27, 2026 Control-145 strict read-only production control - HEALTHY/DEGRADED

Control-145 at ~16:00 UTC (Aug 27 18:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. `/api/equity`, `/api/fees`, `/api/snapshot` return 404 (expected). No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**NEW FINDING: 80-min run-log delivery gap (runs 3908 to 3909).** Time-gap analysis across all trigger types revealed 10 scheduled runs missing from the log between 12:11:13 and 13:31:12 UTC (Aug 27): 8 reconcile_cron (expected at :20/:30/:40/:50/:00/:10/:20/:30) and 2 crypto_cron (expected at 12:37 and 13:07). This is the silent-throw Free-tier pattern (Worker throws before run_log write). Pre-market window, no trading impact, but observability was degraded. This gap was within the 3833-3932 window that Control-144 reported as "0 gaps" because Control-144 did not perform time-gap analysis across all trigger types. The approved paid-plan upgrade remains the remedy.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `a7165ca` (Control-144). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,492.77, ACTIVE, not PDT, not blocked, +$75.06 today (+0.076%). 25 positions all swing, 0 unattributed, MV $8,062.58 (2.18x $3,700 cap, pre-existing fills, no active bypass). AMD/LCID/NXPI exited (sells 720-722 filled at 13:30 UTC open). 100 runs (3833-3932): 0 errors, 0 CYCLE_LEASE_HELD, 1 run-log gap (80 min, 10 missing runs, pre-market). Crypto fail-closed: no rawEdgeBps producer in source (crypto-strategy.ts:29, risk-manager.ts:192), crypto_min_edge_after_costs=8, fee telemetry asOf Aug 19 (cryptoFeeTelemetryStatus=unavailable). 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). 100 filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable (conservative). broker_ledger_synced_until 2026-08-27T14:41:07Z. Crypto cadence confirmed :07/:37 (timestamps :08/:38 = completion times). Daytrading cron confirmed */5 starting 13:31 UTC (market open 13:30). No deploy needed. Code 22b3dba unchanged.

---

## Thursday, August 27, 2026 Control-144 strict read-only production control - HEALTHY/DEGRADED

Control-144 at ~15:00 UTC (Aug 27 17:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. `/api/equity`, `/api/fees`, `/api/snapshot` return 404 (expected). No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `1715640` (Control-143). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Account ACTIVE, not PDT, not blocked. Equity $98,500.29, cash $90,427.32, long MV $8,072.97, change_today +$82.58 (+0.084%). 25 positions, ALL strategy=swing, 0 unattributed. Swing MV $8,073 (2.18x $3,700 cap, pre-existing bypass fills, reduced from 2.53x). Re-tag stable since Control-126.

**Runs window:** 100 runs (3833-3932, Aug 27 02:51-14:38 UTC): 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps >15min. 64 reconcile_cron (ok), 22 crypto_cron (skipped, fail-closed), 14 daytrading cron (skipped, stale bars). 17+ hours clean since Aug 26 21:21 UTC. Crypto cadence :08/:38 matches `7-59/30` schedule.

**DEGRADED external:** Swing MV 2.18x cap (pre-existing). 3 null-strategy trades (703 PLD, 648 NOW, 645 DUK). Crypto fail-closed (SOLUSD stale ~22h, MATICUSD empty, fee telemetry asOf Aug 19, no rawEdgeBps producer). All trades gross/fee/net=null, accounting_status=filled_lot_exact_unavailable. Historical run-log gaps Aug 26.

**Follow-ups:** Paid-plan upgrade (approved not executed). Sep 1 D1 enforcement monitoring. GitHub PAT re-add. 3 null-strategy trades root cause. Crypto rawEdgeBps producer. No caps changed. No deploy needed.

## Thursday, August 27, 2026 Control-143 strict read-only production control - HEALTHY/DEGRADED

Control-143 at ~14:00 UTC (Aug 27 16:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. `/api/equity`, `/api/fees`, `/api/snapshot` return 404 (expected). No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `1482559` (Control-142). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,451.23, last_equity $98,417.71, change_today +$33.52 (+0.034%), ACTIVE, not blocked, not PDT. Cash $90,427.32, buying_power $383,291.76, long_market_value $8,023.91. EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback; observability-only). broker_ledger_synced_until 2026-08-27T13:51:07Z (fresh). last_prune_date 2026-08-27. Snapshot 1021 at 13:56:12 UTC: equity $98,452.84, positions_count 25, daily_pl +$35.13.

**Positions:** 25 broker-authoritative positions (metadata_source=d1, broker quantities/prices are current-state evidence, D1 is metadata only). ALL strategy=swing, 0 unattributed, 0 null, 0 daytrading. MV $8,026.91 (2.17x $3,700 cap, pre-existing Control-101/117 bypass fills, no active bypass). Swing over-cap REDUCED from 2.53x (Control-142) as the 3 pending sells filled at the Aug 27 13:30 UTC open. Swing cap enforcement confirmed (CAPITAL_CAP blocks new entries). Re-tag stable since Control-126.

**Delivery (100 runs, 3819-3918, Aug 27 01:08-13:56 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps. No new gaps/lease holds since Aug 26 21:21 UTC (16+ hours clean). Triggers: 70 reconcile_cron (MAINTENANCE_ONLY every 10 min, ok), 24 crypto_cron (:08/:38 cadence, all fail-closed), 6 daytrading cron (13:31-13:56 UTC, first runs after 13:30 open, skipped EQUITY_DIRECTION_FALLBACK + DAYTRADING_BARS_STALE/UNAVAILABLE — data-feed lag right after open, fail-safe, 0 errors). Swing absent (next 22:00 UTC).

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source (crypto-strategy.ts). CRYPTO_BARS_STALE (AVAXUSD ageSeconds ~79684 vs maxStale 2700, latestBarAt 2026-08-26T15:30Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty, received=0), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). RECONCILIATION_DEFERRED_TO_MAINTENANCE. EQUITY_DIRECTION_FALLBACK each cycle (observability only). Fee telemetry stale (asOf 2026-08-19, status=unavailable, cryptoFeeSampleCount=0).

**Trades (100, 624-722):** 97 filled, 3 now filled (was pending). 3 pending sells (720-722: AMD 0.28 @ $481.16, LCID 209 @ $4.958278, NXPI 0.53 @ $225.57) FILLED at the Aug 27 13:30 UTC open, strategy=swing. AMD/LCID/NXPI exited positions. Filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded (conservative). Fee summary: totalUsd $272.82, all unattributed, regulatoryUsd $3.71. Strategy distribution: swing 20, daytrading 77, null 3. 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK — all sells, filled).

**Filtered run observability:** Runs include analyzed_candidates, filtered_candidates, run_details with structured skip codes (MAINTENANCE_ONLY, RECONCILIATION_DEFERRED_TO_MAINTENANCE, EQUITY_DIRECTION_FALLBACK, CRYPTO_BARS_UNAVAILABLE, CRYPTO_BARS_STALE, CRYPTO_DATA_INSUFFICIENT, DAYTRADING_BARS_STALE, DAYTRADING_BARS_UNAVAILABLE), each with type, scope, message, context, and count. Skips are data-quality fail-closed, not lease holds or errors.

**Status: HEALTHY (code/deploy), DEGRADED (external: pre-existing swing over-cap 2.17x + crypto bars stale 22h+ + historical run-log gaps + 3 null-strategy trades + push blocked).** No deploy needed. Paid-plan upgrade approved but not executed (remedy for Free-tier run-log gaps). Follow-up: rawEdgeBps producer; github_pat; paid-plan upgrade; crypto bar freshness; 3 null-strategy trades.

## Thursday, August 27, 2026 Control-141 strict read-only production control - HEALTHY/DEGRADED

Control-141 at ~12:00 UTC (Aug 27 14:00 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`, `/api/account`) returned HTTP 200, 0 errors. `/api/equity`, `/api/fees`, `/api/snapshot` returned 404 (expected). No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0, `src/version.ts` RELEASE_VERSION='2.6.0'. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `b6fa933` (Control-140). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,504.19, last_equity $98,417.71, change_today +$86.48 (+0.088%), ACTIVE, not blocked, not PDT. Cash $89,136.77, buying_power $381,075.17, long_market_value $9,367.42. EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback; observability-only, does not weaken risk controls). broker_ledger_synced_until 2026-08-27T11:51:12Z (fresh). last_prune_date 2026-08-27. Snapshot 1013 at 11:37:59 UTC: equity $98,503.91, positions_count 28.

**Positions:** 28 broker-authoritative positions (metadata_source=d1 for all, broker quantities/prices are current-state evidence, D1 is metadata only). ALL strategy=swing, 0 unattributed, 0 null, 0 daytrading. MV $9,367.42 (2.53x $3,700 cap, pre-existing Control-101/117 bypass fills, no active bypass). Swing cap enforcement confirmed (CAPITAL_CAP blocks new entries). Re-tag stable since Control-126 (metadata_updated_at 2026-08-26 22:01:2x).

**Delivery (100 runs, 3806-3905, Aug 26 23:31 - Aug 27 11:51 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps >35min. No new gaps/lease holds since Aug 26 21:21 UTC (14+ hours clean). Triggers: 75 reconcile_cron (MAINTENANCE_ONLY every 10 min, ok, brokerOrders 3, 0 lookup failures), 25 crypto_cron (:08/:38 cadence, all fail-closed). Daytrading absent (market closed, opens 13:30 UTC ~1.5h away). Swing absent (next 22:00 UTC).

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source (crypto-strategy.ts). CRYPTO_BARS_STALE (LINKUSD/AVAXUSD ageSeconds ~79685 vs maxStale 2700, latestBarAt 2026-08-26T13:00-13:30Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty, received=0), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). RECONCILIATION_DEFERRED_TO_MAINTENANCE (crypto skips duplicated reconciliation, defers to */10 maintenance schedule). EQUITY_DIRECTION_FALLBACK each cycle (observability only). Fee telemetry stale (asOf 2026-08-19, status=unavailable, cryptoFeeSampleCount=0).

**Trades (100, 624-722):** 97 filled, 3 pending. 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted/new, filled_qty=0, leaves_qty=qty, strategy=swing, day orders for Aug 27 13:30 UTC market open (~1.5h away). Lifecycle fields present: client_order_id, filled_qty, leaves_qty, broker_updated_at, last_reconciled_at (fresh: 2026-08-27 11:50-11:51 UTC). Filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded (conservative — uncertain fees stay unattributed rather than forced weak matches). Fee summary: totalUsd $272.82, all unattributed, regulatoryUsd $3.71. Strategy distribution: swing 20, daytrading 77, null 3. 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK — all sells, filled).

**Filtered run observability:** Runs include analyzed_candidates, filtered_candidates, run_details with structured skip codes (MAINTENANCE_ONLY, RECONCILIATION_DEFERRED_TO_MAINTENANCE, EQUITY_DIRECTION_FALLBACK, CRYPTO_BARS_UNAVAILABLE, CRYPTO_BARS_STALE, CRYPTO_DATA_INSUFFICIENT), each with type, scope, message, context, and count. Crypto skips are data-quality fail-closed, not lease holds or errors.

**Stale workspace root:** The workspace root `/workspace/src/` and `/workspace/wrangler.toml` are an old 3-cron copy (crypto `0 */4`, no reconcile). The actual deployed code is this nested repo `/workspace/alpaca-trading-bot/` with the correct 4-cron config. Not a live bot issue; noted for cleanup.

**Status: HEALTHY (code/deploy), DEGRADED (external: pre-existing swing over-cap + crypto bars stale 22h+ + historical run-log gaps + 3 null-strategy trades + push blocked).** No deploy needed. Paid-plan upgrade approved but not executed (remedy for Free-tier run-log gaps). Follow-up: rawEdgeBps producer; github_pat; paid-plan upgrade; crypto bar freshness; 3 null-strategy trades; swing normalizes as 3 pending sells fill at Aug 27 13:30 UTC open; stale workspace root cleanup.

## Thursday, August 27, 2026 Control-140 strict read-only production control - HEALTHY/DEGRADED

Control-140 at ~11:00 UTC (Aug 27 13:00 +02). Strict GET-only. All eight endpoints returned HTTP 200, 0 errors. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Version identity (all aligned):** Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `aac4770` (Control-139). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Four schedules confirmed: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,519.70, ACTIVE, not blocked. 28 positions all strategy=swing, MV $9,383.32 (2.54x $3,700 cap, pre-existing). 3 pending sells (720-722: AMD/LCID/NXPI) day orders for Aug 27 13:30 UTC open. 100 runs (3798-3897): 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps. 13+ hours clean since Aug 26 21:21 UTC. Crypto fail-closed (bars stale 22h+, validTA=0, fee telemetry unavailable asOf Aug 19). 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). broker_ledger_synced_until 2026-08-27T10:51:11Z (fresh). Status: HEALTHY (code/deploy), DEGRADED (external). No deploy needed.

## Thursday, August 27, 2026 Control-139 strict read-only production control - HEALTHY/DEGRADED

Control-139 at ~10:03 UTC (Aug 27 12:03 +02). Strict GET-only. Six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200, 0 errors. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required.

**Version identity (all aligned):** `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0, `package.json`=2.6.0. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `5f7b4e8` (Control-138). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8). Four schedules confirmed in wrangler.toml and live runs: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, `*/10 * * * *`.

**Live state:** Equity $98,520.58, last_equity $98,417.71, change_today +$102.87 (+0.105%), ACTIVE, not blocked, not PDT. Cash $89,136.77, buying_power $381,156.31, long_market_value $9,394.49. EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback; observability-only, does not weaken risk controls). broker_ledger_synced_until 2026-08-27T09:51:12Z (fresh). last_prune_date 2026-08-27. Snapshot 1009 at 09:37:59 UTC: equity $98,516.38.

**Positions:** 28 broker-authoritative positions (source=alpaca, positionsAvailable=true, freshness confirms broker quantities/prices are current-state evidence, D1 is metadata only). ALL strategy=swing, 0 unattributed, 0 null, 0 daytrading. MV $9,383.81 (2.54x $3,700 cap, pre-existing Control-101/117 bypass fills, no active bypass). Swing cap enforcement confirmed (CAPITAL_CAP blocks new entries). Re-tag stable since Control-126 (metadata_updated_at 2026-08-26 22:01:2x, metadata_source=d1 for all).

**Delivery (30 visible runs, 3861-3890, 07:38-10:01 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps in window. No new gaps/lease holds since Aug 26 21:21 UTC (12+ hours clean). Triggers: 23 reconcile_cron (MAINTENANCE_ONLY every 10 min, ok), 7 crypto_cron (:08/:38 cadence, all fail-closed). Daytrading absent (market closed, opens 13:30 UTC). Swing absent (next 22:00 UTC).

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source (crypto-strategy.ts). CRYPTO_BARS_STALE (SOLUSD/LINKUSD/ETHUSD/AVAXUSD ageSeconds ~79685 vs maxStale 2700, latestBarAt 2026-08-26T09:00-11:30Z), CRYPTO_BARS_UNAVAILABLE (MATICUSD empty, received=0), CRYPTO_DATA_INSUFFICIENT (validTA=0, required=3). RECONCILIATION_DEFERRED_TO_MAINTENANCE (crypto skips duplicated reconciliation, defers to */10 maintenance schedule). EQUITY_DIRECTION_FALLBACK each cycle (observability only). Fee telemetry stale (asOf 2026-08-19, status=unavailable, cryptoFeeSampleCount=0).

**Trades:** 50 most-recent reviewed. 47 filled, 2 accepted, 1 new. 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted/new, filled_qty=0, leaves_qty=qty, strategy=swing, day orders for Aug 27 13:30 UTC market open. Lifecycle fields present: client_order_id, filled_qty, leaves_qty, broker_updated_at, last_reconciled_at (fresh: 2026-08-27 10:00-10:01 UTC). Filled trades: gross/fee/net=null, accounting_status=filled_lot_exact_unavailable, fee_attribution=none-recorded (conservative — uncertain fees stay unattributed rather than forced weak matches). Fee summary: totalUsd $272.82, all unattributed (unattributedUsd $272.82), regulatoryUsd $3.71. Trade 703 (PLD) strategy=null persistent (known observability gap).

**Filtered run observability:** Runs include analyzed_candidates, filtered_candidates, run_details with structured skip codes (RECONCILIATION_DEFERRED_TO_MAINTENANCE, EQUITY_DIRECTION_FALLBACK, CRYPTO_BARS_UNAVAILABLE, CRYPTO_BARS_STALE, CRYPTO_DATA_INSUFFICIENT), each with type, scope, message, context, and count. Crypto skips are data-quality fail-closed, not lease holds or errors.

**Stale workspace root:** The workspace root `/workspace/src/` and `/workspace/wrangler.toml` are an old 3-cron copy (crypto `0 */4`, no reconcile). The actual deployed code is this nested repo `/workspace/alpaca-trading-bot/` with the correct 4-cron config. Not a live bot issue; noted for cleanup.

**Status: HEALTHY (code/deploy), DEGRADED (external: pre-existing swing over-cap + crypto bars stale 22h+ + historical run-log gaps + trade 703 null + push blocked).** No deploy needed. Paid-plan upgrade approved but not executed (remedy for Free-tier run-log gaps). Follow-up: rawEdgeBps producer; github_pat; paid-plan upgrade; crypto bar freshness; trade 703 null strategy; swing normalizes as 3 pending sells fill at Aug 27 13:30 UTC open; stale workspace root cleanup.

## Thursday, August 27, 2026 Control-137 strict read-only production control - HEALTHY/DEGRADED

Control-137 at ~10:10 UTC (Aug 27 12:10 +02). Strict GET-only. All eight endpoints (`/`, `/health`, `/api/dashboard`, `/api/positions`, `/api/trades`, `/api/runs`, `/api/config`, `/api/account`) returned HTTP 200. No code defect; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `41315bf` (Control-136). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

**Live state:** 28 broker-authoritative positions (all strategy=swing, 0 unattributed, 0 null, 0 daytrading, MV $9,347.28 = 2.52x $3,700 cap, pre-existing). Equity $98,484.06 (+$66.35, +0.067%), ACTIVE, not blocked. Cash $89,136.77, buying_power $381,020.14, long_market_value $9,347.29, last_equity $98,417.71. 30 visible runs (from Aug 27 08:11 UTC): 0 errors, 0 CYCLE_LEASE_HELD. No new gaps or lease holds since Aug 26 21:21 UTC. 3 pending sells (720-722: AMD/LCID/NXPI) open, filled_qty=0, strategy=swing, day orders for Aug 27 13:30 UTC open. 1 null-strategy trade persistent (703 PLD). Swing cap enforcement confirmed (no active bypass). Status: HEALTHY (code/deploy), DEGRADED (external: swing over-cap pre-existing + crypto fail-closed + historical run-log gaps + trade 703 null). Paid-plan upgrade approved, not executed. No deploy needed.

## Thursday, August 27, 2026 Control-136 strict read-only production control - HEALTHY/DEGRADED

Control-136 at ~08:00 UTC (Aug 27 10:00 +02). Strict GET-only. All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `8f25ef4` (Control-135). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

**Live state:** 28 broker-authoritative positions (all strategy=swing, 0 unattributed, 0 null, 0 daytrading, MV $9,332.80 = 2.52x $3,700 cap, pre-existing). Equity $98,469.57 (change_today +$51.86, +0.053%), ACTIVE, not blocked. Cash $89,136.77, buying_power $380,981.38. Snapshot 1005 at 07:37:59 UTC: equity $98,461.54, daily_pl -$63.44. broker_ledger_synced_until 2026-08-27T07:51:12Z (fresh). last_prune_date 2026-08-27. 100 visible runs (3774-3874, Aug 26 20:40 - Aug 27 08:01 UTC): 0 errors, 5 CYCLE_LEASE_HELD (all historical Aug 26 20:40-21:10 UTC, self-healed). No new gaps or lease holds since Aug 26 21:21 UTC (10+ hours clean). Triggers: 65 reconcile_cron (ok, MAINTENANCE_ONLY, 3 broker orders, 0 failures), 21 crypto_cron (:08/:38 cadence, all fail-closed: CRYPTO_BARS_STALE ETHUSD ~79684s stale, MATICUSD empty, validTA=0, fee telemetry unavailable asOf Aug 19, no rawEdgeBps), 13 daytrading cron (MARKET_CLOSED), 1 swing_cron (3794, re-tagged all 13 fills strategy=swing). 3 pending sells (720-722: AMD/LCID/NXPI) accepted, no_fill, day orders for Aug 27 13:30 UTC open. 97 filled trades all accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). 3 null-strategy trades persistent (703 PLD, 648 NOW, 645 DUK). Swing cap enforcement confirmed (no active bypass). Four schedules verified. Crypto edge-gate fail-closed. Status: HEALTHY (code/deploy), DEGRADED (external: swing over-cap pre-existing + crypto fail-closed + historical run-log gaps + 3 null-strategy trades + EQUITY_DIRECTION_FALLBACK). Paid-plan upgrade approved, not executed. No deploy needed.

## Thursday, August 27, 2026 Control-135 strict read-only production control - HEALTHY/DEGRADED

Control-135 at ~07:00 UTC (Aug 27 09:00 +02). Strict GET-only. All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `772f6ea` (Control-134). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

Live state: 28 positions all strategy=swing, 0 unattributed, 0 null, 0 daytrading, MV $9,310.44 (2.52x $3,700 cap, pre-existing). Equity $98,447.21 (change_today -$77.77, -0.079%), ACTIVE, not blocked. Cash $89,136.78, buying_power $380,919.86, last_equity $98,524.98. broker_ledger_synced_until 2026-08-27T06:51:12Z, last_prune_date 2026-08-27. 100 runs (3766-3866, Aug 26 18:36 - Aug 27 07:01 UTC): 0 errors, 11 CYCLE_LEASE_HELD (historical Aug 26 20:00-21:10 UTC, self-healed), 2 historical reconcile gaps (40.0 + 40.2 min, Aug 26 20:00-21:21 UTC). No new gaps/lease holds since Aug 26 21:21 UTC (9+ hours clean). Triggers: 60 reconcile_cron (ok every 10 min, MAINTENANCE_ONLY, 3 broker orders, 0 failures), 20 crypto_cron (:08/:38 cadence, all fail-closed: CRYPTO_BARS_STALE AVAXUSD ~22h, MATICUSD empty, validTA=0, fee telemetry unavailable asOf Aug 19, no rawEdgeBps producer), 18 daytrading cron (all MARKET_CLOSED), 1 swing_cron (3794, re-tagged all 13 fills). 3 pending sells (720-722: AMD/LCID/NXPI) accepted, no_fill, day orders for Aug 27 13:30 UTC open. Trade 703 (PLD) strategy=null persistent. Swing cap enforcement confirmed (no active bypass, CAPITAL_CAP blocks new entries). swing_cron re-tag stable since Control-126. Four schedules verified (wrangler.toml crons: */5 13-21 * * 1-5, 0 22 * * 1-5, 7-59/30 * * * *, */10 * * * *). Crypto edge-gate wiring confirmed fail-closed (no rawEdgeBps producer). Status: HEALTHY (code/deploy), DEGRADED (external). No deploy needed. Paid-plan upgrade approved, not executed.

## Thursday, August 27, 2026 Control-134 strict read-only production control - HEALTHY/DEGRADED

Control-134 at ~06:00 UTC (Aug 27 08:00 +02). Strict GET-only. All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `64b0aa7` (Control-133). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

Live state: 28 positions all strategy=swing, 0 unattributed, 0 daytrading, MV $9,311.79 (2.52x $3,700 cap, pre-existing). Equity $98,448.57 (change_today -$76.41, -0.078%), ACTIVE, not blocked. Cash $89,136.78, buying_power $380,923.69, last_equity $98,524.98. broker_ledger_synced_until 2026-08-27T05:51:11Z, last_prune_date 2026-08-27. 30 visible runs (3849-3857, 04:51-05:51 UTC): 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps. Triggers: 7 reconcile_cron (ok every 10 min, MAINTENANCE_ONLY, 3 broker orders, 3 pending lookups, 0 failures), 2 crypto_cron (:08/:38 cadence, all fail-closed: CRYPTO_BARS_STALE AVAXUSD/SOLUSD ~22h, MATICUSD empty, validTA=0, fee telemetry unavailable asOf Aug 19, no rawEdgeBps producer). No daytrading or swing runs in window (expected). 3 pending sells (720-722: AMD/LCID/NXPI) accepted, no_fill, day orders for Aug 27 13:30 UTC open. Trade 703 (PLD) strategy=null persistent. Swing cap enforcement confirmed (no active bypass). swing_cron re-tag stable since Control-126. Status: HEALTHY (code/deploy), DEGRADED (external). No deploy needed.

## Thursday, August 27, 2026 Control-131 strict read-only production control - HEALTHY/DEGRADED

Control-131 at ~03:00 UTC (Aug 27 05:00 +02). Strict GET-only. All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `cb51307` (Control-130, in README). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

Live state: 28 positions all strategy=swing, 0 unattributed, MV $9,335.55 (2.52x cap, pre-existing). Equity $98,472.38 (change_today -$52.60, -0.053%), ACTIVE. broker_ledger_synced_until 2026-08-27T02:51:10Z, last_prune_date 2026-08-27. 60 visible runs (3774-3833, 20:40 Aug 26 - 02:51 Aug 27 UTC): 0 errors, 5 CYCLE_LEASE_HELD (historical, self-healed), 1 historical gap (40.2 min). No new gaps/lease holds in 00:xx-02:xx. Crypto :08/:38 fail-closed (CRYPTO_BARS_STALE, validTA=0, fee telemetry stale asOf Aug 19, no rawEdgeBps). 3 pending sells (720-722: AMD/LCID/NXPI) accepted, no_fill, day orders for Aug 27 13:30 UTC open. Trade 703 (PLD) strategy=null persistent. Swing cap enforcement confirmed (no active bypass).

## Thursday, August 27, 2026 Control-130 strict read-only production control - HEALTHY/DEGRADED

Control-130 at ~02:00 UTC (Aug 27 04:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `f5d9965` (Control-129, in README). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged. (Note: Controls 122-129 were documented in README.md only; this entry resumes OPERATIONS.md coverage.)

**Live state:** 28 broker-authoritative positions (all strategy=swing, 0 unattributed, MV $9,321.63 = 2.52x $3,700 cap, pre-existing). Equity $98,458.42 (-$66.56, -0.068%), ACTIVE, not blocked. 60 runs (3766-3825, 18:36-01:51 UTC): 0 errors, 11 CYCLE_LEASE_HELD (historical, self-healed), 1 historical gap (82.8 min). No new gaps/lease holds in 00:xx-01:xx. Reconcile ok every 10 min (3 broker orders, 0 failures). Crypto :08/:38 fail-closed (validTA=0, bars stale, no rawEdgeBps). 3 pending sells (720-722) for Aug 27 13:30 UTC open. Trade 703 strategy=null persistent. Swing cap enforcement confirmed (CAPITAL_CAP blocks new entries). swing_cron re-tag stable since Control-126. Status: HEALTHY (code/deploy), DEGRADED (external). Paid-plan upgrade approved, not executed.

## August 26, 2026 Control-120 strict read-only production control - HEALTHY/DEGRADED
## August 26, 2026 Control-121 strict read-only production control - HEALTHY/DEGRADED

Control-121 at ~18:00 UTC (Aug 26 20:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this Control-121 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `8a47087` (Control-120). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

**Live state:** 28 broker-authoritative positions (source=alpaca, freshness 2026-08-26T18:00:21Z, 0 broker_error): 15 swing (MV $7,817.97) + 13 unattributed (MV $1,450.82) = $9,268.79 (2.50x $3,700 cap, pre-existing fills). Equity $98,405.59 (change_today -$119.39, -0.12%). last_equity $98,524.98. ACTIVE. cash $89,136.78. buying_power $380,811.18. 30 runs (14:41-17:56 UTC): 0 errors, 1 CYCLE_LEASE_HELD (run 3740). Run-log delivery gap: 109-min gap (15:26-17:15 UTC) with ~37 runs missing (Free-tier silent-throw pattern; run 3740 CYCLE_LEASE_HELD confirms prior invocation held lease unreleased). Triggers: 19 daytrading cron (all skipped DAYTRADING_BARS_STALE ~978s vs 900s + EQUITY_DIRECTION_FALLBACK), 9 reconcile_cron (ok every 10 min, MAINTENANCE_ONLY), 2 crypto_cron (15:08/17:38, :07/:37 cadence, all fail-closed: RECONCILIATION_DEFERRED_TO_MAINTENANCE + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT, validTA=0, rawEdgeBps=None, fee telemetry stale asOf Aug 19). BROKER_ONLY_RECONCILED each cycle (13 unattributed, harmless until swing_cron 22:00 UTC re-tags).

**Control-117 fix stable (4th consecutive control):** 13 swing fills (trades 707-719) strategy=unattributed in /api/positions (NOT daytrading). Trades table retains strategy=swing. Bypass stopped. swing_cron 22:00 UTC re-creates D1 position rows with strategy=swing, ending BROKER_ONLY_RECONCILED noise.

**Trades:** 50 trades (670-719). Trades 707-719 filled at 13:30-13:31 UTC with lifecycle timestamps (submitted_at, filled_at, broker_updated_at, last_reconciled_at). All filled accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). Trade 703 (PLD) strategy=null persistent.

**Status:** HEALTHY (code/deploy), DEGRADED (13 unattributed fills pending 22:00 swing_cron re-tag + run-log delivery gap with 1 CYCLE_LEASE_HELD + external limits + trade 703 strategy=null + bar staleness + crypto fail-closed).

Control-120 at ~17:00 UTC (Aug 26 19:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this Control-120 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `bc58886` (Control-119). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 USD unchanged.

**Live state:** 28 broker-authoritative positions: 15 swing (MV $7,800.10, d1 metadata) + 13 unattributed (MV $1,449.34, broker-projected). Total MV $9,249.43 (2.50x $3,700 cap, pre-existing fills). Equity $98,373.26 (change_today -$151.72, -0.154%). last_equity $98,524.98. ACTIVE. cash $89,136.78. buying_power $380,737.49. 50 runs (12:37-15:26 UTC): 0 errors, 0 CYCLE_LEASE_HELD. Triggers: 28 daytrading_cron (all skipped DAYTRADING_BARS_STALE ~977s vs 900s + EQUITY_DIRECTION_FALLBACK), 16 reconcile_cron (ok every 10 min, MAINTENANCE_ONLY), 6 crypto_cron (:07/:37 cadence — 13:07/13:37/14:08/14:38/15:08, all fail-closed: RECONCILIATION_DEFERRED_TO_MAINTENANCE, validTA=0, rawEdgeBps=None, fee telemetry stale asOf Aug 19). BROKER_ONLY_RECONCILED each cycle (13 unattributed, harmless until swing_cron 22:00 UTC re-tags).

**Control-117 fix confirmed stable (3rd consecutive control):** 13 swing fills (trades 707-719) strategy=unattributed in /api/positions (NOT daytrading). Trades table retains strategy=swing. Bypass stopped. swing_cron 22:00 UTC re-creates D1 position rows with strategy=swing, ending BROKER_ONLY_RECONCILED noise.

**Trades:** 100 trades (620-719). Trades 707-719 filled at 13:30-13:31 UTC with lifecycle timestamps (submitted_at, filled_at, broker_updated_at, last_reconciled_at). All filled accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). Trade 703 (PLD) strategy=null persistent (also 648, 645).

**Status:** HEALTHY (code/deploy), DEGRADED (13 unattributed fills pending 22:00 swing_cron re-tag + external limits + run-log gaps + trade 703/648/645 strategy=null + bar staleness + crypto fail-closed).

Control-119 at ~15:30 UTC (Aug 26 17:30 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Code `22b3dba` (unchanged since Control-117). Docs HEAD: this Control-119 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `945277e` (Control-118). 223 tests / 841 assertions, typecheck clean. Version 2.6.0 aligned. Caps 5000/3700/2000 unchanged.

**Live state:** 28 broker-authoritative positions: 15 swing (MV $7,756.39, d1 metadata) + 13 unattributed (MV $1,451.94, broker-projected). Total MV $9,208.32 (2.49x $3,700 cap, pre-existing fills). Equity $98,354.69 (change_today -$170.29, -0.17%). last_equity $98,524.98. ACTIVE. cash $89,136.78. buying_power $380,698.15. broker_ledger_synced_until 2026-08-26T15:21:07Z (fresh). last_prune_date 2026-08-26. 40 runs (13:20-15:26 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 24 daytrading cron (all skipped DAYTRADING_BARS_STALE ~977s vs 900s + EQUITY_DIRECTION_FALLBACK), 12 reconcile_cron (ok every 10 min), 4 crypto_cron (:07/:37 — 13:37/14:08/14:38/15:08, all fail-closed: CRYPTO_BARS_STALE/UNAVAILABLE, CRYPTO_DATA_INSUFFICIENT, validTA=0, rawEdgeBps=None, fee telemetry stale asOf Aug 19). BROKER_ONLY_RECONCILED each cycle (13 unattributed, harmless until swing_cron 22:00 UTC re-tags).

**Control-117 fix confirmed live (stable):** 13 swing fills (trades 707-719) strategy=unattributed in /api/positions (NOT daytrading). Trades table retains strategy=swing. Bypass stopped. swing_cron 22:00 UTC re-creates D1 position rows with strategy=swing.

**Trades:** 100 trades (620-719). All filled accounting_status=filled_lot_exact_unavailable, gross/fee/net=null (conservative). Trades 707-719 filled at 13:30-13:31 UTC with lifecycle timestamps. Trade 703 (PLD) strategy=null persistent (also 648, 645).

**Status:** HEALTHY (code/deploy), DEGRADED (13 unattributed fills pending 22:00 swing_cron re-tag + external limits + run-log gaps + trade 703/648/645 strategy=null + bar staleness).

## August 26, 2026 Control-117 read-only control + swing cap bypass fix (auto-reconcile) + deploy

Control-117 at ~14:00 UTC (Aug 26 16:00 +02). GET-only control found a second swing cap bypass path: the BROKER_ONLY_RECONCILED auto-reconcile imports swing fills without strategy, then the final sync re-tags them as daytrading. Fix deployed (commit 22b3dba): added getSwingTradeSymbols() and swing-trade exclusion in auto-reconcile upsert, final sync upsert, and close logic. 223 tests / 841 assertions, typecheck clean. Code 22b3dba (new). Prior code e89c786. Version 2.6.0 aligned. Caps 5000/3700/2000 unchanged. Two deploys (14:05 and 14:15 UTC). Post-deploy verified: 13 swing fills now unattributed (not daytrading), swing cap bypass stopped. 0 errors, 0 lease holds. The 13 fills (~$1,449) push swing to ~$9,392 (2.54x cap) — pre-existing, swing_cron at 22:00 UTC will see correct exposure. Follow-up: 13 D1 rows need swing_cron re-tag at 22:00 UTC. Status: HEALTHY (code/deploy, fix verified), DEGRADED (mis-tagged fills pending 22:00 + external limits + run-log gaps + trade 703 + bar staleness).

## August 26, 2026 Control-115 strict read-only production control - HEALTHY/DEGRADED

Control-115 at ~13:00 UTC (Aug 26 15:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-115 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `841823b` (Control-114). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0, src/version.ts=2.6.0, package.json=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T13:00:26Z) all strategy=swing, MV $7,898.09 (2.14x $3,700 cap). Equity $98,480.33 (change_today -$44.65, -0.045%), ACTIVE, cash $90,582.24, buying_power $381,232.09. last_equity $98,524.98. 100 runs (00:40-13:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 75 reconcile_cron (ok every 10 min, brokerOrders=8, pendingLookups=8, 0 failures — stable since 08:20 UTC), 24 crypto_cron (:07/:37 perfect, all fail-closed), 1 cron (daytrading 13:00 MARKET_CLOSED). No swing runs (expected). Crypto fail-closed (MATICUSD empty, LINKUSD stale ~22h, SOLUSD stale, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19, EQUITY_DIRECTION_FALLBACK). Trade 703 strategy=null persistent. All 4 trigger filters verified. Trades pagination verified (100 trades, ids 620-719). broker_ledger_synced_until 2026-08-26T13:00:15Z (fresh). last_prune_date 2026-08-26.

**LIVE RISK (URGENT, ~30 min to market open):** 8 pending swing BUYs at broker (stable since 08:20 UTC). If 8 fill at 13:30 UTC market open (~$984 est) → swing ~$8,882 (2.40x $3,700 cap). Cancel requires broker mutation, not performed. Joachim must decide NOW. Control-101 fix (a206690, src/index.ts:1070-1071) deployed, not yet naturally tested — 13:00 daytrading cron was MARKET_CLOSED, first natural test at 13:30 UTC market open.

**Status:** HEALTHY (code/deploy), DEGRADED (pending orders + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-114 strict read-only production control - HEALTHY/DEGRADED

Control-114 at ~12:00 UTC (Aug 26 14:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-114 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `9dc14d3` (Control-113). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0, src/version.ts=2.6.0, package.json=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T12:00:22Z) all strategy=swing (MV $7,924.13, 2.14x cap). Equity $98,506.37 (change_today -$18.61, -0.019%), ACTIVE, cash $90,582.24, buying_power $381,297.07. 60 runs (04:37-12:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 45 reconcile_cron, 15 crypto_cron. Reconciliation ok every 10 min (brokerOrders=8, pendingLookups=8, lookupFailures=0 — stable since Control-111). Crypto :07/:37 fail-closed (MATICUSD empty, ETHUSD/LINKUSD ~22h stale latestBarAt Aug 25 13:00-13:30, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19, EQUITY_DIRECTION_FALLBACK). Swing run 3574 clean (Aug 25 22:01). Trade 703 (PLD) strategy=null persistent. All 4 trigger filters verified. Trades pagination verified. broker_ledger_synced_until 2026-08-26T12:00:14Z. last_prune_date 2026-08-26.

**LIVE RISK (URGENT, stable):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85 est) in D1; 8 remain at broker (unchanged since 08:20 UTC). If 8 fill at Aug 26 13:30 UTC market open (~$984 est) → swing ~$8,908 (2.41x $3,700 cap). Cancel requires broker mutation, not performed. Joachim must decide before 13:30 UTC (~1.5 hours from this control). Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync 13:00 UTC — first natural test, ~1 hour away).

**Status:** HEALTHY (code/deploy), DEGRADED (pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-113 strict read-only production control - HEALTHY/DEGRADED

Control-113 at ~11:00 UTC (Aug 26 13:00 +02). Strict GET-only. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-113 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `30ce8a5` (Control-112). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0, src/version.ts=2.6.0, package.json=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T11:00:22Z) all strategy=swing (MV $7,962.49, 2.15x cap). Equity $98,544.73 (change_today +$19.75, +0.02%), ACTIVE, cash $90,582.24, buying_power $381,392.62. 60 runs (03:37-11:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 45 reconcile_cron, 15 crypto_cron. Reconciliation ok every 10 min (brokerOrders=8, pendingLookups=8, lookupFailures=0 — stable since Control-111). Crypto :07/:37 fail-closed (MATICUSD empty, LINKUSD/SOLUSD ~22h stale latestBarAt Aug 25 12:00-12:30, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19, EQUITY_DIRECTION_FALLBACK). Swing run 3574 clean (Aug 25 22:01). Trade 703 (PLD) strategy=null persistent. All 4 trigger filters verified. Trades pagination verified. broker_ledger_synced_until 2026-08-26T11:00:14Z. last_prune_date 2026-08-26.

**LIVE RISK (URGENT, stable):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85 est) in D1; 8 remain at broker (unchanged since 08:20 UTC). If 8 fill at Aug 26 13:30 UTC market open (~$984 est) → swing ~$8,946 (2.42x $3,700 cap). Cancel requires broker mutation, not performed. Joachim must decide before 13:30 UTC (~2.5 hours from this control). Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync 13:00 UTC — first natural test, ~2 hours away).

**Status:** HEALTHY (code/deploy), DEGRADED (pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-112 strict read-only production control - HEALTHY/DEGRADED

Control-112 at ~10:00 UTC (Aug 26 12:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-112 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `79dbac6` (Control-111 commit). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0, src/version.ts=2.6.0, package.json=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T10:00:18Z) all strategy=swing (MV $7,974.99, 2.15x cap). Equity $98,557.23 (change_today +$32.25, +0.033%), ACTIVE, cash $90,582.24, buying_power $381,418.59. 60 runs (01:07-10:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 45 reconcile_cron, 15 crypto_cron. Reconciliation ok every 10 min (brokerOrders=8, pendingLookups=8, lookupFailures=0 — stable since Control-111). Crypto :07/:37 fail-closed (MATICUSD empty, ETHUSD ~22h stale latestBarAt Aug 25 11:30, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19, EQUITY_DIRECTION_FALLBACK). Swing run 3574 clean (Aug 25 22:01). Trade 703 (PLD) strategy=null persistent. All 4 trigger filters verified. Trades pagination verified.

**LIVE RISK (URGENT, stable):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85 est) in D1; 8 remain at broker (unchanged since 08:20 UTC). If 8 fill at Aug 26 13:30 UTC market open (~$984 est) → swing ~$8,959 (2.42x $3,700 cap). Cancel requires broker mutation, not performed. Joachim must decide before 13:30 UTC (~3.5 hours from this control). Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync 13:00 UTC — first natural test).

**Status:** HEALTHY (code/deploy), DEGRADED (pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-111 strict read-only production control - HEALTHY/DEGRADED

Control-111 at ~09:00 UTC (Aug 26 11:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-111 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `f59fabb` (Control-110 commit). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0, src/version.ts=2.6.0, package.json=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T09:00:19Z) all strategy=swing (MV $7,961.68, 2.15x cap). Equity $98,543.92 (change_today +$18.94, +0.019%), ACTIVE, cash $90,582.24, buying_power $381,389.27. 60 runs (00:37-09:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 45 reconcile_cron, 15 crypto_cron. Reconciliation ok every 10 min. Crypto :07/:37 fail-closed (MATICUSD empty, ETHUSD/LINKUSD ~22h stale, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19). Swing run 3574 clean (Aug 25 22:01). Trade 703 (PLD) strategy=null persistent. All 4 trigger filters verified. Trades pagination verified (offset distinct).

**NEW FINDING:** brokerOrders dropped from 12 (Control-110, 08:10 UTC) to 8 (as of 08:20 UTC). The 4 "new"-status orders (INTC, WMT, BAC, WFC) re-acknowledged at 08:00 UTC have dropped from broker. 8 "accepted" orders remain. pendingLookups=8, lookupFailures=0.

**LIVE RISK (URGENT, decreasing):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85 est) in D1; 8 remain at broker. If 8 fill at Aug 26 13:30 UTC market open (~$984 est) → swing ~$8,946 (2.42x $3,700 cap). Cancel requires broker mutation, not performed. Joachim must decide before 13:30 UTC. Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync 13:00 UTC).

**Status:** HEALTHY (code/deploy), DEGRADED (pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-110 strict read-only production control - HEALTHY/DEGRADED

Control-110 at ~08:00 UTC (Aug 26 10:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** this Control-110 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `f65ef7a` (Control-109 commit). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T08:00:26Z) all strategy=swing (MV $7,946.07, 2.15x cap). Equity $98,528.31 (intraday +$3.33 vs last_equity $98,524.98), ACTIVE, cash $90,582.24, buying_power $381,346.34. 60 runs (00:37-08:00 UTC): 0 errors, 0 CYCLE_LEASE_HELD. 45 reconcile_cron, 15 crypto_cron (no daytrading — market closed; no swing — next 22:00 UTC). Reconciliation ok every 10 min. At 08:00 UTC, brokerOrders rose from 8 to 12 — 4 pending_new orders (INTC, WMT, BAC, WFC) re-acknowledged by broker with submitted_at updated to 08:00 UTC (queued for today's session). pendingLookups=8, lookupFailures=0. Crypto :07/:37 fail-closed (MATICUSD empty, AVAXUSD/BTCUSD ~22h stale latestBarAt Aug 25 09:00-09:30, validTA=0, no rawEdgeBps, fee telemetry stale asOf Aug 19). Swing run 3574 clean (Aug 25 22:01, errors=0, 13 decisions, 0 trades). Daytrading last run Aug 25 21:56 UTC (historical CYCLE_LEASE_HELD streaks 20:40-21:20 UTC Aug 25 persist in history; all Aug 26 clean). Trade 703 (PLD) strategy=null persistent. All 4 trigger filters verified (reconcile_cron, crypto_cron, daytrading_cron, swing_cron).

**LIVE RISK (URGENT):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85 est) remain at broker. 4 pending_new orders (INTC, WMT, BAC, WFC) re-acknowledged at 08:00 UTC today — brokerOrders now 12 (up from 8). Could fill at Aug 26 13:30 UTC market open → swing ~$9,396 (2.54x $3,700 cap). Cancel requires broker mutation, not performed during read-only control. Joachim must decide before 13:30 UTC. Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync Aug 26 13:00 UTC).

**Status:** HEALTHY (code/deploy), DEGRADED (13 pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-109 strict read-only production control - HEALTHY/DEGRADED

Control-109 at ~07:00 UTC (Aug 26 09:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only (corrected HEAD off-by-one from Control-108 fixup commit `deda553`).

**Current HEAD:** this Control-109 commit (local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `deda553` (Control-108 fixup — docs in `412052b` referenced `412052b`, but `deda553` was actual HEAD; off-by-one corrected). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions all strategy=swing (MV $7,949.04, 2.15x cap). Equity $98,531.34 (+0.147%), ACTIVE. 60 runs: 0 errors, 0 CYCLE_LEASE_HELD. Reconciliation ok every 10 min (8 broker orders, 0 failures). Crypto :07/:37 fail-closed (MATICUSD empty, SOLUSD ~22h stale, no rawEdgeBps, fee telemetry stale). All 4 trigger filters verified. Trade 703 strategy=null persistent. 13 pending swing BUYs URGENT (8 open at broker, could fill 13:30 UTC). Control-101 fix not yet naturally tested (next daytrading sync 13:00 UTC). Status: HEALTHY (code/deploy), DEGRADED (pending orders + external limits + run-log gaps + trade 703 strategy=null).

## August 26, 2026 Control-108 strict read-only production control - HEALTHY/DEGRADED

Control-108 at ~06:00 UTC (Aug 26 08:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** `412052b` (this commit, local only — push BLOCKED: github_pat not in vault). Prior docs HEAD `3b6eb4f` (Control-107's actual commit — Control-107 entry referenced `837259e`, off-by-one corrected here). Code `e89c786` (unchanged since Control-104). 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T06:00:24Z), all strategy=swing, MV $7,948.98. Equity $98,531.28 (+0.147%), ACTIVE, cash $90,582.30, buying_power $381,361.99. Reconciliation ok every 10 min (runs 3629-3637, brokerOrders=8, pendingLookups=8, 0 errors, watermark holding, broker_ledger_synced_until 2026-08-26T06:00:14Z, last_prune_date 2026-08-26). Crypto :07/:37 fail-closed (ETHUSD/LINKUSD ~22h stale, MATICUSD empty, no rawEdgeBps, validTA=0, fee telemetry stale Aug 19). Daytrading MARKET_CLOSED (last Aug 25 21:56, next 13:00 UTC). Swing run 3574 clean (Aug 25 22:01, errors=0). 60 runs in window: 0 errors, 0 CYCLE_LEASE_HELD. Filtered run observability confirmed (all four trigger filters correct).

**URGENT:** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85, broker reports 8 open). Could fill at Aug 26 13:30 UTC → swing ~$8,841 (2.39x $3,700 cap). Joachim notified. Cancel requires broker mutation, not performed during read-only control. Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed, not yet naturally tested (next daytrading sync Aug 26 13:00 UTC). Trade 703 (PLD) strategy=null persistent (also 648 NOW, 645 DUK). Run-log gaps: historical Aug 25 only, all Aug 26 clean. github_pat still missing, push blocked.

## August 26, 2026 Control-107 strict read-only production control - HEALTHY/DEGRADED

Control-107 at ~05:00 UTC (Aug 26 07:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** `837259e` (docs, local only — push BLOCKED: github_pat not in vault), code `e89c786`. 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T05:00:21Z), all strategy=swing, MV $7,948.83. Equity $98,531.13 (+0.147%), ACTIVE, cash $90,582.30. Reconciliation ok every 10 min (43 runs, 0 errors, watermark holding, broker_ledger_synced_until 2026-08-26T05:00:14Z). Crypto :07/:37 fail-closed (SOLUSD/AVAXUSD ~22h stale, MATICUSD empty, no rawEdgeBps, validTA=0). Daytrading market closed (next 13:30 UTC). Swing run 3574 clean (Aug 25 22:01, errors=0). Control-101 fix deployed, not yet naturally tested (next daytrading sync Aug 26 13:00 UTC).

**LIVE RISK (URGENT):** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,449.85, broker reports 8 open — 5 appear expired but D1 still shows accepted). 8 open could fill at Aug 26 13:30 UTC → swing ~$8,841 (2.39x cap). Joachim must decide before market open.

**Run-log gaps:** Daytrading ~12 missing (19:35-21:35 UTC Aug 25), crypto 13 missing (14:07-21:07 UTC Aug 25). All Aug 26 runs clean. No CYCLE_LEASE_HELD streaks in latest 60. Trade 703 strategy=null persistent.

**Status: HEALTHY (code/deployment), DEGRADED (13 pending swing BUYs + external limits + run-log gaps + trade 703 strategy=null).** Remaining: pending BUYs decision (URGENT), rawEdgeBps producer, bar freshness, D1 Sep 1 limits, paid-plan upgrade, trade 703, run-log gaps, github_pat restoration.

---

## August 26, 2026 Control-106 strict read-only production control - HEALTHY/DEGRADED

Control-106 at ~04:00 UTC (Aug 26 06:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only (HEAD reference corrected from `ee86de9` to actual `4c9df61`; prior Control-105 entry self-referenced the previous HEAD).

**Current HEAD:** `4c9df61` (docs, pushed to origin), code `e89c786`. 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** Equity $98,535.53 (+0.15%), ACTIVE, cash $90,582.30, buying_power $381,371.48. 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T04:00:16Z), all strategy=swing, MV $7,953.23 (over $3,700 cap from Control-101 cap bypass defect). Reconciliation ok every 10 min (runs 3613-3621, broker_ledger_synced_until 2026-08-26T04:00:14Z, brokerOrders=8, pendingLookups=8, 0 errors, last_prune_date 2026-08-26). Crypto :07/:37 fail-closed (SOLUSD/ETHUSD ~22h stale, MATICUSD empty, no rawEdgeBps, validTA=0, fee telemetry stale Aug 19). Daytrading MARKET_CLOSED (next 13:30 UTC). Swing run 3574 clean (Aug 25 22:01, errors=0, 40s, placed 13 BUYs under pre-fix defect). Filtered run observability confirmed (crypto_cron, reconcile_cron filters correct). Fee accounting conservative (filled trades 704-706 filled_lot_exact_unavailable, gross/fee/net=null). Control-101 fix (a206690, swingOwnedSymbols at src/index.ts:1070-1071) deployed but not yet naturally tested by daytrading sync (next Aug 26 13:00 UTC).

**LIVE RISK (URGENT):** 13 pending swing BUYs (trades 707-719: INTC, WMT, RTX, AVGO, GE, TXN, BA, BAC, XOM, SNOW, CVX, WFC, C, day-TIF, ~$1,449.85 est, broker reports 8 open — 5 may have expired). Could fill at Aug 26 13:30 UTC → swing ~$9,400 (2.5x cap). Joachim must decide before market open. **Status: HEALTHY (code/deploy), DEGRADED (pending orders + external limits + run-log gaps + trade 703 strategy=null).**

---

## August 26, 2026 Control-105 strict read-only production control - HEALTHY/DEGRADED

Control-105 at ~03:00 UTC (Aug 26 05:00 +02). Strict GET-only production control. All six endpoints HTTP 200. No code defect; no deploy required. Docs update only.

**Current HEAD:** `ee86de9` (docs, pushed to origin), code `e89c786`. 220 tests / 822 assertions, typecheck genuinely clean. Version surfaces aligned: /health=2.6.0, release_version=2.6.0, config.version=2.6.0. Caps 5000/3700/2000 unchanged.

**Live state:** Equity $98,525.30 (+0.14%), ACTIVE, cash $90,582.30. 15 broker-authoritative positions (source=alpaca), all strategy=swing, MV $7,943 (over $3,700 cap from Control-101 cap bypass defect). Reconciliation ok every 10 min (broker_ledger_synced_until 2026-08-26T03:00:15Z, brokerOrders=8, 0 errors). Crypto :07/:37 fail-closed (AVAXUSD ~22h stale, MATICUSD empty, no rawEdgeBps, validTA=0). Daytrading MARKET_CLOSED (next 13:30 UTC). Swing run 3574 clean (Aug 25 22:01, errors=0, 40s). Filtered run observability working. Fee accounting conservative.

**LIVE RISK:** 13 pending swing BUYs (trades 707-719, day-TIF, ~$1,450 est, broker reports 8 open). Could fill at Aug 26 13:30 UTC → swing ~$9,393 (2.5x cap). Joachim must decide before market open. **Status: HEALTHY (code/deploy), DEGRADED (pending orders + external limits + run-log gaps + trade 703 strategy=null).**

---

## August 26, 2026 Control-104 _trigger regression fix (DEPLOYED)

Control-104 at ~02:00 UTC (Aug 26 04:00 +02). Strict GET-only production control found a typecheck regression from Control-103 (c783650): `runSwingCycle` signature renamed `trigger` → `_trigger` but body still references `trigger` (5 TS2552 errors, runtime ReferenceError). Fix: reverted to `trigger` (commit `e89c786`). Deployed via direct Cloudflare API PUT (deployment_id `0173e8ceb625485498b4794e098668a7`). Post-deploy GET: all 6 endpoints HTTP 200, version 2.6.0 aligned, caps 5000/3700/2000 intact. 220 tests / 822 assertions, typecheck clean. No caps or trading behavior changed.

**Current HEAD:** `e89c786` (code+docs). **Live state:** Equity $98,521, ACTIVE, 15 broker-authoritative positions all strategy=swing (MV $7,939), cash $90,582. Reconciliation ok every 10 min. **LIVE RISK:** 12 pending swing BUYs (trades 708-719, day-TIF, ~$1,362) could fill at Aug 26 09:30 ET → swing ~$9,311 (2.5x cap). Joachim must decide before market open. **Status: HEALTHY (code/deploy), DEGRADED (pending orders + external limits + run-log gaps).**

---

## August 26, 2026 Control-103 typecheck fixes + brief 1.0.0 regression + 2.6.0 restoration (DEPLOYED)

Control-103 at ~01:00 UTC (Aug 26 03:00 +02). Found and fixed 12 pre-existing typecheck errors across 6 source files. Prior "typecheck clean" claims in Controls 88-102 were inaccurate; `tsc --noEmit` was never actually clean.

**Typecheck fixes (reliability-only, no caps or trading behavior changes):**
- Removed duplicate `isTradingHalted()` in `swing-risk.ts` (dead code since v2.2.0)
- Removed unused imports: `TAIndicators` (ai-decision.ts, crypto-strategy.ts), `SwingConfig` (swing-strategy.ts)
- Prefixed unused params: `_trigger` (swing-strategy.ts, crypto-strategy.ts), `_allScores` (swing-risk.ts)
- Added `marketRegime?: string` to `AIRefinementConfig` interface (was passed but untyped)
- Fixed bare `reasoning` ReferenceError in `parseLLMResponse` (would throw, silently fall through)
- Added `Position` type annotation to `livePositions` in `api.ts`
- Added `TASignal` import to `index.ts`
- Fixed `undefined`-to-`null` type mismatch with `?? null` (crypto-strategy.ts stop/take profit)
- Restored `docs/OPERATIONS.md` and `docs/DEPLOYMENT_RUNBOOK.md` (missing from disk, only .tmp files remained)

**1.0.0 regression incident:** The initial deploy bundled from `/workspace` (which has an old `wrangler.toml` + `package.json` at version 1.0.0) instead of `/workspace/alpaca-trading-bot`. Wrangler picked up the wrong source tree. The 1.0.0 worker was live for ~3 minutes (01:22-01:25 UTC). Detected immediately via post-deploy GET verification. Re-bundled from the correct directory (315.93 KiB vs 140.32 KiB) and re-deployed. 2.6.0 restored and verified.

**Deploy:** Two direct Cloudflare API PUTs. First (bad): HTTP 200 but version 1.0.0. Second (correct): HTTP 200, version 2.6.0. Bundle verified to contain `RELEASE_VERSION="2.6.0"`, `swingOwnedSymbols` fix, `release_version` in config response, `freshness` in positions/dashboard.

**Post-deploy GET verification (01:25 UTC):** All six endpoints HTTP 200. /health=2.6.0, release_version=2.6.0, config.version=2.6.0 aligned. Caps 5000/3700/2000 intact. 15 broker-authoritative positions (source=alpaca, observed 2026-08-26T01:24:19Z), all strategy=swing, MV $7,940.74. Equity $98,523.04, ACTIVE, cash $90,582.30. Reconciliation ok every 10 min (run 3600, err=0). Crypto :07/:37 fail-closed (LINKUSD stale, MATICUSD empty, no rawEdgeBps). Swing run 3574 clean (errors=0, 40s). Daytrading MARKET_CLOSED. Filtered run observability working.

**Validation:** 220 tests / 822 assertions pass. Typecheck clean (first time genuinely). HEAD `1c0dc00` (code+docs), deployed bundle verified.

**12 pending swing BUYs (LIVE RISK unchanged):** Trades 708-719, all accepted, day-TIF, ~$1,362 est. Could fill at Aug 26 09:30 ET (13:30 UTC). Cancel requires broker mutation. Joachim must decide before market open.

**Status: HEALTHY** (code/deployment, 2.6.0 restored). **DEGRADED** (12 pending swing BUYs + external limits + run-log gaps + 1.0.0 regression incident).

## August 26, 2026 Control-102 strict read-only production control - HEALTHY/DEGRADED

Control-102 at ~00:00 UTC (Aug 26 02:00 +02). All six endpoints HTTP 200. No code defect found; no deploy required. Docs update only.

**Version:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0 aligned. HEAD `58a6ce3` (docs), code `a206690`. 220 tests / 822 assertions. ✓

**Live state:** Equity $98,531.26 (+0.147%), ACTIVE, cash $90,582.30, long_market_value $7,948.96. 15 broker-authoritative positions (`source=alpaca`), all strategy=swing (MV $7,949, over $3,700 cap — positions placed under now-fixed cap bypass defect). Reconciliation every 10 min (3589-3580 ok, err=0, broker_ledger_synced_until 2026-08-26T00:00:14Z, last_prune_date 2026-08-26). Caps unchanged 5000/3700/2000.

**Control-101 fix verified in source:** `src/index.ts:1070-1071` — `swingOwnedSymbols` set excludes swing-tagged D1 positions from daytrading final sync. Deployed as `a206690`. Not yet naturally tested by daytrading sync (next Aug 26 13:00 UTC).

**12 pending swing BUYs (LIVE RISK):** Trades 708-719, all accepted, day-TIF, filled_qty=0, ~$1,362 est. Could fill at Aug 26 09:30 ET market open → swing ~$9,311 (2.5x cap). Cancel requires broker mutation. Joachim must decide before market open.

**Schedules:** Daytrading MARKET_CLOSED (3565-3572). Swing 3574 clean (errors=0, 40s). Crypto :07/:37 fail-closed (LINKUSD ~22h stale, MATICUSD empty, no rawEdgeBps). Reconcile every 10 min. ✓

**Run-log delivery gaps:** Missing crypto runs 14:08-17:08 (7) and 19:08-21:08 (5), missing reconcile at 23:10. CYCLE_LEASE_HELD streaks 20:15-21:20 (cleared 21:36). External resource constraint (Cloudflare Free tier). Trade 703 strategy=null persistent. Paid-plan upgrade remains remedy.

**Status: HEALTHY** (code/deployment). **DEGRADED** (12 pending swing BUYs + external limits + run-log gaps).

## August 26, 2026 Control-101 strict read-only control + swing cap bypass fix (DEPLOYED)

Control-101 found and fixed a critical swing cap bypass defect. The daytrading final sync (step 12, index.ts ~line 1064) was re-attributing swing-owned positions to `strategy='daytrading'`, causing the swing cap check to see $0 exposure on the next swing_cron run and bypass the `swing_max_capital_usd` $3,700 cap. Swing run 3574 (Aug 25 22:01 UTC) placed 12 new BUY orders ($1,450 est) under this defect.

**Fix deployed:** Commit `a206690` — daytrading final sync excludes swing-tagged D1 positions via `swingOwnedSymbols` set. 220 tests / 822 assertions, typecheck clean. Deployed via direct Cloudflare API PUT (HTTP 200). Post-deploy GET: /health=2.6.0, config.version=2.6.0, all endpoints HTTP 200.

**Live risk (Joachim decision needed):** 12 pending swing BUY orders (trades 708-719, day-TIF, accepted) could fill at Aug 26 09:30 ET market open, pushing swing exposure to ~$9,400 (2.5x cap). Cancel is a broker mutation, not performed during read-only control.

**Version:** /health=2.6.0, release_version=2.6.0, config.version=2.6.0 aligned. HEAD `a206690`, code `a206690`. Caps unchanged 5000/3700/2000. Status: HEALTHY (code/deployment), DEGRADED (pending orders + external resource/data-feed + run-log gaps).

## August 25, 2026 Control-100 strict read-only production control - HEALTHY/DEGRADED (swing milestone)

Control-100 was a strict GET-only production control at ~22:00 UTC (Aug 26 00:00 +02). All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required. Documentation update only (HEAD reference corrected to current commit `2936167`).

Version identity all aligned: `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Local HEAD `2936167` (docs), code commits `62ff44f`+`6876a92`+`22e962f`. Live Worker matches local source. Deploy path remains direct Cloudflare API PUT (Wrangler silently exits without uploading).

Live state: equity $98,539.05 (+0.155%), ACTIVE, cash $90,582.30, long_market_value $7,956.75. 15 broker-authoritative positions all daytrading (`source=alpaca`, `positionsAvailable=true`), swing exposure 0. Reconciliation every 10 min (3573/3570/3567 ok, err=0, MAINTENANCE_ONLY, ledgerActivities=0, not truncated; broker_ledger_synced_until 2026-08-25T21:51:03Z, last_prune_date 2026-08-25). Caps unchanged 5000/3700/2000. Daytrading runs 3565-3572 all MARKET_CLOSED (nextOpen 2026-08-26T09:30:00-04:00). Equity-direction fallback confirmed (change_today_pct 0.1549% via equity_delta_fallback). Crypto :07/:37 fail-closed (run 3566 at 21:38, RECONCILIATION_DEFERRED_TO_MAINTENANCE + CRYPTO_BARS_STALE LINKUSD ~22h + CRYPTO_BARS_UNAVAILABLE MATICUSD empty + CRYPTO_DATA_INSUFFICIENT validTA=0, no rawEdgeBps producer). Trades 701-706 filled, `filled_lot_exact_unavailable`, gross/fee/net=null; trade 703 strategy=null persistent. 220 tests / 822 assertions, typecheck clean.

MILESTONE: Swing run 3574 at 22:01:33 UTC completed successfully — trigger=`swing_cron`, status=`skipped`, errors=0, duration=40311ms. First natural swing run on deployed 2.6.0 code and first clean swing run ever. Previous swing runs all errored from subrequest exhaustion (3409=4 err, 3182=8 err, 2200=1 err, 1395=2 err). The Control-90 D1 optimizations reduced subrequest count enough for swing to complete within the free-tier budget. This resolves the natural swing proof thread. Skip reasons: RECONCILIATION_DEFERRED_TO_MAINTENANCE, EQUITY_DIRECTION_FALLBACK, BROKER_AUTHORITATIVE_SYNC_ABSENT (stale swing rows absent from broker, correct), HELD_POSITION.

Operational findings (not code defects): (1) CYCLE_LEASE_HELD streaks 20:20-21:20 UTC in daytrading and maintenance lanes (runs 3557-3564), same silent-throw-before-lease-release pattern; cleared by 21:36; after market close, no trading impact. (2) Run-log delivery gaps: missing crypto runs 13:38-17:38 UTC (~8 missing). (3) Trade 703 strategy=null persistent gap. Root cause is external resource constraint (Cloudflare Workers Free subrequest/CPU limits). Paid-plan upgrade remains the remedy. Filtered run observability confirmed: `/api/runs?strategy=swing` returns 11 runs, `/api/runs?strategy=crypto` returns 30 runs, all correct.

Status: HEALTHY (code/deployment), DEGRADED (external data-feed/resource + run-log delivery gaps + CYCLE_LEASE_HELD streaks). Swing subrequest exhaustion resolved by D1 optimizations.

## August 25, 2026 Control-99 strict read-only production control - HEALTHY/DEGRADED

Live state: equity $98,540.21 (+0.156%), ACTIVE, cash $90,582.30, long_market_value $7,957.91. 15 broker-authoritative positions all daytrading (`source=alpaca`, `positionsAvailable=true`), swing exposure 0. Reconciliation every 10 min (3549/3546/3542/3540/3536/3532/3529/3526 ok, err=0; 3554/3557 CYCLE_LEASE_HELD). Caps unchanged 5000/3700/2000. Daytrading skipped on DAYTRADING_BARS_STALE + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 fail-closed (3507/3517/3525/3535/3545, RECONCILIATION_DEFERRED_TO_MAINTENANCE + CRYPTO_BARS_STALE + no rawEdgeBps). Trades 701-706 filled, `filled_lot_exact_unavailable`, gross/fee/net=null; trade 703 strategy=null persistent. 220 tests / 822 assertions, typecheck clean.

New finding: run-log delivery gaps in 19:00-21:00 UTC window are more extensive than Control-98 noted. Missing daytrading cron runs (unlogged) interleaved with CYCLE_LEASE_HELD streaks (3551/3555/3556 daytrading, 3554/3557 maintenance). Confirms "silent throw before lease release": a cron invocation hits Cloudflare free-tier subrequest/CPU limits, throws before logging or releasing lease, subsequent invocations log CYCLE_LEASE_HELD until 10-min TTL, then repeat. Run 3524 (17:36 UTC) caught explicit "Too many subrequests"; others threw silently. Daytrading lease is strategy-isolated so tonight's 22:00 UTC swing run unaffected. External resource constraint, not code defect. Paid-plan upgrade is the remedy.

Status: HEALTHY (code/deployment), DEGRADED (external data-feed/resource + run-log delivery gaps + CYCLE_LEASE_HELD streaks).

## August 25, 2026 Control-98 strict read-only production control - HEALTHY/DEGRADED

Control-98 was a strict GET-only production control at ~20:00 UTC (22:00 +02). All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required — documentation update only (HEAD reference corrected to current commit `c3801a2`).

Version identity all aligned: `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Local HEAD `c3801a2` (docs), code commits `62ff44f`+`6876a92`+`22e962f`. Live Worker matches local source. Deploy path remains direct Cloudflare API PUT (Wrangler silently exits without uploading).

Live state: equity $98,524.84 (+0.140%), ACTIVE, cash $90,582.30. 15 broker-authoritative positions all daytrading (MV $7,942.54), swing exposure 0. Reconciliation ok every 10 min (MAINTENANCE_ONLY, 0 errors, watermark holding, `broker_ledger_synced_until` 2026-08-25T18:51:04Z, `last_prune_date` 2026-08-25, not truncated). Caps unchanged 5000/3700/2000. Daytrading skipped on DAYTRADING_BARS_STALE (~974s vs 900s) + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 cadence confirmed (runs 3497/3501/3507/3517/3525/3535/3545), all fail-closed (CRYPTO_BARS_STALE + CRYPTO_BARS_UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT, no rawEdgeBps producer). Trades 701/702/703 filled, `filled_lot_exact_unavailable`, gross/fee/net=null (conservative); trade 703 strategy=null persistent. 220 tests / 822 assertions, typecheck clean.

New findings (operational, not code defects): (1) Run-log delivery gaps — ID-contiguous time gaps with zero rows logged: run 3523 (13:56 UTC) → 3524 (17:36 UTC) ≈ 3h40m, and run 3550 (18:51 UTC) → 3551 (19:20 UTC) ≈ 29m. No run_log rows (not even errors) were written despite the cron schedule. Cause not determinable from GET-only data (Cloudflare cron non-invocation or Worker pre-log throw). Observability blind spot; consider a pre-log heartbeat or cron-delivery alert. (2) Run 3524 (17:36 UTC) subrequest exhaustion in daytrading lane (`market_open=0`): `status=error`, "Too many subrequests by single Worker invocation." No recurrence after (runs 3525-3555 all `errors=0` where logged). Isolated. (3) CYCLE_LEASE_HELD streaks at 18:16/18:20 and 19:20-19:50; lease TTL is 10 min so auto-expires; strategy-specific lease so tonight's swing run unaffected; no trading impact; monitor.

Status: HEALTHY (code/deployment), DEGRADED (external data-feed/resource + run-log delivery observability gaps). No deploy required. Tonight 22:00 UTC = first natural swing run on 2.6.0 with empty swing book.

## August 25, 2026 Control-97 strict read-only production control - HEALTHY/DEGRADED

Control-97 was a strict GET-only production control at ~19:00 UTC (21:00 +02). All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required — documentation update only (HEAD reference corrected to current commit `60db928`).

Version identity all aligned: `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Local HEAD `60db928` (docs), code commits `62ff44f`+`6876a92`+`22e962f`. Live Worker matches local source. Deploy path remains direct Cloudflare API PUT (Wrangler silently exits without uploading).

Live state: ACTIVE, equity $98,524.19, cash $90,582.30, 15 broker-authoritative positions all daytrading (MV $7,941.89), swing exposure 0, +0.140% (equity-direction fallback). Caps $5,000/$3,700/$2,000 unchanged. Reconcile_cron ok every 10 min (ledgerActivities 0, 0 errors, watermark holding, not truncated). Daytrading skipped on DAYTRADING_BARS_STALE (~974s vs 900s) + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 skipped (LINKUSD ~22h stale, MATICUSD empty, CRYPTO_DATA_INSUFFICIENT, fail-closed no-rawEdgeBps).

Subrequest-budget follow-up: no recurrence of run 3524 (17:36 UTC) transient exhaustion. Runs 3543-3550 (18:31-18:51) all errors=0. Event remains isolated. Monitor continues.

Status: HEALTHY (code/deployment), DEGRADED (external data-feed/resource). 220 tests / 822 assertions, typecheck clean. Remaining: swing run tonight 22:00 UTC, rawEdgeBps producer, crypto/daytrading bar freshness, D1 Sep 1 enforcement monitoring, paid-plan upgrade decision, trade 703 strategy=null gap.

## August 25, 2026 Control-96 strict read-only production control - HEALTHY/DEGRADED

Control-96 was a strict GET-only production control at ~18:00 UTC (20:00 +02). All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required — documentation update only (HEAD reference corrected to current commit `c39ba36`).

Version identity all aligned: `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Local HEAD `c39ba36` (docs), code commits `62ff44f`+`6876a92`+`22e962f`. Live Worker matches local source. Deploy path remains direct Cloudflare API PUT (Wrangler silently exits without uploading).

Live state: ACTIVE, equity $98,529.85, cash $90,582.30, 15 broker-authoritative positions all daytrading (MV $7,947.55), swing exposure 0, +0.146% (equity-direction fallback). NCLH sold (trade 706). Caps $5,000/$3,700/$2,000 unchanged. Reconcile_cron ok every 10 min (ledgerActivities 5, 0 errors, watermark holding). Daytrading skipped on DAYTRADING_BARS_STALE + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 skipped (BTCUSD ~22h stale, MATICUSD empty, CRYPTO_DATA_INSUFFICIENT, fail-closed no-rawEdgeBps).

New finding: run 3524 (17:36 UTC cron) hit a transient free-tier subrequest exhaustion (errors=1, same class as the swing lane). Isolated — NCLH sell still filled, runs 3525-3533 clean (freshest 3533 at 18:01 err=0). No code defect; external resource constraint, consistent with DEGRADED classification. Monitor for recurrence.

Status: HEALTHY (code/deployment), DEGRADED (external data-feed/resource). 220 tests / 822 assertions, typecheck clean. Remaining: swing run tonight 22:00 UTC, rawEdgeBps producer, crypto/daytrading bar freshness, D1 Sep 1 enforcement monitoring, paid-plan upgrade decision, trade 703 strategy=null gap.

## August 25, 2026 Control-95 strict read-only production control - HEALTHY/DEGRADED

Control-95 was a strict GET-only production control at ~17:00 UTC (19:00 +02). All six endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called. No code defect found; no deploy required — documentation update only (HEAD reference corrected to current commit `a0ae8c3`).

Version identity all aligned: `/health`=2.6.0, `release_version`=2.6.0, `config.version`=2.6.0. Local HEAD `a0ae8c3` (docs), code commits `62ff44f`+`6876a92`+`22e962f`. Live Worker matches local source. Deploy path remains direct Cloudflare API PUT (Wrangler silently exits without uploading).

Equity $98,565.58 (+0.18%), ACTIVE, caps $5,000/$3,700/$2,000 unchanged. 16 broker-authoritative positions all daytrading (MV $8,286.65), swing exposure zero. Reconciliation healthy (run 3521 at 13:51, ledgerActivities=5; run 3518 at 13:41, ledgerActivities=8; 0 errors, watermark holding, not truncated). Daytrading skipping on stale bars (~973s vs 900s, SQ unavailable) + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 cadence confirmed (runs 3517/3507/3501/3497/3493), all skipped: RECONCILIATION_DEFERRED_TO_MAINTENANCE, ETHUSD ~22h stale, MATICUSD empty, fail-closed. Trades 704/705 filled 13:36 UTC; 701/702/703 filled 13:30 UTC; 703 strategy=null (persistent gap). Trade lifecycle and accounting confirmed conservative (gross/fee/net null, filled_lot_exact_unavailable). Crypto edge-gate wiring confirmed in source (`prepareCryptoRiskDecision`, `Number.isFinite(rawEdgeBps)`, `requireCalibratedEdge: true`, fail-closed). 220 tests / 822 assertions, typecheck clean.

**Status: HEALTHY (code/deployment), DEGRADED (external data-feed).** Remaining follow-ups: natural swing run tonight 22:00 UTC, rawEdgeBps producer, crypto/daytrading bar freshness, D1 plan upgrade, Sep 1 free-tier monitoring, trade 703 strategy attribution gap.

## August 25, 2026 Control-94 strict read-only production control - HEALTHY/DEGRADED

Control-94 ran a strict GET-only production control at ~16:00 UTC. All six endpoints HTTP 200. No code defect found; no deploy required. HEAD reference corrected to `cc66ca2` (docs-only change). Version identity all aligned at 2.6.0 (health, release_version, config.version). Code commits `62ff44f`+`6876a92`+`22e962f`.

Live state: equity $98,544.19 (+0.16% via equity-direction fallback), ACTIVE, cash $90,278.93. 16 broker-authoritative positions all daytrading (MV $8,265.26), swing exposure zero. Caps $5,000/$3,700/$2,000 unchanged. Reconciliation ok every 10 min (ledgerActivities 5-8, 0 errors, watermark holding). Daytrading skipped on DAYTRADING_BARS_STALE (~973s vs 900s) + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 skipped (ETHUSD ~22h stale, MATICUSD empty, CRYPTO_DATA_INSUFFICIENT validTA=0, fail-closed no rawEdgeBps). Trades 704/705 filled 13:36 UTC today. Trade 703 strategy=null persists. Accounting conservative (filled_lot_exact_unavailable, gross/fee/net null). Crypto edge-gate wiring confirmed in source.

**Status: HEALTHY (code/deployment), DEGRADED (external data-feed).** Remaining: natural swing run tonight, rawEdgeBps producer, crypto/daytrading bar freshness, D1 plan upgrade, Sep 1 monitoring, trade 703 strategy attribution gap.

## August 25, 2026 Control-93 strict read-only production control - HEALTHY/DEGRADED

Control-93 ran a strict GET-only production control at ~15:04 UTC. All six endpoints HTTP 200. No code defect found; no deploy required. HEAD reference corrected to `f494c06` (docs-only change). Version identity all aligned at 2.6.0 (health, release_version, config.version). Code commits `62ff44f`+`6876a92`+`22e962f`.

Live state: equity $98,523.26 (+0.139% via equity-direction fallback), ACTIVE, cash $90,278.93. 16 broker-authoritative positions all daytrading (MV $8,244.33), swing exposure zero. Caps $5,000/$3,700/$2,000 unchanged. Reconciliation ok every 10 min (ledgerActivities 5-8, 0 errors, watermark holding). Daytrading skipped on DAYTRADING_BARS_STALE (~973s vs 900s) + EQUITY_DIRECTION_FALLBACK. Crypto :07/:37 skipped (ETHUSD ~22h stale, MATICUSD empty, CRYPTO_DATA_INSUFFICIENT validTA=0, fail-closed no rawEdgeBps). Trades 704/705 filled 13:36 UTC today. Trade 703 strategy=null persists. Accounting conservative (filled_lot_exact_unavailable, gross/fee/net null). Crypto edge-gate wiring confirmed in source.

**Status: HEALTHY (code/deployment), DEGRADED (external data-feed).** Remaining: natural swing run tonight, rawEdgeBps producer, crypto/daytrading bar freshness, D1 plan upgrade, Sep 1 monitoring, trade 703 strategy attribution gap.

## August 25, 2026 Control-92 strict read-only production control - HEALTHY/DEGRADED

Control-92 ran a strict GET-only production control at ~14:00 UTC. All six endpoints HTTP 200. No code defect found; no correction or deploy required. Version identity all aligned at 2.6.0 (health, release_version, config.version). Local HEAD `0140fbc`, code `62ff44f`+`6876a92`+`22e962f`.

**Major resolution:** swing sells 701/702/703 (stuck since Aug 24 subrequest-exhausted run 3409) FILLED at broker 2026-08-25 13:30 UTC. Swing exposure now zero. 16 broker-authoritative positions all daytrading (MV $8,218.63). Tonight's 22:00 UTC swing run is first natural test on 2.6.0 with empty swing book.

Live state: equity $98,497.56 (+0.113% via equity-direction fallback), ACTIVE, cash $90,278.93. Caps $5,000/$3,700/$2,000 unchanged. Reconciliation ok every 10 min (watermark holding, ledgerActivities 5-8, 0 errors). Daytrading skipped on DAYTRADING_BARS_STALE (~16min bar lag vs 15min window). Crypto :07/:37 skipped (ETHUSD 22h stale, MATICUSD empty, fail-closed no rawEdgeBps). Trade lifecycle fields populated, accounting_status correct (no_fill vs filled_lot_exact_unavailable). Filters and pagination working (no repeated IDs). D1 optimizations live (watermark, pruning, cached fee summary).

**Status: HEALTHY (code/deployment), DEGRADED (external data-feed).** Prior OPEN FAIL blockers resolved. Remaining degradation is external data-feed staleness, not a bot defect. 220 tests / 822 assertions, typecheck clean. No caps or trading behavior changed. Remaining: natural swing run tonight, rawEdgeBps producer, crypto/daytrading bar freshness, D1 plan upgrade, Sep 1 monitoring, trade 703 strategy attribution gap.

## August 25, 2026 Control-91 strict read-only production control and config.version sync - LIVE VERIFIED

Control-91 ran a strict GET-only production control at ~13:00 UTC. All six endpoints returned HTTP 200. The D1-seeded `config.version` was synced from 2.4.0 to 2.6.0 via a single D1 REST API UPDATE; all three version surfaces now align at 2.6.0. No code change or Worker redeploy needed.

Live state: equity $98,527.39, change_today_pct 0.1431%, status ACTIVE, 21 broker-authoritative positions (source=alpaca, all swing), caps $5,000/$3,700/$2,000 unchanged. D1 watermark holding (ledgerActivities=0/pages=1), retention pruning active (last_prune_date=2026-08-25), fee summary cached and refreshed by maintenance.

Four schedules confirmed: reconciliation ok every 10 min; crypto at :07/:37 UTC all skipped (bars stale/unavailable, no rawEdgeBps producer); daytrading first tick 3505 at 13:01 UTC correctly MARKET_CLOSED; swing last run 3409 still errored on subrequest exhaustion — tonight's 22:00 UTC run is the first live test of deployed code. Filtered observability (trigger/code/status/search) all working. Trade lifecycle and accounting_status (no_fill vs filled_lot_exact_unavailable) confirmed. Crypto edge gate fail-closed.

Branch `fix/remove-premature-position-upsert-entryside`, HEAD `b318fa8` (docs), code `62ff44f`+`6876a92`, release 2.6.0. 220 tests / 822 assertions, typecheck clean. Remaining: natural swing run tonight, rawEdgeBps producer, crypto bar freshness, D1 plan upgrade, Sep 1 free-tier enforcement monitoring.

## August 25, 2026 Control-89 - reliability correction and live verification

Control-89 ran a strict GET-only production control at 11:00 UTC, then applied three bounded reliability corrections that preserve caps and trading behavior. Live state before correction: `/health` = **2.6.0** (matches local), `/api/config` `release_version` = **2.6.0**, but D1-seeded `config.version` still **2.4.0** (unchanged by Worker deploy, needs separate sync). Dashboard equity **$98,516.34**, 21 broker-authoritative positions (all swing), status ACTIVE. Maintenance reconciliation delivering every 10 min, `status=ok`, 111 ledger activities, 2 ledger pages, no truncation or degradation. Crypto cadence confirmed at :07/:37 UTC (runs 3481, 3485), both skipped due to `CRYPTO_BARS_STALE` (latest bar Aug 24 12:00-12:30 UTC, ~22h stale) and `CRYPTO_BARS_UNAVAILABLE` (MATICUSD empty). Equity-direction fallback firing in crypto cycles (`EQUITY_DIRECTION_FALLBACK`). Swing sells 701/702/703 from Aug 24 22:01 run remain `status=accepted`, `filled_qty=0` (subrequest exhaustion unresolved; next natural swing run at 22:00 UTC today). Caps unchanged **$5,000 / $3,700 / $2,000**.

**Corrections (code commit `22e962f`):** (1) `accounting_status` now distinguishes `no_fill` (zero-fill accepted orders) from `filled_lot_exact_unavailable` (filled but no lot-exact gross/fee/net), fixing the Control-86 conflation; (2) terminal `done_for_day`/`stopped` orders with partial fills are classified `executed=1` instead of `executed=2`, preserving partial-fill evidence; (3) equity-direction fallback (`accountWithEquityDirection`) now applied to the `/api/dashboard` account object, not just `/api/account`. **220 tests / 822 assertions** pass; TypeScript and `git diff --check` clean. No caps, order placement, cancellation, reconciliation-write, or trading-behavior change.

The exact current code-bearing checkout is branch `fix/remove-premature-position-upsert-entryside`, code commit `22e962fac86e2e4cb2f0679fa1745992c0eae8d8`, release **2.6.0**. Deployed via direct Cloudflare API PUT with GET-only post-deploy verification. Remaining follow-ups: D1-seeded `config.version` sync to 2.6.0, natural swing run recovery from subrequest exhaustion, crypto bar freshness investigation, `rawEdgeBps` producer for crypto edge gate, D1 free-tier optimization before Sep 1, and plan upgrade decision.

## August 25, 2026 Control-90 D1 free-tier optimization - LIVE VERIFIED

Deployed D1 optimizations (commit `62ff44f` + fee-cache-refresh fix in HEAD `6876a92`) via direct Cloudflare API PUT at 12:04:59 UTC (deployment_id `639c65b5`). Key changes: removed syncBrokerLedger from daytrading cycle, added broker_ledger_synced_until watermark (15-min overlap), batched upsertBrokerActivities with D1 batch(), added indexes (broker_fees order_id/fee_type+date, broker_fills symbol+time), cached getBrokerFeeSummary in bot_config with maintenance refresh every 10 min, LIMIT 500 on strategy comparison time-series, daily retention pruning. 220 tests / 822 assertions. Post-deploy: `/health`=2.6.0, run 3498 at 12:11 UTC ok, equity $98,535.09, 21 positions. No caps or trading behavior changed. Remaining: natural swing run, rawEdgeBps, D1 config.version sync, plan upgrade decision.

## August 25, 2026 Control-88 DEPLOY of release 2.6.0 - LIVE VERIFIED

Deployed release 2.6.0 (code commit `ce58d018`, HEAD `1609e0e`) to live Worker via direct Cloudflare API PUT at 09:51:58 UTC. Joachim granted self-directed deploy authorization Aug 25, 2026. `/health` now reports **2.6.0** (was 1.0.0). Run 3480 `reconcile_cron status=ok` at 10:00:41 UTC confirms new code running. Live filters now work: `code=FEE_DATA_UNAVAILABLE` and `status=filled` return correct filtered results. Dashboard equity $98,533.80, 21 positions, caps $5,000/$3,700/$2,000 unchanged. Wrangler deploy silently exits without uploading; direct API upload is the working path. `/api/config` version still 2.4.0 (D1-seeded). Follow-up: natural swing run, rawEdgeBps producer, D1 optimization, plan upgrade.

## August 25, 2026 Control-87 strict read-only production control - LIVE OPEN FAIL-DEGRADED

Control-87 was strictly read-only. It called only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus same-endpoint GET-only filter and pagination probes. No trigger, submit, cancel, close, replace, retry, migration, deployment, external write, or broker-mutating endpoint was called.

The exact current code-bearing checkout is branch `fix/remove-premature-position-upsert-entryside`, code commit `ce58d018585200af00032e5d624d6c989c2178fe`, release **2.6.0**; absolute HEAD `383f82350408931ac1d9eb18d0dfef9a18df13bd` is a docs-only commit. The live Worker remains unproven and reports the older `1.0.0`/`2.4.0` surfaces. Live deployment identity remains unproven: `/health` reports **1.0.0**, `/api/config.config.version` reports **2.4.0**, and Wrangler authentication is blocked by **`You are not authenticated. Please run \`wrangler login\`.`**

Live positions are broker-authoritative: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and **21** broker rows. Dashboard equity was **$98,492.42** versus `last_equity=$98,386.6243`, positive delta **$105.7957**; `change_today`/`change_today_pct` remain `0` on the live API (equity-direction fallback not deployed). Configured caps remain exactly **$5,000 / $3,700 / $2,000**. Swing market_value **$9,040.54** exceeds the swing cap; no trading mutation authorized here.

Four schedules unchanged: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, reconciliation `*/10 * * * *`. Reconciliation runs **3467-3471** through **08:51:11 UTC**; crypto run **3469** at **08:38:19 UTC** with FEE_DATA_UNAVAILABLE and CONFIDENCE_BELOW_THRESHOLD skips. Completion timestamps ~`:08/:38`; exact `:07/:37` unproven. Fresh daytrading/swing delivery unproven; swing run 3409 subrequest exhaustion persists.

Live filter/pagination defects persist: run `code` filter ignored, trade `status=filled` ignored, trade `offset`/`page` repeat ID 703. Per-fill `gross`/`fee`/`net` null under `unavailable_fill_lot_exact`. Crypto edge gate fail-closed; no `rawEdgeBps` producer in `technical-analysis.ts`. Production remains **OPEN FAIL/DEGRADED**. Local validation: **217 tests / 816 assertions**, typecheck, diff-check, clean worktree. No caps, schedules, trading behavior, deployment, or broker state changed.

Required follow-up: restore Wrangler auth, establish provenance, authorize deployment of `ce58d018`, GET-only post-release verification, natural swing run without exhaustion, resolve `rawEdgeBps` producer.

## August 25, 2026 Control-86 strict read-only production control - DOCS CORRECTED / LIVE OPEN FAIL-DEGRADED

Control-86 was strictly read-only. It called only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus same-endpoint GET-only filter and pagination probes. No trigger, submit, cancel, close, replace, retry, migration, deployment, external write, or broker-mutating endpoint was called.

The exact current code-bearing checkout is branch `fix/remove-premature-position-upsert-entryside`, HEAD `ce58d018585200af00032e5d624d6c989c2178fe`, release **2.6.0**. This commit contains the current reliability source and tests; later commits, if any, are documentation/status records. The live Worker remains unproven and reports the older `1.0.0`/`2.4.0` surfaces. Live deployment identity remains unproven: `/health` reports **1.0.0**, `/api/config.config.version` reports **2.4.0**, the live config has no release identity, and Wrangler authentication is blocked by **`You are not authenticated. Please run \`wrangler login\`.`**

Live positions are broker-authoritative: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and **21** broker rows. In the final GET-only recheck, dashboard equity was **$98,462.83** versus `last_equity=$98,386.6243`, a positive direction delta of **$76.2057**; the latest snapshot was **$98,463.06** at **2026-08-25 08:08:13 UTC**. Configured caps remain exactly **$5,000 daytrading / $3,700 swing / $2,000 crypto**, while the observed swing exposure remains above the configured swing cap and requires no trading mutation under this control.

Local four-schedule wiring is unchanged and exact: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`. The final live sample shows reconciliation runs **3462-3464** through **08:01:15 UTC** and crypto run **3465** at **08:08:18 UTC**, with structured maintenance, fee-data, decision, and no-position skips; completion timestamps are approximately :08/:38, so exact event delivery at :07/:37 remains unproven. Fresh successful daytrading and swing delivery remain unproven, and the latest known swing failure remains the Cloudflare subrequest exhaustion.

Lease/error/skip observability is present in local source and live payloads. However, live `code=LEASE_HELD` and `search=LEASE` probes return the same unfiltered page, live `status=filled` trade filtering is ignored, and trade offset pagination repeats IDs **703-701**, so filtered observability is not production-equivalent. Trade lifecycle fields are present for accepted and filled orders, but sampled per-fill `gross`, `fee`, and `net` remain null under `accounting_status=unavailable_fill_lot_exact`; conservative aggregate gross/fee/net arithmetic is not exact fill-lot proof.

Local crypto edge wiring is fail-closed: `crypto_min_edge_after_costs=8`, fresh fee telemetry is required, and calibrated raw edge is required rather than confidence-derived edge. Source review found no normal signal producer for `rawEdgeBps` in `technical-analysis.ts`; this means crypto BUY admission may be intentionally blocked for lack of calibrated evidence, but the missing producer is an unresolved reliability/strategy-operability defect and no behavior change is authorized here. Production remains **OPEN FAIL/DEGRADED**, not healthy or production-ready. Local reliability corrections now apply the equity-direction fallback to API account/dashboard responses, guarantee maintenance lease release after logging failures, persist schema-gate skips structurally, expose conservative accounting status in the dashboard, and normalize known crypto punctuation during position reconciliation; no caps, schedules, thresholds, sizing, fee policy, edge policy, order semantics, deployment, or broker state changed.

Required follow-up: restore secure Wrangler authentication, establish exact Worker/source provenance, separately authorize any deployment, then perform GET-only post-release verification of identity, all four schedules, natural daytrading/swing/crypto/reconciliation delivery, filters, lifecycle/accounting, cap semantics, and explicit crypto edge telemetry.

Control-85 used only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus GET-only filter and pagination probes on those same approved endpoints. The six approved endpoints returned HTTP 200 at approximately **2026-08-25 07:00:55 UTC**; no trigger, submit, cancel, close, replace, retry, migration, deployment, external-write, or broker-mutating endpoint was called.

The exact current deployable runtime source is HEAD `786be98c5be32f1b5cdfd46dfbcb033a9f3ca44f` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. Documentation-only control commits may advance repository HEAD without changing this runtime source identity; this supersedes the stale `c4c92b4793b78ad5c9b3b10128b910dbe3d31262` reference below; live deployment identity remains unproven because `/health` reports **1.0.0**, `/api/config.config.version` reports **2.4.0**, the live config response exposes no `release_version`, and Wrangler authentication is blocked by **`You are not authenticated. Please run \`wrangler login\`.`**

Live positions remain broker-authoritative: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and **21** rows. Dashboard equity is **$98,450.16** versus `last_equity=$98,504.5039`, a downward delta of **$54.3439**; caps remain exactly **$5,000 daytrading / $3,700 swing / $2,000 crypto**, while live swing market value is **$8,997.779953**, exceeding the configured swing cap and remaining unresolved as a live cap-semantics/freshness defect.

Local four-schedule wiring remains daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`. Fresh live delivery is degraded: crypto runs **3453** at **06:38:20** and **3449** at **06:08:18 UTC** recorded structured fee/decision skips; reconciliation runs **3456** at **07:01:20** and **3455** at **06:51:09 UTC** recorded maintenance-only delivery; current-day daytrading success is not proven and the latest sampled daytrading runs are market-closed skips; swing run **3409** at **2026-08-24 22:01:37 UTC** remains an error from Cloudflare subrequest exhaustion. Crypto completions are around **:08/:38**, not exact **:07/:37**, so exact cadence is not proven.

Trade/fill lifecycle fields are present: the live page contains **47 filled** and **3 accepted** rows, with accepted trades **701-703** still showing zero fills and remaining leaves quantity. All sampled rows retain `accounting_status=unavailable_fill_lot_exact` with null per-fill `gross`, `fee`, and `net`; aggregate crypto accounting remains arithmetically consistent at gross **-$56.616426** less fees **$269.11016882811** equals net **-$325.72659482811**, while non-crypto fees remain explicitly unattributed. Live run code/search/status filters and trade status filters/offset pagination remain ineffective or first-page repeating, so filtered observability is not live-equivalent; live computed crypto edge is not exposed, although local source wiring is fail-closed on fresh fee telemetry and calibrated raw edge with no confidence-derived edge.

Production remains **OPEN FAIL/DEGRADED**, not healthy or production-ready. Local source behavior and documentation identity are corrected only in the status record; no runtime code, caps, schedules, thresholds, sizing, fee policy, edge policy, order semantics, deployment, or broker state was changed. Required follow-up remains secure Wrangler authentication and source-tied deployment proof, then separate GET-only post-release verification of version identity, delivery, filters/pagination, cap/freshness semantics, exact fill accounting, and crypto edge telemetry.

Control-84 used only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus GET-only filter and pagination probes on those same approved endpoints. All six returned HTTP 200 between **2026-08-25 06:00:39 and 06:00:41 UTC**. No trigger, submit, cancel, close, replace, retry, migration, deployment, external-write, or broker-mutating endpoint was called.

The exact current local Alpaca source is HEAD `c4c92b4793b78ad5c9b3b10128b910dbe3d31262` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. The HEAD commit is documentation-only and does not change runtime source identity. Live deployment identity is not proven: `/health` reports **1.0.0**, `/api/config.config.version` reports **2.4.0**, and the live response does not expose the local `release_version`; `bunx wrangler whoami` is blocked by **`You are not authenticated. Please run \`wrangler login\`.`**

Live positions remain broker-authoritative: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and **21** rows. All captured rows are attributed to swing and total **$8,989.034153** market value; `updated_at` ranges from **2026-08-24 19:56:25** to **19:56:30 UTC**, stale against the latest snapshot. Dashboard account equity is **$98,441.41** versus `last_equity=$98,504.5039`, a downward delta of **$63.0939**; the latest snapshot is **$98,434.87** at **2026-08-25 05:38:13 UTC**. Broker daily change fields are zero, so local equity-direction fallback is relevant and present in source. Caps remain exactly **$5,000 daytrading / $3,700 swing / $2,000 crypto**.

Local four-schedule wiring is unchanged: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`. Fresh live delivery is mixed: crypto runs **3441** at **05:08:18** and **3445** at **05:38:19 UTC** were auditable fee/decision skips; reconciliation runs **3440** at **05:01:14** and **3447** at **05:51:11 UTC** completed maintenance-only broker reconciliation; lease skips **3433-3435** are visible; daytrading has no fresh successful current-day run before the market opens, with prior filled activity at **19:16:22 UTC** and later market-closed skips; swing run **3409** at **2026-08-24 22:01:37 UTC** remains an error due to Cloudflare subrequest exhaustion. Crypto runs are around **:08/:38**, with a visible gap from **00:38 to 04:38 UTC**, so exact `:07/:37` delivery is not proven.

Trade/fill lifecycle fields are present: the live page contains **47 filled** and **3 accepted** rows; accepted trades **701-703** have zero filled quantity and nonzero leaves quantity, while filled rows have broker timestamps and `filled_at`. Sampled per-fill `gross`, `fee`, and `net` remain null with `accounting_status=unavailable_fill_lot_exact`, conservatively avoiding unsupported lot matching. Aggregate accounting is arithmetically consistent where exposed: crypto gross **-$56.616426** less broker-attributed fees **$269.11016882811** equals net **-$325.72659482811**; swing and daytrading fees remain account-level unattributed. Live `status=filled` and `status=accepted` trade filters return the same mixed first page; trade offsets/pages repeat IDs **703, 702, 701**; run `code=LEASE_HELD`, `search=LEASE`, and status probes return unfiltered/incorrect pages. Strategy and trigger filters do return narrowed data, but filtered run observability is not live-equivalent to local source.

The checked-out source preserves unchanged caps, broker-first position authority, bounded read-only reconciliation, structured skip/error observability, lifecycle persistence, filtered run/trade pagination, explicit equity fallback, conservative accounting, and the crypto fail-closed gate: `crypto_min_edge_after_costs=8`, fresh fee telemetry required, and calibrated raw edge required rather than confidence-derived edge. Live crypto skips show fee telemetry unavailable/insufficient, and no live computed edge is exposed. Current swing exposure also exceeds both the configured swing cap and global cap, while stale position timestamps leave cap semantics/freshness unresolved.

Production remains **OPEN FAIL/DEGRADED**, not healthy or production-ready. No runtime correction is justified without source-tied deployment provenance; no caps, schedules, thresholds, sizing, fee policy, edge policy, order semantics, or trading behavior were changed, and no deployment was attempted. Follow-up: restore secure Wrangler authentication, establish active source provenance, authorize deployment only if still required under the standing maintenance rule, then perform separate GET-only post-release verification covering version identity, positions, delivery/skip observability, filters/pagination, lifecycle/accounting, caps, and crypto edge gating. Preserve the swing subrequest, exact cadence, live observability, cap/freshness, and exact fill-accounting investigations as open risks.

## August 25, 2026 Control-83 strict read-only production control - LOCAL SOURCE PINNED / LIVE OPEN FAIL-DEGRADED

Control-83 used only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus GET-only filter and pagination probes on those same approved endpoints. All six returned HTTP 200 at approximately **2026-08-25 05:00 UTC**. No trigger, submit, cancel, close, replace, retry, migration, deployment, external-write, or broker-mutating endpoint was called.

The exact current deployable runtime source is HEAD `366df21a782678bf9c277f16b5a0d1712553c6c1` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. This control commit is documentation-only and does not change that runtime source identity. This current HEAD supersedes stale historical references to `1d14eb9` and `1c6914d` in older control records; those hashes remain historical evidence only. Live deployment identity is not proven: `/health` reports **1.0.0**, `/api/config.version` reports **2.4.0**, and `bunx wrangler whoami` is blocked by **You are not authenticated. Please run `wrangler login`.**

Live positions are broker-authoritative: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and **21** rows. Dashboard equity is **$98,408.17** versus `last_equity=$98,504.5039`, a downward delta of **$96.3339**; the latest snapshot is **$98,408.19** at `2026-08-25 04:38:12 UTC`. Caps remain exactly **$5,000 / $3,700 / $2,000 USD** for daytrading, swing, and crypto. Local four-schedule wiring remains daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`.

Fresh live delivery is mixed and fully auditable only as degraded behavior. Crypto run **3437** completed at `04:38:17 UTC` with four decisions and zero trades, including structured `FEE_DATA_UNAVAILABLE`, confidence, and decision-hold skips. Reconciliation run **3439** was observed at `04:51:08 UTC`, with a further latest reconciliation delivery at **05:01:14 UTC**; recurring runs **3430-3435** show explicit `CYCLE_LEASE_HELD` skips. Daytrading activity is present through run **19:16:31 UTC** with one trade, followed by an error at **19:51:29 UTC** and a skip at **19:56:30 UTC**; the latest swing run **3409** at **2026-08-24 22:01:37 UTC** failed with Cloudflare subrequest exhaustion. Category history shows crypto snapshots on the configured :07/:37 pattern, but actual run completions are around **:08/:38** and include a visible **00:08 to 04:38 UTC** gap, so exact cadence is not proven.

Trade lifecycle fields exist for filled and accepted orders. Accepted trades **701-703** have zero filled quantity and remaining leaves quantity; prior filled rows have broker fill timestamps, but sampled rows retain `gross=null`, `fee=null`, `net=null`, and `accounting_status=unavailable_fill_lot_exact`, so exact fill-lot economics remain unavailable. Aggregate strategy accounting is arithmetically consistent where exposed, but it is not exact per-fill proof. Live `status=filled` filtering is ignored, `offset=0&limit=3` and `offset=3&limit=3` repeat IDs **703-701**, and run `code=LEASE_HELD` / `search=LEASE` probes return the unfiltered first page. The local crypto edge gate remains fail-closed at `crypto_min_edge_after_costs=8` with fee telemetry and calibrated raw edge required; live edge admission has no explicit computed-edge value. Live swing market value is approximately **$8,955.81**, exceeding both the configured **$3,700** swing cap and the global **$5,000** cap; position `updated_at` values remain around **2026-08-24 19:56 UTC** despite the **2026-08-25 04:38:12 UTC** snapshot, so cap semantics and freshness remain unresolved.

Production remains **OPEN FAIL/DEGRADED**, not healthy or production-ready. No code, runtime behavior, caps, schedules, config, thresholds, sizing, edge policy, order semantics, or broker state was changed. Deployment remains blocked pending secure Wrangler authentication, source-tied deployment provenance, and a separately authorized deployment followed by a separate GET-only live verification. See `CORRECTION_WORK_ITEM_2026-08-25_CONTROL-83.md`.

## August 25, 2026 Control-82 reliability correction - LOCAL VALIDATED / LIVE OPEN FAIL-DEGRADED

Control-82 corrected reliability and observability only: clean maintenance runs are `ok` with informational `MAINTENANCE_ONLY`; intentional reconciliation deferral no longer mislabels evaluated no-trade cycles; daytrading and crypto reject invalid, stale, or future intraday bars; equity-direction fallback is explicit; and unattributed broker exposure is included conservatively in swing cap checks. Caps, schedules, thresholds, sizing, order semantics, and trading behavior were not changed.

The exact validated Alpaca source is HEAD `1d14eb9f97c16012b54b7ef24e244513c1f3b0bf` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. Full validation passed **213 tests / 796 assertions**, TypeScript, and `git diff --check`. No deployment, migration, trigger, broker mutation, or external mutation occurred. Live production remains **OPEN FAIL/DEGRADED** because the Worker still reports health/config **1.0.0 / 2.4.0**, Wrangler authentication is unavailable, and separate GET-only live verification of this commit has not occurred.

## Tuesday, August 25, 2026 Control-81 strict read-only production control - LOCAL VALIDATED / LIVE OPEN FAIL-DEGRADED

Control-81 used only GET requests against `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus GET-only filter/pagination probes on those same endpoints. All six returned HTTP 200; no trigger, submit, cancel, close, replace, retry, migration, deployment, or broker-mutating endpoint was called.

The exact deployable checkout is HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. Required docs identify this same HEAD. Live identity remains `/health=1.0.0` and `/api/config.version=2.4.0`; `bunx wrangler whoami` is blocked by `You are not authenticated. Please run wrangler login.`, so source-to-Worker provenance and deployment of this HEAD are not verified.

Live positions remain broker-authoritative: `positionsAvailable=true`, `source=alpaca`, 21 rows. Dashboard equity is **98410.64 USD** versus `last_equity=98504.5039`, down **93.8639 USD**; the latest snapshot is **98390.96 USD** with `total_pl=-113.54389999998966`. Caps remain exactly **5000 / 3700 / 2000 USD**. Local four-schedule wiring remains daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`.

Fresh daytrading, crypto, and reconciliation deliveries are observable, but daytrading is skipped/errored in the sampled window, crypto is skipped, reconciliation is repeatedly `CYCLE_LEASE_HELD`, and the latest swing run **3409** at **2026-08-24 22:01:37 UTC** failed with Cloudflare subrequest exhaustion. Crypto completion timestamps are consistently around **:08/:38 UTC**, not literal **:07/:37**, so requested cadence is not verified. Structured skip/error observability is present locally and in live run payloads.

Trade lifecycle fields are present for filled and accepted orders, but sampled rows retain `gross=null`, `fee=null`, `net=null`, `accounting_status=unavailable_fill_lot_exact`, and `fee_attribution=none-recorded`; exact fill-lot economics therefore remains unavailable. Live filter/pagination behavior is stale or defective: status/code/search and offsets/page probes repeat unfiltered or first-page results, while the checked-out source has regression coverage for the corrected behavior. Crypto edge wiring is locally fail-closed at `crypto_min_edge_after_costs=8`, requiring fresh fee telemetry and calibrated raw edge; live fee/edge admission is not proven.

No code/config correction was justified by this capture beyond status documentation: the checked-out reliability implementation passes **204 tests / 775 assertions**, TypeScript, diff-check, and Wrangler dry-run. Keep production **OPEN FAIL/DEGRADED**. Required follow-up remains secure Wrangler authentication and source provenance, authorized deployment only after clean-release review, separate GET-only post-release verification, swing subrequest remediation, exact cadence verification, live filter/pagination verification, exact fill accounting, cap semantics, and fresh fee/edge evidence; no cap or trading-behavior changes are authorized.

## Control-80 operational status - August 25, 2026 - OPEN FAIL/DEGRADED

The Alpaca release checkout is exact HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de` on branch `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. The workspace root is a separate Git tree at HEAD `d145a98321cb928a334f8d3685ece6076c0eacd1`; it is not the Alpaca source identity. Live `/health` is **1.0.0**, `/api/config` is **2.4.0**, and Wrangler provenance/deployment is blocked by `You are not authenticated. Please run `wrangler login`.`

Control-80 was strictly read-only: only the six approved GET endpoints and GET-only filter/pagination probes were called. Live positions report `positionsAvailable=true`, `source=alpaca`, and 21 broker rows. Equity is 98389.56 versus last_equity 98504.5039, down 114.9439; caps remain 5000/3700/2000 USD.

The configured schedules are unchanged: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, and `*/10 * * * *`. Live completion evidence is degraded: no fresh successful daytrading run is proven, swing run 3409 failed on `Too many subrequests`, reconciliation run 3433 is `CYCLE_LEASE_HELD`, and crypto completions are around :08/:38 rather than the requested :07/:37 UTC.

Structured lease/error skip observability exists, but the live artifact does not prove the local implementation: run code/search filters return unfiltered data, trade status filtering is ignored, and offsets repeat. Filled/accepted lifecycle fields exist, while gross/fee/net remain conservatively unavailable at fill level. The crypto gate remains fail-closed locally at 8 bps minimum edge after costs with fresh fee telemetry required; live calculated edge and fee freshness are not proven. Keep the gate OPEN FAIL/DEGRADED and do not change caps or trading behavior.

## Control-79 operational status - August 25, 2026 - OPEN FAIL/DEGRADED

The strict production control used only approved GET requests and safe GET filter/pagination probes. Local source is exact HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de`, branch `fix/remove-premature-position-upsert-entryside`, release **2.6.0**. Live health/config remain **1.0.0 / 2.4.0** and Wrangler provenance is blocked by `You are not authenticated. Please run \`wrangler login\`.`

Live positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 21 rows). Equity is **98394.57 USD** versus `last_equity=98504.5039`, down **109.9339 USD**. Caps remain **5000 / 3700 / 2000 USD** and the four schedules remain unchanged: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, and `*/10 * * * *`.

Fresh daytrading, crypto, and reconciliation activity is observed with structured skips, including `FEE_DATA_UNAVAILABLE`, `MAINTENANCE_ONLY`, and `CYCLE_LEASE_HELD`; crypto completion history remains aligned to :07/:37 UTC with jitter. Swing run 3409 remains a Cloudflare subrequest-exhaustion error, so fresh successful swing delivery is not proven. Live run code/search filtering, trade status filtering, and trade pagination remain defective or stale, while lifecycle fields exist and exact per-fill gross/fee/net remains unavailable by conservative design.

Do not change caps, schedules, edge policy, fees, positions, or order behavior to compensate. Keep the gate **OPEN FAIL/DEGRADED** and track provenance/deployment authorization, swing subrequest remediation, live observability verification, exact fill accounting, cap semantics, and fresh crypto edge/fee evidence under follow-up `864e3971-0655-4d0f-ac81-95ba66595335`. See `CORRECTION_WORK_ITEM_2026-08-25_CONTROL-79.md`.

## Control-78 strict read-only production control - August 25, 2026

Control-78 is **LOCAL VALIDATED / LIVE OPEN FAIL-DEGRADED**, not healthy. The exact current local source is HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**; live `/health` and `/api/config` remain **1.0.0 / 2.4.0**, and Wrangler provenance is blocked by `You are not authenticated. Please run `wrangler login`.`

Only the six approved GET endpoints and same-endpoint GET filter/pagination probes were used. Positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 21 rows); equity is `98390.96` versus `last_equity=98504.5039`, down `113.5439`; caps are exactly `5000/3700/2000 USD`; schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`.

Fresh daytrading, crypto, and reconciliation delivery is present with structured skips. Run `3430` at `2026-08-25 00:50:37 UTC` records `CYCLE_LEASE_HELD`, run `3426` at `00:08:18 UTC` records `CONFIDENCE_BELOW_THRESHOLD`, and run `3422` records `FEE_DATA_UNAVAILABLE`; crypto history confirms approximately `:07/:37 UTC` cadence with seconds-level jitter. Fresh successful swing delivery is not proven: run `3409` at `2026-08-24 22:01:37 UTC` is `error` with Cloudflare subrequest exhaustion.

Live run `code=LEASE_HELD` and `search=LEASE` filters are defective because they return the unfiltered first page; live candidate counters/aliases are absent. Live trades show 97 filled and 3 accepted rows, lifecycle fields are present, but sampled per-fill gross/fee/net remain conservatively null under `unavailable_fill_lot_exact`; status filtering is ignored and offsets repeat `703-702-701`. Aggregate gross/fee/net arithmetic is consistent but exact fill economics are unavailable. The dashboard shows `8938.576216 USD` swing exposure against the unchanged `3700 USD` cap, an unexplained `executed=2` versus run `trades_executed=0`, and contradictory fee attribution. The local crypto gate remains fail-closed with `crypto_min_edge_after_costs=8`, but live edge proof is absent and fee telemetry is insufficient/stale.

No source/config/cap/schedule/trading-behavior change was justified. Keep **OPEN FAIL/DEGRADED**, retain follow-up `864e3971-0655-4d0f-ac81-95ba66595335`, restore authenticated provenance, deploy only the already-validated reliability artifact if authorized by the standing maintenance rule, and require separate GET-only post-release verification. See `CORRECTION_WORK_ITEM_2026-08-25_CONTROL-78.md`.

## Control-77 strict read-only production control - August 24, 2026

Control-77 is **LOCAL VALIDATED / LIVE OPEN FAIL-DEGRADED**, not healthy. The exact current deployable source is HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**; live `/health` and `/api/config` remain **1.0.0 / 2.4.0**, and `bunx wrangler whoami` is blocked by `You are not authenticated. Please run `wrangler login`.`

Only the six approved GET endpoints and same-endpoint GET filter/pagination probes were used. Positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 21 rows); equity is `98390.96` versus `last_equity=98504.5039`, down `113.5439`; caps are exactly `5000/3700/2000 USD`; and the four schedules remain unchanged. Daytrading, crypto, and reconciliation delivery is fresh with structured skips, and filtered crypto history confirms the configured `:07/:37` cadence with seconds-level jitter. Swing run `3409` at `2026-08-24 22:01:37 UTC` failed on Cloudflare subrequest exhaustion.

Live trades show 47 filled and 3 accepted rows with lifecycle fields but null per-fill gross/fee/net under `unavailable_fill_lot_exact`; aggregate gross-fee-net arithmetic is consistent while exact fill economics are unavailable. Status filters are ignored, offsets repeat IDs `703-701`, and a dashboard decision reports `executed=2` with a skip reason while its run reports `trades_executed=0`. Fee totals also expose an attribution contradiction: `unattributedUsd` equals all fees although crypto fees are labeled broker-attributed. Accepted trades 701-703 have persistence timestamps later than submission timestamps without an API explanation. The dashboard reports `8938.576216 USD` swing exposure against the unchanged `3700 USD` cap, a live cap-control **FAIL** if total current gross exposure is governed. Crypto config has `crypto_min_edge_after_costs=8`; local source has fail-closed calibrated-edge/fee wiring, but live computed edge proof is absent and fee telemetry is insufficient/stale. Lease handling is locally tested but not live-proven.

No source/config/cap/schedule/trading-behavior change was justified. Keep **OPEN FAIL/DEGRADED**, retain follow-up `864e3971-0655-4d0f-ac81-95ba66595335`, restore authenticated provenance, and require separate GET-only post-release verification before any healthy designation. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-77.md`.

## Control-76 strict read-only production control - August 24, 2026

Control-76 is **LOCAL VALIDATED / LIVE OPEN FAIL-DEGRADED**, not healthy. The exact deployable HEAD is `1c6914d1766e420fc3cfa3be2f1e2914c5e197de` on `fix/remove-premature-position-upsert-entryside`, release **2.6.0**; live health/config are **1.0.0 / 2.4.0** and Wrangler provenance is blocked by `You are not authenticated. Please run wrangler login.`

All six approved GET endpoints returned HTTP 200. Positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 21 rows); the post-update recheck reports equity `98396.24` versus `last_equity=98504.5039`, down `108.2639`; caps are exactly `5000/3700/2000 USD`; and the four schedules remain unchanged. Daytrading, crypto, and reconciliation delivery is fresh with structured skips, but swing run `3409` at `2026-08-24 22:01:37 UTC` errored on Cloudflare subrequest exhaustion. The live trade page has 47 filled and 3 accepted rows with lifecycle fields, while gross/fee/net remain conservatively null for all 50 rows. Safe probes show live trade `status=accepted` and `status=filled` filters are ignored, and trade offsets repeat the first page; run trigger and strategy filters work, but `code=CYCLE_LEASE_HELD`, `code=FEE_DATA_UNAVAILABLE`, and `search=LEASE` return the same latest unfiltered-looking page. The dashboard currently attributes **$8,943.86301** to 21 open swing positions against the **$3,700** swing cap, a live cap-control **FAIL** if current gross exposure is the governed quantity.

No source change was justified. Local source already contains broker-first position authority, bounded read-only reconciliation, structured skip/error observability, lifecycle persistence, filtered run/pagination and candidate-count contracts, conservative accounting, and crypto fail-closed fee/calibrated-edge gates. The live failures are consistent with the Worker serving an older artifact than exact local HEAD `1c6914d1766e420fc3cfa3be2f1e2914c5e197de`; changing caps, attribution, or entry behavior would be unsafe without provenance. Keep the existing hourly follow-up enabled and do not mark production healthy until source-to-Worker provenance, natural swing delivery, cap enforcement, live filter/candidate behavior, and exact accounting proof are restored. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-76.md`.

## Control-75 production-control reliability inspection - August 24, 2026

The exact checked-out HEAD is `1c6914d1766e420fc3cfa3be2f1e2914c5e197de`, release **2.6.0**; worktree cleanliness is not used as provenance evidence. Required operational documentation was already present. The checked-out source already provides read-only run filters/pagination, durable `trigger`/alias translation and candidate counters, plus crypto fail-closed fee/calibrated-edge admission and structured evidence; focused regressions cover those contracts, so no source or trading-behavior change was necessary.

This work preserved caps exactly at **5000 / 3700 / 2000 USD** and preserved all four schedules. No deployment, migration, trigger, submit, cancel, close, replace, retry, external write, or broker-mutating endpoint was called. Local tests, typecheck, diff check, and deployment dry-run passed. `bunx wrangler whoami` remains blocked by `You are not authenticated. Please run wrangler login.`, leaving active Worker provenance unresolved; live status therefore remains **OPEN FAIL/DEGRADED**, not healthy. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-75.md`.

Control-74 is **OPEN FAIL/DEGRADED**, not healthy. The control used only GET requests to `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`, plus read-only filter/pagination probes; all six returned HTTP 200 JSON. No trigger, submit, cancel, close, replace, retry, deployment, migration, external write, or broker mutation occurred.

Live health/config are **1.0.0 / 2.4.0** versus local release **2.6.0** at commit `cef5a4d826a12311c413fecacdf46cb23f2b63fa`; Wrangler is unauthenticated, so active Worker provenance is unresolved. Positions are broker-authoritative with `positionsAvailable=true`, `source=alpaca`, and 21 rows. Equity is **98395.26 USD** versus `last_equity=98504.5039`, down **109.2439 USD** by current-minus-last comparison; caps remain **5000 / 3700 / 2000 USD**.

Schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` near **:07/:37 UTC**, and reconciliation `*/10 * * * *`. Fresh delivery is evidenced for daytrading run **3387** (`MARKET_CLOSED`), crypto run **3381** (`FEE_DATA_UNAVAILABLE`, `DECISION_HOLD`), and reconciliation run **3386** (`MAINTENANCE_ONLY`); no fresh successful swing run is proven because the latest swing row is run **3182**, an error from **2026-08-23 22:01:16 UTC** with subrequest exhaustion. Lease-held behavior is implemented and locally tested, but no live `CYCLE_LEASE_HELD` row is proven.

Live run filters remain unverified or defective: `code=LEASE_HELD` and `search=LEASE` repeat the unfiltered page, and live rows omit `trigger_alias`, `analyzed_candidates`, and `filtered_candidates`. Trade strategy filtering is observed, but status/pagination metadata is absent and `offset=10` / `page=2` repeat the first page. Lifecycle timestamps, IDs, fill quantities, and statuses are present. Filled `gross`, `fee`, and `net` remain conservatively null under `unavailable_fill_lot_exact` / `none-recorded`; aggregate gross **-241.468094** minus fees **272.320169** equals overall net **-513.788263**, but exact per-fill economics remain unavailable. The crypto fee timestamp is stale, so fail-closed fee and calibrated-edge admission remains required.

Local correction validation passed: focused **86 / 362 assertions**, full **204 / 775**, typecheck, diff-check, and Wrangler dry-run. The source already implements broker-first positions with 503/no-D1 fallback, bounded lease-protected reconciliation, structured skips/errors and candidate counters, lifecycle reconciliation, conservative accounting, unchanged caps/schedules, and fail-closed crypto edge wiring. No source or trading-behavior change was needed. Deployment remains blocked by `bunx wrangler whoami` -> `You are not authenticated. Please run wrangler login.` Restore credentials securely, establish provenance and authorization, then deploy only the validated reliability artifact if authorized and run a separate GET-only verification. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-74.md`.

## Control-73 strict read-only production control - August 24, 2026

Control-73 is **OPEN FAIL/DEGRADED**, not healthy. All six required GET endpoints returned HTTP 200 JSON, but live health/config remain **1.0.0/2.4.0** versus local **2.6.0**; Wrangler is unauthenticated, so active deployment provenance cannot be verified. `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and 26 broker rows. Equity is **98391.48 USD** versus `last_equity=98504.5039`, down **113.0239 USD** by comparison. Caps remain exactly **5000/3700/2000 USD**.

The four schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`. Fresh live delivery includes daytrading run **3346** at **18:01:37 UTC**, reconciliation run **3345** at **18:01:19 UTC** with `MAINTENANCE_ONLY`, and crypto run **3338** at **17:38:15 UTC**. No fresh August 24 swing run or explicit `CYCLE_LEASE_HELD` event is proven. `code=LEASE_HELD` and `search=LEASE` return the same page; live rows omit `trigger_alias`, `analyzed_candidates`, and `filtered_candidates` although local source/tests implement them.

Repeated live daytrading broker defects are recorded in run **3328** at **16:16:17 UTC** (PLUG) and run **3344** at **17:56:27 UTC** (NIO): both are Alpaca **40310000** minimum-order-1 failures. Filled samples including trades **682** and **681** conservatively retain null `gross`/`fee`/`net` under `unavailable_fill_lot_exact` / `none-recorded`; aggregate crypto gross **-56.616426**, fees **269.110169**, and net **-325.726595** are internally consistent but not exact per-fill attribution. Live trade filter metadata is absent and offset/page probes repeat the first page.

This is documentation/status-only. The checked-out source already contains the narrow daytrading BUY minimum-order preflight, broker-authoritative positions, bounded reconciliation, structured observability, four schedules, cap resolution, conservative accounting, and fail-closed crypto gates. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker mutation was used. Restore secure Wrangler credentials, map the active artifact, release only if separately authorized, then repeat separate GET-only verification and observe natural daytrading/swing delivery. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-73.md`.
## Control-70 broker-only reconciliation correction - August 24, 2026

Successful broker-only position reconciliation is now structured as `BROKER_ONLY_RECONCILED` non-error observability. Broker authority, D1-only reconciliation writes, caps **5000/3700/2000**, schedules, and trading semantics are unchanged; quantity mismatches and actual failures remain errors. Local validation must include focused and full regressions, typecheck, and diff-check.

Live remains **OPEN FAIL/DEGRADED**: release identity is stale at health/config **1.0.0/2.4.0** versus local **2.6.0**; current positions are Alpaca-authoritative; swing has no fresh August 24 strategy run; exact per-fill economics and newer observability filters remain unavailable or unproven. The dashboard also reports crypto fee telemetry as available with `cryptoFeeAsOf=2026-08-18T09:37:52.56276Z`, which is stale for the August 24 capture, and conflicting swing exposure aggregates prevent read-only certification of the 3700 USD cap. Wrangler authentication remains blocked.

See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-70.md`.

## Broker-only reconciliation observability correction — August 24, 2026

A successful broker-authoritative position divergence repair continues to upsert broker positions and close D1-only positions exactly as before. The successful outcome is now emitted as structured skip detail with code `BROKER_ONLY_RECONCILED` under reconciliation scope, instead of being appended to the error list. Therefore a run with only this successful repair is not `status=error`; it is `skipped` when no trades ran or `ok` when trades ran. Actual reconciliation exceptions and other runtime failures continue to populate `errors` and produce `status=error`.

Validation completed: focused **100/469** across 8 files, full `bun test` **201/763** across 26 files, typecheck exit 0, and `git diff --check` exit 0. This is reliability/observability-only: preserve broker authority, caps **5000/3700/2000**, schedules, thresholds, sizing, fee freshness, edge policy, and order behavior. Do not deploy or use broker-mutating smoke tests.

## Control-69 status, Monday August 24, 2026

The strict GET-only production control is **OPEN FAIL/DEGRADED**, not healthy. All six endpoints returned HTTP 200, but production identifies as health **1.0.0** and config **2.4.0** while the validated local release is **2.6.0**. Broker positions are authoritative (`positionsAvailable=true`, `source=alpaca`); equity was **98435.77** versus `last_equity=98504.5039` (down); caps remain **5000/3700/2000 USD**.

The four local UTC schedules remain unchanged: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` at `:07/:37`, and reconciliation `*/10 * * * *`. Current live runs include crypto 3315 (`DECISION_HOLD`), reconciliation 3316 (`MAINTENANCE_ONLY` with non-degraded ledger context), and daytrading 3317 (`error`); prior 3313 hit Cloudflare subrequest exhaustion. No lease-held row was observed, so lease behavior is not proven.

Local reliability and observability tests pass, including broker-failure no-fallback, filtered runs/trades, stable pagination, lifecycle fields, conservative accounting, and crypto fee/calibrated-edge fail-closed behavior. Live exact fill-lot gross/fee/net and the newer observability fields remain unavailable or unproven. Do not infer per-fill economics, loosen safety gates, change caps, or deploy a dirty artifact.

Wrangler authentication remains the deployment blocker: `You are not authenticated. Please run wrangler login.` Authenticate securely, isolate a clean artifact, deploy only under the standing reliability-maintenance authorization if still required, and immediately repeat the separate GET-only control afterward.

## Monday, August 24, 2026 Control-68 correction work item - LOCAL COMPLETE / LIVE OPEN FAIL-DEGRADED

Control-68 is a documentation/status correction following a strict GET-only control. All six required production GET endpoints returned HTTP 200, but live release identity is **1.0.0/2.4.0** versus local **2.6.0**, so production remains **OPEN FAIL/DEGRADED**, not healthy. `/api/positions` reports broker-authoritative data (`positionsAvailable=true`, `source=alpaca`, 21 current broker rows), and current dashboard equity is **98461.50** versus `last_equity=98504.5039`.

The local tree already contains the reliability fixes required by the observed failures and the requested control: no D1 fallback for broker position failure, bounded read-only reconciliation, structured skip/error persistence, filtered run/trade query support, durable lifecycle fields, conservative fee/gross/net accounting, and fail-closed crypto fee/calibrated-edge admission. Do not add a runtime patch that changes trading behavior. Preserve caps **5000/3700/2000 USD**, all four UTC schedules, thresholds, sizing, fee freshness, edge policy, and order semantics.

Current live evidence includes crypto **3315 at 13:38:00 UTC**, reconciliation **3316 at 13:40:58 UTC**, and daytrading **3317 at 13:41:16 UTC**. Run 3317 is an error run with broker/internal mismatch details; prior live evidence includes Cloudflare subrequest exhaustion. Structured skip/error records are visible, while live lease-held proof, aliases/candidate counters, complete filtering/pagination, and exact per-fill gross/fee/net remain unresolved on the old artifact. Deployment is blocked by unauthenticated Wrangler and must not use a temporary preview path. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-68.md`.

## Monday, August 24, 2026 Control-67 strict read-only production control - OPEN FAIL/DEGRADED

Control-67 records current GET-only production evidence. All six required GET endpoints returned **HTTP 200**. Keep production **OPEN FAIL/DEGRADED**, not healthy: live health **1.0.0** and `/api/config` **2.4.0** versus local release **2.6.0** leave source/deployment provenance unresolved.

Broker-authoritative positions are confirmed: `positionsAvailable=true`, `source=alpaca`, **29 rows**. Equity is **98537.99** versus `last_equity=98504.5039`; latest/prior snapshots are **98517.58 at 2026-08-24 12:37:50 UTC** and **98488.55 at 12:07:50 UTC**. Direction is upward versus last; broker daily fields are zero. Preserve exact caps **5000/3700/2000** and local schedules: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` (**:07/:37**), reconciliation `*/10 * * * *`.

Crypto run **3299 at 12:37:57 UTC** and reconcile run **3301 at 12:50:51 UTC** confirm recurring **:07/:37** crypto and ten-minute reconciliation cadence. Daytrading current active delivery is present; successful swing delivery is not proven in the current window. Swing run **3182 at 2026-08-23 22:01:16** errored with Cloudflare **Too many subrequests**, with structured broker-authority/held skips.

Run `code`/`search`/`LEASE_HELD` filters return the same unfiltered page; `aliases`, `analyzed_candidates`, and `filtered_candidates` are absent live. Trade `status=filled`/pagination are defective or ignored. Lifecycle fields exist, but filled trade **642** has null gross/fee/net and `accounting_status=unavailable_fill_lot_exact`. Aggregate crypto and overall gross-fee-net arithmetic reconciles, but exact per-fill attribution is unavailable.

Local source retains broker-authoritative position projection, filtered-run fields, and crypto fail-closed edge gating with fresh fee telemetry and `requireCalibratedEdge=true`. Final post-update receipts are `/workspace/alpaca_control_67_focused.txt` (**74/0/361**), `/workspace/alpaca_control_67_full.txt` (**199/0/754**), `/workspace/alpaca_control_67_typecheck.txt` (exit 0), and `/workspace/alpaca_control_67_diff_check.txt` (exit 0). Wrangler is blocked by **`You are not authenticated. Please run wrangler login.`** No deployment or trigger/order/broker mutation occurred. No source or trading behavior changed. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-67.md`.

## Monday, August 24, 2026 Control-66 strict read-only production control - OPEN FAIL/DEGRADED

Control-66 is a GET-only production control captured Monday, August 24, 2026 around 12:00 UTC. All six required endpoints returned **HTTP 200**, while live `/health=1.0.0` and `/api/config.version=2.4.0` remain older than local release **2.6.0**. Keep production **OPEN FAIL/DEGRADED**, not healthy, until source-to-Worker provenance is restored.

Positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, **29 rows**). One `MSTR` row is unattributed and sampled position metadata is stale. The captured equity is **98511.32** versus `last_equity=98504.5039`; latest snapshot equity is **98515.57**; broker daily fields are zero. Capital caps remain **5000/3700/2000 USD**.

The local four-schedule wiring remains exact: daytrading `*/5 13-21 * * 1-5` → `cron`; swing `0 22 * * 1-5` → `swing_cron`; crypto `7-59/30 * * * *` → `crypto_cron` at **:07/:37 UTC**; reconciliation `*/10 * * * *` → `reconcile_cron`. Live crypto and reconciliation are fresh, with current skips `NO_POSITION_TO_EXIT`, `FEE_DATA_UNAVAILABLE`, and `MAINTENANCE_ONLY`. Historical swing run **3182** recorded Cloudflare subrequest exhaustion; current-session daytrading and successful swing delivery are not proven.

Live strategy-filtered run slices are distinguishable, but `code=LEASE_HELD` and `search=LEASE` are ignored or unproven on the active artifact. Run offset/page probes show distinct saved slices. Trade `status=filled` filtering returns mixed statuses, and `offset=10`/`page=2` repeat the first-page IDs beginning **645, 644, 643, 642, 641, 640, 639, 638, 637, 636**. Lifecycle columns exist, but sampled filled `gross`/`fee`/`net` remain null under conservative `unavailable_fill_lot_exact` / `none-recorded` accounting.

No trading code or behavior changed for Control-66. Local filtered-run and crypto edge-gate regressions are present. Validation results and the verified Wrangler blocker (`You are not authenticated. Please run \`wrangler login\`.`) are recorded in `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-66.md`. No deployment or broker mutation occurred.
## Monday, August 24, 2026 Control-65 daytrading position ownership correction - LOCAL PASS / LIVE OPEN FAIL-DEGRADED

Control-65 applies the smallest confirmed source correction: the final daytrading broker-position synchronization write now persists `strategy: 'daytrading'`. Broker-authoritative quantities, prices, market values, and P&L remain unchanged, as does the existing D1 protective stop/take metadata fallback. Swing and crypto sync paths, caps **5000/3700/2000 USD**, schedules, thresholds, sizing, fee freshness, edge policy, order semantics, and trading behavior are unchanged.

Focused and full regression validation is rerun on the exact current tree after the correction. The August 10 runbook candidate section is explicitly historical/superseded. No deployment, endpoint mutation, trigger, submit/cancel/close/replace/retry, migration, or broker mutation was performed.

Production remains **OPEN FAIL/DEGRADED**, not healthy: live `/health=1.0.0` and `/api/config.version=2.4.0` versus local release **2.6.0**; `/api/positions` remains broker-authoritative (`source=alpaca`, 29 rows) but stale timestamps and a prior unattributed MSTR row remain unresolved; live run/trade filters, pagination, aliases, candidate counters, lease evidence, and exact stock fill accounting remain unproven or ignored on the old artifact. Crypto and reconciliation delivery are fresh, crypto aggregate gross/fee/net reconciles, current Monday daytrading/swing execution is not proven from `/api/runs`, and Wrangler remains blocked by **`You are not authenticated. Please run \`wrangler login\`.`** See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-65.md`.

## Final separate GET-only post-correction verification

Captured after the local correction on Monday, August 24, 2026, approximately 11:20 UTC. All six required endpoints returned HTTP 200. The active Worker remains unchanged and uncorrected: `/health.version=1.0.0`, `/api/config.config.version=2.4.0`, and no `release_version`; local source remains release 2.6.0 and was not deployed. `/api/positions` remains `positionsAvailable=true`, `source=alpaca`, with 29 rows. Dashboard equity is 98477.43 versus `last_equity=98504.5039`, latest snapshot equity is 98456.97, and caps remain 5000/3700/2000 USD. `/api/runs` is fresh through 11:20:50 UTC with reconciliation and crypto delivery, while current daytrading/swing execution is not proven in the returned window; `code=LEASE_*` and `search=LEASE` probes return the same recent page, and `/api/trades?status=filled`, `offset=10`, and `page=2` return the same first-page IDs with no pagination metadata. Wrangler still reports `You are not authenticated. Please run \`wrangler login\`.` Production therefore remains **OPEN FAIL/DEGRADED**, not healthy; no deployment or broker mutation occurred.

## Monday, August 24, 2026 Control-64 strict read-only production control - OPEN FAIL/DEGRADED

Control-64 records supplied GET-only evidence from approximately **11:00 UTC on Monday, August 24, 2026**. All six required endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned **HTTP 200**, but production remains **OPEN FAIL/DEGRADED**, not healthy: live health is **1.0.0** and config is **2.4.0** versus local release **2.6.0**, so source/release provenance is unresolved.

Positions remain broker-authoritative: `source=alpaca`, `positionsAvailable=true`, **29 rows**. Dashboard account equity is **98474.26** versus `last_equity=98504.5039`; latest snapshot is **98494.79**; broker daily fields are zero. Caps remain exactly **5000/3700/2000 USD**. Local four-cron wiring and current crypto/reconciliation delivery remain present; Monday daytrading and swing have no current run yet because their schedules begin at **13:00 UTC** and **22:00 UTC**. Historical swing run **3182** recorded Cloudflare subrequest exhaustion.

Live filtered rows omit aliases and candidate counts; code/search/status filters plus trade pagination are not proven or are ignored on the old artifact. Lifecycle fields are present, but sampled `gross`/`fee`/`net` remain unavailable under `unavailable_fill_lot_exact` / `none-recorded`. Local crypto edge admission is fail-closed and not live-proven. This is documentation/status-only: no caps, schedules, thresholds, sizing, fee freshness, edge policy, order semantics, or trading behavior changed. No deployment is required or possible from this state; Wrangler is blocked by **`You are not authenticated. Please run \`wrangler login\`.`**. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-64.md`.

## Monday, August 24, 2026 Control-63 crypto calibrated-edge correction - LOCAL PASS / LIVE PROVENANCE DEGRADED

Control-63 proves the conservative crypto edge contract end to end: `generateSignal` emits no `rawEdgeBps`, crypto decision preparation keeps it unavailable, and `RiskManager` configured with `requireCalibratedEdge=true` and `minEdgeAfterCosts=8` rejects BUY with **`Calibrated raw edge unavailable`**. Existing injected-edge pass/reject coverage remains. No raw-edge producer or trading-behavior change was introduced; a separately versioned, out-of-sample producer remains a future decision. There is also a deferred candidate wiring issue: preparation names candidate-level `rawEdgeBps` from `candidate.signal.rawEdgeBps`, while the decision loop consumes `signal` and does not consume that candidate-level property. Keep this as unavailable/fail-closed until corrected. Any future artifact must carry immutable model ID/version/checksum; target horizon, bar interval, and timezone; dataset ID/version, universe, schema, cutoff, and exact train/calibration/validation windows; leakage-controlled out-of-sample validation splits; calibration method; sample counts and confidence intervals; realized gross/net-after-cost metrics and acceptance thresholds; and the exact candidate-level-to-decision-level `rawEdgeBps` mapping.

Local cron wiring is unchanged: daytrading `*/5 13-21 * * 1-5` → `cron`; swing `0 22 * * 1-5` → `swing_cron`; crypto `7-59/30 * * * *` → `crypto_cron`; reconciliation `*/10 * * * *` → `reconcile_cron`. On Monday, August 24, 2026, daytrading is not due before **13:00 UTC** and swing is not due before **22:00 UTC**. Historical swing **3182** had a subrequest failure. Live release drift remains **1.0.0/2.4.0** versus local **2.6.0**. `POST /api/trigger` is a no-op for this control and was not called.

Exact gross/net remain unavailable by schema and lot identity; no values are inferred. No calibrated edge producer exists. Fee freshness remains **60s** versus **10m** maintenance, which is suppressive and must not be loosened. No cap, schedule, threshold, sizing, fee-freshness, order-semantics, or trading change was made. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-63.md`.

## Monday, August 24, 2026 Control-62 operational status - OPEN FAIL/DEGRADED

Control-62 was a strict GET-only production control at approximately **10:00 UTC on August 24, 2026**. All six required endpoints returned HTTP 200, but live release identity remains unresolved: `/health=1.0.0` and `/api/config.version=2.4.0` versus local release `2.6.0` at clean commit `82e6c7f7da0ae7914d98224c1583389590fdac6f`.

Positions passed the authority check with `positionsAvailable=true`, `source=alpaca`, and 29 broker rows. Account equity `98485.98` and latest snapshot equity `98493.96` are below `last_equity=98504.5039`, so comparison direction is down; broker daily fields remain zero. Caps remain `5000/3700/2000 USD`.

Local schedules and dispatch remain exact: daytrading `*/5 13-21 * * 1-5` → `cron`, swing `0 22 * * 1-5` → `swing_cron`, crypto `7-59/30 * * * *` → `crypto_cron`, and reconciliation `*/10 * * * *` → `reconcile_cron`. Live crypto delivery remains near `:07/:37 UTC`; reconciliation remains near ten-minute cadence with `MAINTENANCE_ONLY` rows. Daytrading and swing are not yet due at this capture time on Monday, August 24, 2026, so later natural delivery remains pending rather than classified as a missed run.

Structured skips are visible and no current `CYCLE_LEASE_HELD` row was observed. Lifecycle fields exist, but sampled filled trades retain null gross/fee/net under conservative unavailable-accounting markers. Repository review confirms gross is intentionally not inferred, fees require exact broker order links, and the fields are synthetic read-time values because authoritative fill-lot attribution is not persisted. Live aliases/candidate counters and complete filter/pagination behavior remain unproven on the old artifact; local broker authority, bounded reconciliation, filtered observability, stable pagination, conservative accounting, and crypto fail-closed edge-gate regressions pass.

No new safe runtime defect was established. This is a documentation-only correction with no cap, schedule, strategy, edge-gate, sizing, order, or broker-state change. Wrangler deployment is blocked by **`You are not authenticated. Please run \`wrangler login\`.`** See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-62.md`.

## Monday, August 24, 2026 Control-61 operational status - OPEN FAIL/DEGRADED

Control-61 was a strict GET-only production control captured around 09:00 UTC. All six required endpoints returned HTTP 200, but live release identity remains unresolved: `/health=1.0.0` and `/api/config.version=2.4.0` versus local release `2.6.0` at commit `82e6c7f7da0ae7914d98224c1583389590fdac6f`.

Positions passed the authority check with `positionsAvailable=true`, `source=alpaca`, and 29 broker rows. Equity direction was down versus `last_equity=98504.5039` (`98458.01` account equity and `98440.07` latest snapshot); broker daily fields remain zero. Caps remain `5000/3700/2000 USD`.

Local schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`, mapped to `cron`, `swing_cron`, `crypto_cron`, and `reconcile_cron`. Live crypto delivery is fresh near `08:07:57` and `08:37:58 UTC`, and reconciliation is fresh at `08:50:52` and `09:00:57 UTC` as `MAINTENANCE_ONLY`; successful current daytrading and swing delivery remain unproven.

Structured skip reasons are visible and no current `CYCLE_LEASE_HELD` row was observed. Lifecycle identifiers and timestamps are present, while sampled filled trades retain null gross/fee/net with conservative unavailable-accounting markers. Live candidate counters/aliases and complete filter/pagination behavior remain unproven, and local fail-closed crypto fee/calibrated-edge wiring is not live-proven.

No new safe runtime defect was found. This is a documentation-only correction with no cap, schedule, strategy, edge-gate, sizing, order, or broker-state change. Wrangler deployment is blocked by **`You are not authenticated. Please run \`wrangler login\`.`** See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-61.md` for evidence and follow-up.

## Monday, August 24, 2026 Control-60 targeted reliability correction - LOCAL COMPLETE / LIVE OPEN FAIL-DEGRADED

Control-60 fixed five non-vital lifecycle/accounting defects without changing caps, thresholds, max-trade limits, strategy universes, schedules, sizing, signals, edge policy, or order semantics. Ambiguous crypto submit exceptions now retain reservations until read-only reconciliation; crypto duplicate admission blocks matching non-terminal or already-partially-filled client IDs; terminal partial fills retain reservation protection; bounded maintenance removes only expired active reservation orphans with no linked trade/order; and all D1 close callers now persist unavailable realized P&L as `NULL` rather than using broker snapshot `unrealized_pl` or zero.

Validation passed **85 focused tests / 272 assertions** and **197 full tests / 738 assertions**, with `bun run typecheck` and `git diff --check` passing. Required live checks were GET-only. The live Worker remains degraded and release-unverified: health `1.0.0`, config `2.4.0`, versus local release `2.6.0`; positions remain broker-authoritative (`source=alpaca`, 29 rows); reconciliation and crypto schedules are delivering; but live trade filtering/pagination, exact per-fill gross/fee/net, reservation state, synchronized account/snapshot values, current swing success, and source provenance remain unresolved. No deployment or broker mutation was performed. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-60.md`.

## Monday, August 24, 2026 Control-59 operational status - OPEN FAIL/DEGRADED

**Capture:** August 24, 2026, around 08:01 UTC; GET-only, no deployment or mutation. All six required GET endpoints returned HTTP 200. Live health/config are `1.0.0`/`2.4.0` versus local `2.6.0` at HEAD `e805da1a4d83a8fa816ebe09c500a57fed5c9c24`; live release provenance is unresolved.

**State:** Positions are broker-authoritative (`source=alpaca`, 29 rows). Equity is `98485.98`, latest snapshot `98493.96`, below `last_equity=98504.5039`; broker daily fields are zero. Caps remain `5000/3700/2000 USD`. Local dispatch remains exact: `*/5 13-21 * * 1-5` → `cron`, `0 22 * * 1-5` → `swing_cron`, `7-59/30 * * * *` → `crypto_cron`, `*/10 * * * *` → `reconcile_cron`.

**Freshness/observability:** Crypto runs were near `07:07:54` and `07:38:11 UTC`; reconcile was `08:01:06 UTC` with `MAINTENANCE_ONLY`. Current daytrading and successful swing freshness are not proven; swing `3182` historically errored with Cloudflare subrequest exhaustion. Structured skips exist; no current `CYCLE_LEASE_HELD` row was observed. Live aliases/candidate counters and run filters, trade status filtering, and pagination are absent, ignored, or unproven.

**Accounting/release:** Lifecycle fields exist, but filled `gross`/`fee`/`net` are null under `unavailable_fill_lot_exact` / `none-recorded`; exact historical fee artifacts are not current per-fill truth. Local crypto fail-closed edge-gate behavior and regressions pass, but live deployment is not proven. Control-57 receipt discrepancy: `189/705` documented versus `184/678` saved. Wrangler blocker: **`You are not authenticated. Please run \`wrangler login\`.`** Dirty worktree; no deployment/mutation.

**Disposition:** Documentation/status correction only; no additional runtime fix is justified and no caps, schedules, thresholds, sizing, edge-gate policy, order semantics, or trading behavior changed. Follow-up: authenticate Wrangler, isolate a clean immutable artifact, deploy only if authorized under the standing reliability-maintenance rule, then separate GET-only verification and natural weekday swing observation.

## Monday, August 24, 2026 Control-58 operational status - OPEN FAIL/DEGRADED

The strict read-only control used only GET requests. All six required endpoints returned HTTP 200, but live release identity remains unresolved: health `1.0.0`, config `2.4.0`, local release `2.6.0` at `e805da1`. Positions remain broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows); equity is down versus `last_equity=98504.5039` while broker daily fields are zero; caps remain `5000/3700/2000 USD`.

Local four-schedule configuration and dispatch remain correct. Live crypto delivery is fresh near `:07/:37 UTC` through `06:37:55`, and reconciliation is fresh near ten-minute cadence through `07:00:56` as `MAINTENANCE_ONLY`. Current daytrading freshness and successful swing freshness are not proven; swing `3182` remains an error with prior subrequest exhaustion and sync-absence evidence.

Live code/search and lease-held filters are ignored or cannot verify lease state; candidate counters and trigger aliases are absent. All live trade status filters tested return the same mixed set, and offset/page pagination repeats the first page. Lifecycle fields are present, but exact per-fill gross/fee/net remain conservatively unavailable. Local broker authority, bounded maintenance, lifecycle/accounting, filtered observability, pagination, and crypto fail-closed edge/fee controls pass regression tests but are not live-proven.

Control-58 changed documentation/status only. Focused `46/261`, full `189/705`, typecheck, and diff-check passed. A prior Control-57 receipt discrepancy remains open: its documentation says `189/705`, while the saved full receipt says `184/678`. No deployment or broker mutation occurred. Wrangler is unauthenticated and the worktree is dirty, so deployment remains blocked. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-58.md`.

## Monday, August 24, 2026 Control-57 strict read-only production control and correction - OPEN FAIL/DEGRADED

Control-57 used only GET observation against the six required production endpoints and safe run/trade filter and pagination probes. All six returned HTTP 200, but the active Worker remains **OPEN FAIL/DEGRADED**: live `/health=1.0.0` and `/api/config.version=2.4.0` do not match local release **2.6.0** at HEAD `e805da1`. Live `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and 29 rows, so broker positions remain authoritative. Account equity is `98470.34` versus `last_equity=98504.5039` and `change_today=0`; caps remain exactly **5000/3700/2000 USD**.

Control-57 completed two reliability-only local corrections. Crypto now records `RECONCILIATION_DEFERRED_TO_MAINTENANCE` and no longer duplicates ledger/order reconciliation already owned by the lease-protected ten-minute maintenance lane. All three strategy lanes restore a bounded persisted equity window before rolling-drawdown evaluation. No cap, schedule, threshold, sizing, signal, order, or broker-authority behavior changed.

Local schedule and regression evidence passes for all four crons and dispatch paths. Separate live verification is fresh through reconciliation run `3248` at `06:10:49 UTC` and crypto run `3247` at `06:07:54 UTC`, with the expected near-ten-minute and `:07/:37 UTC` cadence; structured market-closed/fee/confidence/maintenance skips are visible, and historical swing run `3182` still records subrequest exhaustion and incomplete accepted exits. Current daytrading/swing success freshness and lease-held absence are not proven from the live page.

Live run filters/aliases/candidate counters remain absent or ignored, live trade status filtering is ignored, and offset/page probes repeat the first trade page. Lifecycle fields are present, while exact per-fill gross/fee/net remain unavailable by design under `unavailable_fill_lot_exact` / `none-recorded`; aggregate strategy/account-level arithmetic is not a substitute for deterministic fill-lot attribution. Position timestamps/protective levels and active four-schedule provenance remain unresolved.

Validation receipts: focused **88/391**, full **189/705**, typecheck passed, and diff-check passed. No deployment or broker mutation occurred. Wrangler is unauthenticated with exact error **`You are not authenticated. Please run \`wrangler login\`.`**; the dirty worktree blocks deployment of an immutable artifact. Keep production **OPEN FAIL/DEGRADED** until authenticated provenance, clean release handling, and separate post-release GET-only verification succeed. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-57.md`.

Control-56 is a documentation/status correction based on the saved GET-only audit in `/workspace/audit-2026-08-24`. All six required GET checks (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200, but production remains **OPEN FAIL/DEGRADED**: live health/config are **1.0.0/2.4.0** versus local release **2.6.0** at `e805da1`. Positions remain broker-authoritative (`source=alpaca`, 29 rows); equity is below `last_equity=98504.5039` on the saved account/snapshot comparisons, while daily change fields are zero. Caps remain **5000/3700/2000 USD** and all four local schedules are unchanged.

Live crypto delivery remains near **:07/:37 UTC** with recurring fail-closed `FEE_DATA_UNAVAILABLE` skips; reconciliation remains near **10-minute** cadence as `MAINTENANCE_ONLY`. Daytrading freshness is limited by the latest carried-forward `MARKET_CLOSED` row, and the latest swing run **3182** errored with 8 errors including **Too many subrequests by single Worker invocation** plus broker-authoritative sync absence evidence. Lifecycle fields exist, but sampled `gross`, `fee`, and `net` remain null under `unavailable_fill_lot_exact` / `none-recorded`. Live filters, pagination, candidate fields, and provenance remain unproven or ignored. No missing current-source reliability fix was established; no runtime, trading, cap, schedule, or broker change was made.

Validation receipts: focused Alpaca regressions, full `bun test`, `bun run typecheck`, and `git diff --check` are saved under `/workspace` with the `alpaca_control_56_` prefix. Deployment remains blocked by exact Wrangler error **`You are not authenticated. Please run \`wrangler login\`.`** Safe follow-up is authenticated provenance, clean immutable artifact review, separately authorized deployment only if required, then separate GET-only post-release verification and a legitimate weekday swing run. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-56.md`.

## Monday, August 24, 2026 Control-55 operational status - OPEN FAIL/DEGRADED

The strict GET-only control at approximately **2026-08-24 04:00 UTC** confirmed all six required endpoints returned HTTP 200, but the active Worker is not release-verifiable: `/health=1.0.0` and `/api/config.version=2.4.0`, while the local tested release is `2.6.0` at HEAD `e805da1a4d83a8fa816ebe09c500a57fed5c9c24`.

Live positions are explicitly broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows). Final account equity was **98471.10** versus **last_equity=98504.5039**, a downward comparison of **33.4039**; `change_today=0`. Displayed caps remain **5000/3700/2000 USD**. Local source retains four schedules and separate leases for daytrading, swing, crypto, and maintenance.

Reconciliation is fresh and bounded as `MAINTENANCE_ONLY` near the ten-minute cadence; crypto is fresh near `:07/:37` UTC. Daytrading and swing freshness are not proven in the current returned page, and swing run `3182` remains a live historical error with Cloudflare subrequest exhaustion and incomplete accepted exits. No current `CYCLE_LEASE_HELD` row is visible, so lease-held delivery remains unproven.

Live run aliases/candidate counters and run code/search filtering remain absent or ignored. Trade status filtering and offset/page pagination remain unhonored. Lifecycle fields are present, but sampled gross/fee/net remain null under `unavailable_fill_lot_exact` / `none-recorded`; exact per-fill economics cannot be verified. Local crypto fee/calibrated-edge gating is regression-tested and fail-closed but not live-proven.

Control-55 correction is documentation/status-only. Focused validation passed **75/337**, full validation passed **184/678**, typecheck passed, and `git diff --check` passed. Wrangler is unauthenticated and the worktree is dirty, so deployment is blocked; production remains **OPEN FAIL/DEGRADED**. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-55.md`.

## Monday, August 24, 2026 Control-55 operational status - OPEN FAIL/DEGRADED

Control-55 is the current strict read-only production-control status. All six required GET endpoints returned HTTP 200, but live release identity remains unresolved: `/health` reports `1.0.0` and `/api/config` reports `2.4.0`, while the local tested release is `2.6.0` at HEAD `e805da1a4d83a8fa816ebe09c500a57fed5c9c24`. Live positions remain broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows), and displayed caps remain exactly `$5,000/$3,700/$2,000` for daytrading/swing/crypto.

The current `/api/runs` page only contains skipped `reconcile_cron` and `crypto_cron` rows, with reconciliation at `03:50:48` UTC and crypto at `03:07:57`/`03:37:54` UTC; daytrading and swing freshness are not proven in that page. Historical swing run `3182` errored with Cloudflare subrequest exhaustion. Live run filters/aliases/candidate counters and trade status/offset/page pagination were absent or ignored. Lifecycle fields exist, but sampled gross/fee/net remain null under `unavailable_fill_lot_exact`/`none-recorded`. Local four-schedule, broker-authority, conservative-accounting, and crypto fail-closed gate evidence passes; live proof, lease-held absence, full cap enforcement, and active four-schedule provenance remain open.

No caps, schedules, sizing, thresholds, edge-gate policy, order semantics, trading behavior, deployment, or broker state changed. No production endpoint was called by this documentation correction. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-55.md`; production remains **OPEN FAIL/DEGRADED** pending authenticated provenance, a clean immutable artifact, and separate GET-only post-release verification.

## Monday, August 24, 2026 Control-54 operational status - OPEN FAIL/DEGRADED

The strict production control at approximately **2026-08-24 03:00 UTC** used only GET requests against the six required endpoints and read-only filter/pagination probes. All required endpoints returned HTTP 200, but the active Worker is not release-verifiable: `/health` is **1.0.0** and `/api/config` is **2.4.0**, while the local tested release is **2.6.0** at HEAD **e805da1a4d83a8fa816ebe09c500a57fed5c9c24**.

Current live facts: positions are available and explicitly broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows); account equity is **98527.48** versus **last_equity=98504.5039**; displayed caps remain **5000/3700/2000 USD**. Reconciliation is fresh near the ten-minute cadence as bounded **MAINTENANCE_ONLY**. Crypto is fresh near **:07/:37** with structured fee/confidence skips and zero errors. Daytrading delivery is visible as market-closed five-minute runs. The latest observed swing run **3182** failed with eight errors, including Cloudflare subrequest exhaustion and incomplete accepted exits.

The local implementation and tests preserve the four UTC schedules, broker authority, isolated read-only reconciliation, filtered run aliases/code/search/status handling, stable pagination, lifecycle fields, conservative accounting, and crypto fail-closed fee/calibrated-edge gates. Live rows still omit those newer observability fields, ignore the tested filters/pagination, and retain null exact per-fill gross/fee/net under `unavailable_fill_lot_exact` / `none-recorded`; these are deployment/source-skew and accounting evidence gaps, not grounds to fabricate values or weaken controls. Lease-held absence, complete cap enforcement, explicit UTC encoding, and active four-schedule provenance remain unproven.

Control-54 validation: focused **95 tests / 413 assertions across 9 files**, full **184 tests / 678 assertions across 26 files**, typecheck passed, and `git diff --check` passed. No runtime, cap, schedule, sizing, threshold, trading-behavior, deployment, or broker-state change was made. Wrangler remains unauthenticated and the worktree dirty, so deployment is blocked pending a clean authenticated release path.

## August 24, 2026 Control-53 documentation/status correction - OPEN FAIL/DEGRADED

Control-53 is the current control after Control-52. Production remains **OPEN FAIL/DEGRADED**: all six required GET endpoints previously returned HTTP 200, but live health/config remain `1.0.0`/`2.4.0` versus local `2.6.0` at HEAD `e805da1a4d83a8fa816ebe09c500a57fed5c9c24`; positions remain broker-authoritative (`source=alpaca`, 29 rows), and equity direction is ambiguous (`98497.23-98499.29` versus `last_equity=98504.5039`, `change_today=0`). Caps and local four schedules remain unchanged: `5000/3700/2000`, daytrading `*/5 13-21`, swing `0 22`, crypto `7-59/30` at `:07/:37` UTC, reconciliation `*/10`.

Crypto was fresh around `01:07:55`/`01:37:55`, reconciliation remained near ten-minute cadence, daytrading `3180` was `MARKET_CLOSED`, and swing `3182` errored with 8 errors including Cloudflare subrequest exhaustion. Lease-held is not proven; live rows omit aliases/candidate counts, ignore run code/search and trade status filters, and repeat trade IDs `645/644/643` across offset/page probes. Lifecycle gross/fee/net remain null under `unavailable_fill_lot_exact`/`none-recorded`; local crypto fail-closed wiring passes but is not live-proven. One live schedule artifact lists three crons and omits reconciliation, conflicting with the local/post-release four-schedule capture; historical daytrading exposure `5679.8784` versus `5000` remains an open reconciliation question.

Documentation/status only: no runtime code, tests, schema, caps, schedules, sizing, thresholds, trading behavior, deployment configuration, deployment, or broker state changed. Current receipt is focused **87 passed/388 assertions across 9 files**, full **184 passed/678 assertions across 26 files**, typecheck passed, and Alpaca-repo diff-check passed. Broader workspace diff-check has unrelated pre-existing `data/qdrant/**/LOG` trailing whitespace. `bunx wrangler whoami` remains blocked by **`You are not authenticated. Please run \`wrangler login\``**; the worktree is dirty, so never deploy uncommitted files. Follow up with authenticated provenance, schedule reconciliation, clean immutable commit, separately authorized deployment only if required, separate GET-only verification, a natural weekday swing run, and historical cap-exposure reconciliation. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-53.md`.

## August 24, 2026 Control-52 strict read-only production control - OPEN FAIL/DEGRADED

The strict read-only production control captured around `2026-08-24T02:00Z` remains **OPEN FAIL/DEGRADED**, not healthy. Only GET observation was used; all six required endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. `/health` was `status=ok`, `version=1.0.0`; `/api/config` was `version=2.4.0` versus local release `2.6.0` at HEAD `e805da1a4d83a8fa816ebe09c500a57fed5c9c24`, leaving active provenance unresolved.

Positions were broker-authoritative with `positionsAvailable=true`, `source=alpaca`, and 29 rows. Equity was observed around `98497.23-98499.29` versus `last_equity=98504.5039` and `change_today=0`, so reliable direction is ambiguous/degraded. Caps are exactly `5000`, `3700`, and `2000` USD. Local schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` at `:07/:37` UTC, and reconciliation `*/10 * * * *`.

Fresh crypto runs were observed at `01:07:55` and `01:37:55`; reconciliation was around every 10 minutes; daytrading run `3180` was `MARKET_CLOSED`; swing run `3182` was `error` with 8 errors including Cloudflare subrequest exhaustion. Structured skip/error history exists, but current lease-held is not proven. Live run rows omit `trigger_alias`, `analyzed_candidates`, and `filtered_candidates`; run code/search filters were ignored; trade `status=filled` filtering was ignored; and offset/page repeated IDs `645,644,643`. Lifecycle fields exist, but sampled `gross`/`fee`/`net` remain null under `unavailable_fill_lot_exact` with `none-recorded` fee attribution.

Local crypto fee/calibrated-edge fail-closed wiring and regressions pass but are not live-proven. No additional runtime fix is justified because local source already contains the filter, pagination, broker-authority, lifecycle/accounting, reconciliation-boundary, and crypto edge-gate corrections. Local validation is recorded as focused **87 passed/388 assertions across 9 files**, full **184 passed/678 assertions across 26 files**, typecheck passed, and diff-check passed. No runtime, cap, schedule, sizing, threshold, schema, trading-behavior, deployment, or broker-state change was made.

The exact blocker is `bunx wrangler whoami`: **`You are not authenticated. Please run \\`wrangler login\\``**. The worktree is dirty; never deploy uncommitted files. No deployment or broker mutation occurred. Follow-up is authenticated provenance, a clean immutable commit, separately authorized deployment only if still required, separate GET-only verification, and a natural weekday swing run. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-52.md`.

## August 24, 2026 Control-51 strict read-only production control - OPEN FAIL/DEGRADED

Control-51 used only GET requests and preserved the production boundary. All six required endpoints returned HTTP 200, but live release identity remains `1.0.0`/`2.4.0` versus local `2.6.0`; positions remain available and broker-authoritative (`source=alpaca`, 29 rows); caps remain `$5,000/$3,700/$2,000`; and equity direction is mixed because account equity is above `last_equity` while `change_today=0`. Crypto and reconciliation delivery are fresh, daytrading emits structured `MARKET_CLOSED`, and the latest swing run `3182` still has eight Cloudflare subrequest-limit errors.

Live filtered-run aliases/candidate counters and stable trade pagination remain absent/unproven. Additional live filter defects were confirmed: run `code`/`search` filters and trade `status` filtering were ignored by the active artifact; the local read-only API correction now enforces them. Lifecycle fields exist, but sampled per-fill `gross`/`fee`/`net` remain conservatively unavailable under `unavailable_fill_lot_exact`; crypto fee telemetry is unavailable and live corrected edge-gate wiring is unproven. Aggregate fee math was reviewed and remains unchanged because account-level regulatory fees are subtracted exactly once. Focused validation passed **98/98 tests, 427 assertions**; full validation passed **184/184 tests, 678 assertions**; typecheck and diff-check passed. Final GET-only verification still observed the old live artifact; no cap, schedule, sizing, threshold, order, trading-behavior, deployment, or broker-state change was made. See `CORRECTION_WORK_ITEM_2026-08-24_CONTROL-51.md`; the exact deployment blocker is `You are not authenticated. Please run \`wrangler login\`.`

## August 23, 2026 Control-50 strict read-only correction - OPEN FAIL/DEGRADED

Control-50 used only GET requests against the six required production endpoints. Live release identity remains unresolved (`/health=1.0.0`, `/api/config.version=2.4.0` versus local `2.6.0`), swing run `3182` failed with the Cloudflare subrequest limit, and the local filtered-run/pagination corrections remain unverified in production. Broker-authoritative positions and unchanged caps `$5,000/$3,700/$2,000` still pass; crypto cadence remains near `:07/:37` UTC and reconciliation/daytrading delivery remains visible with structured skips. Exact per-fill accounting remains conservatively unavailable, and no current lease-held row was visible in the returned history.

This correction fixes documentation drift only: the deployment runbook's obsolete expected current baseline of 85 tests/257 assertions is replaced by the current validated baseline of 184 tests/666 assertions across 26 files. No caps, schedules, sizing, thresholds, order semantics, trading behavior, deployment, or broker state changed. Production remains **OPEN FAIL/DEGRADED** pending authenticated provenance, deployment of the exact clean validated artifact if authorized and required, separate GET-only verification, and a natural weekday swing run without subrequest exhaustion. See `CORRECTION_WORK_ITEM_2026-08-23_CONTROL-50.md`.

## August 23, 2026 Control-49 strict read-only production control - OPEN FAIL/DEGRADED

The six required GET endpoints were reachable, but the active Worker is not release-verifiable: live health/config report `1.0.0`/`2.4.0` while the reviewed local tree is `2.6.0` at `e805da1`. A fresh swing run failed with `Too many subrequests by single Worker invocation`; live filtered run fields and stable trade pagination remain absent/broken, and exact per-fill gross/fee/net remain unavailable. Positions remain explicitly broker-authoritative (`source=alpaca`), configured caps remain `$5,000/$3,700/$2,000`, and observed crypto/reconciliation/daytrading delivery remains present, but these partial passes do not clear production. The local reliability corrections are already implemented and tested; do not infer live behavior from them until authenticated provenance, deployment, and separate GET-only post-release verification succeed.

## Sunday, August 23, 2026 Control-48 swing subrequest-limit correction - OPEN FAIL/DEGRADED

A fresh GET-only recheck observed swing run `3182` at `2026-08-23 22:01:16 UTC` ending `error` with 8 errors, including multiple swing sell failures and a fatal `Too many subrequests by single Worker invocation`. Root cause was duplicated swing ledger/order reconciliation plus synchronous `getOrder` polling after each exit submission, on top of scanner, market-data, refresh, and final-sync calls.

The local reliability correction removes duplicated `syncBrokerLedger` and `reconcileBrokerOrders` work from the swing lane, keeps bounded read-only reconciliation in the dedicated `reconcile_cron` schedule, and adds structured `RECONCILIATION_DEFERRED_TO_MAINTENANCE` evidence. Swing exits now support `waitForFill=false`, persist accepted/partial exits, and emit `EXIT_PENDING_RECONCILIATION` until later bounded reconciliation confirms the broker state. Normal order semantics, broker-authoritative positions, four schedules, risk gates, sizing, and caps `$5,000/$3,700/$2,000` are preserved; only synchronous confirmation fan-out is removed to fail safely under the Worker budget.

Focused validation passed **34 tests / 109 assertions** across the swing authority, order reconciliation, audit, and Alpaca order suites. Full validation passed **184 tests / 666 assertions across 26 files**; typecheck, diff-check, and scoped secret scan passed. A normal deployment attempt stopped before upload because Wrangler requires `CLOUDFLARE_API_TOKEN`; `wrangler whoami` reports `You are not authenticated. Please run \`wrangler login\`.` No deployment, preview, or broker mutation occurred. Separate GET-only postcheck returned HTTP 200 for all required endpoints and probes, but live remains the pre-correction 1.0.0/2.4.0 release with swing run 3182 subrequest failures, missing filtered fields, and repeated trade pagination IDs. Production remains **OPEN FAIL/DEGRADED** until authenticated provenance, deployment, and separate post-release verification succeed.

## Sunday, August 23, 2026 Control-47 strict read-only production control - OPEN FAIL/DEGRADED

**Audit capture: 2026-08-23T22:00:47Z.** All six required GET endpoints returned HTTP 200, but production is not healthy or release-verifiable. Live `/health.version` is `1.0.0`, live `/api/config.config.version` is `2.4.0`, while the local release is `2.6.0`; active Worker/source provenance is unresolved and authenticated Wrangler access is unavailable.

Positions are available and explicitly broker-authoritative: `positionsAvailable=true`, `source=alpaca`, 29 long positions, and no crypto positions. Dashboard/account equity is `$98,504.50`, `last_equity=98504.5039`, `change_today=0`; the current-day delta is effectively flat, while captured history declined to the current level. Capital caps remain exactly `$5,000/$3,700/$2,000` for daytrading/swing/crypto.

Local source retains the four UTC schedules: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` at `:07/:37`, and reconciliation `*/10 * * * *`. Live delivery shows fresh daytrading `MARKET_CLOSED`, crypto runs near `:07/:37` through `21:37:55 UTC`, and reconciliation `MAINTENANCE_ONLY` at `22:00:50 UTC` with 3 ledger activities and no degradation. Sunday has no legitimate fresh swing window; the latest dedicated `swing_cron` run is stale at `2026-08-18 22:00:36`, ended `error`, and recorded position divergence plus `RISK_HALTED`. Lease-held, provider-error, and structured skip history remains auditable, but no current lease-held row is visible in the returned page.

Live filtered-run responses omit `trigger_alias`, `analyzed_candidates`, and `filtered_candidates`. Live trade pagination is broken or ignored because `offset=0`, `offset=10`, and `page=2` repeat IDs `642,641,640`. Filled trades expose broker/client IDs, filled quantities, and lifecycle timestamps; sampled `gross`, `fee`, and `net` remain null under `unavailable_fill_lot_exact` with `none-recorded` attribution. Dashboard aggregate fees are `$272.32016882811` total, `$269.11016882811` crypto, and `$3.21` regulatory/account-level; aggregate gross/net arithmetic reconciles where reported but does not prove exact per-fill economics.

The local repository already contains reliability-only fixes and regression coverage for canonical release reporting, broker-authoritative position failure behavior, filtered-run fields, stable offset pagination, conservative accounting, and fail-closed crypto fee/calibrated-edge gating. No additional runtime fix is justified from this evidence. Control-47 is therefore a documentation/status correction; production remains **OPEN FAIL/DEGRADED**, deployment is not attempted, and the exact deployment blocker is `You are not authenticated. Please run wrangler login.` Follow up with authenticated provenance binding, separately authorized deployment if still required, a post-release GET-only verification, and the next legitimate weekday swing-window check.

## Sunday, August 23, 2026 Control-46 daytrading risk-rejection observability correction - OPEN FAIL/DEGRADED

The local reliability correction is complete and remains undeployed. Daytrading `RiskManager` rejections now emit bounded `SkipReasonCollector` events with stable `NO_ENTRY_RISK` or `CAPITAL_CAP` codes and durable strategy/symbol/decision/action/original-reason context. The existing `decisions.executed=2` update and plain `riskCheck.reason`, console log, rejection behavior, caps, sizing, schedules, broker authority, and all trading behavior are unchanged. Focused validation passed **18/18 tests, 59 assertions**; full `bun test` passed **182/182 tests, 657 assertions**; typecheck and `git diff --check` passed. Logs: `/workspace/alpaca_control_46_focused.txt`, `/workspace/alpaca_control_46_full.txt`, `/workspace/alpaca_control_46_typecheck.txt`, `/workspace/alpaca_control_46_diff_check.txt`. No rawEdgeBps producer or vital-cap change was introduced, and no deployment or broker-mutating endpoint was used. Production remains **OPEN FAIL/DEGRADED** because live release/source provenance is unresolved and Wrangler is blocked by `You are not authenticated. Please run wrangler login.` / `wrangler: command not found`. See `CORRECTION_WORK_ITEM_2026-08-23_CONTROL-46.md`.

## Sunday, August 23, 2026 Control-45 strict read-only production control - OPEN FAIL/DEGRADED

**Audit capture: `2026-08-23T21:01:21Z`.** All six required GET endpoints returned HTTP 200, but production is not healthy. Live `/health` reports `1.0.0` and `/api/config.version` reports `2.4.0`, while local HEAD is `e805da1a4d83a8fa816ebe09c500a57fed5c9c24` with release `2.6.0`; active Worker/source provenance is unresolved.

Live positions pass the availability contract as broker-authoritative: `positionsAvailable=true`, `source=alpaca`, 29 rows. Equity is `98504.50` versus `last_equity=98504.5039`, with `change_today=0`; the current-day delta is flat, but the observed history fell from `98556.33` at `2026-08-21 23:37:58` to `98504.50` by `2026-08-22 02:37:58` and remained flat through `2026-08-23 20:37:48`, so the broader observed equity direction is downward. Caps remain exactly `5000/3700/2000` USD. Local source retains daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` for `:07/:37`, and reconciliation `*/10 * * * *`; active deployed schedule provenance is not bound.

Fresh live daytrading delivery is structured `MARKET_CLOSED`, crypto delivery is present at `20:07:55` and `20:37:55 UTC` with zero trades and structured skips, and reconciliation is fresh at `20:50:48 UTC` as `MAINTENANCE_ONLY`. Sunday swing absence is inconclusive because its local schedule is weekday-only. Current structured skip details are visible, but no lease-held row is visible in the fetched page.

Live filtered runs omit `trigger_alias`, `analyzed_candidates`, and `filtered_candidates`; trade pagination remains unresolved because prior filtered/disjoint probes repeat IDs across offsets. Filled trades expose lifecycle timestamps and broker IDs, but exact `gross`/`fee`/`net` remain null under `unavailable_fill_lot_exact` with `none-recorded` attribution. Local filtered observability, conservative accounting, and crypto fail-closed calibrated-edge wiring are regression-tested but not live-proven.

This is a documentation/status-only correction recorded in `CORRECTION_WORK_ITEM_2026-08-23_CONTROL-45.md`; no runtime code, caps, schedules, trading behavior, deployment, or broker state changed. Focused/full regressions, typecheck, and diff-check are rerun after this update. Deployment remains blocked by unavailable authenticated Wrangler tooling, so separate authorized deployment and GET-only post-release verification remain follow-ups.

## Sunday, August 23, 2026 Control-44 reconciliation observability correction - OPEN FAIL/DEGRADED

**Exact defect and fix:** `reconcileBrokerOrders()` already counted individual read-only `getOrder` exceptions as `lookupFailures`, but scheduled maintenance omitted `pendingLookups`/`lookupFailures` from durable `MAINTENANCE_ONLY` context and left the run skip-only. `runScheduledMaintenance` now persists both counts, records `BROKER_ORDER_LOOKUP_DEGRADED` when `lookupFailures > 0`, and uses existing severity ordering: `error`, then `degraded`, then skip-only. The correction preserves the bounded read-only lookup path and changes no caps, schedules, sizing, trading behavior, broker authority, or broker calls.

`test/maintenance-reconciliation.test.ts` forces one read-only lookup failure, verifies durable context and `status=degraded`, and asserts only GET-style broker calls. The live service remains OPEN FAIL/DEGRADED because release/source provenance is unresolved, filtered-run/pagination behavior is stale, swing delivery is unproven, current crypto fee telemetry is unavailable, and exact trade-level gross/fee/net is not auditable. Wrangler remains blocked by `You are not authenticated. Please run wrangler login.` No deployment or broker mutation occurred. See `CORRECTION_WORK_ITEM_2026-08-23_CONTROL-44.md`.

## Sunday, August 23, 2026 Control-43 strict read-only production control - OPEN FAIL/DEGRADED

**Audit capture: 2026-08-23 19:55:49Z.** All six required endpoints returned HTTP 200 through GET-only verification. Live versions `1.0.0` and `2.4.0` are observations, not release identity; local HEAD is `e805da1` / release `2.6.0`, so active bundle/source provenance remains unresolved. Broker-authoritative positions remained available (`source=alpaca`, 29 rows); equity direction remained unverified because `change_today=0`; caps remained `5000/3700/2000` USD.

The live schedule artifact listed all four expected crons. Fresh daytrading was `MARKET_CLOSED`, crypto ran near `:07/:37` with zero trades and structured skips, and reconciliation was `MAINTENANCE_ONLY`; Sunday swing absence was inconclusive. Live filtered-run fields and trade pagination drifted from local behavior. Lifecycle fields existed, but exact per-fill `gross`/`fee`/`net` were null under `unavailable_fill_lot_exact`; dashboard fee availability versus run-level `FEE_DATA_UNAVAILABLE` remained unresolved by scope. This was documentation/status-only, with no runtime or deployment change.

## Sunday, August 23, 2026 Control-42 strict read-only production control - OPEN FAIL/DEGRADED

**Audit capture: 2026-08-23 19:00:49Z.** This control was GET-only across all six required endpoints. Live `/health` observed `1.0.0` and `/api/config` observed `2.4.0`, while local release `2.6.0` is at HEAD `e805da1`; active source/SHA and schedule provenance remain unresolved. Positions were broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows), equity was `98504.50` versus `last_equity=98504.5039` with `change_today=0`, and caps remained `5000/3700/2000` USD.

Local schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` (`:07/:37`), and reconciliation `*/10 * * * *`. Live daytrading and reconciliation delivery was fresh, crypto delivery was near `:07/:37` with structured zero-trade skips, while the inspected page did not expose `swing_cron`, which was inconclusive. Live filtered runs omitted local aliases/candidate counters, trade offsets repeated IDs, and exact per-fill `gross`/`fee`/`net` remained null under `unavailable_fill_lot_exact`; Wrangler was unauthenticated. No deployment or broker mutation occurred.

## August 22, 2026 Control-18 strict read-only production control - FAIL/DEGRADED

The **2026-08-22 23:00:16-23:00:17 UTC** control was GET-only and all six required endpoints returned HTTP 200. Keep production **OPEN FAIL/DEGRADED**, not healthy: live `/health=1.0.0` and persisted `/api/config.version=2.4.0` conflict with local deployable HEAD `131898b9e4cab3544ae9b793123c1c86d5763cdc`, version `2.6.0`.

The position contract is available only in the broker-authoritative sense: `positionsAvailable=true`, `source=alpaca`, 29 rows. Equity is `98,504.50` versus `last_equity=98,504.5039`, about `-0.0039`, with `change_today=0`, so material direction is unverified. Caps remain `$5,000/$3,700/$2,000`.

Local `wrangler.toml` and dispatch source retain four UTC schedules: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, and `*/10 * * * *`. Active deployed schedule identity is unresolved. Reconciliation delivered fresh `MAINTENANCE_ONLY` runs through `2889` at `23:00:53`; crypto delivered around `:07/:37`, including `22:07:57` and `22:37:56`. Daytrading has no run on August 21 or 22 and its newest filtered row is August 20 run `2556` with `CYCLE_LEASE_HELD`; swing has no fresh weekday proof and its newest known run is August 18 run `2200` with divergence and `RISK_HALTED`.

Structured lease/error/skip observability remains present. The live old release omits locally validated alias response fields, and candidate analyzed/filtered counts remain non-durable. Lifecycle fields are present, but sampled trades retain null gross/fee/net under `unavailable_fill_lot_exact`, so exact per-fill economics are not verifiable. Local crypto fee and calibrated-edge gates remain fail-closed, with live `FEE_DATA_UNAVAILABLE` skips and no positive-edge producer proof.

No source or configuration correction is justified. Control-18 is recorded in `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-18.md`; focused validation passed 78 tests / 327 assertions, full validation passed 168 tests / 584 assertions, and typecheck/diff checks passed. Wrangler reports **`You are not authenticated`**. Do not use a temporary preview or any trading/broker mutation; restore authentication, inspect provenance, obtain separate deployment authorization if needed, and then repeat a separate GET-only release and natural weekday verification. Evidence is preserved under `/workspace/alpaca-control-18-live-20260822T230209Z/`.

## August 22, 2026 Control-18 strict read-only production control - FAIL/DEGRADED

At **2026-08-22 23:00:16-23:00:17 UTC**, all six required GET endpoints (`/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, `/api/trades`) returned HTTP 200. Keep production **OPEN FAIL/DEGRADED, not healthy**: live health is `1.0.0`, config is `2.4.0`, local HEAD is `131898b9e4cab3544ae9b793123c1c86d5763cdc`, and deployable version is `2.6.0`; active release provenance is unresolved.

Positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows). Dashboard equity is `98504.50` versus `last_equity=98504.5039` (about `-0.0039`), with `change_today=0`, so material direction cannot be verified. Caps remain exactly `5000/3700/2000`. Local UTC schedules are daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`; active deployed four-schedule identity remains unresolved.

Fresh reconciliation runs `2888` at `22:50:50` and `2887` at `22:40:49` are structured `MAINTENANCE_ONLY`; fresh crypto runs `2886` at `22:37:56`, `2882` at `22:07:57`, `2878` at `21:37:56`, and `2874` at `21:07:57` are around `:07/:37` with structured skips. Saturday has no fresh daytrading/swing proof, and historical lease/error/risk skips remain. Live trades return 50 filled rows with submission/fill timestamps, null inapplicable terminal fields, null gross/fee/net, `unavailable_fill_lot_exact`, and `none-recorded`; aggregate arithmetic is not exact fill-lot proof. Live old output omits `trigger_alias`, and durable analyzed/filtered counts remain absent from `run_log`.

Local filtered-alias and crypto fee/`rawEdgeBps` fail-closed source/test wiring passes; live positive calibrated-edge producer evidence is unavailable. No code defect was isolated, so no trading/reliability code, config, cap, schedule, or edge-gate change is made. Wrangler's exact blocker is **`You are not authenticated`**. Required follow-up is authenticated Wrangler restoration, active provenance/schedule inspection, separate deployment authorization, deployment only if required, then separate GET-only verification. No trigger, submit, cancel, close, replace, retry, migration, deployment, or broker mutation was used. See `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-18.md`.

## August 22, 2026 Control-17 strict read-only production control - FAIL/DEGRADED

The **2026-08-22 22:00:07-22:00:09 UTC** production control was strictly GET-only. All six required endpoints returned HTTP 200, but production remains **OPEN FAIL/DEGRADED**, not healthy: live `/health=1.0.0` and persisted `/api/config.version=2.4.0` do not identify deployable source HEAD `1013f3dc979fd9b56a7cae1b843177bb3ab5f21f`, version `2.6.0`.

Positions pass the availability contract only: `positionsAvailable=true`, `source=alpaca`, and 29 broker rows. Equity is `98,504.50` versus `last_equity=98,504.5039`, approximately `-0.0039`; with `change_today=0`, material direction is unverified. Caps remain `$5,000/$3,700/$2,000`. Source/release artifacts retain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`, but conflicting captured schedule responses leave deployed four-schedule identity unverified.

Reconciliation is fresh and structured as `MAINTENANCE_ONLY` near ten-minute cadence, including run `2880` at `21:50:51 UTC`. Crypto is fresh at `21:07:57` and `21:37:56 UTC`, matching `:07/:37` with jitter. Saturday cannot prove weekday daytrading or swing delivery; filtered evidence remains stale at daytrading run `2556` (`CYCLE_LEASE_HELD`, August 20) and swing run `2200` (position divergence plus `RISK_HALTED`, August 18). Lease/error/skip reasons remain visible.

Filtered aliases return canonical rows but the live old release omits local `trigger_alias`; analyzed/filtered candidate counts remain a known source observability gap because they are not persisted in `run_log`. Trade lifecycle fields are present, but sampled filled rows conservatively retain null `gross`, `fee`, and `net` with `unavailable_fill_lot_exact` and `none-recorded` attribution. Crypto entry remains fail-closed because fee telemetry and calibrated `rawEdgeBps` are required, and no calibrated positive edge is supplied.

This is a documentation/status-only correction. Focused regressions passed **50 tests / 243 assertions**; the full `bun test` passed **168 tests / 584 assertions**; typecheck and diff checks passed. Do not change caps, schedules, broker authority, leases, accounting, edge gates, or trading behavior. Deployment is not performed until authenticated Wrangler access and separate authorization are available.

## August 22, 2026 Control-16 strict read-only production control - FAIL/DEGRADED

The **2026-08-22 21:00:23-21:01:42 UTC** control was strictly GET-only. All six required production endpoints returned HTTP 200, with timestamped bodies, headers, filtered-run captures, and checksums preserved under `/workspace/alpaca-control-16-live-20260822T2100Z/`. Keep production **FAIL/DEGRADED**, not healthy: live `/health=1.0.0` and persisted `/api/config.version=2.4.0` conflict with deployable source HEAD `4cc5df6c1cb7979ffefc7ddb751fdc8e1331d3cd` at version `2.6.0`.

The live position contract is currently healthy for availability only: `positionsAvailable=true`, `source=alpaca`, and 29 broker rows. Equity is `98,504.50` versus `last_equity=98,504.5039`, a rounding-level `-0.0039` delta with `change_today=0`; material current-day direction is unverified. Caps remain `$5,000/$3,700/$2,000`. Checked-out source retains the four UTC schedules: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and read-only reconciliation `*/10 * * * *`.

Reconciliation is fresh `MAINTENANCE_ONLY` delivery near ten-minute cadence, including `2026-08-22 21:00:54` run `2873`; crypto runs arrived at `20:07:59` and `20:37:59` near the expected `:07/:37`. Saturday does not prove weekday daytrading or swing delivery. Lease-held, `RISK_HALTED`, provider-error, `MAINTENANCE_ONLY`, `NO_POSITION_TO_EXIT`, and `FEE_DATA_UNAVAILABLE` records remain auditable. Alias filters return canonical rows, but live responses omit the locally validated response-only `trigger_alias` field.

Filled trade lifecycle fields are present, but sampled per-fill `gross`, `fee`, and `net` remain null under `unavailable_fill_lot_exact` with `none-recorded` attribution. Aggregate crypto gross/fee/net arithmetic is consistent but not exact fill-lot accounting. Local crypto edge admission remains fail-closed on missing fee telemetry or calibrated `rawEdgeBps`; live positive-edge evidence is not exposed.

No new code defect was isolated. This is a documentation/status-only correction in `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-16.md`; do not change caps, schedules, broker authority, leases, accounting, edge gates, or trading behavior. Focused regressions passed 54 tests / 263 assertions; full `bun test` passed 168 tests / 584 assertions; `bun run typecheck`, `git diff --check`, and synchronized documentation checks passed after the update. Wrangler reports **`You are not authenticated`**. Restore authenticated provenance, obtain separate deployment authorization, and only then consider deployment followed by separate GET-only release and natural weekday verification. No broker-mutating endpoint was used.

## August 22, 2026 Control-15 strict read-only production control - FAIL/DEGRADED

At **2026-08-22 20:00:26-20:00:28 UTC**, all six required production endpoints returned HTTP 200 through GET-only requests. Production remains **FAIL/DEGRADED, not healthy**: live `/health=1.0.0` and persisted `/api/config.version=2.4.0` conflict with checked-out deployable source `2.6.0`, so active Worker/source identity and the locally validated correction set remain unproven.

Current live evidence: `/api/positions` is broker-authoritative with `positionsAvailable=true`, `source=alpaca`, and 29 rows. Dashboard equity is `98,504.50` versus `last_equity=98,504.5039`, a `-0.0039` delta; `change_today=0`, so material equity direction is **CANNOT VERIFY** from this snapshot. Live config caps remain exactly `$5,000/$3,700/$2,000`. Source retains the four UTC schedules: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and read-only reconciliation `*/10 * * * *`.

Reconciliation is fresh and structured as `MAINTENANCE_ONLY` near the ten-minute cadence, while crypto runs are fresh near `:07/:37` with minute-level jitter. Saturday provides no fresh weekday daytrading or swing proof. Lease-held and error skips remain auditable, including reconciliation Alpaca 503 at `12:10:40 UTC` and retained SQL-variable/subrequest failures. Filtered-run outputs are constrained, but live responses omit the locally validated response-only `trigger_alias` field. Sampled filled trades expose lifecycle fields, while exact per-fill accounting remains conservatively unavailable with `gross=null`, `fee=null`, `net=null`, `accounting_status=unavailable_fill_lot_exact`, and `fee_attribution=none-recorded`. Local crypto gates remain fail-closed on missing fee telemetry or calibrated `rawEdgeBps`; live positive-edge evidence is unavailable.

No new code defect was isolated. Control-15 is a documentation/status-only correction work item: `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-15.md`. Local source/tests preserve broker-authoritative positions, all four schedules, filtered-run wiring, lifecycle/accounting semantics, leases, unchanged caps, and crypto edge-gate behavior. Wrangler remains blocked by **`You are not authenticated`**; no deployment or temporary preview was attempted. Focused/full regressions, typecheck, diff-check, and synchronized documentation updates passed for this work item: focused 54 tests/263 assertions, full 168 tests/584 assertions, typecheck passed, and diff-check passed.

Required follow-up: restore authenticated Wrangler access, inspect deployment provenance, obtain separate deployment authorization, deploy only the validated artifact if still required, then perform separate GET-only release and endpoint verification plus natural weekday strategy observation. No trigger, submit, cancel, close, replace, retry, migration, or broker-mutating endpoint was used.

## August 22, 2026 Control-14 strict read-only production control - FAIL/DEGRADED

At approximately **2026-08-22 19:02:10 UTC**, all six required production endpoints returned HTTP 200 through GET-only access. Live release identity remains unresolved: `/health=1.0.0` and persisted `/api/config=2.4.0` conflict with the checked-out deployable source `2.6.0`; live filtered-run responses also omit the locally validated response-only `trigger_alias` field. Keep production **FAIL/DEGRADED**, not healthy.

The broker contract is currently visible: `/api/positions` reports `positionsAvailable=true`, `source=alpaca`, and 29 broker rows, while dashboard equity is `98,504.50` versus `last_equity=98,504.5039`, a `-0.0039` current-minus-last delta; with `change_today=0`, material equity direction is **CANNOT VERIFY** from this snapshot. Local source retains the four UTC schedules, unchanged caps of `$5,000/$3,700/$2,000`, broker-authoritative position projection, bounded structured skip/error logging, isolated leases, and fail-closed crypto fee/calibrated-edge admission. Live reconciliation is delivering `MAINTENANCE_ONLY` skips near ten-minute cadence; crypto runs are present at `15:37:58`, `16:07:57`, `16:37:58`, `17:07:58`, `17:37:57`, `18:07:58`, and `18:37:58`, approximately matching `:07/:37` with jitter. No fresh weekday daytrading or swing delivery can be proven on Saturday.

Filtered alias requests return canonical rows but do not expose `trigger_alias` on the live release. Sampled filled trades retain lifecycle timestamps and broker fields, but all 50 rows have conservative `gross`, `fee`, and `net` null under `unavailable_fill_lot_exact` with `fee_attribution=none-recorded`; aggregate arithmetic is not exact fill-lot attribution. Structured skip reasons include `MAINTENANCE_ONLY`, `NO_POSITION_TO_EXIT`, `FEE_DATA_UNAVAILABLE`, and historical lease/error states.

No new code defect was isolated in this control. The correction is documentation/status-only and is recorded in `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-14.md`. Focused/full regressions, typecheck, and diff checks are rerun after the documentation update. Deployment is blocked by Wrangler's exact response **`You are not authenticated`**; do not use a temporary preview. After separately authorized authenticated deployment, repeat separate GET-only verification and natural weekday checks. No trigger, submit, cancel, close, replace, retry, migration, or broker-mutating route was used.

## Control-13 observability contract: `trades_executed` means full fills

`run_log.trades_executed` is a filled-count metric. It increments only after the broker confirms a full fill: order status `filled` with filled quantity at least 99.9% of the requested quantity. It does not count a submitted order, broker acceptance, a pending order, a rejection/cancellation, or a partial fill; those are lifecycle states, not fully filled trades.

The `trades` table records the broader order lifecycle, so submitted, accepted, pending, and partially filled orders may be present there even when `trades_executed` remains zero; only fully filled trades contribute to that count. Use the persisted trade status, filled quantity, leaves quantity, and broker lifecycle timestamps to distinguish order progress from the run's confirmed-full-fill count. This correction is documentation and regression coverage only; caps, schedules, leases, broker authority, edge gates, order behavior, and mutation boundaries are unchanged.

## August 22, 2026 Control-11 strict read-only production control — FAIL/DEGRADED

At approximately 17:00 UTC, the six required production GET endpoints all returned HTTP 200 without any trigger or broker mutation. Live release identity remains unresolved: `/health=1.0.0` and `/api/config` persisted version `2.4.0` conflict with local deployable `2.6.0`, and live trade responses lack the locally validated pagination/estimate-vs-fill correction. Positions remain broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 29 rows). Equity direction is rounding-level down, `98,504.50 - 98,504.5039 = -0.0039`.

Reconciliation is delivering structured `MAINTENANCE_ONLY` skips near the ten-minute schedule. Crypto is delivering around the configured `:07/:37 UTC` cadence, commonly at `:08/:38`, with structured fee/confidence/position skips. No fresh daytrading or swing proof is available in the Saturday run page. Lifecycle fields are populated on sampled filled trades, while exact per-fill `gross`, `fee`, and `net` remain conservatively null under `unavailable_fill_lot_exact`; aggregate strategy arithmetic is not a substitute for fill-lot attribution. Caps remain `$5,000/$3,700/$2,000`, and local four-schedule, broker-authority, filtered-run, and fail-closed crypto-edge regressions remain green.

Local state is ready on commit `b229eb3255097d5c6c13684a351ed2d867731021`; focused validation passed 42 tests / 204 assertions and full validation passed 164 tests / 562 assertions, with TypeScript and diff-check passing. Deployment is blocked because Wrangler reports `You are not authenticated`. Keep production **FAIL/DEGRADED**. After authenticated, separately authorized deployment, first perform read-only release/endpoints/schedule/filter/cap/source/lifecycle/accounting checks, then observe natural weekday daytrading and swing delivery. If any acceptance check fails, restore traffic to the last known-good authenticated deployment and repeat GET-only verification. Never use trigger, submit, cancel, close, replace, retry, migration, or other broker-mutating routes as smoke tests.

## August 22, 2026 Control-12 strict read-only production control - FAIL/DEGRADED

At approximately 18:00 UTC, all six required production GET endpoints returned HTTP 200 without any trigger or broker mutation. Live identity remains unresolved: `/health=1.0.0` and `/api/config=2.4.0` conflict with local deployable `2.6.0`, while live trades still lacks the locally validated pagination/enrichment fields. Positions remain broker-authoritative (`source=alpaca`, 29 rows), and equity is `98,504.50 - 98,504.5039 = -0.0039`.

Reconciliation continues with fresh skips near ten-minute cadence, and crypto skips arrive around `:07/:37 UTC` with observed records at `17:07:58` and `17:37:57`. No fresh weekday daytrading or swing evidence is available on Saturday. Filled lifecycle timestamps are present, but exact gross/fee/net and filled-notional enrichment remain null under `unavailable_fill_lot_exact`; caps remain `$5,000/$3,700/$2,000`.

Local four-schedule, filtered-run, broker-authority, and fail-closed crypto-edge regressions remain green. The reliability correction is validated on `b229eb3255097d5c6c13684a351ed2d867731021`, but Wrangler is unauthenticated, so keep production **FAIL/DEGRADED** and restore authentication before any separately authorized deployment and GET-only verification.

No trigger, submit, cancel, close, replace, retry, migration, or other broker-mutating route was used.

## August 22, 2026 GET `/api/trades` pagination correction - locally validated, not deployed

The contained read-only correction now applies `offset` to `GET /api/trades` queries, supports page-to-offset requests, and returns `limit`, `offset`, and `page` metadata. Stable ordering prevents repeated leading records across pages. No trading behavior changed: strategy filters, the 500-record endpoint cap, broker authority, `$5,000/$3,700/$2,000` caps, schedules, edge gates, and mutation boundaries remain unchanged.

Focused validation passed **28 tests / 157 assertions**; full `bun test` passed **163 tests / 560 assertions**; `bun run typecheck` and `git diff --check` passed. No deployment or broker-mutating endpoint was used.

## August 22, 2026 Control-10 production state: FAIL/DEGRADED

Control-10 was strictly GET-only: no deployment, trigger, or broker-mutating endpoint was used. Live release identity remains `/health=1.0.0` and `/api/config=2.4.0` versus repository deployable `2.6.0`; `/workspace/src` is stale reference material, not deployable source. Fresh crypto/reconciliation skips were present, crypto was approximately `:08/:38`, and Saturday offers no fresh weekday daytrading/swing proof. Provider 503 run errors were recorded at `12:00:46`, `12:07:40`, and `12:10:40 UTC`.

Positions remain broker-authoritative (`source=alpaca`) and equity direction was slightly down (`98,504.50` versus `last_equity 98,504.5039`). Filtered run observability works; lifecycle fields are present, while sampled `gross`/`fee`/`net` remain null under `unavailable_fill_lot_exact`. Caps remain `$5,000/$3,700/$2,000`; local four-schedule wiring and fail-closed crypto edge-gate wiring are correct. No trading behavior changed.

The exact blocker is Wrangler's **`You are not authenticated`** response. A local reliability correction now makes `/api/trades` honor `offset` and `page` with deterministic `timestamp DESC, id ASC` ordering, and exposes read-only estimate-basis, filled-notional, and estimate-delta fields. Final focused validation passed **69 tests / 282 assertions**; full `bun test` passed **164 tests / 562 assertions**; TypeScript, diff-check, and Wrangler dry-run passed with a **282.79 KiB** upload preview. Historical D1-variable and subrequest errors remain live risk evidence, while current reconciliation, activity, bars, and fee paths are explicitly bounded. The separate final GET-only check still shows production on 1.0.0/2.4.0 and old trade pagination, so the correction is not live. `estimated_value` is an order-time estimate, not realized fill accounting. Restore authentication, deploy the exact validated artifact only if authorized, then run separate GET-only verification and natural weekday checks. See `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-10.md`.

## August 22, 2026 Control-9 production correction state: FAIL/DEGRADED

The broker-unavailable read path is corrected locally. `GET /api/positions` now reads Alpaca first and does not touch D1 metadata when the broker snapshot fails, preserving broker authority and returning HTTP 503 with an empty position set and `positionsAvailable: false`. `/api/config` also exposes `release_version` from the Worker artifact separately from persisted D1 `config.version`, which prevents a stale database seed from being mistaken for active Worker identity.

Scheduled reconciliation provider failures remain fail-closed: the maintenance cycle records an `Order reconciliation failed: ...` error in `run_log`, releases its lease, and performs no submit, cancel, close, replace, retry, or other broker mutation. No reconciliation behavior change was needed. Caps remain `$5,000/$3,700/$2,000`; schedules, broker authority, and trading behavior are unchanged.

Focused validation passed **26 tests / 153 assertions** and full validation passed **161 tests / 537 assertions**; typecheck, diff-check, and Wrangler dry-run passed. Deployment remains blocked because `bunx wrangler whoami` reports unauthenticated. Keep production **FAIL/DEGRADED** until authenticated deployment and separate GET-only verification confirm the active release identity, broker positions, and reconciliation recovery. See `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-9.md`.

## August 22, 2026 Control-9 production state: FAIL/DEGRADED

The strict GET-only control found and corrected a local failure-path defect: `/api/positions` now retrieves broker positions before reading D1 metadata. If Alpaca is unavailable, the endpoint returns `503`, `positionsAvailable: false`, `source: alpaca`, and no D1 positions query or fallback. Local validation passed focused **61/0** and full **161/0** tests, with typecheck, diff-check, and dry-run passing.

The final live pass recovered broker reads, but release identity remains unresolved: `/health` is `1.0.0`, `/api/config` is `2.4.0`, and local deployable source is `2.6.0`. Recent run history contains fresh reconciliation maintenance skips and crypto skips, plus Alpaca 503 errors at `12:00:46`, `12:07:40`, and `12:10:40` UTC; daytrading and swing lack fresh successful delivery, crypto is around `:08/:38`, and sampled per-trade gross/fee/net remain conservatively unavailable. Caps remain `$5,000/$3,700/$2,000`; no schedule, broker-authority, edge-gate, TIF, sizing, or trading behavior changed.

Normal deployment is blocked by unauthenticated Wrangler access. Follow `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-9.md`, restore authenticated source verification, deploy only the validated artifact if authorized, and repeat separate GET-only verification. No trigger or broker-mutating route was used.

## August 22, 2026 Control-7 production state: FAIL/DEGRADED

The strict read-only control observed Alpaca account and positions HTTP 503 responses. `/api/positions` correctly returned `positionsAvailable: false` with `source: alpaca` and an empty position list, and `/api/dashboard` retained the same fail-closed state instead of presenting D1 positions as current broker state. Treat this as degraded until Alpaca reads recover and a separate GET-only check confirms broker-authoritative positions.

The live Worker still reports `1.0.0` at `/health` and `2.4.0` at `/api/config`, versus local deployable `2.6.0`; this is unresolved source-to-Worker identity evidence. Do not deploy or mutate trading state from this control without authenticated release verification. Capital caps remain $5,000 daytrading, $3,700 swing, and $2,000 crypto, and all four schedules and crypto fail-closed edge behavior remain unchanged in source.

The correction record is `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-7.md`. The reliability-only swing fix now records the actual kill-state reason in `RISK_HALTED` context and logs, never the boolean halt flag. It does not change schedules, caps ($5,000/$3,700/$2,000), broker authority, edge gates, TIF, sizing, or mutation boundaries. The related crypto fee-telemetry timing mismatch (runs commonly observed around `:08/:38` versus configured `:07/:37`) is deferred; fee and calibrated-edge admission remain fail-closed when telemetry or calibration is unavailable. Required follow-up is authenticated source verification, Alpaca recovery confirmation, fresh natural daytrading/swing delivery evidence, and closure of exact fill/lot accounting and live calibrated-edge evidence gaps. No trigger or broker-mutating route was used.

## August 22, 2026 Control-6 source and observability audit update — FAIL/DEGRADED

Source review confirms filtered/analyzed counts are console-only, production has no calibrated `rawEdgeBps` producer, and crypto positive-edge BUYs remain correctly fail-closed. Historical live crypto `time_in_force: "day"` conflicts with the current GTC source path and must be resolved through authenticated source identity, not by changing trading behavior. No cap, schedule, edge-gate, TIF, or order-behavior change was made.

## August 22, 2026 Control-6 saved-artifact evidence update — FAIL/DEGRADED

Saved artifacts remain contradictory: `/workspace/alpaca-post-release-schedules.json` has all four schedules, while older `/workspace/alpaca-live-schedules-api.json` omits reconciliation; alias captures for `daytrading_cron` and `reconciliation_cron` are empty while canonical filters contain rows, and the requested limited reconciliation artifact is missing. Saved daytrading evidence ends at `2026-08-20 21:55:24` with `CYCLE_LEASE_HELD`, saved swing evidence ends at `2026-08-18 22:00:36` with divergence/RISK_HALTED, and current positions include an unattributed MSTR row. Keep production **FAIL/DEGRADED**; no trigger or broker mutation is authorized by this evidence.

## August 22, 2026 Control-6 strict read-only production control — FAIL/DEGRADED

The current control remains **FAIL/DEGRADED**, not healthy. All six required GET endpoints returned HTTP 200 at approximately 11:00 UTC, but live release identity is inconsistent: `/health` is `1.0.0`, `/api/config` is `2.4.0`, and the validated local deployable source is `2.6.0`. Positions remain broker-authoritative (`positionsAvailable: true`, `source: "alpaca"`, 29 rows), equity direction is current `98,504.50` versus `last_equity` `98,504.5039` (`-0.0039`), and caps remain `$5,000/$3,700/$2,000`.

Reconciliation is delivering fresh structured `MAINTENANCE_ONLY` skips near ten-minute cadence. Crypto delivery is near `:07/:37` but observed around `:08/:38`; timestamps have no timezone suffix, so UTC labeling is not proven. No fresh successful daytrading or swing run is evidenced. Filled trades expose lifecycle fields, while sampled gross/fee/net remain conservatively null under `unavailable_fill_lot_exact`; live numeric crypto edge-gate wiring is not exposed. Live alias filters return canonical rows without `trigger_alias`; the local response-only alias fix is regression-tested but not live-proven.

Local focused control tests passed 59 tests / 253 assertions, full regression passed, typecheck and diff check passed, and Wrangler dry-run passed. Deployment is blocked because `bunx wrangler whoami` reports `You are not authenticated`; no deployment, preview, trigger, migration, or broker mutation was attempted. Keep the system degraded until authenticated source verification, deployment if still required, separate GET-only verification, and natural daytrading/swing observation are complete. See `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-6.md`.

## August 22, 2026 Control-5 correction: explicit filtered-run aliases — FAIL/DEGRADED

The live defect was unreliable filtered-run observability: strict GET-only captures showed `daytrading_cron` and `reconciliation_cron` alias filters returning empty rows while canonical `cron` and `reconcile_cron` filters returned rows, and alias-filtered responses lacked an explicit stable alias indicator. The smallest correction is limited to the read-only API boundary in `src/api.ts`: retain existing SQL alias filtering, add response-only `trigger_alias` values (`daytrading_cron` or `reconciliation_cron`) for alias requests, and preserve canonical stored/returned `trigger` values (`cron` or `reconcile_cron`). Canonical requests receive no invented alias. No DDL, migration, broker access/calls, schedules, caps, broker authority, or trading logic changed.

Required validation and local results: focused `test/dashboard-readonly.test.ts` alias/canonical/no-broker/no-DDL regression **11 tests / 101 assertions passed**; full `bun test` **157 tests / 520 assertions passed**; `bunx tsc --noEmit` passed; and `git diff --check` passed. Deployment was not performed: Wrangler is blocked by the missing `CLOUDFLARE_API_TOKEN` with the exact error `In a non-interactive environment, it's necessary to set a CLOUDFLARE_API_TOKEN environment variable for wrangler to work.` No broker-mutating endpoint was used. Production remains **FAIL/DEGRADED**, not healthy, pending authenticated deployment and separate GET-only live verification.

## August 22, 2026 Control-3 correction: filtered runs, release identity, and evidence gaps

Additional control evidence keeps production **FAIL/DEGRADED**: one saved live schedule artifact omits reconciliation, although the post-release artifact and current `wrangler.toml` contain all four UTC schedules. Filtered/analyzed counts are console-only and not persisted in `run_log`; per-trade gross/net remain null until deterministic fill/lot matching, while aggregate gross/net are broker-snapshot-derived and not fill/lot exact. Source contracts pass for broker-authoritative positions, structured skip logging, unchanged caps, filtered-run predicates, and fail-closed crypto edge gates, but live proof remains partial or failed for active schedule identity, fresh daytrading/swing success, exact cadence, complete lifecycle scenarios, direct cap enforcement, production `rawEdgeBps`, and source-to-Worker identity. No cap, schedule, broker-authority, edge-gate, or trading behavior change was made.

## August 22, 2026 Control-3 correction: filtered runs and release identity

Production control found a release/version mismatch: live `/health` reports `1.0.0` and live `/api/config` reports `2.4.0`, while the deployable source reports `2.6.0`. An earlier capture also showed `/api/runs` filter loss; fresh post-attempt GET probes now return correctly filtered rows, but the corrected source is still not live-proven. The local reliability-only correction is present in `src/api.ts`, `src/database.ts`, and `src/version.ts`; it preserves broker-authoritative positions, all four schedules, caps of **$5,000/$3,700/$2,000**, crypto calibrated-edge fail-closed behavior, and trading semantics.

The required correction work item is `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-3.md`; production remains **FAIL/DEGRADED** until deployment and separate GET-only verification succeed. Deployment evidence is contradictory: `final-deployments.json` records `f181f9c3...` at `2026-08-21T21:03:38Z`, while `direct-upload-5bb8153-20260822.json` records deployment `b6293793...` modified at `2026-08-22T01:14:22Z`; neither reconciles with live `1.0.0/2.4.0`, so neither proves the active source/version. No broker-mutating endpoint or trading trigger was used.

## August 22, 2026 release-version observability correction — source-to-Worker identity unresolved

The canonical local release version is **`2.6.0`**, established by the deployable source's `schema.sql` `bot_config.version` seed and the existing dashboard footer. Local metadata and tests agree on `2.6.0`, but live `/health` remains `1.0.0` and live `/api/config` remains `2.4.0`. Historical deployment receipts claim successful uploads, yet they conflict with live identity and with a later direct-upload artifact, so none independently proves the active source/version. This is reliability-only observability work: no trading behavior, schedules, caps, edge gates, broker calls, D1 mutation semantics, or endpoint methods changed.

Focused validation: `bun test test/release-version.test.ts test/dashboard-readonly.test.ts crypto-runtime.test.ts` — **26 tests passed, 154 expect() calls**; full `bun test` — **157 tests passed, 518 expect() calls**; `bunx tsc --noEmit` passed; repo-scoped `git diff --check -- .` passed; and `bunx wrangler deploy --dry-run --outdir /tmp/alpaca-trading-bot-version-dry-run` passed with no deployment. Deployment was attempted but blocked because Wrangler requires `CLOUDFLARE_API_TOKEN`; the separate GET-only recheck still shows live `/health` version `1.0.0`. No broker-mutating endpoint was used.

Caps remain **$5,000/$3,700/$2,000**; crypto calibrated-edge and fee gates remain fail-closed; schedules, broker calls, and trading behavior are unchanged. Production remains **FAIL/DEGRADED**, not healthy, because fresh daytrading/swing success, direct cap enforcement, exact cadence, exact fill-lot accounting, and historical runtime/divergence follow-up remain unresolved.

## August 21, 2026 strict read-only Alpaca control — FAIL/DEGRADED (latest)

**Status: FAIL/DEGRADED, not healthy.** Final separate GET-only verification completed at approximately **23:01 UTC**. No trigger, cycle, submit, cancel, close, replace, retry, deployment, or other broker-mutating route was used for this control.

- All six required GET endpoints returned **HTTP 200**: `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`.
- `/api/positions` returned `positionsAvailable: true`, `source: alpaca`, and **29 positions**. Final dashboard equity was **98,552.09**, above `last_equity` **98,270.0927**, confirming positive equity direction.
- Caps remain unchanged at **$5,000 daytrading / $3,700 swing / $2,000 crypto**. Source, release-bundle, and dry-run evidence preserve all four schedules: daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and read-only reconciliation `*/10 * * * *`, all UTC.
- Filtered run observability and alias wiring pass source, bundle, regression, and prior live checks: `daytrading_cron` maps to stored `cron`, and `reconciliation_cron` maps to stored `reconcile_cron` without rewriting history. Current run evidence still does not prove a fresh successful daytrading or swing strategy run; historical daytrading lease-held/error skips remain.
- Reconciliation delivery is fresh and read-only: the latest run at **23:01:01 UTC** was `reconcile_cron`, `MAINTENANCE_ONLY`, with `ledgerPages=1`, `ledgerPageBudget=5`, `ledgerTruncated=false`, and `ledgerDegraded=false`.
- Crypto cadence is approximately the expected **:07/:37 UTC** schedule, with natural runs at **21:08:11, 21:38:05, 22:08:04, and 22:38:04 UTC** completing as structured skips with zero errors. Earlier natural crypto runs at **19:38:03, 20:08:01, and 20:38:00 UTC** failed with `D1_ERROR: too many SQL variables`; older evidence also records Worker subrequest-limit failures.
- The SQL-variable failures at **18:08, 18:38, 19:08, 19:38, 20:08, and 20:38 UTC on August 21, 2026** are pre-release history, before the recorded **21:03:38 UTC** batching release. Fresh post-release crypto runs through **03:38 UTC on August 22** show no recurrence; retain the historical errors as risk evidence, not as a newly active failure.
- `/api/trades` exposes lifecycle fields including `submitted_at`, `filled_at`, `canceled_at`, `expired_at`, `failed_at`, and `replaced_at`; all sampled current rows are `filled` with submitted/filled timestamps and null inapplicable terminal timestamps. All 50 sampled rows have `gross`, `fee`, and `net` as `null`, `accounting_status: unavailable_fill_lot_exact`, and `fee_attribution: none-recorded`, so per-trade fee/gross/net consistency is not demonstrable.
- Crypto edge-gate wiring passes source, deployed-bundle, and regression checks, including calibrated-edge fail-closed behavior; a live positive `rawEdgeBps` comparison remains unproven. Fee telemetry is currently unavailable/stale, producing expected fail-closed `FEE_DATA_UNAVAILABLE` skips.
- Full regression passed **154/0** with **488 assertions**; focused dashboard/read-only regression passed **9/0** with **76 assertions**; TypeScript, `git diff --check`, and Wrangler dry-run passed.
- Cap audit: configured values are unchanged and observed daytrading exposure remained below $5,000, but direct cap-denial/enforcement evidence for daytrading, swing, and crypto is absent. An unattributed MSTR broker position of approximately **$1,901.48** prevents complete strategy attribution; crypto history showed zero observed positions, so the $2,000 cap was not exercised.
- Additional live-evidence gaps: schedule captures disagree between three and four configured expressions, the requested snapshot has no schedule artifact or response headers, and no current-window delivery evidence proves the stock `*/5` or weekday `22:00` paths. Crypto delivery is approximately :07/:37 but commonly records :08/:38 jitter; current crypto history includes `FEE_DATA_UNAVAILABLE` skips and zero observed crypto positions.
- Final release review confirms the bounded/read-only design and deployed wiring, but the bounded ledger page budget does not eliminate total Worker subrequest exhaustion in historical live runs. Further partitioning requires a separate reliability work item; exact fill-lot matching is also still required before non-null per-trade gross/fee/net can be claimed.
- The captured trade page contains only 50 filled rows, so non-filled lifecycle paths are unverified; daily `change_today`/`daily_pl` fields are zero despite fluctuating equity history, limiting independent daily-direction validation. Reconciliation is repeatedly `MAINTENANCE_ONLY`, and historical lease/subrequest failures remain unresolved evidence.
- Release/live mismatch remains explicit: checked-in four-schedule wiring and run aliasing pass source and regression checks, but live crypto timestamps show minute-level jitter rather than exact :07/:37 delivery, historical crypto and reconciliation runs include fatal subrequest-limit errors, per-trade accounting remains null/unavailable_fill_lot_exact despite aggregate gross/net, and fresh successful daytrading and swing delivery is still absent.

**Required follow-up:** repair read-only Cloudflare credentials and independently verify the active Worker/source identity; obtain fresh natural daytrading and swing strategy delivery evidence; and close the deterministic fill/lot accounting gap before any health claim. Keep production marked FAIL/DEGRADED.

## August 21, 2026 D1 SQL-variable correction deployed and post-release verified

Reliability-only correction `f5fddcbe829a4b6a6436b110ab0d19e3ab11c5aa` batches read-only `broker_fees` enrichment queries in groups of 50, removing the production `too many SQL variables` failure without changing caps, schedules, thresholds, sizing, order behavior, or broker authority. Focused validation passed 9 tests / 76 assertions; full validation passed 154 tests / 488 assertions, typecheck, diff-check, and Wrangler dry-run.

Direct Cloudflare upload succeeded on **August 21, 2026 at 21:03:38 UTC** as deployment `f181f9c3-4c72-4854-8173-fe88e0ed8cb1`, Worker version `84069389-3596-49b4-98dd-795c694e8d19`, at 100% traffic, annotated with the correction commit. Live settings retain D1 `2bc505a2-d744-4322-8c3b-5f5ebe35f9a1`, `nodejs_compat`, and all four schedules: `*/5 13-21 * * 1-5`, `0 22 * * 1-5`, `7-59/30 * * * *`, and `*/10 * * * *`.

Separate GET-only verification after the first natural post-release crypto tick at **2026-08-21 21:08:11 UTC** returned a structured `crypto_cron` skip with 5 decisions and 0 errors; the prior SQL-variable failure did not recur. No trigger or broker-mutating endpoint was used.

Production remains **FAIL/DEGRADED, not healthy** because daytrading lease/error delivery, fresh swing success, cadence jitter, null or incomplete lifecycle evidence, aggregate-only gross/fee/net accounting, unattributed exposure, and live calibrated `rawEdgeBps` comparison remain unresolved. Caps remain **$5,000/$3,700/$2,000**.

## August 21, 2026 D1 SQL-variable overflow correction — locally validated, source identity unresolved

Incident: live `/api/runs` showed a D1 `too many SQL variables` error at **2026-08-21 18:38:03 UTC**. The affected read-only/scheduled observability path was `Database.enrichTradeAccounting()`, which built one dynamic `broker_fees.order_id IN (...)` statement for all returned trades.

Correction: query order IDs in sequential read-only batches of 50 and merge fee rows into the existing map. This preserves fee semantics, broker-linked attribution, conservative null/gross/net behavior, broker-authoritative positions, bounded/read-only reconciliation, all schedules, thresholds, sizing, broker actions, and caps **$5,000/$3,700/$2,000**. Local validation passed: focused 9 tests / 76 assertions, full 154 tests / 488 assertions, typecheck, diff-check, and Wrangler dry-run.

Deployment artifact `2bf8e6c6-3d6d-456d-ad65-0bb6bfeef07b` with Worker version `a23c13a1-6b61-4c03-aae9-738d35118af9` is recorded at 100% traffic on **August 21, 2026 at 17:15:44 UTC**, but it is not mapped to the current local source/bundle. Current release state is **SOURCE-TO-WORKER IDENTITY UNRESOLVED**. Wrangler remains unauthenticated and the prior Cloudflare credential-reference error remains unresolved. No broker endpoint was called. Follow-up: repair identity/credential access, verify the exact bundle-to-Worker mapping, then perform only GET-only and natural scheduled verification. Do not use triggers or broker-mutating endpoints as validation.

Test-layout note: crypto runtime regression coverage is stored at the repository root as `crypto-runtime.test.ts`, not `test/crypto-runtime.test.ts`; `bun test crypto-runtime.test.ts` passes 14 tests / 48 assertions.

## August 21, 2026 strict read-only control update — FAIL/DEGRADED

The latest GET-only control confirms `/api/positions` source labeling (`alpaca`) but historical run evidence still records broker/internal quantity mismatches: SOFI 73 vs 114 at 16:10:47 UTC, MSTR 3 vs 7 at 16:20:46 UTC, and NOW 1 vs 2 at 16:35:42 UTC. Daytrading is lease-held/error without fresh healthy delivery, swing delivery is unverified, crypto cadence has gaps and errors, reconciliation is maintenance-only, sampled lifecycle and gross/fee/net fields are null, and calibrated edge-after-costs is not live-verifiable. Keep production marked FAIL/DEGRADED; caps and trading behavior are unchanged and no broker mutation or deployment was performed.

## August 21, 2026 additive trade observability correction — deployed and GET-only verified

The reliability correction is live from commit `71aad14b0df1fc693de0e002e1b91d5cb6460eb5`. Broker `time_in_force` is now persisted on new and reconciled trades, including crypto GTC; `/api/trades` adds conservative `gross`, `fee`, and `net` plus explicit accounting and fee-attribution metadata; gross/net remain null until fill/lot matching is provable; direct order-linked fees are exposed only when all linked USD values are known and non-negative; and bounded ledger truncation now persists top-level `degraded` status while retaining `BROKER_LEDGER_DEGRADED` details. No caps, schedules, thresholds, sizing, order behavior, or broker safety boundary changed.

Validation and release receipt: **153 tests / 483 assertions**, TypeScript, diff-check, and Wrangler dry-run passed. Direct upload deployment `061b8e22-184d-4c46-8f54-2bf0c4682dc8` serves Worker version `07c901cc-d936-4bb8-a7e9-8dc6689b0fa3` at 100% traffic with all four schedules. Separate GET-only verification confirmed all six required endpoints, broker-authoritative positions (`source: alpaca`, 29 rows), positive equity direction, caps 5000/3700/2000, filtered run observability, and the new trade fields.

Production remains **FAIL/DEGRADED**, not healthy: latest natural crypto and reconciliation runs are structured skips, historical subrequest-limit errors and position divergence remain, fresh daytrading/swing success is not proven, lifecycle timestamps remain null on sampled historical fills, current crypto fee telemetry is unavailable, calibrated edge is not supplied in production, and unattributed exposure limits full cap attribution. No broker-mutating endpoint was used.

## August 21, 2026 additive trade observability correction — release candidate

The reliability correction preserves broker `time_in_force` in `trades`, including crypto GTC orders, exposes conservative per-trade `gross`, `fee`, and `net` fields, and marks unresolved fill/lot accounting explicitly as `accounting_status: unavailable_fill_lot_exact`. Fees are populated only from complete positive-USD broker-fee rows linked directly to the trade `order_id`; uncertain, orderless, and account-level fees remain unattributed. Ledger truncation now sets the persisted run status to `degraded` while retaining structured `BROKER_LEDGER_DEGRADED` details. Caps remain **$5,000/$3,700/$2,000**, schedules and trading behavior are unchanged, and no broker-mutating endpoint is used for validation.

Focused validation passed: **19 tests / 37 assertions**. Full suite, typecheck, diff-check, Wrangler dry-run, deployment, and separate GET-only verification remain required. Production is still **FAIL/DEGRADED**, not healthy, because live strategy-delivery, lifecycle population, calibrated edge, cap-attribution, and fill/lot-exact accounting evidence remain incomplete.

## August 21, 2026 additive per-trade accounting and crypto TIF observability correction

`GET /api/trades` now preserves every existing trade field and adds `gross`, `fee`, and `net` plus explicit attribution/status metadata. Gross and net remain `null` when D1 cannot prove a single trade/lot attribution; the existing model P&L is not projected onto order rows. `fee` is populated only from `broker_fees` rows with a non-empty matching `order_id` and complete non-negative USD values. Orderless or account-level fees remain unattributed and are never assigned to a trade. `net` is computed only when both gross and fee are known. This is read-only observability and does not change caps, schedules, thresholds, sizing, submitted orders, or trading behavior.

The same correction persists the broker-provided `time_in_force` on `trades`, including crypto `gtc`; the submitted TIF and all execution behavior are unchanged. Historical rows remain null only where the broker/D1 record did not provide reliable evidence. Validate field presence, linked-fee-only behavior, conservative nulls, account-level fee exclusion, and crypto GTC persistence before any release.

## August 21, 2026 bounded broker-ledger subrequest correction — deployed and post-release verified

Scope is reliability-only and addresses the confirmed failing path `Broker ledger sync failed: Too many subrequests by single Worker invocation`. Scheduled `syncBrokerLedger` now uses `getAccountActivitiesBounded` with an explicit **5-page / 500-activity budget**, replacing the prior 30-page loop. When the broker exposes another page at the budget boundary, the read returns `truncated: true, degraded: true`; scheduled callers emit structured `BROKER_LEDGER_DEGRADED` details and continue using the existing 3-day overlap/idempotent activity IDs for later convergence. Pending read-only `getOrder` reconciliation lookups remain capped at 8 per invocation. No broker state is mutated, and all four schedules, trading decisions, order behavior, and caps **$5,000/$3,700/$2,000** are unchanged.

Focused entry-authority, bounded ledger/activity, and reconciliation validation passed: **24 tests / 90 assertions**. Full `bun test` passed with **149 tests / 470 assertions**; TypeScript, diff-check, and Wrangler dry-run also passed. Direct upload of commit `656cefd1b647c4127e01ddfbebaa8a451e80bd0b` produced deployment `2bf8e6c6-3d6d-456d-ad65-0bb6bfeef07b`, Worker version `a23c13a1-6b61-4c03-aae9-738d35118af9`, at 100% traffic. Separate GET-only verification passed all six endpoints; the first post-release reconciliation at `2026-08-21 17:21:00 UTC` completed with one activity page within the five-page budget and no degradation. Production remains **DEGRADED, not healthy** because fresh daytrading/swing success, populated historical lifecycle fields, per-trade gross/fee/net accounting, and live cap enforcement remain incomplete. Post-release crypto delivery is verified at `2026-08-21 17:38:12 UTC` as a structured skip with no subrequest error (`NO_POSITION_TO_EXIT`, `DECISION_HOLD`). No trigger, submit, cancel, close, replace, retry, or broker-mutating endpoint was used.

## August 21, 2026 bounded dashboard and order-lifecycle correction — deployed and GET-only verified

The correction is limited to reliability and observability: the dashboard derives `account.market_value` from the broker positions returned in that same response and sets it to `null` when that broker read is unavailable; `latestSnapshot.positions_count` follows the same broker position set. All shared account-wide snapshots count the complete broker position set, including swing and crypto schedule writers, and `submitOrder` normalizes broker responses so non-null lifecycle timestamps reach persistence. Read-only reconciliation revisits terminal rows with status-relevant missing lifecycle fields and persists only non-null timestamps returned by Alpaca; inapplicable nullable fields remain null and no timestamp is inferred when Alpaca returns null. Caps remain $5,000/$3,700/$2,000, all four schedules and strategy behavior are unchanged. Validation passed with **138 tests / 422 assertions**, TypeScript, diff-check, and Wrangler dry-run; direct upload produced version/deployment `45d067bc-1944-4041-ae8e-0f7fc261dd55`, and separate GET-only verification passed. Production remains DEGRADED because fresh daytrading/swing success, historical lifecycle population, per-trade gross/fee/net accounting, fee telemetry, quantity-divergence resolution, the observed crypto cadence gap, and calibrated crypto-edge proof remain incomplete.

## August 21, 2026 strict read-only production control and alias-correction status

Status: **LOCAL VALIDATION COMPLETE; PRODUCTION DEPLOYMENT AND SOURCE IDENTITY NOT ESTABLISHED; production remains DEGRADED and is not healthy.** The alias correction is limited to the read-only `GET /api/runs?trigger=` boundary: `daytrading_cron` maps to stored `cron` and `reconciliation_cron` maps to stored `reconcile_cron`. Canonical filters continue to work, historical rows retain stored trigger values, and no storage, scheduler dispatch, schedule, cap, threshold, sizing, or broker behavior changed.

The August 21 GET audit confirmed all six required endpoints returned HTTP 200, `/api/positions` reported `source: alpaca` with 29 broker-backed rows, configured caps remained `$5,000/$3,700/$2,000`, and equity direction was positive. Fresh daytrading and swing success is not proven; lease/error/fee skips are present; lifecycle timestamps are null in the sample; per-trade gross/fee/net fields are absent; crypto edge comparison is not live-proven; and current Cloudflare/source identity is not independently verified. No trigger, cycle, or broker-mutating endpoint was used.

Local validation remains: focused dashboard-readonly tests **5 tests / 53 assertions**, full suite **131 tests / 403 assertions**, TypeScript, `git diff --check`, and Wrangler dry-run passed. Deployment is a separate authorized follow-up and is not part of this strict read-only control.

## August 21, 2026 strategy-filtered trades and broker-authoritative swing reconciliation — deployed

Status: **DEPLOYED AND READ-ONLY VERIFIED; production remains DEGRADED.** `GET /api/trades` validates `strategy=daytrading|swing|crypto`, bounds `limit` to 500 with default 50, and returns explicit `limit`/strategy metadata while preserving the default unfiltered trade list. `/api/runs` rejects invalid strategy filters. Read-only API construction performs no DDL. Swing reconciliation closes D1-only rows after a complete broker snapshot via `closePosition(..., 'broker_authoritative_sync_absent')` and emits structured `BROKER_AUTHORITATIVE_SYNC_ABSENT`; only actual broker/internal quantity mismatches retain the current-cycle new-BUY halt. No caps, thresholds, schedules, sizing, order behavior, or broker endpoints changed.

Validation passed: **125 tests / 374 assertions**, TypeScript, diff-check, and Wrangler dry-run. Cloudflare deployment `1b286e9a-6d2f-45b9-a439-72fd12654f9c` serves Worker version `ced43daf-ed03-4add-ac07-1d8bf562b72c` at 100% traffic with all four schedules. Separate GET-only verification passed all six endpoints, broker source `alpaca` with 29 positions, caps `$5,000/$3,700/$2,000`, invalid filters HTTP 400, and 20 crypto-only filtered trades. Production remains DEGRADED until fresh natural swing and daytrading evidence exists.

## August 21, 2026 bounded Alpaca lifecycle-timestamp correction — deployed and read-only verified

The correction additively persists the existing Alpaca Order fields `submitted_at`, `filled_at`, `canceled_at`, `expired_at`, `failed_at`, and `replaced_at` on `trades`. Incoming non-null values are monotonic per lifecycle field and NULL broker snapshots cannot erase stored evidence; no trading behavior, edge gate, schedule, sizing, or cap changed. Caps remain **$5,000 / $3,700 / $2,000**.

Validation passed: **123 tests / 361 assertions**, TypeScript, `git diff --check`, and Wrangler dry-run. Remote D1 contains all six columns. Deployment `6ef8737a-85ca-4fbb-8886-c938237dc993`, version `5ff1ee08-bdc1-46b7-9aa6-93962d25beb4`, is at 100% traffic with all four schedules; a separate post-deployment GET-only verification at **11:04:24–11:04:25 UTC on August 21, 2026** returned HTTP 200 for all six endpoints, with broker-authoritative positions and the configured caps intact. `/api/trades` exposes the new fields, but historical lifecycle timestamps remain null until natural broker updates populate them. Production remains **DEGRADED**, not healthy, pending fresh daytrading/swing success and because prior swing runs include errors.

## August 21, 2026 additive trade-lifecycle persistence correction — deployed

The lifecycle persistence gap is corrected with additive broker timestamp columns on `trades`: `submitted_at`, `filled_at`, `canceled_at`, `expired_at`, `failed_at`, and `replaced_at`. Initial imports, direct status updates, and scheduled read-only reconciliation retain non-null timestamps monotonically; trading behavior, schedules, sizing, thresholds, and caps remain unchanged at $5,000/$3,700/$2,000. Remote D1 contains all six columns.

Full validation passed **123 tests / 361 assertions**, TypeScript, diff-check, and Wrangler dry-run. Deployment `6ef8737a-85ca-4fbb-8886-c938237dc993` is live at 100% traffic; all six GET endpoints passed separately and `/api/trades` exposes the new fields. Production remains **DEGRADED** pending fresh August 21 daytrading/swing delivery; prior swing history includes errors.

## August 21, 2026 runtime-cap and scheduler DDL correction — deployed and read-only verified

Scope is limited to two reliability defects. Daytrading and swing runtime config loaders now use the same existing camelCase/snake_case aliases as `/api/config`, while missing or malformed overrides preserve the current defaults. The scheduled handler no longer performs `ALTER TABLE positions ADD COLUMN strategy` on every invocation; strategy cron paths perform a read-only `pragma_table_info` readiness check and fail closed with a recorded error when `positions.strategy` is absent. Legacy databases must receive the explicit `positions-strategy-column-migration.sql` migration once; current schema and behavior are unchanged.

Validation passed locally: focused `bun test test/runtime-config-schema.test.ts test/capital-caps.test.ts` (**12 tests / 31 assertions**), full `bun test` (**121 tests / 359 assertions**), `bunx tsc --noEmit`, `git diff --check`, and Wrangler dry-run. Source commit `2637a1e07bedbc72592f546302a94fd9c195b927` is deployed as Cloudflare deployment `2c222e36-a64c-414e-898c-cbdfb10cb58f`, Worker version `e7425217-78c6-4bd2-bc2b-ee1e14cbd123`, at 100% traffic. All four schedules and six required GET endpoints passed; remote D1 confirmed `positions.strategy` exists and no migration was required. No trigger or broker-mutating operation was used. Production remains **DEGRADED** because swing delivery is incomplete, crypto history/fee-edge and ownership/GTC lifecycle gaps remain, P&L is not fill-exact, and fresh natural scheduled evidence is pending.

## August 21, 2026 swing-cap correction

The confirmed swing admission gap is corrected locally. Swing BUY checks now carry approved cycle-level entry notional into subsequent checks, so current broker-backed swing exposure plus planned entries cannot exceed the unchanged **$3,700** cap; exhausted headroom is recorded as structured `CAPITAL_CAP` observability. Exits, protective behavior, thresholds, turnover/minimum-size behavior, daytrading, crypto, and all vital caps remain unchanged.

Validation passed on August 21, 2026: focused swing/risk/cap/skip/pagination tests, full suite **115 tests / 346 assertions**, TypeScript, `git diff --check`, and Wrangler dry-run. The correction is deployed and separately read-only verified. Commit `d9c8ec6fd0315980549078169c3e2d69986700d0` is live as Cloudflare deployment `602cdd72-1a49-4db5-bd86-898efea14315`, Worker version `7b20c401-fe15-41e5-ac71-a8d798e8112d`, at 100% traffic. All four schedules and all six GET endpoints passed; no broker-mutating endpoint was used.

Known remaining gaps remain explicit: crypto positive-edge BUYs fail closed as `EDGE_CALIBRATION_UNAVAILABLE` because no production caller supplies calibrated `rawEdgeBps`; several broker lifecycle timestamps and crypto GTC `time_in_force` are not fully persisted; P&L remains model/gross-style plus conservative attributed fees rather than fill/lot-exact accounting; and fresh natural post-release strategy/reconciliation success is still required before health can be declared.

## August 21, 2026 targeted swing-cap correction — local validation status

The confirmed swing capital-cap enforcement gap is corrected locally. Swing cycle entry admission now carries approved BUY notional from earlier proposals into each subsequent `checkEntry` call, enforcing the existing **$3,700** cap across the complete proposal batch despite unchanged broker positions. This is an entry-admission reliability fix only: turnover limits, minimum trade size, confidence/edge thresholds, all strategy caps, exits, and protective exits remain unchanged.

Release validation requirements are: focused swing risk regressions, full `bun test`, `bunx tsc --noEmit`, `git diff --check`, and `bunx wrangler deploy --dry-run`. Validation must remain local/read-only; never trigger a strategy cycle or call submit, close, cancel, replace, retry, or another broker-mutating endpoint. After any authorized release, require natural swing-cycle evidence that proposed and submitted BUY notional remains at or below **$3,700**.

## August 21, 2026 strict read-only production control, current sample through 12:50:59 UTC

Control result: **DEGRADED, not healthy**. All six GET endpoints returned HTTP 200. Broker positions remained authoritative (`/api/positions`: 29 positions, `source: "alpaca"`), equity direction was positive (`$98,480.48` versus `$98,270.0927` last equity), and configured caps remained `$5,000/$3,700/$2,000`.

The checked-in and dispatched schedules are daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and reconciliation `*/10 * * * *`. Crypto delivered repeatedly around `:07/:37` UTC through `12:38:10`; reconciliation delivered about every 10 minutes through `12:50:59`, including `MAINTENANCE_ONLY` and `CYCLE_LEASE_HELD` skips. The latest known daytrading strategy run ended in broker/internal quantity-divergence error at `2026-08-20 16:35:42`; no swing run was visible in the fetched history, so fresh successful daytrading and swing delivery is not established.

Run observability and structured skip reporting pass. The sampled 50 trades expose broker fill/status fields, but all six nullable lifecycle timestamps are null; their API presence is exposure only, not proof of population. No per-trade gross, fee, or net fields are exposed. Crypto source/test edge-gate wiring passes inspection, while live end-to-end comparison against the configured threshold remains unverified. The historical August 10 daytrading exposure of `$5,679.878` remains a prior-release follow-up. This check required documentation/status correction only, not deployment, and used no broker-mutating endpoint.

## Bounded entry-identity fix — August 18, 2026 (deployed and live-verified)

A bounded, non-vital fix removes `Date.now()` from stock/swing entry identity and makes retry duplicate protection deterministic. It does **not** alter the vital caps (daytrading **$5,000**, swing **$3,700**, crypto **$2,000**), confidence gates, max-trade limits, strategy universes, or risk parameters, and it does **not** change the exit decision-correlation gap or the running crypto reservation semantics.

- Daytrading and swing BUY submissions now use a deterministic `client_order_id` derived from the decision ID and symbol (`bot_<decisionId>_<symbol>` / `swing_<decisionId>_<symbol>`) instead of `Date.now()`. This matches the existing crypto pattern (`crypto_<decisionId>_<symbol>`) so every entry has stable retry identity.
- Those BUY submissions now persist immediately through `db.logOrderTrade(order, …)` (the same order-shaped path crypto already uses) rather than a hand-mapped `logTrade` shape, so broker fields, fill state, and timestamps are recorded from the broker response.
- A new reusable `Database.findNonTerminalTradeByClientOrderId(clientOrderId)` detects an existing non-terminal trade before a stock/swing BUY is submitted, so a retry/duplicate of the same decision is skipped before reaching the broker with an auditable `DUPLICATE_ORDER_PREVENTED` skip reason and a decision-status note. Terminal rows (`rejected`/`canceled`/`expired`/`done_for_day`/`stopped`/`replaced`) do **not** block a retry, because they prove the prior order no longer leads to an open position. The lookup orders by `COALESCE(broker_updated_at, timestamp)` then `id` so ties are deterministic.
- Crypto fee telemetry is now routed through the canonical `feeTelemetryFromAggregate` gate with an explicit freshness `maxAgeMs` of **60,000 ms**: telemetry older than the max age fails closed to `unavailable`, sub-threshold samples return `insufficient`, and a failed ledger sync or missing summary returns `unavailable` so an unproven rate is never used for new-entry cost estimation. `getBrokerFeeSummary` now also exposes `cryptoUsdRecent` (the seven-day curated CFEE window) so the aggregate's computed rate matches the existing recent-window rate.

Local validation on August 18, 2026: **101 tests, 294 assertions**, TypeScript typecheck and diff-check passed. Deployment and live verification completed from source commit `f122287703087ab959768d02ec931e21d85319a3`: Cloudflare deployment `03e3ef01-bb25-4010-b4b3-03829e7c09d5`, Worker version `b5b4cb6e-71d2-4b78-924c-fd12acd4ac69`, 100% traffic, all four schedules, HTTP 200 read-only endpoints, `/api/positions` source `alpaca` with 38 broker positions, and `/api/dashboard` caps `5000/3700/2000`. Remote D1 reservation/lifecycle schema verification passed. No trading trigger, order, cancellation, close, replace, retry, or other broker mutation was used.

## Lifecycle hardening candidate — August 10, 2026

The hardening candidate keeps broker positions authoritative and keeps GET/read-only reconciliation free of broker mutations. Current-position rows are created or updated only from broker snapshots; requested quantities are never written as filled positions. Decision metadata converges from broker order status, including pending, partial, filled, and terminal rejection states.

Daytrading admission now carries approved entry notional across the current cycle so the existing **$5,000** cap cannot be exceeded by multiple individually valid entries. Swing entries retain immutable decision linkage and newly filled positions are included in the post-submit broker sync. Crypto reservation state is persistent across cycles and fail-closed: active/committed reservation notional is included in the **$2,000** cap calculation, live orders keep reservations beyond the short rate window, unknown post-submit outcomes retain reservations, and terminal broker evidence releases them.

Committed crypto reservations are never released by local TTL alone; only terminal broker evidence releases them. Crypto BUYs below **$10** estimated notional are skipped before reservation/submission with an auditable `MIN_ORDER_NOTIONAL` reason. ATR stop/target intent is stored with the crypto entry trade and used to reconstruct missing position protection after broker-confirmed sync.

Validation receipt: 92 tests, 273 assertions, typecheck, and diff-check passed locally on August 10, 2026. No broker mutation was used. Remote D1 schema verification is complete for the reservation table/index and both trade intent columns. Live deployment is verified: deployment `32fdaa9c-0609-4be1-b16c-6369af4dfc8e`, Worker version `dff3e198-1cb3-49d1-ac5d-706a7d292258`, 100% traffic, all four schedules, and read-only endpoint checks passed. Natural paper-session observation remains the follow-up gate for proving no cap breach or live-order reservation expiry under real post-release cycles.

# Operations and release notes

## August 21, 2026 read-only trade-filter and stale-D1 correction

The bounded correction addresses two observed reliability defects. `GET /api/trades` now supports bounded `strategy=daytrading|swing|crypto` filtering and rejects invalid values with HTTP 400; `/api/runs` rejects invalid strategy filters the same way. Swing reconciliation closes broker-absent D1-only rows through `broker_authoritative_sync_absent` only when no pending broker order explains them, while real broker/internal quantity mismatches still block new swing BUY entries for the current cycle.

Validation passed: focused and full suites **125 tests / 374 assertions**, TypeScript, diff-check, and Wrangler dry-run. Deployed as Cloudflare deployment `1b286e9a-6d2f-45b9-a439-72fd12654f9c`, Worker version `ced43daf-ed03-4add-ac07-1d8bf562b72c`, at 100% traffic; all four schedules remain present. Separate GET-only verification confirmed HTTP 200 for the six required endpoints, broker-authoritative positions (`source: alpaca`, 29 positions), caps `$5,000/$3,700/$2,000`, invalid-filter HTTP 400 responses, and 20 crypto-only rows from `/api/trades?strategy=crypto`.

Production remains **DEGRADED**, not healthy: latest daytrading evidence is lease-held skips through August 20, latest swing evidence is the August 18 divergence error, and no fresh natural post-release daytrading/swing success exists. The six nullable lifecycle fields are exposed but remain null in the historical sample; fee telemetry can be unavailable, and exact per-trade fee/gross/net accounting is not exposed.

## August 21, 2026 `/api/runs` pagination reliability/observability fix

The local read-only correction fixes pagination metadata for `GET /api/runs`: an explicitly supplied `offset` now reports `page = floor(offset / limit) + 1`, while page-based requests preserve their existing page/offset behavior. The fix is limited to response metadata and regression coverage; no caps, strategy thresholds, trade budgets, order sizing, or trading behavior changed.

Required local validation: `bun test test/dashboard-readonly.test.ts`, `bun test`, `bunx tsc --noEmit`, and `git diff --check`. Do not deploy or call any live endpoint for this work. If released later, validate only with GET/read-only checks and require natural scheduled-run evidence before changing the operational health assessment.

Current production status remains **DEGRADED, not healthy** because swing delivery evidence is incomplete, crypto history and fee/edge blocks remain, crypto ownership and GTC/lifecycle persistence still require a separate correction, P&L accounting is not fill-exact, and fresh natural daytrading/swing success evidence is pending. Vital caps remain daytrading **$5,000**, swing **$3,700**, and crypto **$2,000**.

## August 21, 2026 crypto edge-gate correction

Crypto BUYs now fail closed when `minEdgeAfterCosts` is positive but no calibrated `rawEdgeBps` is available. Confidence is never converted to basis points. The rejection is exposed as `EDGE_CALIBRATION_UNAVAILABLE`, so the missing economics calibration is visible in `run_log.error_details` and dashboard run details instead of being silently treated as a generic risk decision. Daytrading and swing callers do not opt into this gate.

Validation passed with 111 tests and 330 assertions, typecheck, diff-check, and Wrangler dry-run. The correction is deployed as Cloudflare deployment `47158569-968b-4bae-83ad-0c24134d42d2`, Worker version `2756aeb6-e71a-4a11-ab7c-a3a1a6dbbf4e`, at 100% traffic; all four schedules are present. Read-only live checks confirmed `/health`, `/api/config`, `/api/positions`, and `/api/dashboard` returned HTTP 200, with broker-authoritative positions still available. The filtered `/api/runs?trigger=crypto_cron&limit=1` probe returned the latest natural pre-release crypto run at `2026-08-21 07:37:34` CPH with 7 decisions, 0 trades, 0 errors, and explicit skip details. No broker-mutating endpoint was used. A natural post-release crypto run remains pending, and the system must not be called healthy until it confirms the new `EDGE_CALIBRATION_UNAVAILABLE` detail.

Vital caps remain daytrading `$5,000`, swing `$3,700`, and crypto `$2,000`.

## August 21, 2026 reliability correction candidate

The August 21 reliability correction is deployed and live-verified. It aligns legacy strategy `totalPl` with `netTotalPl` while preserving `grossTotalPl`, adds bounded `/api/runs` pagination/filter observability, persists broker-authoritative quantity corrections while retaining the current-cycle BUY safety block, and adds non-terminal stock/swing exit guards with structured `PENDING_EXIT_EXISTS` fields. The final August 21 correction includes mismatch-count context in the cycle event and direct regression coverage; it does not change trading behavior. Caps remain daytrading `$5,000`, swing `$3,700`, and crypto `$2,000`.

Final release receipt: source commit `dab504cb091b2bf20120d9f8d3fd2d18ca61a4dc`; follow-up Cloudflare deployment `b1c1bc11ce6a451da97a8325a70f89bb` accepted after base deployment `07615065-0302-41c6-8a22-4203ea38b5c9`; all four schedules remain present. Local validation passed with 109 tests/321 assertions, TypeScript, diff-check, and dry-run. Separate final GET-only checks passed for health, config, dashboard, positions, runs, and trades.

Remaining degraded follow-up: no post-release successful strategy/reconciliation run is visible yet; the latest reconciliation at `2026-08-21 07:20:24` UTC was `CYCLE_LEASE_HELD`. The unresolved historical partial-exit/mismatch lifecycle remains monitored.

## Dashboard 1102 hotfix — August 10, 2026

The deployed dashboard hotfix makes all GET/read-only `Database` instances skip runtime schema-repair DDL, `ALTER TABLE`, index creation, and schema checks. Trading and write paths retain the existing schema-readiness process, and the Worker `fetch` path has no unconditional `ALTER TABLE positions` repair.

Dashboard fan-out is reduced by removing duplicate per-strategy history queries and bounding performance and category history to 90 rows per series. Current positions remain Alpaca-authoritative; broker position failure returns an unavailable state and never falls back to D1 positions.

Historical August 10 evidence, superseded by the August 18 release below: source commit `4261009` was pushed to `origin/main`; read-only Worker endpoints returned HTTP 200 with broker-backed positions, while Cloudflare identity was not then verifiable because Wrangler was unauthenticated and API requests returned HTTP 403. Remote D1 contained the reservation table/index; 85 tests/257 assertions, typecheck, diff-check, and dry-run passed; no broker mutation was used.

## Current control status

The latest recorded release receipt is the August 21 strategy-filter and broker-authoritative reconciliation correction. Deployment identity below is a chronology of captured receipts, not singular current proof: the current read-only control did not independently authenticate Cloudflare deployment identity or traffic. No broker order, cancellation, close, or manual trading trigger was used.

- Latest recorded source worktree: branch `fix/remove-premature-position-upsert-entryside`
- Latest captured deployment receipt: `47158569-968b-4bae-83ad-0c24134d42d2` / Worker version `2756aeb6-e71a-4a11-ab7c-a3a1a6dbbf4e`, created August 21, 2026 at 07:57:51 UTC, 100% recorded traffic
- Earlier captured release receipts: `1b286e9a-6d2f-45b9-a439-72fd12654f9c` / `ced43daf-ed03-4add-ac07-1d8bf562b72c`, and lifecycle `6ef8737a-85ca-4fbb-8886-c938237dc993` / `5ff1ee08-bdc1-46b7-9aa6-93962d25beb4`
- Latest local correction validation: 130 tests / 388 assertions, TypeScript, diff-check, and Wrangler dry-run; 125 / 374 remains the historical deployed release receipt
- Identity caveat: source-to-deployment mapping and current live control-plane identity were not independently revalidated in this control
- Live read-only status: **DEGRADED, not healthy**
- Current evidence: broker-authoritative positions and unchanged caps pass; crypto `:07/:37` and reconciliation cadence pass; explicit lease/error/skip observability passes.
- Open gaps: broker/internal quantity divergence, no fresh successful daytrading or swing run, all six exposed nullable lifecycle timestamps null across the sampled 50 trades, no per-trade gross/fee/net fields, fee telemetry unavailable in fresh crypto skips, and no end-to-end live proof of the configured crypto edge comparison. Dashboard aggregate gross/fee/net values exist, but fill-exact per-trade accounting is unavailable.

Dashboard capital-cap evidence remains read-only and server-resolved: `/api/dashboard` returned `{ daytrading: 5000, swing: 3700, crypto: 2000 }` with broker positions available. The UI never substitutes buying power, cash, equity, portfolio value, or positions for a missing cap.

## Release verification

Before any deployment that can submit crypto BUY orders, an authorized operator must apply the idempotent reservation migration and complete the read-only verification. The August 10 hardening release recorded that the migration was applied and verified in remote D1; these commands remain the reproducible release gate for future deployments:

```bash
bun run db:migrate:crypto-reservations:remote
bun run db:verify:crypto-reservations:remote
```

The verification must return both `crypto_entry_reservations` and `idx_crypto_entry_reservations_expiry`; the trade-intent verification must return `intent_stop_loss_price` and `intent_take_profit_price`. Do not substitute a Worker-triggered CREATE TABLE for this gate. If the table is absent, crypto BUY reservation calls fail closed and no BUY may proceed.

1. Run `git diff --check`, `bunx tsc --noEmit`, `bun test`, and `bunx wrangler deploy --dry-run`.
2. Commit and push to the active release branch; for this release line use `origin/fix/remove-premature-position-upsert-entryside`, then confirm local `HEAD` equals `git ls-remote origin refs/heads/fix/remove-premature-position-upsert-entryside`.
3. Build a fresh explicit bundle with `bunx wrangler deploy --dry-run --outdir <new-directory>`.
4. Upload that exact bundle through the direct Cloudflare multipart API. In this proxy environment, do not trust a successful `wrangler deploy` exit code as proof of a new version.
5. Verify the newest Cloudflare deployment has a new version at `100%` traffic.
6. Verify all four schedules, including the read-only `*/10 * * * *` maintenance schedule.
7. Query `/health`, `/api/dashboard`, `/api/trades`, and `/api/runs`; expect HTTP 200. Confirm `/api/dashboard.capitalCaps` contains only finite, non-negative numbers or `null`, and confirm each strategy tab shows either a USD cap or `Unavailable` without using account metrics as a fallback.
8. Query `/api/positions` when checking broker availability and confirm `positionsAvailable: true`, `source: "alpaca"`, and broker-matching symbols.
9. Confirm GitHub Pages contains the Worker URL and no direct Alpaca URL.
10. Never use trading triggers, cycle endpoints, order endpoints, or close endpoints as deployment tests.

See [`docs/DEPLOYMENT_RUNBOOK.md`](DEPLOYMENT_RUNBOOK.md) for exact commands and the direct-upload procedure.

## Documentation rule

Documentation is a release requirement. Every source, configuration, schema, migration, test, or operational change must update the relevant README, operations/runbook, release receipt, and next-step status in the same work item. Work is not complete until documented behavior, validation results, deployment state, known risks, and remaining follow-ups match reality.

## Fee-aware P&L and decision policy

- Strategy comparison exposes `grossTotalPl`, `feesUsd`, `netTotalPl`, and `feeAttribution` for each row.
- CFEE is defensibly attributed to crypto. Orderless/regulatory FEE remains account-level and is exposed as `accountLevelFeesUsd`; it is not fabricated into daytrading or swing.
- Current category snapshots and historical cumulative curves remain gross-only. Strategy comparison exposes aggregate gross/fee/net values, but those model/ledger aggregates are not fill-exact per-trade accounting. The fee ledger uses a bounded three-day import overlap, while the crypto entry fee-rate uses only positive curated-universe samples from the most recent seven days.
- Daytrading and crypto BUY decisions use quantity/notional-aware estimated costs. Crypto entries default to one per cycle and discretionary signal SELL/CLOSE actions default to a separate two-exit budget; protective exits bypass both budgets and the fee gate. Loss-reducing, EOD, and manual closes bypass the discretionary fee gate.
- Swing logs round-trip spread/slippage/fee costs with explicit bps units. `expectedEdgeBps` defaults to zero, so swing BUY is not rejected from an invented z-score-to-bps conversion; configure a calibrated edge before enabling rejection.
- All fee-gate skips must retain the estimated costs and reason in decision/run observability.

## Position-state contract

Alpaca supplies current symbol, quantity, side, prices, market value, and unrealized P&L. D1 supplies matching strategy, stop, timestamp, and historical metadata only. D1-only rows are excluded from current API positions. Broker-only symbols are `unattributed` until ownership is established. A broker-fetch failure is surfaced as unavailable; D1 is never used as a silent live fallback.

## Schedules

- Daytrading: `*/5 13-21 * * 1-5` UTC, gated by Alpaca's market clock.
- Swing: `0 22 * * 1-5` UTC.
- Crypto: `7-59/30 * * * *` UTC, at approximately `:07` and `:37`, 24/7.
- Maintenance/reconciliation: `*/10 * * * *` UTC, read-only broker/order reconciliation under a separate `maintenance` lease. Daytrading, swing, and crypto use isolated leases; each lease expires after 10 minutes so a stalled broker call cannot block unrelated strategies indefinitely.

## Prior-release natural reconciliation evidence

The prior-release natural reconciliation check completed on August 8, 2026 using only live GET endpoints. `/api/runs` recorded 23 `reconcile_cron` entries between `2026-08-08 06:40:53` and `2026-08-08 10:30:51` UTC. Sixteen runs completed with `MAINTENANCE_ONLY`; seven recorded `CYCLE_LEASE_HELD`. `/api/trades` showed 19 rows with all five lifecycle fields populated: `client_order_id`, `filled_qty`, `leaves_qty`, `broker_updated_at`, and `last_reconciled_at`; the observed reconciliation window was `2026-08-07 20:09:02` through `2026-08-08 10:20:06` UTC.

The maintenance run details reported `trades_executed: 0` and `imported: 0`, and source inspection shows reconciliation calls only Alpaca order reads (`getRecentOrders` and `getOrder`) before persisting D1 state. No reconciliation-caused broker mutation was observed or indicated. A categorical broker before/after conclusion is not available because the Worker exposes no read-only `/api/orders` route and no same-window order snapshot pair exists.

The August 9, 2026 audit found lease starvation: maintenance shared the strategy lease and repeated `CYCLE_LEASE_HELD` skips could block trading. The deployed fix uses separate `maintenance`, `daytrading`, `swing`, and `crypto` lease keys, a 10-minute default TTL, and a 12-second timeout for each Alpaca HTTP request. Source inspection confirms distinct `maintenance` and `daytrading` lease keys. Available D1 artifacts do not reconstruct exact historical lease ownership for each skipped invocation, so the verification confirms the design and current evidence boundary, not a complete historical ownership timeline. A maintenance run may still be skipped by another maintenance run, but it must not block a strategy lease.

## Deferred-risk monitoring

The active weekly read-only review job `Alpaca deferred-risk review` (schedule ID `56199d0b-dd75-4f3b-acb6-14c58c4e055b`) runs Mondays at 10:00 Europe/Copenhagen. It reviews partial-fill/retry lifecycle, deterministic attribution, fill/FIFO accounting, swing peak-price state, and live integration coverage without triggering broker mutations.

## Known risks

- The crypto patch is deployed, but Cloudflare deployment artifacts do not embed the Git SHA; the release bundle-to-commit mapping is recorded by the release process.
- Current validation and live evidence: 123 tests, 361 assertions, TypeScript, diff-check, and fresh dry-run passed; current source commit `5b01066430cf529db8e7329c970882718d0d8d2c` maps to deployment `6ef8737a-85ca-4fbb-8886-c938237dc993` / Worker version `5ff1ee08-bdc1-46b7-9aa6-93962d25beb4` at 100%; all read-only smoke endpoints returned HTTP 200; remote lifecycle schema passed; no broker mutation was used. The older `f122287` receipt is historical.
- `git diff --check` from the workspace root is contaminated by unrelated generated `/workspace/data` changes; the bot repository diff must be checked with `git -C /workspace/alpaca-trading-bot diff --check`.
- Wrangler dry-run remains validation-only and was not used as a deployment.

- August 6 live evidence showed repeated partial-filled exits and quantity mismatches. Current stock/swing SELL/CLOSE paths have non-terminal pending-exit guards (`PENDING_EXIT_EXISTS`), and the guard behavior is covered by regression tests; broker-authoritative mismatch protection remains in place and paper-session evidence is still required after deployment.
- Partial/fill lifecycle on the entry side: the August 18 release gives stock/swing BUYs deterministic `client_order_id` values and pre-submit `DUPLICATE_ORDER_PREVENTED` guards against retrying a non-terminal order. The August 21 local correction now guards stock/swing SELL/CLOSE paths against repeat non-terminal exits with `PENDING_EXIT_EXISTS`; it does not cancel, replace, retry, or weaken broker authority. Broader partial-fill/cancel/replace lifecycle and paper-session evidence remain follow-ups. Crypto has its own pending-exit guard and deterministic client IDs, but no complete broker retry/cancel/replace lifecycle.
- Order-to-decision correlation: as of the August 18 source candidate, daytrading/swing BUY entries carry a deterministic decision-derived `client_order_id`, and the local generic entry path persists `logOrderTrade` with the decision ID. It remains incomplete that stock/swing **exits** still do not carry a decision-derived deterministic ID, so exit correlation and historical attribution need completion and paper-session evidence. Historical swing/crypto rows without stable attribution remain excluded from deterministic lifecycle attribution.
- At the last verified D1 query on August 8, 2026, 365 trades existed and 84 had `strategy IS NULL`; those rows remain excluded from strategy-attributed history unless deterministic attribution is available.
- Swing batch-bar and degraded-data safeguards have been verified in production; future changes should preserve the bounded request and completed-session checks.
- Daytrading/crypto position sync can preserve an existing strategy-less row.
- Some historical and broker-only trades still need stronger deterministic strategy attribution and lifecycle correlation.
- Automated coverage includes reconciliation, partial-fill persistence, terminal statuses, idempotency, broker projection, fee attribution, quantity-aware costs, calibrated swing-edge behavior, and no-side-effect assertions, but not full live-broker integration.

## Next steps

1. Observe the first natural paper-session behavior of the deployed deterministic entry identity and retry guard without manually triggering a cycle.
2. Verify the remaining partial-fill, cancel, replace, and retry lifecycle under a paper session, separate from read-only reconciliation.
3. Strengthen deterministic strategy attribution and lifecycle correlation for historical and broker-only trades.
4. Add targeted live-broker integration checks without using trading actions as smoke tests.
5. Finish swing trigger attribution and decision-row accounting consistency work.
6. Keep the documented source-to-deployment mapping synchronized on every future release; Cloudflare artifacts do not embed the Git SHA.

## August 21, 2026 strict read-only control update

Control result: **FAIL/DEGRADED, not healthy**. All six required GET endpoints returned HTTP 200. The control was strictly read-only and called no trigger, order, cancellation, close, replace, retry, or broker-mutating endpoint.

Live evidence: `/api/positions` reported `positionsAvailable: true`, `source: alpaca`, and 29 broker positions. Dashboard equity was `$98,546.76` versus `last_equity` `$98,270.0927`, with latest snapshot equity `$98,556.33` at `2026-08-21 23:37:58` UTC. Daily change fields remain zero and limit independent daily validation. Caps remain `$5,000/$3,700/$2,000`; source and `wrangler.toml` preserve daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *`, and read-only reconciliation `*/10 * * * *`.

Fresh reconciliation rows through `2026-08-22 00:01:01` UTC are structured `MAINTENANCE_ONLY` skips with `ledgerPages: 1`, `ledgerPageBudget: 5`, `ledgerTruncated: false`, and `ledgerDegraded: false`. Fresh crypto rows at `23:08:04` and `23:38:04` UTC have zero errors and structured skip details. Crypto is approximately on the expected `:07/:37` cadence but shows recurring `:08/:38` jitter. Filtered `daytrading_cron` maps correctly to canonical `cron` and shows repeated `CYCLE_LEASE_HELD` skips, but no fresh successful daytrading run is proven; no fresh successful swing run is proven.

The sampled trade page exposes lifecycle fields and conservative accounting metadata, but current filled rows have `gross`, `fee`, and `net` null with `unavailable_fill_lot_exact`; exact fill/lot linkage and non-crypto strategy fee attribution remain open. Crypto edge-gate source and regression checks pass, including fail-closed fee telemetry and calibrated raw-edge requirements, but live positive `rawEdgeBps` evidence remains absent. Full local regression passed **154 tests / 488 assertions**.

The existing correction work item remains open. No code or deployment correction was applied during this control because no new reproducible trading-behavior defect was found. Exact blocker for independent release verification: `bunx wrangler whoami` reports `You are not authenticated`, leaving current Worker/source identity and control-plane schedule state unresolved despite a stored local Cloudflare credential record. Follow-up must remain read-only until authenticated, and must not alter caps or trading behavior.

## Crypto edge-gate and TIF investigation, August 21, 2026

**Disposition: documentation/observability only; trading logic unchanged.** A repository-wide source and history audit found no production producer for calibrated `rawEdgeBps`. The only concrete positive value is in regression-test input. Crypto `RiskManager` is intentionally configured with `requireCalibratedEdge: true`, while the production crypto caller supplies no `rawEdgeBps`; therefore positive-edge BUYs fail closed as `EDGE_CALIBRATION_UNAVAILABLE`. Confidence, TA, sentiment, fee telemetry, and ranking are not valid substitutes and must not be converted into basis points.

The crypto TIF mismatch is not reproducible from the current source path. `src/crypto-strategy.ts` explicitly passes `time_in_force: 'gtc'` to `AlpacaClient.submitOrder`; the client’s `day` default applies only when a caller omits the field. `Database.logOrderTrade` persists the broker-returned TIF, and `test/trade-identity.test.ts` covers crypto GTC persistence. Keep monitoring the structured skip code and reconcile any future `day` row against the originating decision/order ID, caller bundle, and broker response before considering a code change.

No caps, thresholds, sizing, schedules, order behavior, or broker authority were changed. No migration, live endpoint, broker mutation, or deployment is required for this investigation. The blocker is calibrated-edge evidence/production sourcing, not permission to weaken the gate.

## Control-82 reliability operations note — August 25, 2026

- Clean scheduled maintenance/reconciliation is `ok`, with `MAINTENANCE_ONLY` retained as structured informational detail. Lookup failures or ledger truncation remain `degraded`; lease-held, market-closed, stale/future-data, and other blocking conditions remain `skipped` or `error` as applicable.
- `RECONCILIATION_DEFERRED_TO_MAINTENANCE` is informational for swing/crypto. It must not label an otherwise evaluated no-trade cycle `skipped`; inspect the remaining structured details for true blockers.
- Daytrading 5-minute and crypto 15-minute paths reject invalid, stale, or future latest bars before TA. Review `DAYTRADING_BARS_*` and `CRYPTO_BARS_*` structured skip codes, including `latestBarAt`, `futureBarAt`, `ageSeconds`, and `maxStaleSeconds`; no signal or order is generated from rejected data.
- Account snapshots and strategy risk contexts expose `EQUITY_DIRECTION_FALLBACK` when broker `change_today_pct` is zero/unavailable and a valid equity delta is used. This is observability/fallback only; configured kill switches, thresholds, caps, and order semantics are unchanged.
- Swing cap checks include positive unattributed broker market value conservatively. The unattributed amount is not assigned to a strategy or dashboard category, but it is included in swing cap admission when a configured swing cap is active.

Control-82 is local-only and **OPEN FAIL/DEGRADED** for production. Full validation: **213 tests / 796 assertions**; TypeScript and diff-check passed. No deployment, migration, trigger, or broker mutation occurred.

## August 22, 2026 read-only trade-shape correction

The completed reliability patch normalizes legacy trade response rows so lifecycle and conservative accounting keys are always present, and adds combined strategy/status/trigger/pagination run-filter regression coverage. `/workspace/alpaca-trading-bot` is the deployable source; `/workspace/src` is stale reference material and is not a release source. No DDL, broker mutation, cap, schedule, threshold, sizing, edge-gate, TIF, or trading-behavior change was made.

Focused validation passed **24 tests / 126 assertions**; full validation passed **156 tests / 511 assertions**; TypeScript, diff-check, and Wrangler dry-run passed. Deployment is authorized only as this reliability correction, followed by separate GET-only verification. Production remains **FAIL/DEGRADED** because live source identity, fresh daytrading/swing delivery, direct cap enforcement, cadence exactness, and fill-lot accounting remain unresolved.

## August 22, 2026 Control-3 final outcome — correction not live-proven

The strict read-only control completed at **2026-08-22 05:03 UTC**. All six required GET endpoints returned HTTP 200; broker positions remained authoritative (`source: alpaca`, 29 rows); equity direction was positive by **$234.4073** versus `last_equity`; and caps remained **$5,000/$3,700/$2,000**. Reconciliation was fresh through **05:01:02 UTC** as `MAINTENANCE_ONLY`; crypto was fresh through **04:38:03 UTC** near `:07/:37` with expected fail-closed fee/confidence skips; daytrading and swing fresh-success delivery remained unproven, and trade gross/fee/net remained conservatively null.

Focused/full regressions, typecheck, diff-check, and Wrangler dry-run passed. The secure deployment retry produced no usable receipt, and separate GET-only verification still reports live release identity **1.0.0 / 2.4.0** versus local **2.6.0**. Keep production **FAIL/DEGRADED** and follow up by restoring verified Cloudflare deployment, then repeat GET-only validation; no caps or trading behavior may change.

## August 22, 2026 Control-4 strict read-only production control

Production remains **FAIL/DEGRADED**, not healthy. All six required GET endpoints returned HTTP 200, but live release identity is failed: `/health` is `1.0.0` and `/api/config` is `2.4.0` versus local deployable `2.6.0`. Positions report `positionsAvailable: true`, `source: alpaca`, and 29 broker rows; equity is `98504.50` versus `last_equity 98270.0927` (+`234.4073`), while daily direction remains unproven because daily change fields are zero.

Caps remain **$5,000 daytrading / $3,700 swing / $2,000 crypto**. Fresh reconciliation is a structured `MAINTENANCE_ONLY` delivery; crypto is delivered near `:07/:37 UTC` with observed `:08/:38` jitter; category history is populated through `2026-08-22 06:37:57`, but fresh successful daytrading and swing runs are not proven. Recent daytrading rows are repeated `CYCLE_LEASE_HELD` skips, the latest available swing row is an `2026-08-18 22:00:36` position-divergence/RISK_HALTED error, and historical crypto SQL-variable/subrequest failures remain relevant risk evidence. Sampled lifecycle fields are present, but per-trade gross/fee/net are conservatively null until deterministic fill-lot matching, and aggregate strategy accounting is not fill-lot exact. Local four-schedule wiring, broker-authoritative position projection, filtered run aliases, structured skip logging, conservative fees, unchanged caps, and fail-closed crypto edge gates pass source/tests, but live source identity, schedule identity, full run delivery, exact cadence, direct cap enforcement, and production raw-edge evidence remain open.

Correction work item: `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-4.md`. Release-control disposition is **FAIL** and live operations disposition is **DEGRADED**, not healthy. Owner is Joachim; trigger is restored authenticated Cloudflare deployment access. Acceptance requires an authenticated receipt tied to the exact validated 2.6.0 source commit and all four UTC cron expressions, matching 2.6.0 across `/health`, `/api/config`, and local release markers, then separate GET-only verification of all six endpoints, filtered runs, broker position source, equity direction, fresh structured terminal records for each scheduled strategy or documented no-op/skip, lifecycle/accounting, crypto edge observability, and unchanged caps. Deployment is not claimed because source-to-Worker identity and credentials remain unresolved. Fresh canonical and alias `/api/runs` probes currently match, but older saved captures returned empty alias arrays, so alias behavior remains unresolved pending post-deploy convergence. Run-log analyzed/filtered counts remain console-only, production has no calibrated `rawEdgeBps` producer, and daily direction remains unproven because daily change/P&L fields are zero. No code, configuration, migration, trigger, or broker-mutating action was performed.

The authorized Wrangler retry at **08:02 UTC** again failed before upload with the exact non-interactive `CLOUDFLARE_API_TOKEN` error despite the stored credential being selected through the secure process path. The exact local artifact is committed as `6bbc315b8069962340ef2b338934b108ff88c3ff`; retain **FAIL/DEGRADED** and require authenticated deployment plus separate GET-only verification before closure.

## August 22, 2026 Control-5 post-attempt verification

Commit `57a4efbfc2b3e0949829d9951776e8d7115b4f1f` passed local validation but was not uploaded. Wrangler stopped before upload with `In a non-interactive environment, it's necessary to set a CLOUDFLARE_API_TOKEN environment variable for wrangler to work.` Separate GET-only verification at approximately **09:04 UTC** confirms all six endpoints remain HTTP 200, positions remain broker-authoritative (`source: alpaca`, 29 rows), and caps remain `$5,000/$3,700/$2,000`; live release identity remains `1.0.0/2.4.0`, and alias responses lack `trigger_alias`, so production remains **FAIL/DEGRADED**.

## August 22, 2026 Control-6 documentation correction — FAIL/DEGRADED

**Disposition: FAIL/DEGRADED, not healthy.** This strict read-only control recorded HTTP 200 for all six required GET endpoints: `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades`. Live `/health` reports version **1.0.0** and `/api/config` reports **2.4.0**, conflicting with the local deployable **2.6.0**; live source identity remains unresolved.

Verified live facts: `/api/positions` reports `positionsAvailable: true`, `source: alpaca`, and 29 rows. Dashboard equity is **98504.50** versus `last_equity` **98504.5039**, current-vs-last direction **-0.0039**; daily fields are zero, and history fell from **98572.37 at 22:07:58** to **98504.50 at 09:37:57**. Caps are exactly **5000/3700/2000**.

Local source preserves all four schedules, broker authority, isolated leases, filtered aliases, conservative accounting, and fail-closed crypto edge gates. Fresh reconciliation is `MAINTENANCE_ONLY` around a 10-minute cadence. Crypto is near `:07/:37` but was observed at `:08/:38`; the latest observed row at **09:38:02** was a structured skip with 0 errors. No fresh daytrading or swing trigger/success is evidenced in the fetched run pages. Historical errors include crypto D1 `too-many-SQL-variables` and `too-many-subrequests`, plus stock/reconciliation errors.

Latest trade rows expose lifecycle fields, but sampled filled rows have `gross`, `fee`, and `net` null, `accounting_status: unavailable_fill_lot_exact`, and `fee_attribution: none-recorded`. Aggregate crypto fee telemetry is available but not per-trade exact. Filtered run alias behavior is source/test verified but not live-proven against 2.6.0.

Local focused validation is **89 tests / 323 assertions**; full validation is **157 tests / 520 assertions**; typecheck passed. Deployment was not attempted in this work item because the current Wrangler path lacks a usable `CLOUDFLARE_API_TOKEN` despite stored metadata, and live source identity remains unresolved. Preserve caps, schedules, thresholds, sizing, edge gates, broker authority, and trading behavior. Do not deploy, call any broker endpoint, call triggers, migrations, or mutate production.

**Follow-up owner: Joachim.** Restore authenticated reproducible Cloudflare deployment/source verification, then separately repeat GET-only checks and observe natural day/swing windows. Keep production **FAIL/DEGRADED**, not healthy. Correction work item: `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-6.md`.

## August 22, 2026 Control-6 strict read-only production control

Control-6 remains **FAIL/DEGRADED**, not healthy. All six required GET endpoints returned HTTP 200 with no broker-mutating endpoint, trigger, deployment, or migration used. Live `/health` is **1.0.0** and `/api/config` is **2.4.0**, conflicting with the local deployable **2.6.0** release markers; `/api/positions` remains `positionsAvailable: true`, `source: "alpaca"`, with 29 broker positions. Live equity is **98,504.50** versus `last_equity` **98,504.5039** (delta **-0.0039**), while daily fields remain zero and recent history declined from **98,572.37 at 2026-08-21 22:07:58 UTC** to **98,504.50 at 2026-08-22 09:37:57 UTC**. Caps remain **$5,000/$3,700/$2,000**.

Reconciliation delivery is fresh and structured as `MAINTENANCE_ONLY` near ten-minute cadence; crypto delivery is near `:07/:37 UTC` with observed `:08/:38` jitter and latest zero-error structured skips. No fresh daytrading or swing trigger/success is evidenced; historical `CYCLE_LEASE_HELD`, D1 SQL-variable, and Worker subrequest errors remain relevant. Lifecycle fields are exposed, but sampled filled rows retain conservative null `gross`/`fee`/`net` under `unavailable_fill_lot_exact`; aggregate crypto accounting is internally consistent but not fill-lot exact, and fee telemetry availability conflicts with current fail-closed skips. Local source, focused/full regressions, typecheck, and dry-run pass. Deployment/source identity remains unresolved because `wrangler whoami` is unauthenticated; owner Joachim must restore authenticated deployment access before any release claim. See `CORRECTION_WORK_ITEM_2026-08-22_CONTROL-6.md`.

## Control-118 production status - August 26, 2026 15:00 UTC

Strict GET-only control. All six endpoints HTTP 200, 0 errors, 0 lease holds. Verdict: **HEALTHY (code/deploy), DEGRADED (external).** Version 2.6.0 aligned across all surfaces. Code 22b3dba, docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 unchanged.

Live state: equity $98,356 vs last_equity $98,525 (-$169 today, EQUITY_DIRECTION_FALLBACK). 28 broker-authoritative positions: 15 swing (MV ~$7,770, d1 metadata) + 13 unattributed (MV ~$1,448, none metadata). Total swing ~$9,219 (2.49x cap) from pre-fix fills. **Control-117 fix confirmed live:** the 13 swing fills show strategy=unattributed (not daytrading) — auto-reconcile bypass stopped. BROKER_ONLY_RECONCILED fires each cycle (harmless) until swing_cron 22:00 UTC re-tags them.

Delivery: daytrading all skipped DAYTRADING_BARS_STALE (~977s vs 900s); crypto fail-closed at :38 UTC cadence (bars 22h stale, MATICUSD empty, no rawEdgeBps, fee asOf Aug 19); reconcile ok every 10 min. Trade 703 (PLD) strategy=null persistent. All filled trades conservative null gross/fee/net. No deploy, no code change, no broker mutation. Follow-up: 13 D1 re-tag at 22:00 UTC swing_cron; rawEdgeBps producer; D1 Sep 1 limits; github_pat; run-log gaps (paid upgrade); bar freshness.

## Control-122 production status - August 26, 2026 19:00 UTC

Strict GET-only control. All six endpoints HTTP 200, 0 errors across 50 visible runs (3718-3767, 14:21-18:38 UTC). 1 CYCLE_LEASE_HELD (run 3740, same gap as Control-121). Verdict: **HEALTHY (code/deploy), DEGRADED (external).** Version 2.6.0 aligned across all surfaces. Code 22b3dba, docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 unchanged.

Live state: equity $98,392.11 vs last_equity $98,524.98 (-$132.87 today, -0.135%, EQUITY_DIRECTION_FALLBACK). 28 broker-authoritative positions: 15 swing (MV ~$7,804, d1 metadata) + 13 unattributed (MV ~$1,451, none metadata). Total swing ~$9,255 (2.50x cap) from pre-fix fills. **Control-117 fix stable (5th consecutive control):** the 13 swing fills (trades 707-719) show strategy=unattributed (not daytrading) — auto-reconcile bypass stopped. BROKER_ONLY_RECONCILED fires each cycle (harmless) until swing_cron 22:00 UTC re-tags them.

Delivery: daytrading all skipped DAYTRADING_BARS_STALE (~978s vs 900s); crypto fail-closed at :07/:37 UTC cadence (bars 22h stale, MATICUSD empty, no rawEdgeBps, fee asOf Aug 19); reconcile ok every 10 min. Run-log gap 15:26-17:15 UTC (~109 min, ~22 runs missing, Free-tier silent throw). Trade 703 (PLD) strategy=null persistent. All filled trades conservative null gross/fee/net, filled_lot_exact_unavailable. No deploy, no code change, no broker mutation. Follow-up: 13 D1 re-tag at 22:00 UTC swing_cron; rawEdgeBps producer; D1 Sep 1 limits; github_pat; run-log gaps (paid upgrade); bar freshness.

## Control-123 production status - August 26, 2026 20:00 UTC

Strict GET-only control. All six endpoints HTTP 200, 0 errors in 20 visible runs (3749-3768, 17:41-20:00 UTC). Verdict: **HEALTHY (code/deploy), DEGRADED (external).** Version 2.6.0 aligned across all surfaces. Code 22b3dba, docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 unchanged.

Live state: equity $98,420.95 vs last_equity $98,524.98 (-$104.03 today, -0.1056%, ACTIVE, not blocked). 28 broker-authoritative positions: 15 swing (MV ~$7,826, d1 metadata) + 13 unattributed (MV ~$1,452, none metadata). Total swing ~$9,277 (2.51x $3,700 cap) from pre-fix fills. **Control-117 fix stable (6th consecutive control):** the 13 swing fills (trades 707-719) show strategy=unattributed (not daytrading) — auto-reconcile bypass stopped. BROKER_ONLY_RECONCILED fires each cycle (harmless) until swing_cron 22:00 UTC re-tags them.

NEW finding: second run-log delivery gap 18:38-20:00 UTC (~82 min, ~25 runs missing during market hours). Run 3768 CYCLE_LEASE_HELD at 20:00:56 confirms Free-tier silent-throw pattern (same root cause as the 15:26-17:15 gap from Controls 121-122). No code defect; approved paid-plan upgrade remains the remedy.

Delivery: daytrading all skipped DAYTRADING_BARS_STALE (978s vs 900s) + EQUITY_DIRECTION_FALLBACK (informational); crypto fail-closed at :07/:37 UTC cadence (CRYPTO_BARS_STALE AVAXUSD 22h stale, MATICUSD empty, no rawEdgeBps, fee asOf Aug 19); reconcile ok every 10 min. Trade 703 (PLD) strategy=null persistent. All filled trades conservative null gross/fee/net, filled_lot_exact_unavailable. No deploy, no code change, no broker mutation. Follow-up: 13 D1 re-tag at 22:00 UTC swing_cron; rawEdgeBps producer; D1 Sep 1 limits; github_pat; run-log gaps (paid upgrade); bar freshness.

## Control-73 production status - August 24, 2026

Keep production **OPEN FAIL/DEGRADED**. The strict control used only GET requests and read-only filter/pagination probes. The six endpoint responses were HTTP 200, but live release identity remains **1.0.0/2.4.0** versus local **2.6.0** and Wrangler remains blocked by `You are not authenticated. Please run wrangler login.`

Verified live state: positions are broker-authoritative (`positionsAvailable=true`, `source=alpaca`, 27 rows); equity is **98,394.23** versus `last_equity=98,504.5039` and therefore down by **110.2739 USD**; caps remain **5000/3700/2000 USD**. Local schedules remain daytrading `*/5 13-21 * * 1-5`, swing `0 22 * * 1-5`, crypto `7-59/30 * * * *` at `:07/:37`, and reconciliation `*/10 * * * *`.

Fresh live delivery is present for daytrading, reconciliation, and crypto, but no fresh August 24 swing run is proven. Crypto rows occurred at **18:08:12** and **17:38:15 UTC**, near but not exactly on the configured minute. Recent live errors include repeated Alpaca 40310000 minimum-notional failures in runs **3328, 3344, and 3350**. Live run filtering does not expose the locally implemented aliases or candidate counters, and `code=LEASE_HELD` / `search=LEASE` do not produce a distinct filtered result.

Filled trade lifecycle fields are present, while the sampled 48 filled rows conservatively retain null gross, fee, and net under `unavailable_fill_lot_exact` / `none-recorded`. Local crypto edge-gate wiring is fail-closed and tested, but live positive calibrated-edge behavior is not proven. Focused validation passed **54/319**, full validation **204/775**, typecheck exit 0, and diff-check exit 0.

No cap, schedule, threshold, sizing, fee policy, edge policy, order behavior, or broker state was changed. Follow-up is to restore authenticated source-tied deployment, release only the already-validated reliability artifact if still required, and perform a separate GET-only verification plus natural daytrading/swing observation.

## Control-125 production status - August 26, 2026 21:00 UTC

Strict GET-only control. All six endpoints HTTP 200 (8 working live GET endpoints confirmed: /, /health, /api/dashboard, /api/positions, /api/trades, /api/runs, /api/config, /api/account). Verdict: **HEALTHY (code/deploy), DEGRADED (external).** Version 2.6.0 aligned across /health, release_version, config.version. Code 22b3dba unchanged, docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Schedules confirmed in wrangler.toml: daytrading */5 13-21 * * 1-5, swing 0 22 * * 1-5, crypto 7-59/30 * * * *, reconcile */10 * * * *.

Live state: equity $98,447.32 vs last_equity $98,524.98 (-$77.66 today, -0.079%, ACTIVE, not blocked). 28 broker-authoritative positions: 15 swing (MV $7,854.40, d1 metadata) + 13 unattributed (MV $1,454.39, none metadata) = $9,308.79 total (2.52x $3,700 cap, pre-existing fills). **Control-117 fix stable (8th consecutive control):** the 13 swing fills (trades 707-719) remain strategy=unattributed (not daytrading) — auto-reconcile bypass stopped. Unattributed tickers: AVGO, BA, BAC, C, CVX, GE, INTC, RTX, SNOW, TXN, WFC, WMT, XOM. swing_cron at 22:00 UTC will re-create D1 rows with strategy='swing', ending BROKER_ONLY_RECONCILED cycle noise.

NEW finding: sustained CYCLE_LEASE_HELD streak 20:00-20:55 UTC (9 consecutive runs 3768-3776) affecting both daytrading and maintenance leases. This continues the 18:38-20:00 delivery gap (run 3768 CYCLE_LEASE_HELD at 20:00:56 marks the resumption). Root cause is the same Free-tier silent-throw pattern — invocations hit subrequest/CPU limits and throw before releasing leases. No code defect; approved paid-plan upgrade remains the remedy. The streak should self-heal once leases expire (600s TTL) and the daytrading window closes at 21:00 UTC.

Delivery: daytrading all skipped EQUITY_DIRECTION_FALLBACK (informational, change_today_pct=0 fallback to equity delta); crypto fail-closed at :07/:37 UTC cadence (RECONCILIATION_DEFERRED_TO_MAINTENANCE; no rawEdgeBps producer, fee asOf 2026-08-19, cryptoFeeTelemetryStatus=insufficient); reconcile ok every 10 min (MAINTENANCE_ONLY) when lease not held. Trade 703 (PLD) strategy=null persistent. All filled trades conservative null gross/fee/net, filled_lot_exact_unavailable, estimated_value_basis=order_time_estimate. No deploy, no code change, no broker mutation.

CANNOT VERIFY: crypto positive-edge path (no rawEdgeBps producer in source, fail-closed by design); origin sync (github_pat missing, docs commits local only). Follow-up: 13 D1 re-tag at 22:00 UTC swing_cron; rawEdgeBps producer; D1 Sep 1 limits; github_pat; run-log gaps + lease streaks (paid upgrade); bar freshness; trade 703 null strategy.

## Control-127 production status - August 26, 2026 23:00 UTC

**HEALTHY (code/deploy), DEGRADED (external).** All six GET endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,484.97 (-$40.01, -0.041%). 28 positions ALL strategy=swing, 0 unattributed, MV $9,348.03 (2.53x swing cap, pre-existing). broker_ledger_synced_until 22:51 UTC.

**Delivery:** Daytrading MARKET_CLOSED, 5-min cadence, 0 errors. CYCLE_LEASE_HELD streak 20:25-21:10 (7 runs), self-healed 21:16. Swing run 3794 22:01:28, skipped, 0 errors — submitted 3 sells (AMD, LCID, NXPI, trades 720-722). Crypto :07/:37 cadence, all RECONCILIATION_DEFERRED_TO_MAINTENANCE, 0 errors. Reconciliation MAINTENANCE_ONLY.

**Trades:** 707-719 filled (swing, filled_lot_exact_unavailable, gross/fee/net=null). 720-722 accepted sells (no_fill). Trade 703 strategy=null persistent. Crypto fail-closed (no rawEdgeBps, fee telemetry stale).

**DEGRADED:** Swing over-cap (pre-existing), CYCLE_LEASE_HELD streak, run-log gaps, trade 703 null, crypto fee stale, github_pat missing. Paid-plan upgrade approved, not executed.

## Control-126 production status - August 26, 2026 22:00 UTC

Strict GET-only control. All six endpoints HTTP 200. Verdict: **HEALTHY (code/deploy), DEGRADED (external).** Version 2.6.0 aligned across /health, release_version, config.version. Code 22b3dba unchanged, docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged. Schedules confirmed: daytrading */5 13-21 * * 1-5, swing 0 22 * * 1-5, crypto 7-59/30 * * * *, reconcile */10 * * * *.

**KEY FINDING: swing_cron 22:01:28 UTC re-tagged all 13 unattributed fills as strategy='swing'.** Run 3794 (swing_cron, 31.8s, 0 errors, status=skipped) successfully re-created D1 position rows for trades 707-719 with strategy='swing'. All 28 positions now strategy=swing (0 unattributed). This RESOLVES the thread from Controls 117-125 and the prior heartbeat expectation. BROKER_ONLY_RECONCILED cycle noise should end (no unattributed positions remain). During the swing evaluation, unattributedExposureUsd=$1,455.09 was included conservatively in cap accounting (UNATTRIBUTED_BROKER_EXPOSURE skip), confirming the Control-117 getSwingTradeSymbols() fix correctly identified the 13 fills via the trades table even before D1 re-tagging. Swing cap enforcement confirmed: CAPITAL_CAP skip blocked LLY entry ("$9,337.83 allocated against $3,700.00").

Live state: equity $98,474.19 vs last_equity $98,524.98 (-$50.79 today, -0.052%, ACTIVE, not blocked). 28 broker-authoritative positions: all strategy=swing, MV $9,335.76 (2.52x $3,700 cap, pre-existing fills from Control-101 bypass, no new entries). AMD EXIT_PENDING_RECONCILIATION (accepted swing sell, filledQty=0, remains open). LCID HELD_NO_SCORE_EXIT (protective data-integrity exit, no current score). Crypto fee telemetry stale asOf 2026-08-19, cryptoFeeTelemetryStatus=insufficient. broker_ledger_synced_until 2026-08-26T21:51:07Z. last_prune_date 2026-08-26.

Run-log: 80 visible runs (3715-3794, 14:08-22:01 UTC), 0 errors. 2 pre-existing gaps (15:26-17:15 109.6 min, 18:38-20:00 82.8 min). No new gaps in 21:00-22:01 window. 12 CYCLE_LEASE_HELD (runs 3740, 3768-3778) — streak 20:00-21:10 self-healed by 21:16 (run 3779 MARKET_CLOSED, 600s TTL expired). Crypto :07/:37 fail-closed (6 runs: 14:38, 15:08, 17:38, 18:08, 18:38, 21:38; bars stale AVAXUSD 22h, MATICUSD empty, no rawEdgeBps, fee asOf Aug 19). Daytrading MARKET_CLOSED (window closed 21:00 UTC). Reconcile MAINTENANCE_ONLY ok every 10 min. Trade 703 (PLD) strategy=null persistent. All filled trades conservative null gross/fee/net, filled_lot_exact_unavailable, estimated_value_basis=order_time_estimate.

CANNOT VERIFY: crypto positive-edge path (no rawEdgeBps producer in source, fail-closed by design); origin sync (github_pat missing, docs commits local only). Follow-up: rawEdgeBps producer; D1 Sep 1 enforcement limits; github_pat re-add; run-log gaps + lease streaks (paid upgrade); bar freshness; trade 703 null strategy.

## Control-129 production status - August 27, 2026 01:00 UTC

**HEALTHY (code/deploy), DEGRADED (external). No deploy required.** Strict GET-only. All six endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). Docs HEAD this commit (local only, push blocked — github_pat missing). Prior docs HEAD 2d827e7 (Control-128). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,486.19, last_equity $98,524.98, change_today -$38.79 (-0.039%). Cash $89,136.78, buying_power $381,018.63, long_market_value $9,349.41. broker_ledger_synced_until 2026-08-27T00:51:11Z.

**Positions:** 28 broker-authoritative, ALL strategy=swing, 0 unattributed. MV $9,349.41 (2.53x $3,700 cap, pre-existing Control-101/117 bypass fills). metadata_updated_at 2026-08-26 22:01:xx (swing_cron re-tag stable). 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted, no_fill, day orders for Aug 27 13:30 UTC market open.

**Delivery (60 visible runs, 3758-3817, 18:11-00:51 UTC):** 0 errors. 11 CYCLE_LEASE_HELD (3768-3778, Aug 26 ~20:00-21:10 streak, self-healed — historical). 89.6 min cron/daytrading gap (3766-3769, historical Free-tier). No new gaps, no lease holds in 00:xx window. Daytrading MARKET_CLOSED. Swing 1 run (3794) 22:01:28, skipped, 3 sells submitted. Crypto 8 runs at :08/:38 cadence, all RECONCILIATION_DEFERRED + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT (validTA=0, fail-closed). Reconciliation 27 runs MAINTENANCE_ONLY every 10 min, brokerOrders=3, 0 lookup failures.

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer. Fee telemetry stale (asOf 2026-08-19, status=unavailable). EQUITY_DIRECTION_FALLBACK each cycle (observability only).

**Trades:** 720-722 sells no_fill (conservative null gross/fee/net). 707-719 filled BUYs filled_lot_exact_unavailable (conservative null). Trade 703 (PLD) strategy=null persistent.

**Status:** HEALTHY (code/deploy), DEGRADED (swing 2.53x cap pre-existing + historical run-log gap + 11 CYCLE_LEASE_HELD self-healed + crypto fail-closed + trade 703 null + github_pat missing).

## Control-128 production status - August 27, 2026 00:00 UTC

**HEALTHY (code/deploy), DEGRADED (external). No deploy required.** Strict GET-only. All six endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). Docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,490.24, last_equity $98,524.98, change_today -$34.74 (-0.035%). Cash $89,136.78, buying_power $381,031.76, long_market_value $9,353.46. broker_ledger_synced_until 2026-08-26T23:51:10Z.

**Positions:** 28 broker-authoritative, ALL strategy=swing, 0 unattributed. MV $9,353.46 (2.53x $3,700 cap, pre-existing Control-101/117 bypass fills). metadata_updated_at 2026-08-26 22:01:xx (swing_cron re-tag stable). 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted, no_fill, day orders for Aug 27 13:30 UTC market open.

**Delivery (30 visible runs, 3781-3810, 21:21-00:01 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, NO gaps in this window. Daytrading 8 runs MARKET_CLOSED (21:21-21:56). Swing 1 run (3794) 22:01:28, skipped, 31.8s. Crypto 5 runs at :07/:37 cadence, all RECONCILIATION_DEFERRED + CRYPTO_BARS_UNAVAILABLE (MATICUSD) + CRYPTO_BARS_STALE (AVAXUSD ~22h) + CRYPTO_DATA_INSUFFICIENT (validTA=0). Reconciliation 16 runs MAINTENANCE_ONLY every 10 min, brokerOrders=3, 0 lookup failures.

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source. Fee telemetry stale (asOf 2026-08-19, status=insufficient). EQUITY_DIRECTION_FALLBACK each cycle (observability only).

**Trades:** 720-722 sells no_fill (conservative null gross/fee/net). 707-719 filled BUYs filled_lot_exact_unavailable (conservative null). Trade 703 (PLD) strategy=null persistent.

**DEGRADED:** Swing over-cap (pre-existing, no bypass), crypto fail-closed, trade 703 null, historical run-log gaps, paid-plan upgrade approved not executed, github_pat missing.

CANNOT VERIFY: crypto positive-edge path (fail-closed by design); origin sync (github_pat missing). Follow-up: rawEdgeBps producer; D1 Sep 1 enforcement; github_pat; paid-plan upgrade; bar freshness; trade 703 null strategy.

## Control-132 production status - August 27, 2026 04:00 UTC

**HEALTHY (code/deploy), DEGRADED (external). No deploy required.** Strict GET-only. All six endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). Docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,458.27, last_equity $98,524.98, change_today -$66.71 (-0.068%). Cash $89,136.78, buying_power $380,948.08, long_market_value $9,321.49. broker_ledger_synced_until 2026-08-27T03:51:11Z.

**Positions:** 28 broker-authoritative, ALL strategy=swing, 0 unattributed. MV $9,321.49 (2.52x $3,700 cap, pre-existing Control-101/117 bypass fills). metadata_updated_at 2026-08-26 22:01:xx (swing_cron re-tag stable). 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted, no_fill, day orders for Aug 27 13:30 UTC market open.

**Delivery (140 visible runs, 3702-3841, Aug 26 13:25 - Aug 27 03:51 UTC):** 0 errors. 11 CYCLE_LEASE_HELD (3768-3778, Aug 26 ~20:00-21:10 streak, self-healed — historical). 2 historical gaps (109.6 min 15:26-17:15, 82.8 min 18:38-20:00, Free-tier). No new gaps, no lease holds in 00:xx-03:xx window. Daytrading MARKET_CLOSED. Swing 1 run (3794) 22:01:28, skipped, 3 sells submitted. Crypto :08/:38 cadence, all RECONCILIATION_DEFERRED + CRYPTO_BARS_STALE/UNAVAILABLE + CRYPTO_DATA_INSUFFICIENT (validTA=0, fail-closed). Reconciliation MAINTENANCE_ONLY every 10 min, brokerOrders=3, 0 lookup failures.

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer. Fee telemetry stale (asOf 2026-08-19, status=unavailable). EQUITY_DIRECTION_FALLBACK each cycle (observability only).

**Trades:** 720-722 sells no_fill (conservative null gross/fee/net). 707-719 filled BUYs filled_lot_exact_unavailable (conservative null). Trade 703 (PLD) strategy=null persistent.

**Status:** HEALTHY (code/deploy), DEGRADED (swing 2.52x cap pre-existing + historical run-log gaps + 11 CYCLE_LEASE_HELD self-healed + crypto fail-closed + trade 703 null + github_pat missing).

CANNOT VERIFY: crypto positive-edge path (fail-closed by design); origin sync (github_pat missing). Follow-up: rawEdgeBps producer; D1 Sep 1 enforcement; github_pat; paid-plan upgrade; bar freshness; trade 703 null strategy; swing normalizes as 3 pending sells fill at Aug 27 13:30 UTC open.

## Control-133 production status - August 27, 2026 05:00 UTC

**HEALTHY (code/deploy), DEGRADED (external). No deploy required.** Strict GET-only. All six endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). Docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,452.70, last_equity $98,524.98, change_today -$72.28 (-0.073%). Cash $89,136.78, buying_power $380,933.24, long_market_value $9,315.92. EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback; observability-only). current_state_source=alpaca, observed 2026-08-27T05:00:24Z.

**Positions:** 28 broker-authoritative, ALL strategy=swing, 0 unattributed. MV $9,315.92 (2.52x $3,700 cap, pre-existing Control-101/117 bypass fills, no active bypass). Swing cap enforcement confirmed. metadata_updated_at 2026-08-26 22:01:27 (swing_cron re-tag stable since Control-126). 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted, no_fill, day orders for Aug 27 13:30 UTC market open.

**Delivery (30 visible runs, 3820-3849, 01:11-04:51 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps in this window. Daytrading MARKET_CLOSED. Crypto 7 runs at :08/:38 cadence, all RECONCILIATION_DEFERRED + CRYPTO_BARS_STALE (AVAXUSD ~22h stale) + CRYPTO_BARS_UNAVAILABLE (MATICUSD empty) + CRYPTO_DATA_INSUFFICIENT (validTA=0, fail-closed). Reconciliation 23 runs MAINTENANCE_ONLY every 10 min, brokerOrders=3, 0 lookup failures.

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source. Fee telemetry stale (asOf 2026-08-19, status=unavailable). EQUITY_DIRECTION_FALLBACK each cycle (observability only).

**Trades:** 720-722 sells no_fill (conservative null gross/fee/net). 707-719 filled BUYs filled_lot_exact_unavailable (conservative null). Trade 703 (PLD) strategy=null persistent.

**DEGRADED:** Swing over-cap (pre-existing, no bypass), crypto fail-closed, trade 703 null, historical run-log gaps, paid-plan upgrade approved not executed, github_pat missing.

CANNOT VERIFY: crypto positive-edge path (fail-closed by design); origin sync (github_pat missing). Follow-up: rawEdgeBps producer; D1 Sep 1 enforcement; github_pat; paid-plan upgrade; bar freshness; trade 703 null strategy; swing normalizes as 3 pending sells fill at Aug 27 13:30 UTC open.

## Control-138 production status - August 27, 2026 09:00 UTC

**HEALTHY (code/deploy), DEGRADED (external). No deploy required.** Strict GET-only. All eight endpoints HTTP 200, 0 errors. Version 2.6.0 aligned. Code 22b3dba (unchanged since Control-117). Docs HEAD this commit (local only, push blocked — github_pat missing). 223 tests / 841 assertions, typecheck clean. Caps 5000/3700/2000 USD unchanged.

**Account:** ACTIVE, not blocked. Equity $98,514.19, last_equity $98,417.71, change_today +$96.48 (+0.098%). Cash $89,136.77, buying_power $381,099.56, long_market_value $9,377.42. EQUITY_DIRECTION_FALLBACK (broker change_today_pct=0, equity_delta_fallback; observability-only). broker_ledger_synced_until 2026-08-27T08:51:12Z.

**Positions:** 28 broker-authoritative, ALL strategy=swing, 0 unattributed, 0 null. MV $9,377.36 (2.53x $3,700 cap, pre-existing Control-101/117 bypass fills, no active bypass). Swing cap enforcement confirmed. metadata_updated_at 2026-08-26 22:01:2x (swing_cron re-tag stable since Control-126). 3 pending sells (720-722: AMD 0.28, LCID 209, NXPI 0.53) accepted/new, no_fill, day orders for Aug 27 13:30 UTC market open.

**Delivery (30 visible runs, 3852-3881, 05:11-08:51 UTC):** 0 errors, 0 CYCLE_LEASE_HELD, 0 gaps in window. No new gaps/lease holds since Aug 26 21:21 UTC (14+ hours clean). Daytrading MARKET_CLOSED. Crypto 7 runs at :08/:38 cadence, all RECONCILIATION_DEFERRED + CRYPTO_BARS_STALE (AVAXUSD ~22h stale) + CRYPTO_BARS_UNAVAILABLE (MATICUSD empty) + CRYPTO_DATA_INSUFFICIENT (validTA=0, fail-closed). Reconciliation 23 runs MAINTENANCE_ONLY every 10 min, brokerOrders=3, 0 lookup failures.

**Crypto edge-gate:** Fail-closed. No rawEdgeBps producer in source. Fee telemetry stale (asOf 2026-08-19, status=unavailable). EQUITY_DIRECTION_FALLBACK each cycle (observability only).

**Trades:** 720-722 sells no_fill (conservative null gross/fee/net). 707-719 filled BUYs filled_lot_exact_unavailable (conservative null). Trade 703 (PLD) strategy=null persistent.

**DEGRADED:** Swing over-cap (pre-existing, no bypass), crypto fail-closed, trade 703 null, historical run-log gaps, paid-plan upgrade approved not executed, github_pat missing.

CANNOT VERIFY: crypto positive-edge path (fail-closed by design); origin sync (github_pat missing). Follow-up: rawEdgeBps producer; D1 Sep 1 enforcement; github_pat; paid-plan upgrade; bar freshness; trade 703 null strategy; swing normalizes as 3 pending sells fill at Aug 27 13:30 UTC open.

**Monday, August 31, 2026 Control-247 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-247 at ~12:00 UTC Aug 31 (14:00 +02). Pre-open heartbeat, market CLOSED (next open Mon 13:30 UTC TODAY, 1.5h away — first weekday open since Aug 28 + D1 Sep 1 enforcement starts TODAY). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). Control-217 reliability redeploy holding ~26h: PAST 8-16h upper bound, NO fifth cron dispatch failure, longest clean stretch ever continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3549-4175ms avg 3862ms 9 runs in 30-run window NO sustained creep, well below ~20s pre-gap threshold; crypto_cron :07/:37 cadence 3 runs in window fail-closed MATICUSD empty + AVAXUSD stale latestBarAt 2026-08-30T14:00Z age 77835s ~21.6h validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ Aug 30 22:00, 3 sells 769-771 accepted/no_fill pending Mon 13:30 UTC open, tonight 22:00 UTC is first DOW 2-6 dispatch test). Run-log contiguous 5371-5400 (30 runs, 0 gaps, 0 errors; triggers cron 18/reconcile_cron 9/crypto_cron 3). Latest run 5400 @ 12:00:17 UTC reconcile_cron ok 4058ms 0 errors. 23 swing positions broker-authoritative (source=alpaca observed 12:01:17Z, metadata_source=d1 all 23), 0 unattributed, 0 strategy=None, MV sum $8,240.48 (positions sum matches dashboard long_market_value $8,242.93 within live price drift between fetches). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Equity direction: broker change_today -$12.81 / -0.013% non-zero (broker provides direction, no fallback needed). Skip reasons structured with codes/scopes/context (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT, MAINTENANCE_ONLY), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells submitted Aug 30 22:00 UTC, fill or expire at Mon open), 768 filled with complete timestamps. Gross/fee/net=null conservative (accounting_status=no_fill for pending, filled_lot_exact_unavailable for filled, fee_attribution=none-recorded). Null-strategy trades 0 in 10-trade window (762-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,206.25 (live account portfolio_value/equity), cash $89,963.32 (91.6%), buying power $382,576.62, ACTIVE, not PDT, change_today -$12.81, last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Aug 28). broker_ledger_synced_until 2026-08-31T12:00:17.156Z, last_prune_date 2026-08-31, snapshot 1260 @ 11:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD cb1fbfd (local, Control-246). Docs HEAD: this commit (Control-247, local only). Follow-up: 13:30 UTC market open (pending sells 769-771 fill/expire + first D1 Sep 1 enforcement trading-day test), 22:00 UTC swing_cron first DOW 2-6 dispatch test.

**Monday, August 31, 2026 Control-248 strict read-only pre-open control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-248 at ~13:00 UTC Aug 31 (15:00 +02). Pre-open heartbeat, market CLOSED (next open Mon 13:30 UTC TODAY, 30 min away — first weekday open since Aug 28 + D1 Sep 1 enforcement starts TODAY). Strict GET-only on 6 endpoints (all 200: /health, /api/config, /api/dashboard, /api/positions, /api/runs, /api/trades). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Code bd6205e (Control-241, unchanged). Control-217 reliability redeploy holding ~27h: PAST 8-16h upper bound, NO fifth cron dispatch failure, longest clean stretch ever continues. All 4 schedules dispatching (daytrading cron every 5 min MARKET_CLOSED nextOpen 2026-08-31T09:30-04:00; reconcile_cron every 10 min ok dur 3693-4312ms avg 3979ms 9 runs in 30-run window NO sustained creep, well below ~20s pre-gap threshold; crypto_cron :07/:37 cadence 3 runs in window fail-closed MATICUSD empty + AVAXUSD/ETHUSD stale validTA=0, edge gate not reached but producer wired; swing_cron no new run since 5120 @ Aug 30 22:00, 3 sells 769-771 accepted/no_fill pending Mon 13:30 UTC open, tonight 22:00 UTC is first DOW 2-6 dispatch test). Run-log contiguous 5391-5420 (30 runs, 0 gaps, 0 errors; triggers cron 18/reconcile_cron 9/crypto_cron 3). Latest run 5420 @ 13:00:17 UTC reconcile_cron ok 4146ms 0 errors. 23 swing positions broker-authoritative (source=alpaca observed, metadata_source=d1 all 23), 0 unattributed, 0 strategy=None, MV sum $8,210.04 (positions sum matches dashboard long_market_value $8,209.12 within live price drift). D1 fallback guard confirmed in code (api.ts:196-208 positionsAvailable=false on broker failure, no D1 live-state substitution). Daytrading MV $0, crypto MV $0 fail-closed. Equity direction: broker change_today -$46.62 / -0.0475% non-zero (broker provides direction, no fallback needed). Skip reasons structured (RECONCILIATION_DEFERRED_TO_MAINTENANCE, MARKET_CLOSED, CRYPTO_BARS_UNAVAILABLE/STALE, EQUITY_DIRECTION_FALLBACK, CRYPTO_DATA_INSUFFICIENT, MAINTENANCE_ONLY), 0 errors, 0 lease errors. Trade lifecycle: 769-771 pending accepted/no_fill (swing sells submitted Aug 30 22:00 UTC, fill or expire at Mon 13:30 UTC open 30 min away), 768 filled with complete timestamps. Gross/fee/net=null conservative (accounting_status=no_fill/filled_lot_exact_unavailable, fee_attribution=none-recorded). Null-strategy trades 0 in 10-trade window (762-771). Fee telemetry stale (Aug 19, cryptoFeeTelemetryStatus=unavailable). Equity $98,172.44 (live account portfolio_value/equity), cash $89,963.32 (91.6%), buying power $382,482.94, ACTIVE, not PDT, trading_blocked false, change_today -$46.62, last_equity $98,219.06. 771 total trades / 768 executed, win 14.5% (no new trades since Aug 28). broker_ledger_synced_until 2026-08-31T13:00:17.226Z, last_prune_date 2026-08-31, snapshot 1262 @ 12:37:14. Caps 5000/3700/2000 USD unchanged (capital-caps.ts:6-8, config + dashboard confirmed). 224 tests/845 assertions pass, typecheck clean, git diff --check clean. Repo HEAD d62cad7 (local, Control-247). Docs HEAD: this commit (Control-248, local only). Follow-up: 13:30 UTC market open (pending sells 769-771 fill/expire + first D1 Sep 1 enforcement trading-day test), 22:00 UTC swing_cron first DOW 2-6 dispatch test.
**Tuesday, September 1, 2026 Control-280 strict read-only control (HEALTHY code/deploy, DEGRADED external, no deploy needed)**

Control-280 at ~17:15 UTC Sep 1 (19:15 +02). Evening heartbeat, market OPEN (daytrading active). Strict GET-only on 6 endpoints (all 200). HEALTHY code/deploy (2.6.0), DEGRADED external. No defect found, no deploy needed. Deployed fd2309c (Control-255), no source diff. D1 Sep 1 enforcement ~17h clean under active trading. ~29h post-Control-257, NO eighth cron dispatch failure. Run-log contiguous 5914-5973 (60 runs, 0 gaps, 0 leases; cron 36/reconcile_cron 18/crypto_cron 6). 4 transient known error runs: VZ qty-mismatch (5920), QCOM qty-mismatch (5964), 2 subrequest spikes (5929+5963, fd2309c class) — recovered, not defects. 22 positions broker-authoritative (source=alpaca, metadata_source=d1): 20 swing MV $7,714.84 cost $8,052.83 + 2 daytrading QCOM/SLB MV $4,987.66 cost $5,001.36, 0 crypto/null. WATCH: daytrading cost $1.36 OVER $5k cap (EOD accumulation). Caps 5000/3700/2000 unchanged. Equity $97,992.96, change_today -$166.19 (-0.169%), last_equity $98,159.15, ACTIVE not PDT. WATCH: reconcile 11813-15591ms, 7/8 over 12s, avg ~13.2s, no gaps (Aug 31 pressure pattern). Crypto fail-closed at bar gate (ETHUSD stale 21.9h, MATICUSD empty, validTA=0). Fee telemetry stale (Aug 19). 224 tests/845 assertions typecheck clean.

**Known DEGRADED:** Crypto fail-closed (stale bars + Aug 19 fee telemetry), daytrading cost $1.36 over cap (WATCH, EOD), reconcile duration pressure under trading load (WATCH, no gaps), side effects from the 8 recurring cron dispatch failures.

**Follow-up:** 22:00 UTC swing_cron tonight (Tue DOW=3, second real 2-6 fire; must NOT fire Sun Sep 6); reconcile duration WATCH; daytrading cost-cap margin WATCH; crypto bar + fee freshness.

**Tuesday, September 1, 2026 Control-286 strict read-only control (HEALTHY code/deploy 2.6.0, DEGRADED external, no deploy needed; D1 free-tier read+write limits still exceeded hard-blocked)**

Control-286 at ~22:00 UTC Sep 1 (~00:00 +02 Sep 2). Strict GET-only on 6 endpoints. /health ok (2.6.0). /api/account OK (broker-direct, no D1): equity $97,953.61, change_today -$205.54 (-0.21%), long MV $12,568.21, ACTIVE not PDT, trading_blocked false. /api/config OK (cached, version 2.6.0, caps 5000/3700/2000 confirmed, fee telemetry stale Aug 19, broker_ledger_synced_until 2026-09-01T19:01:01Z). /api/dashboard, /api/positions, /api/runs, /api/trades ALL D1_ERROR (hard-blocked, 5 rapid retries failed). D1 free-tier read+write limits still exceeded since ~16:31 UTC; run log frozen at 6008/19:01 UTC (writes silently rejected). 4 CF schedules verified LIVE (all modified 2026-08-31T22:06:50Z): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. swing_cron fires 22:00 UTC tonight (Tue DOW=3) likely blocked by D1; next real fire Wed 22:00 (DOW=4); must NOT fire Sun Sep 6 (DOW=1). Deployed 0b3b2a3 (Control-283), local HEAD e3da71e, no source diff. 224 tests/845 assertions pass, typecheck clean. No deploy, no code change. CANNOT VERIFY D1-backed observability during block; /api/account is reliable live-state proxy (long MV $12,568.21 confirms positions intact at broker). FOLLOW-UP: verify D1 reads <5M after 00:00 UTC reset; D1 paid-tier upgrade (separate from Workers Paid, cost) needs Joachim's decision; confirm swing_cron outcome + run-log resume after reset; reconcile duration WATCH after reset. Caps 5000/3700/2000 unchanged.

**Control-293 (Sep 2 ~05:00 UTC): STRICT READ-ONLY, no deploy.** HEALTHY code/deploy (2.6.0), DEGRADED external. No defect, no code change, no deploy. All 6 endpoints 200 (D1 post-reset healthy 5h+, Control-283 read-budget optimizations holding). /health ok 2.6.0. /api/config OK (caps 5000/3700/2000, broker_ledger_synced_until 2026-09-02T05:00:41Z, fee telemetry stale Aug 19 cryptoFeeTelemetryStatus=unavailable, last_prune 2026-09-02). /api/dashboard OK (886 trades, equity $97,876.26, -$282.89/-0.29% today, long MV $12,490.86, 23 positions, ACTIVE not PDT, trading_blocked false). /api/positions OK (23 broker-authoritative metadata_source=d1 all 23: 20 swing cost $8,052.83 MV $7,728.75 pre-existing + 3 daytrading NIO/NOW/SOFI cost $4,875.28 MV $4,763.40 UNDER $5k cap, 0 crypto $0). /api/runs OK (contiguous 6090-6109, 0 gaps, 0 leases, 0 errors; reconcile_cron ok dur 12210-12974ms, cron daytrading MARKET_CLOSED, crypto_cron :07/:37 fail-closed). /api/trades OK (886 filled, gross/fee/net null filled_lot_exact_unavailable conservative). 4 CF schedules verified LIVE via CF API (all modified 2026-08-31T22:06:50Z): daytrading 1-59/5, swing 0 22 * * 2-6, crypto 7-59/30, reconcile */10. Crypto edge-gate fail-closed (ETHUSD stale 21.9h, AVAXUSD stale 22.1h, MATICUSD empty, validTA=0, CRYPTO_DATA_INSUFFICIENT, edge gate wired not reached). Reconcile durations ~12.5s WATCH. Equity direction EQUITY_DIRECTION_FALLBACK observability-only. Repo HEAD 1849b4e (Control-292 docs), deployed 0b3b2a3 (Control-283), no source diff, 224 tests/845 assertions pass, typecheck clean.

**Known DEGRADED:** Crypto fail-closed, fee telemetry stale, reconcile duration ~12s WATCH, D1 paid-tier upgrade pending Joachim's decision.

Follow-up: verify D1 reads <5M throughout Sep 2 (5h clean); D1 paid-tier needs Joachim decision; confirm swing_cron Wed 22:00 UTC DOW=4 (first clean 2-6 fire, must NOT fire Sun Sep 6 DOW=1); fee telemetry + crypto bar freshness; reconcile duration WATCH ~12s. Caps 5000/3700/2000 unchanged.
