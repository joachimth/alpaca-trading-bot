# Alpaca production control correction work item: Control-3

- **Opened:** August 22, 2026 UTC, during strict read-only production control.
- **Live defect:** the primary unresolved live defect is release/version drift: `/health` reports `1.0.0` while deployable source reports `2.6.0`. An earlier capture showed `/api/runs` ignoring filters, but fresh post-attempt GET probes returned correctly filtered rows for strategy, status, trigger, combined filters, and compatibility aliases; the corrected source is still not live-proven because deployment was blocked.
- **Release identity defect:** live `/health` reports `1.0.0` and live `/api/config` reports `2.4.0`, while the deployable source and local release metadata report `2.6.0`; the local correction is not live.
- **Current local correction:** `src/api.ts` implements `strategy`, `status`, `trigger`, pagination, and compatibility aliases `daytrading_cron -> cron` and `reconciliation_cron -> reconcile_cron`; `src/database.ts` applies read-only SQL predicates. `src/version.ts` is the shared `2.6.0` health version. No trading behavior, schedules, caps, crypto edge gate, broker authority, broker calls, or D1 mutation semantics changed.
- **Other read-only findings:** `/health`, `/api/config`, `/api/dashboard`, `/api/positions`, `/api/runs`, and `/api/trades` returned HTTP 200. Positions report `positionsAvailable: true`, `source: alpaca`, 29 rows. Equity is `98504.50` versus `last_equity: 98270.0927`, an upward direction of `+234.4073`; `change_today` fields are zero. Caps remain `5000/3700/2000` USD. Crypto runs delivered at `:08/:38` rather than exact `:07/:37`; reconciliation delivered every ~10 minutes as `MAINTENANCE_ONLY`; no fresh successful daytrading or swing run is proven. Recent filled trade rows expose lifecycle fields but all sampled gross/fee/net values are null with `accounting_status: unavailable_fill_lot_exact` and `fee_attribution: none-recorded`; aggregate crypto fees remain broker-attributed and net P&L equals gross P&L minus fees. The six SQL-variable failures from 18:08-20:38 UTC on August 21 predate the 21:03:38 UTC batching release; no recurrence appears in post-release crypto runs through 03:38 UTC on August 22.
- **Required action:** run focused/full regressions, typecheck, diff-check, Wrangler dry-run, deploy the reliability-only correction under the standing maintenance rule if credentials permit, then repeat separate GET-only verification. Do not call trigger, submit, cancel, close, replace, retry, migration, or any broker-mutating endpoint.
- **Current disposition:** FAIL/DEGRADED. A secure Wrangler deployment retry produced no usable deployment receipt, and the separate post-attempt GET-only verification still reports live `/health` `1.0.0` and `/api/config` `2.4.0`; corrected Worker identity and live filters remain unverified. Caps and trading behavior must remain unchanged.

- **Release gates completed:** focused `bun test test/release-version.test.ts test/dashboard-readonly.test.ts crypto-runtime.test.ts` passed **26 tests / 154 assertions**; full `bun test` passed **157 tests / 518 assertions**; `bunx tsc --noEmit` passed; `git diff --check -- .` passed; and `bunx wrangler deploy --dry-run --outdir /workspace/alpaca-control/dry-run-control-3` passed with a 281.23 KiB upload preview.
- **Deployment blocker:** `bunx wrangler deploy` stopped before upload with `In a non-interactive environment, it's necessary to set a CLOUDFLARE_API_TOKEN environment variable for wrangler to work.` The temporary claim flow was not used.
- **Credential retry:** a second deployment attempt using the stored secure Cloudflare credential reference also stopped before upload with the same `CLOUDFLARE_API_TOKEN` requirement; no secret was exposed and no temporary claim flow was used.
- **Disposition:** correction is locally validated but not deployed or live-verified; production remains **FAIL/DEGRADED**. Explicit follow-up: restore the secure Cloudflare credential flow, deploy this exact source, then separately repeat only the six GET checks plus filtered `/api/runs`, release-version consistency, schedule/deployment metadata, and post-release regression verification.

- **Final synthesis additions:** source review found no named daily equity-direction implementation beyond `equity - last_equity`; live `change_today` is zero, so daily direction is not independently validated. Strategy analyzed/filtered counts are generally console-only and not persisted in `run_log`. Crypto `requireCalibratedEdge` and fee telemetry are wired fail-closed, but no production `rawEdgeBps` producer exists, so live positive-edge comparison is unavailable.

- **Final audit additions:** broker-only positions remain intentionally unattributed, including unresolved MSTR attribution; stock/swing exit identity and cancel/replace/retry correlation remain incomplete; `closePosition()` stores broker snapshot unrealized P&L as `closed_pl`, so aggregate gross/net is not fill/lot exact. Historical above-cap exposure is prior-release evidence only and does not establish a current cap violation.
